
name: 24/7 Master Stream Scheduler

on:
  workflow_dispatch:
    inputs:
      stop_time:
        description: 'Auto-stop time in PKT (e.g., 3:00 AM, 12:00 AM, 12:30 PM) or type "None"'
        required: true
        default: '3:00 AM'
      delete_kachra:
        description: 'Delete old workflows history (Kachra)?'
        required: true
        type: boolean
        default: true

permissions:
  contents: write
  actions: write

jobs:
  super-manager:
    runs-on: ubuntu-latest
    timeout-minutes: 360 # 6 Hours Max Limit

    steps:
      - name: Get Code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run Super Manager Loop (Every 5 Minutes)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          STOP_TIME: ${{ inputs.stop_time }}
          DELETE_KACHRA: ${{ inputs.delete_kachra }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          cat << 'EOF' > super_manager.js
          const fs = require('fs');
          const { execSync } = require('child_process');

          const LOOP_DURATION = 21000000; // 5 hours 50 minutes
          const CHECK_INTERVAL = 300000; // Exact 5 Minutes
          const startMs = Date.now();
          const currentRunId = process.env.GITHUB_RUN_ID;
          const stopTimeInput = (process.env.STOP_TIME || '3:00 AM').trim();
          
          // Strict Boolean Logic Apply Kar Di Gayi Hai
          const rawDeleteInput = (process.env.DELETE_KACHRA || 'true').trim().toLowerCase();
          const deleteKachra = rawDeleteInput === 'true'; 

          function getPktTime(offsetMs = 0) {
            return new Date(Date.now() + offsetMs).toLocaleString('en-US', { 
              timeZone: 'Asia/Karachi', 
              hour12: true, month: 'short', day: '2-digit', hour: '2-digit', minute: '2-digit', second: '2-digit' 
            });
          }

          console.log('\n==================================================');
          console.log(`[*] SUPER MANAGER STARTED AT: ${getPktTime()}`);
          console.log(`[*] TARGET AUTO-STOP TIME: ${stopTimeInput}`);
          console.log(`[*] AUTO-DELETE KACHRA: ${deleteKachra ? 'TRUE (Enabled)' : 'FALSE (Disabled)'}`);
          console.log('==================================================');

          async function startLoop() {
            let loopCount = 1;

            while (Date.now() - startMs < LOOP_DURATION) {
              try {
                const currentUptime = ((Date.now() - startMs) / 60000).toFixed(1);
                
                console.log('\n--------------------------------------------------');
                console.log(`[⏱️] SYSTEM CHECK ROUND #${loopCount}`);
                console.log(`[📍] Current Time: ${getPktTime()} (PKT)`);
                console.log('--------------------------------------------------');

                const nowUtc = new Date();
                const pktDate = new Date(nowUtc.getTime() + (5 * 60 * 60 * 1000));
                const pktHours = pktDate.getUTCHours();
                const pktMinutes = pktDate.getUTCMinutes();

                // =========================================================================
                // 🛑 AUTO CLEANUP RESET CHECK (Stop Time Logic)
                // =========================================================================
                let triggerCleanup = false;
                if (stopTimeInput.toLowerCase() !== 'none') {
                  const timeMatch = stopTimeInput.match(/(\d{1,2}):(\d{2})\s*(AM|PM)/i);
                  if (timeMatch) {
                    let [_, stopHoursStr, stopMinutesStr, stopAmpm] = timeMatch;
                    let stopHours = parseInt(stopHoursStr);
                    let stopMinutes = parseInt(stopMinutesStr);

                    if (stopAmpm.toUpperCase() === 'PM' && stopHours < 12) stopHours += 12;
                    if (stopAmpm.toUpperCase() === 'AM' && stopHours === 12) stopHours = 0;

                    const currentTotalMins = (pktHours * 60) + pktMinutes;
                    const stopTotalMins = (stopHours * 60) + stopMinutes;

                    let diffMins = currentTotalMins - stopTotalMins;
                    if (diffMins < 0) diffMins += 24 * 60; 

                    if (diffMins >= 0 && diffMins <= 15) {
                      triggerCleanup = true;
                    }
                  }
                }

                if (triggerCleanup) {
                  console.log('\n==================================================');
                  console.log(`[🛑] TARGET TIME (${stopTimeInput}) REACHED! STARTING SYSTEM SHUTDOWN...\n`);
                  
                  let otherRunsPending = true;
                  let attempts = 0;

                  // Cancellations for other pending streams
                  while (otherRunsPending && attempts < 6) {
                    attempts++;
                    const runsJson = execSync('gh run list --status in_progress --json databaseId,workflowName', { encoding: 'utf8' });
                    const runs = JSON.parse(runsJson || '[]');
                    const otherRuns = runs.filter(r => String(r.databaseId) !== String(currentRunId));

                    if (otherRuns.length === 0) {
                      otherRunsPending = false;
                      break;
                    }
                    for (const r of otherRuns) {
                      try { execSync(`gh run cancel ${r.databaseId}`, { stdio: 'ignore' }); } catch(e) {}
                    }
                    if (otherRunsPending) await new Promise(r => setTimeout(r, 30000));
                  }

                  // Kachra Deletion Logic (Now strictly Boolean)
                  if (deleteKachra) {
                    console.log('\n[*] Deleting ALL old workflows (Kachra)...');
                    try {
                      const kachraJson = execSync('gh run list --limit 1000 --json databaseId', { encoding: 'utf8' });
                      const kachraRuns = JSON.parse(kachraJson || '[]');
                      
                      let delCount = 0;
                      for (const r of kachraRuns) {
                        if (String(r.databaseId) !== String(currentRunId)) {
                          try { 
                            execSync(`gh api -X DELETE /repos/${process.env.GH_REPO}/actions/runs/${r.databaseId}`, { stdio: 'ignore' }); 
                            delCount++; 
                          } catch(e) {}
                        }
                      }
                      console.log(`[+] Safai Mukammal! ${delCount} purane workflows delete ho gaye.`);
                    } catch(err) {
                      console.log('[-] Deletion Error:', err.message);
                    }
                  }

                  console.log(`[🛑] Manager shutting down cleanly...`);
                  process.exit(0);
                }

                // =========================================================================
                // 🔄 NORMAL MODE: STREAM PULL & CHECKS
                // =========================================================================
                execSync('git pull origin main --rebase', { stdio: 'ignore' });
                
                // =========================================================================
                // 🛑 MIDDLE CANCEL LOGIC (Scheduled & Multi-Channel)
                // =========================================================================
                if (fs.existsSync('cancel.csv')) {
                  const cancelData = fs.readFileSync('cancel.csv', 'utf8');
                  const cLines = cancelData.trim().split('\n');
                  let cChangesMade = false;
                  let updatedCancelCsv = [cLines[0]]; // Header
                  let channelsToCancel = [];
                  const nowUtcCancel = new Date();

                  for (let i = 1; i < cLines.length; i++) {
                    if (!cLines[i].trim()) continue;
                    const parts = cLines[i].split(',').map(s => s.trim());
                    if (parts.length < 2) { updatedCancelCsv.push(cLines[i]); continue; }
                    
                    const [channelName, cancelTimeStr] = parts;

                    if (!cancelTimeStr || cancelTimeStr.toLowerCase() === 'none' || cancelTimeStr.toLowerCase() === 'done') {
                      updatedCancelCsv.push(cLines[i]);
                      continue;
                    }

                    if (cancelTimeStr.toLowerCase() === 'now') {
                      channelsToCancel.push(channelName);
                      updatedCancelCsv.push(`${channelName},Done`);
                      cChangesMade = true;
                      continue;
                    }

                    const match = cancelTimeStr.match(/(\d{4}-\d{2}-\d{2})\s+(\d{1,2}):(\d{2})\s+(AM|PM)/i);
                    if (match) {
                      let [_, datePart, hours, minutes, ampm] = match;
                      hours = parseInt(hours);
                      if (ampm.toUpperCase() === 'PM' && hours < 12) hours += 12;
                      if (ampm.toUpperCase() === 'AM' && hours === 12) hours = 0;

                      const targetDateStr = `${datePart}T${hours.toString().padStart(2, '0')}:${minutes}:00+05:00`;
                      const targetTime = new Date(targetDateStr);
                      
                      const diffMinutesCancel = (targetTime - nowUtcCancel) / (1000 * 60);

                      if (diffMinutesCancel <= 0 && diffMinutesCancel >= -30) { 
                        channelsToCancel.push(channelName);
                        updatedCancelCsv.push(`${channelName},Done`);
                        cChangesMade = true;
                      } else {
                        updatedCancelCsv.push(cLines[i]); 
                      }
                    } else {
                      updatedCancelCsv.push(cLines[i]); 
                    }
                  }

                  if (channelsToCancel.length > 0) {
                    console.log(`\n[🚨] MID-STREAM CANCEL TRIGGERED FOR: ${channelsToCancel.join(', ')}`);
                    try {
                      const activeRunsJson = execSync('gh run list --status in_progress --json databaseId,name,displayTitle', { encoding: 'utf8' });
                      const activeRuns = JSON.parse(activeRunsJson || '[]');
                      
                      for (const chan of channelsToCancel) {
                        let found = false;
                        for (const r of activeRuns) {
                          if (r.name.includes(chan) || r.displayTitle.includes(chan)) {
                            console.log(`[-] Killing Run ID: ${r.databaseId} for Channel: ${chan}`);
                            execSync(`gh run cancel ${r.databaseId}`, { stdio: 'ignore' });
                            found = true;
                          }
                        }
                        if (!found) console.log(`[!] No active workflow found for ${chan}`);
                      }
                    } catch(e) {
                      console.log('[!] Error cancelling specific streams:', e.message);
                    }
                  }

                  if (cChangesMade) {
                    fs.writeFileSync('cancel.csv', updatedCancelCsv.join('\n') + '\n');
                    execSync('git add cancel.csv && git commit -m "Bot: Processed Scheduled Cancels" && git push', { stdio: 'ignore' });
                  }
                }

                // 🎬 EXISTING STREAMS.CSV LOGIC 🎬
                if (fs.existsSync('streams.csv')) {
                  const csvData = fs.readFileSync('streams.csv', 'utf8');
                  const lines = csvData.trim().split('\n');
                  const header = lines[0];
                  let updatedCsv = [header];
                  let changesMade = false;
                  const nowUtcStreams = new Date();

                  for (let i = 1; i < lines.length; i++) {
                    if (!lines[i].trim()) continue;
                    const parts = lines[i].split(',').map(s => s.trim());
                    if (parts.length < 6) { updatedCsv.push(lines[i]); continue; }
                    
                    const [url, channel, quality, server, startTimeStr, duration] = parts;
                    if (!startTimeStr || startTimeStr === 'None' || startTimeStr === '') {
                      updatedCsv.push(lines[i]); continue;
                    }

                    const match = startTimeStr.match(/(\d{4}-\d{2}-\d{2})\s+(\d{1,2}):(\d{2})\s+(AM|PM)/i);
                    if (match) {
                      let [_, datePart, hours, minutes, ampm] = match;
                      hours = parseInt(hours);
                      if (ampm.toUpperCase() === 'PM' && hours < 12) hours += 12;
                      if (ampm.toUpperCase() === 'AM' && hours === 12) hours = 0;

                      const targetDateStr = `${datePart}T${hours.toString().padStart(2, '0')}:${minutes}:00+05:00`;
                      const targetTime = new Date(targetDateStr);
                      const diffMinutesStream = (targetTime - nowUtcStreams) / (1000 * 60);

                      if (diffMinutesStream <= 20 && diffMinutesStream >= -5) {
                        const formattedUrls = url.replace(/\|/g, ',');
                        console.log(`\n[🚀] TRIGGER WINDOW HIT! Starting Channel ${channel}`);
                        const cmd = `gh workflow run main.yml -f target_urls="${formattedUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f custom_duration="${duration}"`;
                        try {
                          execSync(cmd, { stdio: 'inherit' });
                          changesMade = true;
                        } catch(e) { updatedCsv.push(lines[i]); }
                      } else if (diffMinutesStream < -5) {
                        changesMade = true; 
                      } else {
                        updatedCsv.push(lines[i]); 
                      }
                    } else {
                      updatedCsv.push(lines[i]);
                    }
                  }

                  if (changesMade) {
                    fs.writeFileSync('streams.csv', updatedCsv.join('\n') + '\n');
                    execSync('git add streams.csv && git commit -m "Bot: Stream queue updated" && git push', { stdio: 'ignore' });
                  }
                }

              } catch(err) {
                console.log('[!] Loop warning: ' + err.message);
              }

              console.log(`[*] Check round ${loopCount} completed.`);
              loopCount++;
              await new Promise(r => setTimeout(r, CHECK_INTERVAL));
            }

            console.log('\n==================================================');
            console.log(`[*] 5h 50m Completed at ${getPktTime()}! Triggering next Stream Manager...`);
            try {
              // Yahan boolean value ko strictly true/false string format mein aagay bheja ja raha hai
              const nextManagerCmd = `gh workflow run stream-manager.yml -f stop_time="${stopTimeInput}" -f delete_kachra="${deleteKachra}"`;
              execSync(nextManagerCmd, { stdio: 'inherit' });
              console.log('[+] Handover complete! Shutting down cleanly.');
            } catch(err) {}
            process.exit(0);
          }

          startLoop();
          EOF
          
          node super_manager.js


































# name: 24/7 Pro Stream Bot (OBS Test)

# run-name: "OBS Stream - Channel ${{ inputs.okru_stream_channel }}"

# on:
#   workflow_dispatch:
#     inputs:
#       target_urls:
#         description: 'Comma-separated Target URLs (Server1, Server2, Server3)'
#         required: true
#         type: string
#         default: 'https://dadocric.st/player.php?id=starsp3&v=m'
#       okru_stream_channel:
#         description: 'OK.ru Broadcast Channel (1 to 8)'
#         required: true
#         type: string
#         default: '1'
#       server_selection:
#         description: 'Target Server Button - Type "None" to skip'
#         required: true
#         type: string
#         default: 'None'
#       stream_quality:
#         description: 'Select Stream Quality'
#         required: true
#         type: choice
#         options:
#           - 'Original (1080p Max)'
#           - '1080p'
#           - '720p'
#           - '480p'
#           - '360p'
#         default: 'Original (1080p Max)'
#       proxy_engine:
#         description: 'Network Proxy Engine (Bypass Blocking)'
#         required: true
#         type: choice
#         options:
#           - 'Cloudflare WARP (Recommended)'
#           - 'None (Direct GitHub IP)'
#         default: 'Cloudflare WARP (Recommended)'
#       custom_duration:
#         description: 'Exact Duration to run (e.g., 4h 30m, 5h). Type "None" for auto-overlap.'
#         required: true
#         type: string
#         default: 'None'

# permissions:
#   contents: write
#   actions: write 

# jobs:
#   stream-and-record:
#     runs-on: ubuntu-latest
#     timeout-minutes: 360 

#     steps:
#       - name: Get Code
#         uses: actions/checkout@v3

#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'

#       # 🔥 YAHAN CHANGE KIYA HAI - xdotool aur wmctrl add kiye hain
#       - name: Install Dependencies & OBS Studio
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y xvfb pulseaudio dbus-x11 obs-studio xdotool wmctrl
#           npm install

#       # 🌟 CONDITIONAL CLOUDFLARE WARP INSTALLATION
#       - name: Install & Connect Cloudflare WARP (Anti-Block Bypass)
#         if: github.event.inputs.proxy_engine == 'Cloudflare WARP (Recommended)'
#         run: |
#           curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
#           echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
#           sudo apt-get update && sudo apt-get install -y cloudflare-warp
          
#           # Stable Cloudflare WARP Proxy Engine
#           warp-cli --accept-tos registration new
#           warp-cli --accept-tos mode proxy
#           warp-cli --accept-tos proxy port 40000
#           warp-cli --accept-tos connect
#           sleep 5
#           echo "[+] Cloudflare WARP Proxy Shield is UP on Port 40000"

#       # 👇 NAYA AUTOMATED DOWNLOAD & EXTRACT STEP 👇
#       - name: Download & Extract uBlock Lite Extension
#         run: |
#           wget -O ublock-lite.zip https://github.com/uBlockOrigin/uBOL-home/releases/download/2026.705.2152/uBOLite_2026.705.2152.chromium.zip
#           unzip ublock-lite.zip -d ublock-lite
#           echo "[+] Extension Downloaded & Extracted Successfully in 'ublock-lite' folder!"
      
#       - name: Run OBS Stream Engine
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
#           TARGET_URLS: ${{ github.event.inputs.target_urls }}
#           OKRU_STREAM_ID: ${{ github.event.inputs.okru_stream_channel }}
#           SERVER_SELECTION: ${{ github.event.inputs.server_selection }}
#           STREAM_QUALITY: ${{ github.event.inputs.stream_quality }}
#           PROXY_ENGINE: ${{ github.event.inputs.proxy_engine }}
#           CUSTOM_DURATION: ${{ github.event.inputs.custom_duration }}
#         run: |
#           # Virtual Audio Setup
#           pulseaudio -D --exit-idle-time=-1
#           pacmd load-module module-virtual-sink sink_name=v1
#           pacmd set-default-sink v1
#           pacmd set-default-source v1.monitor
          
#           # 🌟 SMOOTHNESS FIX: Enforced 60Hz refresh rate on the virtual display to eliminate tearing
#           xvfb-run -s "-screen 0 1920x1080x24 -ac -r -extension RANDR" node stream.js





















































# ========================================================================================
# //     ================================================================================================
# //     ========================================================================================================
# ========================================================================================
# //     ================================================================================================
# //     ========================================================================================================
# ========================================================================================
# //     ================================================================================================
# //     ========================================================================================================
# ========================================================================================
# //     ================================================================================================
# //     ========================================================================================================









































# firebox

# name: 24/7 Pro Stream Bot (OBS Test)

# run-name: "OBS Stream - Channel ${{ inputs.okru_stream_channel }}"

# on:
#   workflow_dispatch:
#     inputs:
#       target_urls:
#         description: 'Comma-separated Target URLs (Server1, Server2, Server3)'
#         required: true
#         type: string
#         default: 'https://dadocric.st/player.php?id=starsp3&v=m'
#       okru_stream_channel:
#         description: 'OK.ru Broadcast Channel (1 to 8)'
#         required: true
#         type: string
#         default: '1'
#       server_selection:
#         description: 'Target Server Button - Type "None" to skip'
#         required: true
#         type: string
#         default: 'None'
#       stream_quality:
#         description: 'Select Stream Quality'
#         required: true
#         type: choice
#         options:
#           - 'Original (1080p Max)'
#           - '1080p'
#           - '720p'
#           - '480p'
#           - '360p'
#         default: 'Original (1080p Max)'
#       proxy_engine:
#         description: 'Network Proxy Engine (Bypass Blocking)'
#         required: true
#         type: choice
#         options:
#           - 'Cloudflare WARP (Recommended)'
#           - 'None (Direct GitHub IP)'
#         default: 'Cloudflare WARP (Recommended)'
#       custom_duration:
#         description: 'Exact Duration to run (e.g., 4h 30m, 5h). Type "None" for auto-overlap.'
#         required: true
#         type: string
#         default: 'None'

# permissions:
#   contents: write
#   actions: write 

# jobs:
#   stream-and-record:
#     runs-on: ubuntu-latest
#     timeout-minutes: 360 

#     steps:
#       - name: Get Code
#         uses: actions/checkout@v3

#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'

#       # 🔥 YAHAN UPDATE KIYA HAI - Firefox binaries aur dependencies add ki hain
#       - name: Install Dependencies & OBS Studio
#         env:
#           PUPPETEER_SKIP_DOWNLOAD: "true" # 👈 Puppeteer ko download se rokne ke liye
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y xvfb pulseaudio dbus-x11 obs-studio xdotool wmctrl firefox
#           npm install
#           # npx puppeteer browsers install firefox (Yeh line remove kar di hai)
          

#       # 🌟 CONDITIONAL CLOUDFLARE WARP INSTALLATION
#       - name: Install & Connect Cloudflare WARP (Anti-Block Bypass)
#         if: github.event.inputs.proxy_engine == 'Cloudflare WARP (Recommended)'
#         run: |
#           curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
#           echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
#           sudo apt-get update && sudo apt-get install -y cloudflare-warp
          
#           # Stable Cloudflare WARP Proxy Engine
#           warp-cli --accept-tos registration new
#           warp-cli --accept-tos mode proxy
#           warp-cli --accept-tos proxy port 40000
#           warp-cli --accept-tos connect
#           sleep 5
#           echo "[+] Cloudflare WARP Proxy Shield is UP on Port 40000"

#       # NOTE: Purana uBlock Lite zip download step yahan se delete kar diya gaya hai kyunke wo chromium ke liye tha.
      
#       - name: Run OBS Stream Engine
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
#           TARGET_URLS: ${{ github.event.inputs.target_urls }}
#           OKRU_STREAM_ID: ${{ github.event.inputs.okru_stream_channel }}
#           SERVER_SELECTION: ${{ github.event.inputs.server_selection }}
#           STREAM_QUALITY: ${{ github.event.inputs.stream_quality }}
#           PROXY_ENGINE: ${{ github.event.inputs.proxy_engine }}
#           CUSTOM_DURATION: ${{ github.event.inputs.custom_duration }}
#         run: |
#           # Virtual Audio Setup
#           pulseaudio -D --exit-idle-time=-1
#           pacmd load-module module-virtual-sink sink_name=v1
#           pacmd set-default-sink v1
#           pacmd set-default-source v1.monitor
          
#           # 🌟 SMOOTHNESS FIX: Enforced 60Hz refresh rate on the virtual display to eliminate tearing
#           xvfb-run -s "-screen 0 1920x1080x24 -ac -r -extension RANDR" node stream.js







          





# name: 24/7 Pro Stream Bot (OBS Test)

# run-name: "OBS Stream - Channel ${{ inputs.okru_stream_channel }}"

# on:
#   workflow_dispatch:
#     inputs:
#       target_urls:
#         description: 'Comma-separated Target URLs (Server1, Server2, Server3)'
#         required: true
#         type: string
#         default: 'https://dadocric.st/player.php?id=starsp3&v=m'
#       okru_stream_channel:
#         description: 'OK.ru Broadcast Channel (1 to 8)'
#         required: true
#         type: string
#         default: '1'
#       server_selection:
#         description: 'Target Server Button - Type "None" to skip'
#         required: true
#         type: string
#         default: 'None'
#       stream_quality:
#         description: 'Select Stream Quality'
#         required: true
#         type: choice
#         options:
#           - 'Original (1080p Max)'
#           - '1080p'
#           - '720p'
#           - '480p'
#           - '360p'
#         default: 'Original (1080p Max)'
#       proxy_engine:
#         description: 'Network Proxy Engine (Bypass Blocking)'
#         required: true
#         type: choice
#         options:
#           - 'Cloudflare WARP (Recommended)'
#           - 'None (Direct GitHub IP)'
#         default: 'Cloudflare WARP (Recommended)'
#       custom_duration:
#         description: 'Exact Duration to run (e.g., 4h 30m, 5h). Type "None" for auto-overlap.'
#         required: true
#         type: string
#         default: 'None'

# permissions:
#   contents: write
#   actions: write 

# jobs:
#   stream-and-record:
#     runs-on: ubuntu-latest
#     timeout-minutes: 360 

#     steps:
#       - name: Get Code
#         uses: actions/checkout@v3

#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'

#       # 🔥 YAHAN CHANGE KIYA HAI - xdotool aur wmctrl add kiye hain
#       - name: Install Dependencies & OBS Studio
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y xvfb pulseaudio dbus-x11 obs-studio xdotool wmctrl
#           npm install

#       # 🌟 CONDITIONAL CLOUDFLARE WARP INSTALLATION
#       - name: Install & Connect Cloudflare WARP (Anti-Block Bypass)
#         if: github.event.inputs.proxy_engine == 'Cloudflare WARP (Recommended)'
#         run: |
#           curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
#           echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
#           sudo apt-get update && sudo apt-get install -y cloudflare-warp
          
#           # Stable Cloudflare WARP Proxy Engine
#           warp-cli --accept-tos registration new
#           warp-cli --accept-tos mode proxy
#           warp-cli --accept-tos proxy port 40000
#           warp-cli --accept-tos connect
#           sleep 5
#           echo "[+] Cloudflare WARP Proxy Shield is UP on Port 40000"

#       # 👇 NAYA AUTOMATED DOWNLOAD & EXTRACT STEP 👇
#       - name: Download & Extract uBlock Lite Extension
#         run: |
#           wget -O ublock-lite.zip https://github.com/uBlockOrigin/uBOL-home/releases/download/2026.705.2152/uBOLite_2026.705.2152.chromium.zip
#           unzip ublock-lite.zip -d ublock-lite
#           echo "[+] Extension Downloaded & Extracted Successfully in 'ublock-lite' folder!"
      
#       - name: Run OBS Stream Engine
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
#           TARGET_URLS: ${{ github.event.inputs.target_urls }}
#           OKRU_STREAM_ID: ${{ github.event.inputs.okru_stream_channel }}
#           SERVER_SELECTION: ${{ github.event.inputs.server_selection }}
#           STREAM_QUALITY: ${{ github.event.inputs.stream_quality }}
#           PROXY_ENGINE: ${{ github.event.inputs.proxy_engine }}
#           CUSTOM_DURATION: ${{ github.event.inputs.custom_duration }}
#         run: |
#           # Virtual Audio Setup
#           pulseaudio -D --exit-idle-time=-1
#           pacmd load-module module-virtual-sink sink_name=v1
#           pacmd set-default-sink v1
#           pacmd set-default-source v1.monitor
          
#           # 🌟 SMOOTHNESS FIX: Enforced 60Hz refresh rate on the virtual display to eliminate tearing
#           xvfb-run -s "-screen 0 1920x1080x24 -ac -r -extension RANDR" node stream.js










# // ================================= upper jina bey code hai woo simple and single url k hai short ====================================
# // ================================= upper jina bey code hai woo simple and single url k hai short ====================================
# // ================================= upper jina bey code hai woo simple and single url k hai short ====================================
# // ================================= upper jina bey code hai woo simple and single url k hai short ====================================
# // ================================= upper jina bey code hai woo simple and single url k hai short ====================================
# // ================================= upper jina bey code hai woo simple and single url k hai short ====================================
































































































# ================== yeh king hey below yeh original haoi %^^%$%&***@^#%%##&#*( =================

















# name: 24/7 Pro Stream Bot (OBS Test)

# run-name: "OBS Stream - Channel ${{ inputs.okru_stream_channel }}"

# on:
#   workflow_dispatch:
#     inputs:
#       target_urls:
#         description: 'Comma-separated Target URLs (Server1, Server2, Server3)'
#         required: true
#         type: string
#         default: 'https://dadocric.st/player.php?id=starsp3&v=m'
#       okru_stream_channel:
#         description: 'OK.ru Broadcast Channel (1 to 8)'
#         required: true
#         type: string
#         default: '1'
#       server_selection:
#         description: 'Target Server Button - Type "None" to skip'
#         required: true
#         type: string
#         default: 'None'
#       stream_quality:
#         description: 'Select Stream Quality'
#         required: true
#         type: choice
#         options:
#           - 'Original (1080p Max)'
#           - '1080p'
#           - '720p'
#           - '480p'
#           - '360p'
#         default: 'Original (1080p Max)'
#       proxy_engine:
#         description: 'Network Proxy Engine (Bypass Blocking)'
#         required: true
#         type: choice
#         options:
#           - 'Cloudflare WARP (Recommended)'
#           - 'None (Direct GitHub IP)'
#         default: 'Cloudflare WARP (Recommended)'
#       custom_duration:
#         description: 'Exact Duration to run (e.g., 4h 30m, 5h). Type "None" for auto-overlap.'
#         required: true
#         type: string
#         default: 'None'

# permissions:
#   contents: write
#   actions: write 

# jobs:
#   stream-and-record:
#     runs-on: ubuntu-latest
#     timeout-minutes: 360 

#     steps:
#       - name: Get Code
#         uses: actions/checkout@v3

#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'

#       - name: Install Dependencies & OBS Studio
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y xvfb pulseaudio dbus-x11 obs-studio
#           npm install

#       # 🌟 CONDITIONAL CLOUDFLARE WARP INSTALLATION
#       - name: Install & Connect Cloudflare WARP (Anti-Block Bypass)
#         if: github.event.inputs.proxy_engine == 'Cloudflare WARP (Recommended)'
#         run: |
#           curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
#           echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
#           sudo apt-get update && sudo apt-get install -y cloudflare-warp
          
#           # Stable Cloudflare WARP Proxy Engine
#           warp-cli --accept-tos registration new
#           warp-cli --accept-tos mode proxy
#           warp-cli --accept-tos proxy port 40000
#           warp-cli --accept-tos connect
#           sleep 5
#           echo "[+] Cloudflare WARP Proxy Shield is UP on Port 40000"


#       # 👇 NAYA AUTOMATED DOWNLOAD & EXTRACT STEP 👇
#       - name: Download & Extract uBlock Lite Extension
#         run: |
#           wget -O ublock-lite.zip https://github.com/uBlockOrigin/uBOL-home/releases/download/2026.705.2152/uBOLite_2026.705.2152.chromium.zip
#           unzip ublock-lite.zip -d ublock-lite
#           echo "[+] Extension Downloaded & Extracted Successfully in 'ublock-lite' folder!"
#       # 👆 ======================================== 👆
      
#       - name: Run OBS Stream Engine
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
#           TARGET_URLS: ${{ github.event.inputs.target_urls }}
#           OKRU_STREAM_ID: ${{ github.event.inputs.okru_stream_channel }}
#           SERVER_SELECTION: ${{ github.event.inputs.server_selection }}
#           STREAM_QUALITY: ${{ github.event.inputs.stream_quality }}
#           PROXY_ENGINE: ${{ github.event.inputs.proxy_engine }}
#           CUSTOM_DURATION: ${{ github.event.inputs.custom_duration }}
#         run: |
#           # Virtual Audio Setup
#           pulseaudio -D --exit-idle-time=-1
#           pacmd load-module module-virtual-sink sink_name=v1
#           pacmd set-default-sink v1
#           pacmd set-default-source v1.monitor
          
#           # 🌟 SMOOTHNESS FIX: Enforced 60Hz refresh rate on the virtual display to eliminate tearing
#           xvfb-run -s "-screen 0 1920x1080x24 -ac -r -extension RANDR" node stream.js

#       - name: Final Leftover Upload
#         if: always() 
#         uses: softprops/action-gh-release@v1
#         with:
#           tag_name: "live-stream-logs"
#           name: "Live Broadcast Logs"
#           body: "Auto updated live stream status."
#           files: |
#             screenshots/*.png
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}















# (* =========== ALhamdullah yeh bilkul teek hai bas iss mey yeh ublock origin lite k setup add karty hai jokey package.json mei hai abey ============== *)





# name: 24/7 Pro Stream Bot (OBS Test)

# run-name: "OBS Stream - Channel ${{ inputs.okru_stream_channel }}"

# on:
#   workflow_dispatch:
#     inputs:
#       target_urls:
#         description: 'Comma-separated Target URLs (Server1, Server2, Server3)'
#         required: true
#         type: string
#         default: 'https://dadocric.st/player.php?id=starsp3&v=m'
#       okru_stream_channel:
#         description: 'OK.ru Broadcast Channel (1 to 8)'
#         required: true
#         type: string
#         default: '1'
#       server_selection:
#         description: 'Target Server Button - Type "None" to skip'
#         required: true
#         type: string
#         default: 'None'
#       stream_quality:
#         description: 'Select Stream Quality'
#         required: true
#         type: choice
#         options:
#           - 'Original (1080p Max)'
#           - '1080p'
#           - '720p'
#           - '480p'
#           - '360p'
#         default: 'Original (1080p Max)'
#       proxy_engine:
#         description: 'Network Proxy Engine (Bypass Blocking)'
#         required: true
#         type: choice
#         options:
#           - 'Cloudflare WARP (Recommended)'
#           - 'None (Direct GitHub IP)'
#         default: 'Cloudflare WARP (Recommended)'
#       custom_duration:
#         description: 'Exact Duration to run (e.g., 4h 30m, 5h). Type "None" for auto-overlap.'
#         required: true
#         type: string
#         default: 'None'

# permissions:
#   contents: write
#   actions: write 

# jobs:
#   stream-and-record:
#     runs-on: ubuntu-latest
#     timeout-minutes: 360 

#     steps:
#       - name: Get Code
#         uses: actions/checkout@v3

#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'

#       - name: Install Dependencies & OBS Studio
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y xvfb pulseaudio dbus-x11 obs-studio
#           npm install

#       # 🌟 CONDITIONAL CLOUDFLARE WARP INSTALLATION
#       - name: Install & Connect Cloudflare WARP (Anti-Block Bypass)
#         if: github.event.inputs.proxy_engine == 'Cloudflare WARP (Recommended)'
#         run: |
#           curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
#           echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
#           sudo apt-get update && sudo apt-get install -y cloudflare-warp
          
#           # Stable Cloudflare WARP Proxy Engine
#           warp-cli --accept-tos registration new
#           warp-cli --accept-tos mode proxy
#           warp-cli --accept-tos proxy port 40000
#           warp-cli --accept-tos connect
#           sleep 5
#           echo "[+] Cloudflare WARP Proxy Shield is UP on Port 40000"

#       - name: Run OBS Stream Engine
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
#           TARGET_URLS: ${{ github.event.inputs.target_urls }}
#           OKRU_STREAM_ID: ${{ github.event.inputs.okru_stream_channel }}
#           SERVER_SELECTION: ${{ github.event.inputs.server_selection }}
#           STREAM_QUALITY: ${{ github.event.inputs.stream_quality }}
#           PROXY_ENGINE: ${{ github.event.inputs.proxy_engine }}
#           CUSTOM_DURATION: ${{ github.event.inputs.custom_duration }}
#         run: |
#           # Virtual Audio Setup
#           pulseaudio -D --exit-idle-time=-1
#           pacmd load-module module-virtual-sink sink_name=v1
#           pacmd set-default-sink v1
#           pacmd set-default-source v1.monitor
          
#           # 🌟 SMOOTHNESS FIX: Enforced 60Hz refresh rate on the virtual display to eliminate tearing
#           xvfb-run -s "-screen 0 1920x1080x24 -ac -r -extension RANDR" node stream.js

#       - name: Final Leftover Upload
#         if: always() 
#         uses: softprops/action-gh-release@v1
#         with:
#           tag_name: "live-stream-logs"
#           name: "Live Broadcast Logs"
#           body: "Auto updated live stream status."
#           files: |
#             screenshots/*.png
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}






















































































































































































































































































































# // ======= below code is : Aapka yeh idea bohot hi professional aur smart hai! Ek Drop-down Menu banana jisme option ho ke "Cloudflare WARP" use karna hai ya "Direct GitHub IP", is se aapko code baar baar change nahi karna padega.

# // Maine system mein ek "Dynamic Proxy Injector" bana diya hai. Ab:

# // Aapko GitHub Actions ke menu mein ek naya option dikhega: Proxy Engine.

# // By default usme Cloudflare WARP (Recommended) selected hoga.

# // Agar aap usay None (Direct GitHub IP) par set karte hain, toh code khud-ba-khud Cloudflare ki installation skip kar dega aur Puppeteer (Chrome) ko direct internet par chala dega ===================


# // ================================= Alhamdullah =====================



# name: 24/7 Pro Stream Bot (OBS Test)

# run-name: "OBS Stream - Channel ${{ inputs.okru_stream_channel }}"

# on:
#   workflow_dispatch:
#     inputs:
#       target_urls:
#         description: 'Comma-separated Target URLs (Server1, Server2, Server3)'
#         required: true
#         type: string
#         default: 'https://dadocric.st/player.php?id=starsp3&v=m'
#       okru_stream_channel:
#         description: 'OK.ru Broadcast Channel (1 to 8)'
#         required: true
#         type: string
#         default: '1'
#       server_selection:
#         description: 'Target Server Button - Type "None" to skip'
#         required: true
#         type: string
#         default: 'None'
#       stream_quality:
#         description: 'Select Stream Quality'
#         required: true
#         type: choice
#         options:
#           - 'Original (1080p Max)'
#           - '1080p'
#           - '720p'
#           - '480p'
#           - '360p'
#         default: 'Original (1080p Max)'
#       custom_duration:
#         description: 'Exact Duration to run (e.g., 4h 30m, 5h). Type "None" for auto-overlap.'
#         required: true
#         type: string
#         default: 'None'

# permissions:
#   contents: write
#   actions: write 

# jobs:
#   stream-and-record:
#     runs-on: ubuntu-latest
#     timeout-minutes: 360 

#     steps:
#       - name: Get Code
#         uses: actions/checkout@v3

#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'

#       - name: Install Dependencies & OBS Studio
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y xvfb pulseaudio dbus-x11 obs-studio
#           npm install

#       - name: Install & Connect Cloudflare WARP (Anti-Block Bypass)
#         run: |
#           curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
#           echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
#           sudo apt-get update && sudo apt-get install -y cloudflare-warp
          
#           # Stable Cloudflare WARP Proxy Engine
#           warp-cli --accept-tos registration new
#           warp-cli --accept-tos mode proxy
#           warp-cli --accept-tos proxy port 40000
#           warp-cli --accept-tos connect
#           sleep 5
#           echo "[+] Cloudflare WARP Proxy Shield is UP on Port 40000"

#       - name: Run OBS Stream Engine
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
#           TARGET_URLS: ${{ github.event.inputs.target_urls }}
#           OKRU_STREAM_ID: ${{ github.event.inputs.okru_stream_channel }}
#           SERVER_SELECTION: ${{ github.event.inputs.server_selection }}
#           STREAM_QUALITY: ${{ github.event.inputs.stream_quality }}
#           CUSTOM_DURATION: ${{ github.event.inputs.custom_duration }}
#         run: |
#           # Virtual Audio Setup
#           pulseaudio -D --exit-idle-time=-1
#           pacmd load-module module-virtual-sink sink_name=v1
#           pacmd set-default-sink v1
#           pacmd set-default-source v1.monitor
          
#           # 🌟 SMOOTHNESS FIX: Enforced 60Hz refresh rate on the virtual display to eliminate tearing
#           xvfb-run -s "-screen 0 1920x1080x24 -ac -r -extension RANDR" node stream.js

#       - name: Final Leftover Upload
#         if: always() 
#         uses: softprops/action-gh-release@v1
#         with:
#           tag_name: "live-stream-logs"
#           name: "Live Broadcast Logs"
#           body: "Auto updated live stream status."
#           files: |
#             screenshots/*.png
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
