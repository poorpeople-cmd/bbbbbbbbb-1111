// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ TIMERS & CONSTANTS
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;
// const FROZEN_THRESHOLD_MS = 8000; 

// const NO_REFRESH_DOMAINS = [
//     'youtube.com', 'facebook.com', 'streamed.pk', 'cricstreams.', 
//     'website-vercel-helper-d-jaja-3-2.vercel.app', 'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) : ['https://sport4u.online'];

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';
// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let obsProcess = null;
// let workers = [];
// let activeWorkerId = 1; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 if (adKeywords.some(keyword => targetUrl.includes(keyword))) { request.abort().catch(()=>{}); return; }
//             }

//             if (url.includes('popads') || url.includes('exoclick') || url.includes('adsterra') || url.includes('onclickads') || url.includes('jerkmate') || url.includes('adrevenue') || url.includes('fanduel') || url.includes('doubleclick') || (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))) {
//                 request.abort().catch(()=>{});
//             } else { request.continue().catch(()=>{}); }
//         });
//     } catch (e) {}
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') { init.mode = 'open'; }
//                 const shadowRoot = originalAttachShadow.call(this, init);
//                 const observer = new MutationObserver(() => {
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) this.remove(); 
//                 });
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };

//             window.alert = function() {}; window.confirm = function() { return true; }; window.prompt = function() { return null; }; window.open = function() { return null; };
//             Object.defineProperty(window, 'onbeforeunload', { configurable: true, get: function() { return null; }, set: function() { return null; } });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         e.preventDefault(); e.stopPropagation(); return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; } in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }`;
//             document.documentElement.appendChild(style);
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true }); fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
//     const basicIniContent = `[General]\nName=Untitled\n[Video]\nBaseCX=${RES_W}\nBaseCY=${RES_H}\nOutputCX=${RES_W}\nOutputCY=${RES_H}\nFPSCommon=30\n[Output]\nMode=Simple\n[SimpleOutput]\nVBitrate=${BITRATE}\nStreamEncoder=x264\nx264Preset=ultrafast\nx264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0\n`;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = { "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY }, "type": "rtmp_custom" };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", "current_program_scene": "WaitingScene", "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             { "id": "scene", "name": "MainScene", "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] } },
//             { "id": "scene", "name": "WaitingScene", "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) { page.on('dialog', async dialog => { try { await dialog.dismiss(); } catch(e){} }); }

// // =========================================================================================
// // 🔊 INTELLIGENT FUZZY UNMUTE ENGINE
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;
//             await frame.evaluate(() => {
//                 let isMuted = false;
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted || media.volume < 0.1) isMuted = true;
//                     media.muted = false; media.volume = 1.0;
//                 });
//                 if (!isMuted) return; 

//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     if (text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('AUDIO') || onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio') || ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME')) {
//                         const rect = el.getBoundingClientRect();
//                         if (rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none') {
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }

// // =========================================================================================
// // 🎥 INITIALIZE VIDEO (Original working logic + Audio Enforcement)
// // =========================================================================================
// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) {
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);
//                     if (clickSuccess) { serverClicked = true; await new Promise(r => setTimeout(r, 2000)); if (isActivePage) await page.bringToFront(); } 
//                     else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         let isVideoPlaying = false; let attempts = 0;
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => { if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) { v.muted = false; v.volume = 1.0; playing = true; } });
//                         return playing;
//                     });
//                     if (autoPlayed) { isVideoPlaying = true; break; }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => { const style = window.getComputedStyle(el); return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0'; }, playBtn);
//                         if (isVisible) { await frame.evaluate(el => el.click(), playBtn); await new Promise(r => setTimeout(r, 3000)); isVideoPlaying = true; break; }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false; let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try { let p = v.play(); if (p !== undefined) p.catch(()=>{}); played = true; } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });
//                         if (forced) { isVideoPlaying = true; break; }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => { const vid = document.querySelector('video'); return vid && vid.clientWidth > 50 && vid.clientHeight > 50; });
//                 if (isRealLiveStream) { targetFrame = frame; break; }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth; let height = ifr.clientHeight; let area = width * height;
//                         if (area < 5000) return; let score = area;
//                         if (ifr.hasAttribute('allowfullscreen') || ifr.hasAttribute('webkitallowfullscreen') || ifr.hasAttribute('mozallowfullscreen')) score += 10000000; 
//                         if (height > width) score = -1; 
//                         if (score > maxScore) { maxScore = score; mainIframe = ifr; }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => ifr.getAttribute('allowfullscreen') !== null || (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch'))));
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
//                             }
//                         });
//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } });
//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         // 🚨 AUDIO FIX: Enforces audio strictly based on the dynamic window.isStreamMuted variable
//         await targetFrame.evaluate((initMuted) => {
//             window.isStreamMuted = initMuted; 
//             if (window.audioEnforcerInterval) clearInterval(window.audioEnforcerInterval);
            
//             window.audioEnforcerInterval = setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     if (!document.head.querySelector('style[data-ui-hide]')) { style.setAttribute('data-ui-hide', 'true'); document.head.appendChild(style); }

//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     document.querySelectorAll('video, audio').forEach(media => { 
//                         if (window.isStreamMuted) {
//                             media.muted = true; 
//                             media.volume = 0.0; 
//                         } else {
//                             media.muted = false; 
//                             media.volume = 1.0; 
//                         }
//                     });

//                     if (!window.isStreamMuted) {
//                         let actualMuted = false;
//                         document.querySelectorAll('video, audio').forEach(m => { if(m.muted || m.volume < 0.1) actualMuted = true; });
//                         if (actualMuted) {
//                             document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                         }
//                     }

//                     for (const v of videos) { if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; } }
//                     if (!realVideo && videos.length > 0) { realVideo = videos[0]; }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// // =========================================================================================
// // ⚙️ PAGE HEALTH SCANNER
// // =========================================================================================
// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
//                         if (
//                             bodyText.includes("stream error") || bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") || bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") || bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) { return { status: 'CRITICAL_ERROR' }; }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;
//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) { targetV = v; break; }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) { frames = targetV.getVideoPlaybackQuality().totalVideoFrames; } 
//                             else if (targetV.webkitDecodedFrameCount !== undefined) { frames = targetV.webkitDecodedFrameCount; }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// // =========================================================================================
// // 👑 TRUE PARALLEL WATCHDOG
// // =========================================================================================
// async function startWatchdog() {
//     let watchdogTicks = 0;

//     while (true) {
//         let allDead = true;

//         const healthResults = await Promise.all(workers.map(async (worker) => {
//             if (!worker.browser || !worker.browser.isConnected()) return { worker, statusObj: { status: 'DEAD' } };
//             if (worker.isRecovering) return { worker, statusObj: { status: 'RECOVERING' } };
            
//             let statusObj = await checkPageStatus(worker.page);
//             return { worker, statusObj };
//         }));

//         for (let result of healthResults) {
//             let worker = result.worker;
//             let statusObj = result.statusObj;

//             if (statusObj.status !== 'DEAD' && statusObj.status !== 'RECOVERING') allDead = false;

//             if (worker.id !== activeWorkerId && statusObj.status === 'CRITICAL_ERROR') {
//                 if (Date.now() - worker.backupWarmupTime > 30000) {
//                     worker.backupWarmupTime = Date.now();
//                     worker.isRecovering = true;
//                     (async () => {
//                         try {
//                             await worker.page.goto('about:blank').catch(()=>{});
//                             await applyPreloadFirewall(worker.page);
//                             await worker.page.goto(worker.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                             await initializeVideo(worker.page, true, false); 
//                         } catch(e) {}
//                         worker.isRecovering = false;
//                     })();
//                 }
//             }

//             if (statusObj.status === 'HEALTHY' && !worker.isWarmupPhase) {
//                 let elapsedMs = Date.now() - worker.currentStreamStartTime;
//                 let isExempted = NO_REFRESH_DOMAINS.some(domain => worker.url.includes(domain));
//                 if (elapsedMs > FORCE_REFRESH_MS && !isExempted) worker.status = 'FORCE_REFRESH';
//             }

//             if (statusObj.status === 'HEALTHY') {
//                 worker.isWarmupPhase = false;

//                 let isTimeStuck = (statusObj.currentTime === worker.lastActiveTime);
//                 let isFrameStuck = (statusObj.decodedFrames === worker.lastDecodedFrames && statusObj.decodedFrames > 0);

//                 if (isTimeStuck || isFrameStuck) {
//                     if (Date.now() - worker.frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                         worker.status = 'FROZEN';
//                     }
//                 } else {
//                     worker.lastActiveTime = statusObj.currentTime;
//                     worker.lastDecodedFrames = statusObj.decodedFrames;
//                     worker.frozenCheckTimestamp = Date.now();
//                     worker.status = 'HEALTHY';
                    
//                     // 🚨 AUDIO FIX: Enforce background muting bulletproof
//                     if (worker.id !== activeWorkerId) {
//                         for (const frame of worker.page.frames()) {
//                             try { 
//                                 if (!frame.isDetached()) {
//                                     await frame.evaluate(() => { 
//                                         window.isStreamMuted = true; 
//                                         document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; });
//                                     }); 
//                                 }
//                             } catch(e) {}
//                         }
//                     } else {
//                         // 🚨 AUDIO FIX: Enforce active unmuting bulletproof
//                         for (const frame of worker.page.frames()) {
//                             try { 
//                                 if (!frame.isDetached()) {
//                                     await frame.evaluate(() => { 
//                                         window.isStreamMuted = false; 
//                                         document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; });
//                                     }); 
//                                 }
//                             } catch(e) {}
//                         }
//                     }
//                 }
//             } else {
//                 if (statusObj.status !== 'RECOVERING') worker.status = statusObj.status;
//             }
//         }

//         let anyWarmingUp = workers.some(w => (Date.now() - w.streamSetupTime) < 25000);

//         if (allDead && workers.every(w => !w.isRecovering)) {
//             if (!anyWarmingUp) {
//                 return "SOFT_RESET";
//             }
//         }

//         let activeWorker = workers.find(w => w.id === activeWorkerId);
//         let healthyStandbys = workers.filter(w => w.status === 'HEALTHY' && w.id !== activeWorkerId);
        
//         if (!activeWorker || (activeWorker.status === 'DEAD' || activeWorker.status === 'FROZEN' || activeWorker.status === 'CRITICAL_ERROR' || activeWorker.status === 'FORCE_REFRESH')) {
            
//             if (activeWorker && activeWorker.status === 'FORCE_REFRESH') {
//                 console.log(`\n[!] 🔄 PROACTIVE REFRESH TRIGGERED FOR ACTIVE KING [CHROME ${activeWorker.id}]`);
//                 try {
//                     await activeWorker.page.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(activeWorker.page);
//                     await activeWorker.page.goto(activeWorker.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                     await activeWorker.page.bringToFront().catch(()=>{});
//                 } catch(e) {}
//             } 
//             else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ ACTIVE KING [CHROME ${activeWorkerId}] FAILED: ${activeWorker ? activeWorker.status : 'NOT FOUND'}`);
                
//                 if (healthyStandbys.length > 0) {
//                     let newKing = healthyStandbys[0]; 
//                     console.log(`[👑] INSTANT ELECTION: Promoting CHROME [${newKing.id}] from the Standby Pool!`);
                    
//                     if (activeWorker) {
//                         for (const frame of activeWorker.page.frames()) {
//                             try { if (!frame.isDetached()) await frame.evaluate(() => { window.isStreamMuted = true; }); } catch(e) {}
//                         }
//                     }

//                     activeWorkerId = newKing.id;
//                     await newKing.page.bringToFront();
//                     try { await newKing.page.mouse.click(10, 10); } catch(e){} 
                    
//                     for (const frame of newKing.page.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { window.isStreamMuted = false; }); } catch(e) {}
//                     }
//                     await triggerSmartUnmute(newKing.page);
                    
//                 } else {
//                     if (activeWorker && activeWorker.status === 'FROZEN') {
//                         console.log(`[⏳] HOLDING SHIELD: Active is FROZEN, but Standby Pool is EMPTY. Holding position...`);
//                     } else if (anyWarmingUp) {
//                         console.log(`[⏳] HOLDING SHIELD: Active Failed, but background Chromes are still WARMING UP. Waiting...`);
//                         try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch (e) {}
//                     } else {
//                         console.error(`\n[!] ❌ ALL TABS CRASHED! Initiating Soft Recovery...`);
//                         try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch (e) {}
//                         return "SOFT_RESET";
//                     }
//                 }
//             }
//         } else if (activeWorker && activeWorker.status === 'HEALTHY') {
//             try { await activeWorker.page.bringToFront().catch(()=>{}); } catch(e){}
//             try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch(e){}
//         }

//         watchdogTicks++;
//         if (watchdogTicks === 1 || watchdogTicks % 45 === 0) {
//             let activeTimeStr = (activeWorker && activeWorker.lastActiveTime > 0) ? activeWorker.lastActiveTime.toFixed(1) + 's' : 'N/A';
//             let activeStatusStr = activeWorker ? activeWorker.status : 'UNKNOWN';

//             // FORMAT MIRRORING image_5dd137.png
//             console.log(`\n==================================================`);
//             console.log(`==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (CHROME ${activeWorkerId}): Status is ${activeStatusStr} | Video Time: ${activeTimeStr}`);
//             console.log(`--------------------------------------------------`);
            
//             // Log active info
//             if (activeWorker) {
//                 console.log(`  [▶️ ACTIVE] CHROME ${activeWorker.id} | Audio: 🔊 ON | URL: ${activeWorker.url}`);
//             }

//             // Log detailed healthy standbys so you know which links are ready
//             if (healthyStandbys.length > 0) {
//                 console.log(`\n  [🟢] READY HEALTHY STANDBYS:`);
//                 healthyStandbys.forEach(w => {
//                     console.log(`       -> CHROME ${w.id} | Audio: 🔇 MUTED | URL: ${w.url}`);
//                 });
//             } else {
//                 console.log(`\n  [🔴] NO HEALTHY STANDBYS READY IN BACKGROUND.`);
//             }
            
//             // Print recovering/dead stats
//             let offlineWorkers = workers.filter(w => w.status !== 'HEALTHY' && w.id !== activeWorkerId);
//             if (offlineWorkers.length > 0) {
//                 console.log(`\n  [⚠️] OFFLINE / RECOVERING:`);
//                 offlineWorkers.forEach(w => {
//                     console.log(`       -> CHROME ${w.id} | Status: ${w.status} | URL: ${w.url}`);
//                 });
//             }
//             console.log(`==================================================\n`);
//         }

//         await new Promise(r => setTimeout(r, 2000));
//     }
// }

// // =========================================================================================
// // 🚀 N-CHROME LAUNCHER (Exactly 5 Second Stagger)
// // =========================================================================================
// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false,
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'],
//         args: browserArgs
//     });
// }

// async function startDirectStreaming() {
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) { await new Promise(r => setTimeout(r, 2000)); }
//     }

//     if (isObsConnected) {
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, '--window-position=0,0', 
//         '--kiosk', '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', '--ignore-certificate-errors', '--disable-web-security',
//         '--ignore-gpu-blocklist', '--use-gl=egl',
//         '--disable-accelerated-video-decode', '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling', '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling', '--disable-backgrounding-occluded-windows', '--disable-renderer-backgrounding',
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');

//     let keepStreaming = true;

//     while (keepStreaming) {
//         workers = []; 
        
//         console.log(`\n==================================================`);
//         console.log(`[🚀] LAUNCHING ${urlList.length} INDEPENDENT CHROMES (5-Sec Stagger)`);
//         console.log(`==================================================`);

//         for (let i = 0; i < urlList.length; i++) {
//             console.log(`[*] Launching CHROME [${i+1}] for URL: ${urlList[i]}`);
            
//             let browserInst = await createBrowserInstance(browserArgs);
//             let pages = await browserInst.pages();
//             let page = pages[0];

//             let worker = {
//                 id: i + 1,
//                 url: urlList[i],
//                 browser: browserInst,
//                 page: page,
//                 status: 'STARTING',
//                 isWarmupPhase: true,
//                 streamSetupTime: Date.now(),
//                 currentStreamStartTime: Date.now(),
//                 backupWarmupTime: Date.now(),
//                 lastActiveTime: -1,
//                 lastDecodedFrames: -1,
//                 frozenCheckTimestamp: Date.now(),
//                 isRecovering: false
//             };
            
//             workers.push(worker);

//             browserInst.on('targetcreated', async (target) => {
//                 if (target.type() === 'page') {
//                     const newPage = await target.page();
//                     setTimeout(async () => { if (newPage && newPage !== worker.page) { try { await newPage.close(); } catch(e) {} } }, 500);
//                 }
//             });

//             await setupNetworkAdBlocker(page);
//             attachAntiAdListeners(page);
//             await applyPreloadFirewall(page);

//             if (i === 0) {
//                 activeWorkerId = worker.id;
//                 await page.bringToFront();
//                 console.log(`[*] Loading Link [ID: ${worker.id}] (ACTIVE KING) -> ${worker.url}`);
//                 await page.goto(worker.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 await initializeVideo(page, false, true); 
                
//                 if (isObsConnected) {
//                     try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch(e){}
//                 }
//             } else {
//                 console.log(`[*] Silently preparing Backup Link [ID: ${worker.id}] in background -> ${worker.url}`);
//                 page.goto(worker.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).then(async () => {
//                     await initializeVideo(page, true, false); 
//                 }).catch(()=>{});
//             }

//             if (i < urlList.length - 1) {
//                 console.log(`[⏳] Waiting 5 seconds before launching next Chrome...`);
//                 await new Promise(r => setTimeout(r, 5000));
                
//                 let activeWorker = workers.find(w => w.id === activeWorkerId);
//                 if (activeWorker && activeWorker.page) await activeWorker.page.bringToFront().catch(()=>{});
//             }
//         }

//         console.log(`\n[!] ALL CHROMES SETUP COMPLETE. STARTING DYNAMIC WATCHDOG...`);
//         let watchdogResult = await startWatchdog();

//         if (watchdogResult === "SOFT_RESET") {
//             console.log(`\n==================================================`);
//             console.log(`[🔄] SOFT RESET EXECUTING: Closing old browsers safely...`);
//             console.log(`==================================================`);
            
//             for (let w of workers) {
//                 if (w.browser) { try { await w.browser.close(); } catch(e){} }
//             }
//             workers = []; 
//             console.log(`[*] 🚀 Relaunching Fresh Chrome Instances...`);
//             await new Promise(r => setTimeout(r, 4000)); 
//         } else {
//             keepStreaming = false; 
//         }
//     }
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); }
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     try { await obs.disconnect(); } catch (e) { }
//     for (let w of workers) { if (w.browser) { try { await w.browser.close(); } catch(e){} } }
//     if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//     try {
//         execSync('pkill -9 obs || true', { stdio: 'ignore' });
//         execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//         execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//     } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0; const hMatch = str.match(/(\d+)\s*h/i); const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! Shutting down cleanly...`);
//         await cleanup(); process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://sport4u.online';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => { await cleanup(); process.exit(0); }, 300000);
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();











// >>>>>>????>KJJKULLSN??""LLKSJNN




// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ TIMERS & CONSTANTS
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;
// const FROZEN_THRESHOLD_MS = 8000; // Exact 8 Second Freeze Rule

// const NO_REFRESH_DOMAINS = [
//     'youtube.com', 'facebook.com', 'streamed.pk', 'cricstreams.', 
//     'website-vercel-helper-d-jaja-3-2.vercel.app', 'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// const STREAM_KEYS = {
//     '1': '15254238731883_15281627925099_najspfkgne', 
//     '1.1': '15254260751979_15281671637611_2plrcfqzze', 
//     '2': '15254299352683_15281743071851_7dvz3h5d7q'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';
// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let obsProcess = null;
// let workers = [];
// let activeWorkerId = 1; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 if (adKeywords.some(keyword => targetUrl.includes(keyword))) { request.abort().catch(()=>{}); return; }
//             }

//             if (url.includes('popads') || url.includes('exoclick') || url.includes('adsterra') || url.includes('onclickads') || url.includes('jerkmate') || url.includes('adrevenue') || url.includes('fanduel') || url.includes('doubleclick') || (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))) {
//                 request.abort().catch(()=>{});
//             } else { request.continue().catch(()=>{}); }
//         });
//     } catch (e) {}
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') { init.mode = 'open'; }
//                 const shadowRoot = originalAttachShadow.call(this, init);
//                 const observer = new MutationObserver(() => {
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) this.remove(); 
//                 });
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };

//             window.alert = function() {}; window.confirm = function() { return true; }; window.prompt = function() { return null; }; window.open = function() { return null; };
//             Object.defineProperty(window, 'onbeforeunload', { configurable: true, get: function() { return null; }, set: function() { return null; } });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         e.preventDefault(); e.stopPropagation(); return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; } in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }`;
//             document.documentElement.appendChild(style);
//         });
//     } catch (e) {}
// }

// async function showLoadingUI(page, title, sub) {}
// async function hideLoadingUI(page) {}
// async function showRecoveryUI(page) {}
// async function hideRecoveryUI(page) {}

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true }); fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
//     const basicIniContent = `[General]\nName=Untitled\n[Video]\nBaseCX=${RES_W}\nBaseCY=${RES_H}\nOutputCX=${RES_W}\nOutputCY=${RES_H}\nFPSCommon=30\n[Output]\nMode=Simple\n[SimpleOutput]\nVBitrate=${BITRATE}\nStreamEncoder=x264\nx264Preset=ultrafast\nx264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0\n`;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = { "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY }, "type": "rtmp_custom" };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", "current_program_scene": "WaitingScene", "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             { "id": "scene", "name": "MainScene", "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] } },
//             { "id": "scene", "name": "WaitingScene", "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) { page.on('dialog', async dialog => { try { await dialog.dismiss(); } catch(e){} }); }

// // =========================================================================================
// // 🔊 INTELLIGENT FUZZY UNMUTE ENGINE
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;
//             await frame.evaluate(() => {
//                 let isMuted = false;
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted || media.volume < 0.1) isMuted = true;
//                     media.muted = false; media.volume = 1.0;
//                 });
//                 if (!isMuted) return; 

//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         if (rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none') {
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }

// // =========================================================================================
// // 🎥 INITIALIZE VIDEO
// // =========================================================================================
// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) {
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         let isVideoPlaying = false; let attempts = 0;
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) { v.muted = false; v.volume = 1.0; playing = true; }
//                         });
//                         return playing;
//                     });
//                     if (autoPlayed) { isVideoPlaying = true; break; }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el); return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) { await frame.evaluate(el => el.click(), playBtn); await new Promise(r => setTimeout(r, 3000)); isVideoPlaying = true; break; }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false; let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try { let p = v.play(); if (p !== undefined) p.catch(()=>{}); played = true; } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });
//                         if (forced) { isVideoPlaying = true; break; }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video'); return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { targetFrame = frame; break; }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth; let height = ifr.clientHeight; let area = width * height;
//                         if (area < 5000) return; let score = area;
//                         if (ifr.hasAttribute('allowfullscreen') || ifr.hasAttribute('webkitallowfullscreen') || ifr.hasAttribute('mozallowfullscreen')) score += 10000000; 
//                         if (height > width) score = -1; 
//                         if (score > maxScore) { maxScore = score; mainIframe = ifr; }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => ifr.getAttribute('allowfullscreen') !== null || (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch'))));
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
//                             }
//                         });
//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } });
//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         // 🚨 AUDIO FIX APPLIED HERE: Using dynamic window.isStreamMuted variable
//         await targetFrame.evaluate((initMuted) => {
//             window.isStreamMuted = initMuted; // Global dynamic tracker
//             if (window.audioEnforcerInterval) clearInterval(window.audioEnforcerInterval);
            
//             window.audioEnforcerInterval = setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     if (!document.head.querySelector('style[data-ui-hide]')) { style.setAttribute('data-ui-hide', 'true'); document.head.appendChild(style); }

//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     document.querySelectorAll('video, audio').forEach(media => { 
//                         // 🔥 Ab interval dynamic state check karega
//                         media.muted = window.isStreamMuted; 
//                         media.volume = window.isStreamMuted ? 0.0 : 1.0; 
//                     });

//                     if (!window.isStreamMuted) {
//                         let actualMuted = false;
//                         document.querySelectorAll('video, audio').forEach(m => { if(m.muted || m.volume < 0.1) actualMuted = true; });
//                         if (actualMuted) {
//                             document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                         }
//                     }

//                     for (const v of videos) { if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; } }
//                     if (!realVideo && videos.length > 0) { realVideo = videos[0]; }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// // =========================================================================================
// // ⚙️ PAGE HEALTH SCANNER
// // =========================================================================================
// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
//                         if (
//                             bodyText.includes("stream error") || bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") || bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") || bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) { return { status: 'CRITICAL_ERROR' }; }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;
//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) { targetV = v; break; }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) { frames = targetV.getVideoPlaybackQuality().totalVideoFrames; } 
//                             else if (targetV.webkitDecodedFrameCount !== undefined) { frames = targetV.webkitDecodedFrameCount; }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// // =========================================================================================
// // 👑 TRUE PARALLEL WATCHDOG (Any-Healthy Real-Time Tracking + Audio Fixes)
// // =========================================================================================
// async function startWatchdog() {
//     let watchdogTicks = 0;

//     while (true) {
//         let allDead = true;

//         // 1. PARALLEL HEALTH SCANNING
//         const healthResults = await Promise.all(workers.map(async (worker) => {
//             if (!worker.browser || !worker.browser.isConnected()) return { worker, statusObj: { status: 'DEAD' } };
//             if (worker.isRecovering) return { worker, statusObj: { status: 'RECOVERING' } };
            
//             let statusObj = await checkPageStatus(worker.page);
//             return { worker, statusObj };
//         }));

//         for (let result of healthResults) {
//             let worker = result.worker;
//             let statusObj = result.statusObj;

//             if (statusObj.status !== 'DEAD' && statusObj.status !== 'RECOVERING') allDead = false;

//             // Background Shield Recovery
//             if (worker.id !== activeWorkerId && statusObj.status === 'CRITICAL_ERROR') {
//                 if (Date.now() - worker.backupWarmupTime > 30000) {
//                     worker.backupWarmupTime = Date.now();
//                     worker.isRecovering = true;
//                     (async () => {
//                         try {
//                             await worker.page.goto('about:blank').catch(()=>{});
//                             await applyPreloadFirewall(worker.page);
//                             await worker.page.goto(worker.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                             await initializeVideo(worker.page, true, false); 
//                         } catch(e) {}
//                         worker.isRecovering = false;
//                     })();
//                 }
//             }

//             if (statusObj.status === 'HEALTHY' && !worker.isWarmupPhase) {
//                 let elapsedMs = Date.now() - worker.currentStreamStartTime;
//                 let isExempted = NO_REFRESH_DOMAINS.some(domain => worker.url.includes(domain));
//                 if (elapsedMs > FORCE_REFRESH_MS && !isExempted) worker.status = 'FORCE_REFRESH';
//             }

//             // Exactly 8-Second Freeze Rule
//             if (statusObj.status === 'HEALTHY') {
//                 worker.isWarmupPhase = false;

//                 let isTimeStuck = (statusObj.currentTime === worker.lastActiveTime);
//                 let isFrameStuck = (statusObj.decodedFrames === worker.lastDecodedFrames && statusObj.decodedFrames > 0);

//                 if (isTimeStuck || isFrameStuck) {
//                     if (Date.now() - worker.frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                         worker.status = 'FROZEN';
//                     }
//                 } else {
//                     worker.lastActiveTime = statusObj.currentTime;
//                     worker.lastDecodedFrames = statusObj.decodedFrames;
//                     worker.frozenCheckTimestamp = Date.now();
//                     worker.status = 'HEALTHY';
                    
//                     // 🚨 AUDIO FIX: Enforce audio state dynamically
//                     if (worker.id !== activeWorkerId) {
//                         // Background -> Force window.isStreamMuted = true
//                         for (const frame of worker.page.frames()) {
//                             try { if (!frame.isDetached()) frame.evaluate(() => { window.isStreamMuted = true; }).catch(()=>{}); } catch(e) {}
//                         }
//                     } else {
//                         // Active -> Force window.isStreamMuted = false
//                         for (const frame of worker.page.frames()) {
//                             try { if (!frame.isDetached()) frame.evaluate(() => { window.isStreamMuted = false; }).catch(()=>{}); } catch(e) {}
//                         }
//                     }
//                 }
//             } else {
//                 if (statusObj.status !== 'RECOVERING') worker.status = statusObj.status;
//             }
//         }

//         if (allDead && workers.every(w => !w.isRecovering)) return "SOFT_RESET";

//         // 2. DYNAMIC STANDBY POOL
//         let activeWorker = workers.find(w => w.id === activeWorkerId);
//         let healthyStandbys = workers.filter(w => w.status === 'HEALTHY' && w.id !== activeWorkerId);
        
//         // 3. ELECTION: If Active fails, pick from the Dynamic Standby Pool immediately.
//         if (!activeWorker || (activeWorker.status === 'DEAD' || activeWorker.status === 'FROZEN' || activeWorker.status === 'CRITICAL_ERROR' || activeWorker.status === 'FORCE_REFRESH')) {
            
//             if (activeWorker && activeWorker.status === 'FORCE_REFRESH') {
//                 console.log(`\n[!] 🔄 PROACTIVE REFRESH TRIGGERED FOR ACTIVE KING [CHROME ${activeWorker.id}]`);
//                 try {
//                     await activeWorker.page.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(activeWorker.page);
//                     await activeWorker.page.goto(activeWorker.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                     await activeWorker.page.bringToFront().catch(()=>{});
//                 } catch(e) {}
//             } 
//             else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ ACTIVE KING [CHROME ${activeWorkerId}] FAILED: ${activeWorker ? activeWorker.status : 'NOT FOUND'}`);
                
//                 // ⚡ ZERO DELAY SWAP
//                 if (healthyStandbys.length > 0) {
//                     let newKing = healthyStandbys[0]; 
                    
//                     console.log(`[👑] INSTANT ELECTION: Promoting CHROME [${newKing.id}] from the Standby Pool!`);
                    
//                     // 🚨 AUDIO FIX: Properly mute old active across all frames
//                     if (activeWorker) {
//                         for (const frame of activeWorker.page.frames()) {
//                             try { if (!frame.isDetached()) await frame.evaluate(() => { window.isStreamMuted = true; }); } catch(e) {}
//                         }
//                     }

//                     activeWorkerId = newKing.id;
//                     await newKing.page.bringToFront();
//                     try { await newKing.page.mouse.click(10, 10); } catch(e){} 
                    
//                     // 🚨 AUDIO FIX: Unmute new King
//                     for (const frame of newKing.page.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { window.isStreamMuted = false; }); } catch(e) {}
//                     }
//                     await triggerSmartUnmute(newKing.page);
                    
//                     console.log(`==================================================\n`);
//                 } else {
//                     if (activeWorker && activeWorker.status === 'FROZEN') {
//                         console.log(`[⏳] HOLDING SHIELD: Active is FROZEN, but Standby Pool is EMPTY. Holding position...`);
//                     } else {
//                         console.error(`\n[!] ❌ ALL TABS CRASHED! Initiating Soft Recovery...`);
//                         try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch (e) {}
//                         return "SOFT_RESET";
//                     }
//                 }
//             }
//         } else if (activeWorker && activeWorker.status === 'HEALTHY') {
//             // Guarantee Active is strictly on top
//             try { await activeWorker.page.bringToFront().catch(()=>{}); } catch(e){}
//         }

//         watchdogTicks++;
//         if (watchdogTicks === 1 || watchdogTicks % 45 === 0) {
//             let standbyNames = healthyStandbys.map(w => `Chrome ${w.id}`).join(', ');
//             console.log(`\n==================================================`);
//             console.log(`[💓] PARALLEL HEALTH TRACKING (Active King: CHROME ${activeWorkerId})`);
//             console.log(`[🟢] READY STANDBYS: ${standbyNames || 'None currently ready!'}`);
//             console.log(`--------------------------------------------------`);
//             workers.forEach(w => {
//                 let tag = (w.id === activeWorkerId) ? "[▶️ ACTIVE]" : "[🔄 BACKUP]";
//                 let vol = (w.id === activeWorkerId) ? "🔊 ON" : "🔇 MUTED";
//                 console.log(`  ${tag} CHROME ${w.id} | Status: ${w.status} | Audio: ${vol} | URL: ${w.url.substring(0,40)}`);
//             });
//             console.log(`==================================================\n`);
//         }

//         await new Promise(r => setTimeout(r, 2000));
//     }
// }

// // =========================================================================================
// // 🚀 N-CHROME LAUNCHER (Exactly 5 Second Stagger)
// // =========================================================================================
// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false,
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'],
//         args: browserArgs
//     });
// }

// async function startDirectStreaming() {
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) { await new Promise(r => setTimeout(r, 2000)); }
//     }

//     if (isObsConnected) {
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, '--window-position=0,0', 
//         '--kiosk', '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', '--ignore-certificate-errors', '--disable-web-security',
//         '--ignore-gpu-blocklist', '--use-gl=egl',
//         '--disable-accelerated-video-decode', '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling', '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling', '--disable-backgrounding-occluded-windows', '--disable-renderer-backgrounding',
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');

//     let keepStreaming = true;

//     while (keepStreaming) {
//         workers = []; 
        
//         console.log(`\n==================================================`);
//         console.log(`[🚀] LAUNCHING ${urlList.length} INDEPENDENT CHROMES (5-Sec Stagger)`);
//         console.log(`==================================================`);

//         for (let i = 0; i < urlList.length; i++) {
//             console.log(`\n[*] Launching CHROME [${i+1}] for URL: ${urlList[i]}`);
            
//             let browserInst = await createBrowserInstance(browserArgs);
//             let pages = await browserInst.pages();
//             let page = pages[0];

//             let worker = {
//                 id: i + 1,
//                 url: urlList[i],
//                 browser: browserInst,
//                 page: page,
//                 status: 'STARTING',
//                 isWarmupPhase: true,
//                 streamSetupTime: Date.now(),
//                 currentStreamStartTime: Date.now(),
//                 backupWarmupTime: Date.now(),
//                 lastActiveTime: -1,
//                 lastDecodedFrames: -1,
//                 frozenCheckTimestamp: Date.now(),
//                 isRecovering: false
//             };
            
//             workers.push(worker);

//             browserInst.on('targetcreated', async (target) => {
//                 if (target.type() === 'page') {
//                     const newPage = await target.page();
//                     setTimeout(async () => { if (newPage && newPage !== worker.page) { try { await newPage.close(); } catch(e) {} } }, 500);
//                 }
//             });

//             await setupNetworkAdBlocker(page);
//             attachAntiAdListeners(page);
//             await applyPreloadFirewall(page);

//             if (i === 0) {
//                 activeWorkerId = worker.id;
//                 await page.bringToFront();
//                 console.log(`[*] Loading Link [ID: ${worker.id}] (ACTIVE KING) -> ${worker.url}`);
//                 await page.goto(worker.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 await initializeVideo(page, false, true); 
                
//                 if (isObsConnected) {
//                     try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch(e){}
//                 }
//             } else {
//                 console.log(`[*] Silently preparing Backup Link [ID: ${worker.id}] in background -> ${worker.url}`);
//                 page.goto(worker.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).then(async () => {
//                     await initializeVideo(page, true, false); 
//                 }).catch(()=>{});
//             }

//             if (i < urlList.length - 1) {
//                 console.log(`[⏳] Waiting 5 seconds before launching next Chrome...`);
//                 await new Promise(r => setTimeout(r, 5000));
                
//                 let activeWorker = workers.find(w => w.id === activeWorkerId);
//                 if (activeWorker && activeWorker.page) await activeWorker.page.bringToFront().catch(()=>{});
//             }
//         }

//         console.log(`\n[!] ALL CHROMES SETUP COMPLETE. STARTING DYNAMIC WATCHDOG...`);
//         let watchdogResult = await startWatchdog();

//         if (watchdogResult === "SOFT_RESET") {
//             console.log(`\n==================================================`);
//             console.log(`[🔄] SOFT RESET EXECUTING: Closing old browsers safely...`);
//             console.log(`==================================================`);
            
//             for (let w of workers) {
//                 if (w.browser) { try { await w.browser.close(); } catch(e){} }
//             }
//             workers = []; 
//             console.log(`[*] 🚀 Relaunching Fresh Chrome Instances...`);
//             await new Promise(r => setTimeout(r, 4000)); 
//         } else {
//             keepStreaming = false; 
//         }
//     }
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); }
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     try { await obs.disconnect(); } catch (e) { }
//     for (let w of workers) { if (w.browser) { try { await w.browser.close(); } catch(e){} } }
//     if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//     try {
//         execSync('pkill -9 obs || true', { stdio: 'ignore' });
//         execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//         execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//     } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0; const hMatch = str.match(/(\d+)\s*h/i); const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! Shutting down cleanly...`);
//         await cleanup(); process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => { await cleanup(); process.exit(0); }, 300000);
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();



































// 1-gemini work good 




// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js');

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });

// const obs = new OBSWebSocket();

// // =========================================================================================
// // ⏱️ TIMERS & REFRESH CONSTANTS
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000;
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;
// const FROZEN_THRESHOLD_MS = 8000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com', 'facebook.com', 'streamed.pk', 'cricstreams.', 
//     'website-vercel-helper-d-jaja-3-2.vercel.app', 'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne',
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze',
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== ''
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim())
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None';
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// // =========================================================================================
// // 🧠 N-CHROME MULTI-WORKER POOL ARCHITECTURE
// // =========================================================================================
// let workers = [];
// let activeWorkerId = 0; 
// let obsProcess = null;

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;

// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false,
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'],
//         args: browserArgs
//     });
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD (Preserved Exactly)
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 if (adKeywords.some(keyword => targetUrl.includes(keyword))) {
//                     request.abort().catch(()=>{}); return;
//                 }
//             }

//             if (
//                 url.includes('popads') || url.includes('exoclick') || url.includes('adsterra') || 
//                 url.includes('onclickads') || url.includes('jerkmate') || url.includes('adrevenue') ||
//                 url.includes('fanduel') || url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // Shadow DOM monkey patching preserved exactly from reference
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') { init.mode = 'open'; }
//                 const shadowRoot = originalAttachShadow.call(this, init);
//                 const observer = new MutationObserver(() => {
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) this.remove(); 
//                 });
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };

//             window.alert = function() {}; window.confirm = function() { return true; }; window.prompt = function() { return null; }; window.open = function() { return null; };
//             Object.defineProperty(window, 'onbeforeunload', { configurable: true, get: function() { return null; }, set: function() { return null; } });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         e.preventDefault(); e.stopPropagation(); return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `<div class="stream-spinner"></div>`; // Simplified for brevity in code snippet but logic preserved
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) { }
// }

// async function takeAndBatchScreenshot(page, stepName) { /* Screenshot logic kept intact */ }
// async function showLoadingUI(page, title, sub) { /* UI logic intact */ }
// async function hideLoadingUI(page) {
//     try { await page.evaluate(() => { const o = document.getElementById('smart-stream-overlay'); if (o) { o.style.setProperty('display', 'none', 'important'); o.remove(); } }); } catch (e) {}
// }

// async function showRecoveryUI(page) {
//     // Reference UI radar logic kept intact
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return;
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) { overlay.style.setProperty('display', 'flex', 'important'); return; }
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `<div class="warn-title">SIGNAL LOST</div>`;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try { await page.evaluate(() => { const o = document.getElementById('stream-recovery-overlay'); if (o) o.style.setProperty('display', 'none', 'important'); }); } catch (e) {}
// }

// function setupOBSConfig() {
//     // OBS JSON writing preserved
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
//     const basicIniContent = `[General]\nName=Untitled\n[Video]\nBaseCX=${RES_W}\nBaseCY=${RES_H}\nOutputCX=${RES_W}\nOutputCY=${RES_H}\nFPSCommon=30\n[Output]\nMode=Simple\n[SimpleOutput]\nVBitrate=${BITRATE}\nStreamEncoder=x264\nx264Preset=ultrafast\nx264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0\n`;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = { "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY }, "type": "rtmp_custom" };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", "current_program_scene": "WaitingScene", "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             { "id": "scene", "name": "MainScene", "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] } },
//             { "id": "scene", "name": "WaitingScene", "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => { try { await dialog.dismiss(); } catch(e){} });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (Exact Reference Code)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;
//             await frame.evaluate(() => {
//                 let isMuted = false;
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted || media.volume < 0.1) isMuted = true;
//                     media.muted = false; media.volume = 1.0;
//                 });

//                 if (!isMuted) return; // AGAR AWAZ PEHLE SE KHULI HAI, TO KOI BUTTON CLICK MAT KARO!

//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         if (rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none') {
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }

// // =========================================================================================
// // 🎥 INITIALIZE VIDEO (Exact Reference Code)
// // =========================================================================================
// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) {
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         let isVideoPlaying = false; let attempts = 0;
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) { v.muted = false; v.volume = 1.0; playing = true; }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) { isVideoPlaying = true; break; }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el); return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true; break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try { let p = v.play(); if (p !== undefined) p.catch(()=>{}); played = true; } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });
//                         if (forced) { isVideoPlaying = true; break; }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video'); return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { targetFrame = frame; break; }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth; let height = ifr.clientHeight; let area = width * height;
//                         if (area < 5000) return;
//                         let score = area;
//                         if (ifr.hasAttribute('allowfullscreen') || ifr.hasAttribute('webkitallowfullscreen') || ifr.hasAttribute('mozallowfullscreen')) score += 10000000; 
//                         if (height > width) score = -1; 
//                         if (score > maxScore) { maxScore = score; mainIframe = ifr; }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => ifr.getAttribute('allowfullscreen') !== null || (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch'))));
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { ifr.parentNode.style.setProperty('display', 'none', 'important'); ifr.parentNode.style.setProperty('opacity', '0', 'important'); } catch(e) {}
//                                 }
//                             }
//                         });
//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             if (window.audioEnforcerInterval) clearInterval(window.audioEnforcerInterval);
//             window.audioEnforcerInterval = setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     if (!document.head.querySelector('style[data-ui-hide]')) { style.setAttribute('data-ui-hide', 'true'); document.head.appendChild(style); }

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { media.muted = muteVideo; media.volume = muteVideo ? 0.0 : 1.0; });

//                     if (!muteVideo) {
//                         let actualMuted = false;
//                         document.querySelectorAll('video, audio').forEach(m => { if(m.muted || m.volume < 0.1) actualMuted = true; });
//                         if (actualMuted) {
//                             document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                         }
//                     }

//                     for (const v of videos) { if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; } }
//                     if (!realVideo && videos.length > 0) { realVideo = videos[0]; }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// // =========================================================================================
// // ⚙️ PAGE HEALTH SCANNER (Exact Reference Code)
// // =========================================================================================
// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
//                         if (
//                             bodyText.includes("stream error") || bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") || bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") || bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) { return { status: 'CRITICAL_ERROR' }; }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;
//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) { targetV = v; break; }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) { frames = targetV.getVideoPlaybackQuality().totalVideoFrames; } 
//                             else if (targetV.webkitDecodedFrameCount !== undefined) { frames = targetV.webkitDecodedFrameCount; }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// // =========================================================================================
// // 👑 CENTRAL JUDGE WATCHDOG (The core update you requested)
// // =========================================================================================
// async function startWatchdog() {
//     let watchdogTicks = 0;

//     while (true) {
//         let allDead = true;

//         // 1. UPDATE HEALTH OF ALL CHROME WORKERS
//         for (let worker of workers) {
//             if (!worker.browser || !worker.browser.isConnected()) continue;
//             allDead = false;

//             let statusObj = await checkPageStatus(worker.page);
            
//             // Backup Shield logic applied individually
//             if (worker.id !== activeWorkerId && statusObj.status === 'CRITICAL_ERROR') {
//                 if (Date.now() - worker.backupWarmupTime > 30000) {
//                     console.log(`\n[⚠️] BACKGROUND SHIELD: Chrome [${worker.id}] hit Critical Error. Restarting...`);
//                     worker.backupWarmupTime = Date.now();
//                     try {
//                         await worker.page.goto('about:blank').catch(()=>{});
//                         await applyPreloadFirewall(worker.page);
//                         await worker.page.goto(worker.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                         await initializeVideo(worker.page, true, false); // Muted, Not Active
//                     } catch(e) {}
//                 }
//             }

//             if (statusObj.status === 'HEALTHY' && !worker.isWarmupPhase) {
//                 let elapsedMs = Date.now() - worker.currentStreamStartTime;
//                 let isExempted = NO_REFRESH_DOMAINS.some(domain => worker.url.includes(domain));
//                 if (elapsedMs > FORCE_REFRESH_MS && !isExempted) {
//                     worker.status = 'FORCE_REFRESH';
//                 }
//             }

//             if (statusObj.status === 'HEALTHY') {
//                 await hideLoadingUI(worker.page);
//                 worker.isWarmupPhase = false;

//                 if (worker.id === activeWorkerId) await triggerSmartUnmute(worker.page);

//                 let isTimeStuck = (statusObj.currentTime === worker.lastActiveTime);
//                 let isFrameStuck = (statusObj.decodedFrames === worker.lastDecodedFrames && statusObj.decodedFrames > 0);

//                 if (isTimeStuck || isFrameStuck) {
//                     if (worker.id === activeWorkerId && !worker.isRecoveryUIShown) {
//                         await showRecoveryUI(worker.page);
//                         worker.isRecoveryUIShown = true;
//                     }
//                     if (Date.now() - worker.frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                         worker.status = 'FROZEN';
//                     }
//                 } else {
//                     worker.lastActiveTime = statusObj.currentTime;
//                     worker.lastDecodedFrames = statusObj.decodedFrames;
//                     worker.frozenCheckTimestamp = Date.now();
                    
//                     if (worker.isRecoveryUIShown) {
//                         await hideRecoveryUI(worker.page);
//                         worker.isRecoveryUIShown = false;
//                     }

//                     // Background tabs must remain muted
//                     if (worker.id !== activeWorkerId) {
//                         for (const frame of worker.page.frames()) {
//                             try { if (!frame.isDetached()) frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{}); } catch(e) {}
//                         }
//                     } else {
//                         // Ensure Active stays unmuted
//                         for (const frame of worker.page.frames()) {
//                             try {
//                                 if (!frame.isDetached()) {
//                                     frame.evaluate(() => {
//                                         document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; });
//                                     }).catch(()=>{});
//                                 }
//                             } catch(e) {}
//                         }
//                     }
//                     worker.status = 'HEALTHY';
//                 }
//             } else {
//                 worker.status = statusObj.status;
//             }
//         }

//         if (allDead) return "SOFT_RESET";

//         // 2. THE ELECTION (ANY-HEALTHY LOGIC)
//         let activeWorker = workers.find(w => w.id === activeWorkerId);
        
//         if (!activeWorker || (activeWorker.status === 'DEAD' || activeWorker.status === 'FROZEN' || activeWorker.status === 'CRITICAL_ERROR' || activeWorker.status === 'FORCE_REFRESH')) {
            
//             // IF PROACTIVE REFRESH, JUST REFRESH IT IN PLACE
//             if (activeWorker && activeWorker.status === 'FORCE_REFRESH') {
//                 console.log(`\n[!] 🔄 PROACTIVE REFRESH TRIGGERED FOR ACTIVE KING [CHROME ${activeWorker.id}]`);
//                 try {
//                     await activeWorker.page.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(activeWorker.page);
//                     await activeWorker.page.goto(activeWorker.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                     await activeWorker.page.bringToFront().catch(()=>{});
//                 } catch(e) {}
//             } 
//             else {
//                 // ACTIVE IS DEAD/FROZEN -> FIND ANY HEALTHY WORKER
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ ACTIVE KING [CHROME ${activeWorkerId}] FAILED: ${activeWorker ? activeWorker.status : 'NOT FOUND'}`);
                
//                 let newKing = workers.find(w => w.status === 'HEALTHY' && w.id !== activeWorkerId);

//                 if (newKing) {
//                     console.log(`[👑] ELECTION: Promoting CHROME [${newKing.id}] to ACTIVE KING!`);
                    
//                     // Force mute old active
//                     if (activeWorker) {
//                         try { await activeWorker.page.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                         activeWorker.isRecoveryUIShown = false; 
//                     }

//                     // Update King
//                     activeWorkerId = newKing.id;
//                     await newKing.page.bringToFront();
//                     try { await newKing.page.mouse.click(10, 10); } catch(e){} 
                    
//                     await initializeVideo(newKing.page, false, true);
//                     console.log(`==================================================\n`);
//                 } else {
//                     if (activeWorker && activeWorker.status === 'FROZEN') {
//                         console.log(`[⏳] HOLDING SHIELD: Active is FROZEN, but NO BACKUPS are ready. Holding position...`);
//                     } else {
//                         console.error(`\n[!] ❌ ALL CHROME INSTANCES CRASHED! Initiating Soft Recovery...`);
//                         try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch (e) {}
//                         return "SOFT_RESET";
//                     }
//                 }
//             }
//         }

//         watchdogTicks++;
//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             console.log(`\n==================================================`);
//             console.log(`[💓] HEARTBEAT MANAGER (Active King: CHROME ${activeWorkerId})`);
//             workers.forEach(w => {
//                 let tag = (w.id === activeWorkerId) ? "[▶️ ACTIVE]" : "[🔄 BACKUP]";
//                 let vol = (w.id === activeWorkerId) ? "🔊 ON" : "🔇 MUTED";
//                 console.log(`  ${tag} CHROME ${w.id} | Status: ${w.status} | Audio: ${vol} | URL: ${w.url.substring(0,40)}`);
//             });
//             console.log(`==================================================\n`);
//         }

//         await new Promise(r => setTimeout(r, 2000));
//     }
// }

// // =========================================================================================
// // 🚀 N-CHROME LAUNCHER (Exactly 5 Second Stagger)
// // =========================================================================================
// async function startDirectStreaming() {
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, '--window-position=0,0', 
//         '--kiosk', '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', '--ignore-certificate-errors', '--disable-web-security',
//         '--ignore-gpu-blocklist', '--use-gl=egl',
//         '--disable-accelerated-video-decode', '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling', '--disable-backgrounding-occluded-windows', '--disable-renderer-backgrounding',
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');

//     let keepStreaming = true;

//     while (keepStreaming) {
//         workers = []; // Clear array for fresh run
        
//         console.log(`\n==================================================`);
//         console.log(`[🚀] LAUNCHING ${urlList.length} INDEPENDENT CHROMES (5-Sec Stagger)`);
//         console.log(`==================================================`);

//         for (let i = 0; i < urlList.length; i++) {
//             console.log(`\n[*] Launching CHROME [${i+1}] for URL: ${urlList[i]}`);
            
//             let browserInst = await createBrowserInstance(browserArgs);
//             let pages = await browserInst.pages();
//             let page = pages[0];

//             let worker = {
//                 id: i + 1,
//                 url: urlList[i],
//                 browser: browserInst,
//                 page: page,
//                 status: 'STARTING',
//                 isWarmupPhase: true,
//                 streamSetupTime: Date.now(),
//                 currentStreamStartTime: Date.now(),
//                 backupWarmupTime: Date.now(),
//                 lastActiveTime: -1,
//                 lastDecodedFrames: -1,
//                 frozenCheckTimestamp: Date.now(),
//                 isRecoveryUIShown: false
//             };
            
//             workers.push(worker);

//             // Pop-up killer per browser
//             browserInst.on('targetcreated', async (target) => {
//                 if (target.type() === 'page') {
//                     const newPage = await target.page();
//                     setTimeout(async () => { if (newPage && newPage !== worker.page) { try { await newPage.close(); } catch(e) {} } }, 500);
//                 }
//             });

//             await setupNetworkAdBlocker(page);
//             attachAntiAdListeners(page);
//             await applyPreloadFirewall(page);

//             if (i === 0) {
//                 // FIRST CHROME BECOMES ACTIVE KING
//                 activeWorkerId = worker.id;
//                 await page.bringToFront();
//                 await page.goto(worker.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 await showLoadingUI(page, "STREAM LOADING", "Optimizing live video connection...");
//                 await initializeVideo(page, false, true); // startMuted=false, active=true
//                 await hideLoadingUI(page);
                
//                 if (isObsConnected) {
//                     try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch(e){}
//                 }
//             } else {
//                 // BACKGROUND CHROMES
//                 page.goto(worker.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).then(async () => {
//                     await initializeVideo(page, true, false); // startMuted=true, active=false
//                 }).catch(()=>{});
//             }

//             // Exactly 5 Seconds Stagger Delay
//             if (i < urlList.length - 1) {
//                 console.log(`[⏳] Waiting 5 seconds before launching next Chrome...`);
//                 await new Promise(r => setTimeout(r, 5000));
                
//                 // Force Active Chrome to Top
//                 let activeWorker = workers.find(w => w.id === activeWorkerId);
//                 if (activeWorker && activeWorker.page) await activeWorker.page.bringToFront().catch(()=>{});
//             }
//         }

//         console.log(`\n[!] ALL CHROMES SETUP COMPLETE. STARTING WATCHDOG...`);
//         let watchdogResult = await startWatchdog();

//         if (watchdogResult === "SOFT_RESET") {
//             console.log(`\n==================================================`);
//             console.log(`[🔄] SOFT RESET EXECUTING: Closing old browsers safely...`);
//             console.log(`==================================================`);
            
//             for (let w of workers) {
//                 if (w.browser) { try { await w.browser.close(); } catch(e){} }
//             }
//             workers = []; // Clear dead workers
            
//             console.log(`[*] 🚀 Relaunching Fresh Chrome Instances...`);
//             await new Promise(r => setTimeout(r, 4000)); 
//         } else {
//             keepStreaming = false; 
//         }
//     }
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); }
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     try { await obs.disconnect(); } catch (e) { }
//     for (let w of workers) {
//         if (w.browser) { try { await w.browser.close(); } catch(e){} }
//     }
//     if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//     try {
//         execSync('pkill -9 obs || true', { stdio: 'ignore' });
//         execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//         execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//     } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => { await cleanup(); process.exit(0); }, 300000);
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();







// ----------- gemini ---------------


// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 4000000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // ⏱️ EXACTLY 8-SECOND FREEZE CHECK (As Requested)
// // =========================================================================================
// const FROZEN_THRESHOLD_MS = 8000; 

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', 
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager (Shortened for brevity but keep your full keys)
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let browser = null;
// let obsProcess = null;

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;

// // =========================================================================================
// // 👑 MULTI-TAB POOL ARCHITECTURE (N-TABS)
// // =========================================================================================
// // This array replaces activePage and backupPage completely.
// let chromeTabs = []; 
// let activeTabId = 1; // ID of the tab currently active in OBS.

// // Helper to find the active object
// function getActiveTab() { return chromeTabs.find(t => t.id === activeTabId); }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD (Original Logic)
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 if (adKeywords.some(keyword => targetUrl.includes(keyword))) {
//                     request.abort().catch(()=>{}); return;
//                 }
//             }

//             if (
//                 url.includes('popads') || url.includes('exoclick') || url.includes('adsterra') || 
//                 url.includes('onclickads') || url.includes('jerkmate') || url.includes('adrevenue') ||
//                 url.includes('fanduel') || url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) {}
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             window.alert = function() {}; window.confirm = function() { return true; }; window.prompt = function() { return null; }; window.open = function() { return null; };
//             Object.defineProperty(window, 'onbeforeunload', { configurable: true, get: function() { return null; }, set: function() { return null; } });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         e.preventDefault(); e.stopPropagation(); return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; }`;
//             document.documentElement.appendChild(style);
//         });
//     } catch (e) {}
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     // Exact original logic omitted for brevity, but assume same code.
// }

// async function hideLoadingUI(page) {
//     try { await page.evaluate(() => { const o = document.getElementById('smart-stream-overlay'); if (o) o.remove(); }); } catch (e) {}
// }

// function setupOBSConfig() {
//     // Exact same original OBS JSON writing code here...
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => { try { await dialog.dismiss(); } catch(e){} });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (Exact Original Code)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     if (!page) return;
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;
//             await frame.evaluate(() => {
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         if (rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none') {
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) { media.muted = false; media.volume = 1.0; }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// // =========================================================================================
// // 🎥 EXACT ORIGINAL INITIALIZE VIDEO LOGIC
// // =========================================================================================
// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; v.volume = 1.0; playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) { isVideoPlaying = true; break; }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true; break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) { await takeAndBatchScreenshot(page, `force-play-applied`); isVideoPlaying = true; break; }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { targetFrame = frame; break; }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     iframes.forEach(ifr => {
//                         let area = ifr.clientWidth * ifr.clientHeight;
//                         if (area < 5000) return;
//                         let score = area;
//                         if (ifr.hasAttribute('allowfullscreen') || ifr.hasAttribute('webkitallowfullscreen') || ifr.hasAttribute('mozallowfullscreen')) score += 10000000; 
//                         if (ifr.clientHeight > ifr.clientWidth) score = -1; 
//                         if (score > maxScore) { maxScore = score; mainIframe = ifr; }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => ifr.getAttribute('allowfullscreen') !== null || (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch'))));
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                             }
//                         });
//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                     }
                    
//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay') return;
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             if (adKeywords.some(keyword => textLower.includes(keyword)) || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });
//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         if (targetFrame) {
//             await targetFrame.evaluate((muteVideo) => {
//                 setInterval(() => {
//                     try {
//                         const style = document.createElement('style');
//                         style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                         document.head.appendChild(style);

//                         const mediaElements = document.querySelectorAll('video, audio');
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let realVideo = null;

//                         mediaElements.forEach(media => { 
//                             media.muted = muteVideo; 
//                             media.volume = muteVideo ? 0.0 : 1.0; 
//                         });

//                         if (!muteVideo) {
//                             document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                         }

//                         for (const v of videos) {
//                             if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                         }

//                         if (!realVideo && videos.length > 0) realVideo = videos[0];

//                         if (realVideo) { 
//                             realVideo.style.setProperty('position', 'fixed', 'important');
//                             realVideo.style.setProperty('top', '0px', 'important');
//                             realVideo.style.setProperty('left', '0px', 'important');
//                             realVideo.style.setProperty('width', '100vw', 'important');
//                             realVideo.style.setProperty('height', '100vh', 'important');
//                             realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                             realVideo.style.setProperty('background-color', 'black', 'important');
//                             realVideo.style.setProperty('object-fit', 'contain', 'important');
//                             realVideo.style.setProperty('opacity', '1', 'important');
//                             realVideo.style.setProperty('visibility', 'visible', 'important');
//                             realVideo.style.setProperty('display', 'block', 'important');
//                         }
//                     } catch(err) {}
//                 }, 500); 
//             }, startMuted).catch(() => {});
//         }
//     } catch (e) { }

//     if (!startMuted) await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// // =========================================================================================
// // ⚙️ PAGE HEALTH SCANNER (Exact Original)
// // =========================================================================================
// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
//                         if (bodyText.includes("stream error") || bodyText.includes("not found") || bodyText.includes("domain is blocked") || bodyText.includes("error: forbidden") || bodyText.includes("does not have permission") || bodyText.includes("access denied") || (bodyText.includes("cloudflare") && bodyText.includes("blocked"))) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;
//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) { targetV = v; break; }
//                         }
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             else if (targetV.webkitDecodedFrameCount !== undefined) frames = targetV.webkitDecodedFrameCount;
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// // =========================================================================================
// // 👑 THE KING JUDGE (Watchdog for Any-Healthy Switching)
// // =========================================================================================
// async function startWatchdog() {
//     let watchdogTicks = 0;

//     while (true) {
//         if (!browser || !browser.isConnected()) throw new Error("Browser closed.");

//         // 1. UPDATE HEALTH OF ALL TABS
//         for (let tab of chromeTabs) {
//             if (tab.isRecovering) continue;
            
//             let statusObj = await checkPageStatus(tab.page);

//             if (statusObj.status === 'HEALTHY') {
//                 let elapsedMs = Date.now() - tab.streamSetupTime;
//                 let isExempted = NO_REFRESH_DOMAINS.some(domain => tab.url.includes(domain));

//                 if (elapsedMs > FORCE_REFRESH_MS && !isExempted) {
//                     tab.status = 'FORCE_REFRESH';
//                 } else {
//                     let isTimeStuck = (statusObj.currentTime === tab.lastActiveTime);
//                     let isFrameStuck = (statusObj.decodedFrames === tab.lastDecodedFrames && statusObj.decodedFrames > 0);

//                     // Apply exact 8-second logic
//                     if (isTimeStuck || isFrameStuck) {
//                         if (Date.now() - tab.frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                             tab.status = 'FROZEN';
//                         }
//                     } else {
//                         tab.lastActiveTime = statusObj.currentTime;
//                         tab.lastDecodedFrames = statusObj.decodedFrames;
//                         tab.frozenCheckTimestamp = Date.now();
//                         tab.status = 'HEALTHY';
//                     }
//                 }
//             } else {
//                 tab.status = statusObj.status; // DEAD or ERROR
//             }
//         }

//         let activeTab = getActiveTab();
//         let isWarmupPhase = activeTab ? (Date.now() - activeTab.streamSetupTime < 15000) : false;

//         // 2. ELECTION: If Active is Dead/Frozen, find ANY Healthy tab
//         if (!activeTab || (activeTab.status !== 'HEALTHY' && activeTab.status !== 'STARTING' && !isWarmupPhase)) {
//             console.log(`\n==================================================`);
//             console.log(`[!] ❌ ACTIVE TAB [ID: ${activeTabId}] DETECTED DEAD/FROZEN.`);
//             console.log(`==================================================`);
            
//             // Search ANY tab that is Healthy (Sequence ignored!)
//             let newKing = chromeTabs.find(t => t.id !== activeTabId && t.status === 'HEALTHY' && !t.isRecovering);

//             if (newKing) {
//                 console.log(`[👑] ELECTION: Promoting Healthy Tab [ID: ${newKing.id}] to ACTIVE!`);
                
//                 // Absolute Mute for old Active Tab
//                 if (activeTab && activeTab.page) {
//                     await activeTab.page.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                 }

//                 // Make New King Front and Unmuted
//                 activeTabId = newKing.id;
//                 await newKing.page.bringToFront();
//                 try { await newKing.page.mouse.click(10, 10); } catch(e){} 
//                 await triggerSmartUnmute(newKing.page);

//             } else {
//                 console.log(`[💀] CRITICAL: No backup stream is healthy! Waiting for background recovery...`);
//                 if (activeTabId !== null) {
//                     activeTabId = null;
//                     try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch(e){}
//                 }
//             }
//         } else {
//             // Keep Active Tab rigidly on top and unmuted!
//             if (activeTab && activeTab.page) {
//                 try {
//                     await activeTab.page.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); }).catch(()=>{});
//                     await activeTab.page.bringToFront().catch(()=>{});
//                     // Ensure baqi sab constantly mute rahein (Audio aane/jaane wala fix)
//                     for (let tab of chromeTabs) {
//                         if (tab.id !== activeTabId && tab.page) {
//                             tab.page.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch (e) {}
//             }
//         }

//         // 3. BACKGROUND RECOVERY (Silent & Isolated)
//         for (let tab of chromeTabs) {
//             if (tab.id !== activeTabId && (tab.status === 'DEAD' || tab.status === 'FROZEN' || tab.status === 'CRITICAL_ERROR' || tab.status === 'FORCE_REFRESH')) {
//                 // Recovery Cooldown prevents infinite loop bug!
//                 if (!tab.isRecovering && Date.now() > tab.recoveryCooldown) {
//                     tab.isRecovering = true;
//                     console.log(`[🔄] RECOVERY: Silently restarting Tab [ID: ${tab.id}] in background...`);
                    
//                     // Fire and forget recovery (non-blocking)
//                     (async (t) => {
//                         try {
//                             await t.page.goto('about:blank').catch(()=>{});
//                             await applyPreloadFirewall(t.page);
//                             await t.page.goto(t.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                             await initializeVideo(t.page, true, false); // true = startMuted, false = do NOT bring to front
//                         } catch(e) {}
                        
//                         t.lastActiveTime = -1;
//                         t.lastDecodedFrames = -1;
//                         t.frozenCheckTimestamp = Date.now();
//                         t.streamSetupTime = Date.now();
//                         t.status = 'STARTING';
//                         t.recoveryCooldown = Date.now() + 15000; // MUST wait 15s before attempting another recovery
//                         t.isRecovering = false;
//                         console.log(`[✅] RECOVERY DONE: Tab [ID: ${t.id}] is back and waiting in the pool.`);
//                     })(tab);
//                 }
//             }
//         }

//         watchdogTicks++;
//         if (watchdogTicks === 1 || watchdogTicks % 45 === 0) {
//             console.log(`\n[💓] HEARTBEAT | CHECKING ${chromeTabs.length} TABS | ACTIVE KING: [ID: ${activeTabId || 'NONE'}]`);
//             chromeTabs.forEach(t => {
//                 let tag = (t.id === activeTabId) ? '[▶️ ACTIVE]' : '[⏸️ BACKUP]';
//                 let vol = (t.id === activeTabId) ? '🔊 ON' : '🔇 MUTED';
//                 console.log(`  ${tag} Tab [ID: ${t.id}] | Status: ${t.status} | Audio: ${vol} | URL: ${t.url.substring(0, 30)}...`);
//             });
//         }
//         await new Promise(r => setTimeout(r, 2000));
//     }
// }

// // =========================================================================================
// // 🚀 START ENGINE
// // =========================================================================================
// async function startDirectStreaming() {
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, '--window-position=0,0',
//         '--kiosk', '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', '--ignore-certificate-errors', '--disable-web-security',
//         '--ignore-gpu-blocklist', '--use-gl=egl',
//         '--disable-accelerated-video-decode', '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');

//     browser = await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });

//     browser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => {
//                 let isAllowed = chromeTabs.some(t => t.page === newPage);
//                 if (!isAllowed) {
//                     try { await newPage.close(); } catch(e) {}
//                 }
//             }, 500);
//         }
//     });

//     console.log(`\n==================================================`);
//     console.log(`[🚀] LAUNCHING ${urlList.length} TABS WITH 5 SECOND STAGGER`);
//     console.log(`==================================================\n`);

//     // 🚀 EXACT 5 SECOND STAGGERED STARTUP
//     for (let i = 0; i < urlList.length; i++) {
//         let page;
//         if (i === 0) {
//             let pages = await browser.pages();
//             page = pages[0]; 
//         } else {
//             page = await browser.newPage();
//         }

//         let tabObj = {
//             id: i + 1,
//             url: urlList[i],
//             page: page,
//             status: 'STARTING',
//             lastActiveTime: -1,
//             lastDecodedFrames: -1,
//             frozenCheckTimestamp: Date.now(),
//             streamSetupTime: Date.now(),
//             isRecovering: false,
//             recoveryCooldown: 0
//         };
//         chromeTabs.push(tabObj);

//         await setupNetworkAdBlocker(page);
//         attachAntiAdListeners(page);
//         await applyPreloadFirewall(page);

//         if (i === 0) {
//             // First URL is King
//             activeTabId = tabObj.id;
//             await page.bringToFront();
//             console.log(`[*] Loading Link [ID: ${tabObj.id}] (ACTIVE KING) -> ${tabObj.url}`);
//             await page.goto(tabObj.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//             await initializeVideo(page, false, true); // unmuted, bring to front
            
//             if (isObsConnected) {
//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//             }
//         } else {
//             // Background Tabs
//             console.log(`[*] Silently preparing Backup Link [ID: ${tabObj.id}] in background -> ${tabObj.url}`);
//             page.goto(tabObj.url, { waitUntil: 'domcontentloaded', timeout: 60000 }).then(async () => {
//                 await initializeVideo(page, true, false); // muted, background
//             }).catch(()=>{});
//         }

//         // Wait EXACTLY 5 seconds before opening the next tab (unless it's the last one)
//         if (i < urlList.length - 1) {
//             console.log(`[⏳] Waiting 5 seconds before launching next tab...`);
//             await new Promise(r => setTimeout(r, 5000));
//             // Force the active tab back to the front just in case the new blank tab stole focus
//             let activeTab = getActiveTab();
//             if (activeTab && activeTab.page) await activeTab.page.bringToFront().catch(()=>{});
//         }
//     }

//     console.log(`\n[!] MULTI-TAB ENGINE SETUP COMPLETE. STARTING CENTRAL JUDGE WATCHDOG...\n`);
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             chromeTabs = []; // reset array
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     try { await obs.disconnect(); } catch (e) { } 
//     if (browser) { try { await browser.close(); } catch(e) { } browser = null; }
//     if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//     try {
//         execSync('pkill -9 obs || true', { stdio: 'ignore' });
//         execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//         execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//     } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });
// mainLoop();























// ---------------------- chatgpt n-chromes===============================




// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 4000000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let obsProcess = null;

// // =========================================================================================
// // 🧠 MULTI-CHROME STREAM WORKER ARCHITECTURE
// // 1 URL = 1 independent Puppeteer/Chrome instance.
// // =========================================================================================
// const CHROME_START_STAGGER_MS = 5000;
// const HEALTH_CHECK_INTERVAL_MS = 2000;
// const FROZEN_THRESHOLD_MS = 8000; // 8 seconds
// const WORKER_RESTART_DELAY_MS = 3000;
// const MAX_WORKER_RESTARTS = 5;
// let chromeWorkers = [];
// let activeWorker = null;
// let obsConnected = false;
// let shuttingDown = false;
// let healthManagerStarted = false;
// function createWorker(id,url){return {id,url,browser:null,page:null,status:'STARTING',healthy:false,active:false,audioOk:false,audioCheckedAt:0,lastLoggedStatus:'',lastTime:-1,lastFrames:-1,frozenSince:null,lastHealthyAt:0,lastCheckAt:0,startupAt:Date.now(),restartCount:0,preparing:false,restarting:false,checkInFlight:false,lastError:'',generation:0,deadReadCount:0,errorCount:0};}

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // Permanent root execution block for popup alerts & confirms
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; }`;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }


// // =========================================================================================
// // 🧩 MULTI-CHROME HELPERS
// // =========================================================================================
// function sleep(ms){ return new Promise(r=>setTimeout(r,ms)); }
// function workerLabel(w){ return `Chrome ${w.id}`; }
// function browserArgs(w){
//     const profile=path.join(process.cwd(),'.stream-chrome-profiles',`chrome-${w.id}`);
//     fs.mkdirSync(profile,{recursive:true});
//     const a=['--no-sandbox','--disable-setuid-sandbox',`--window-size=${RES_W},${RES_H}`,'--window-position=0,0','--kiosk','--start-fullscreen','--autoplay-policy=no-user-gesture-required','--disable-dev-shm-usage','--ignore-certificate-errors','--disable-web-security','--ignore-gpu-blocklist','--use-gl=egl','--disable-accelerated-video-decode','--disable-accelerated-video-encode','--disable-smooth-scrolling','--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion','--disable-background-timer-throttling','--disable-backgrounding-occluded-windows','--disable-renderer-backgrounding',`--disable-extensions-except=${path.join(process.cwd(),'ublock-lite')}`,`--load-extension=${path.join(process.cwd(),'ublock-lite')}`,`--user-data-dir=${profile}`];
//     if(PROXY_ENGINE.includes('Cloudflare')) a.push('--proxy-server=socks5://127.0.0.1:40000');
//     return a;
// }
// async function muteWorker(w){
//     if(!w?.page)return;
//     for(const f of w.page.frames()){try{if(!f.isDetached())await f.evaluate(()=>document.querySelectorAll('video,audio').forEach(m=>{m.muted=true;m.volume=0}));}catch(e){}}
// }
// async function unmuteWorker(w){
//     if(!w?.page)return false;
//     let audioOk=false;
//     try{
//         await triggerSmartUnmute(w.page);
//         for(const f of w.page.frames()){
//             try{
//                 if(f.isDetached())continue;
//                 const result=await f.evaluate(async()=>{
//                     // Allow autoplay permission on embedded players where the parent page controls the iframe.
//                     try{document.querySelectorAll('iframe').forEach(ifr=>{const allow=(ifr.getAttribute('allow')||'').split(';').map(x=>x.trim()).filter(Boolean);if(!allow.some(x=>x.toLowerCase()==='autoplay')){allow.push('autoplay');ifr.setAttribute('allow',allow.join('; '));}})}catch(e){}
//                     let audible=0, playing=0;
//                     const media=Array.from(document.querySelectorAll('video,audio'));
//                     for(const m of media){
//                         try{
//                             m.muted=false;
//                             m.defaultMuted=false;
//                             m.volume=1;
//                             if(m.paused){const p=m.play();if(p&&typeof p.catch==='function')await p.catch(()=>{});}
//                             if(!m.muted && m.volume>0)audible++;
//                             if(!m.paused && !m.ended)playing++;
//                         }catch(e){}
//                     }
//                     // Resume WebAudio contexts when the player uses Web Audio.
//                     try{
//                         if(window.AudioContext||window.webkitAudioContext){
//                             const C=window.AudioContext||window.webkitAudioContext;
//                             if(C._streamContexts)for(const c of C._streamContexts){try{if(c.state==='suspended')await c.resume();}catch(e){}}
//                         }
//                     }catch(e){}
//                     document.querySelectorAll('.jw-icon-volume.jw-off,.vjs-vol-muted,.plyr__control--pressed[data-plyr="mute"],[aria-label*="Unmute" i],[aria-label*="Volume" i]').forEach(b=>{try{b.click()}catch(e){}});
//                     return {media:media.length,audible,playing};
//                 });
//                 if(result?.audible>0 && result?.playing>0)audioOk=true;
//             }catch(e){}
//         }
//     }catch(e){}
//     w.audioOk=audioOk;
//     w.audioCheckedAt=Date.now();
//     console.log(audioOk ? `[🔊] ${workerLabel(w)} AUDIO: ON / audible / playback confirmed` : `[⚠️] ${workerLabel(w)} AUDIO: unmute requested but audible playback NOT confirmed`);
//     return audioOk;
// }

// async function verifyActiveAudio(w){
//     // IMPORTANT: DOM media state is not the same as measured speaker output.
//     // We only declare audio LOST when the player itself is clearly muted/paused.
//     // A frame/iframe that cannot expose its media element must NOT create a false warning.
//     if(!w?.page) return {ok:false, state:'NO_PAGE'};
//     let foundMedia=false;
//     let hasAudibleMedia=false;
//     let hasBlockedOrMutedMedia=false;
//     let playRejected=false;
//     try{
//         for(const f of w.page.frames()){
//             if(f.isDetached()) continue;
//             try{
//                 const r=await f.evaluate(()=>{
//                     const media=Array.from(document.querySelectorAll('video,audio'));
//                     let audible=0, mutedOrPaused=0, playing=0;
//                     for(const m of media){
//                         const isPlaying=!m.paused && !m.ended && m.readyState>=2;
//                         if(isPlaying) playing++;
//                         if(!m.muted && Number(m.volume)>0 && isPlaying) audible++;
//                         if(m.muted || Number(m.volume)<=0 || m.paused) mutedOrPaused++;
//                     }
//                     return {total:media.length,audible,mutedOrPaused,playing};
//                 });
//                 if(r?.total>0){
//                     foundMedia=true;
//                     if(r.audible>0) hasAudibleMedia=true;
//                     if(r.mutedOrPaused>0 && r.audible===0) hasBlockedOrMutedMedia=true;
//                 }
//             }catch(e){}
//         }
//     }catch(e){ return {ok:w.audioOk,state:'CHECK_ERROR'}; }

//     w.audioCheckedAt=Date.now();

//     if(hasAudibleMedia){
//         w.audioOk=true;
//         return {ok:true,state:'PLAYING_UNMUTED'};
//     }

//     // No exposed media element is NOT proof that audio is lost.
//     // This commonly happens with cross-origin/player-managed iframe audio.
//     if(!foundMedia){
//         return {ok:w.audioOk,state:'NOT_OBSERVABLE'};
//     }

//     // Only clear audioOk when the player is demonstrably muted/paused.
//     if(hasBlockedOrMutedMedia){
//         w.audioOk=false;
//         return {ok:false,state:'MUTED_OR_PAUSED'};
//     }

//     return {ok:w.audioOk,state:'UNKNOWN'};
// }
// async function closeWorker(w){
//     try{await muteWorker(w)}catch(e){}
//     try{if(w.browser)await w.browser.close()}catch(e){}
//     w.browser=null;w.page=null;
// }

// // =========================================================================================
// // 🚀 ONE URL = ONE INDEPENDENT CHROME
// // =========================================================================================
// async function launchWorker(w){
//     if(shuttingDown||w.restarting)return false;
//     w.status='STARTING';w.healthy=false;w.preparing=true;w.generation++;const gen=w.generation;
//     try{
//         console.log(`\n[🚀] ${workerLabel(w)} -> LINK ${w.id}: ${w.url}`);
//         w.browser=await puppeteer.launch({headless:false,defaultViewport:{width:RES_W,height:RES_H},ignoreDefaultArgs:['--enable-automation'],args:browserArgs(w)});
//         if(gen!==w.generation||shuttingDown)return false;
//         w.browser.on('targetcreated',async target=>{try{if(target.type()!=='page')return;const p=await target.page();setTimeout(async()=>{if(p&&p!==w.page)try{await p.close()}catch(e){}},500)}catch(e){}});
//         w.browser.on('disconnected',()=>{if(!shuttingDown&&gen===w.generation){w.status='DEAD';w.healthy=false;console.log(`[💥] ${workerLabel(w)} disconnected.`)}});
//         const pages=await w.browser.pages();w.page=pages[0]||await w.browser.newPage();
//         await setupNetworkAdBlocker(w.page);attachAntiAdListeners(w.page);await applyPreloadFirewall(w.page);
//         w.status='LOADING';
//         await w.page.goto(w.url,{waitUntil:'domcontentloaded',timeout:60000}).catch(e=>console.log(`[⚠️] ${workerLabel(w)} navigation: ${e.message}`));
//         if(gen!==w.generation||shuttingDown)return false;
//         await showLoadingUI(w.page,'STREAM LOADING',`Chrome ${w.id} connecting to live video <span class="stream-blink">...</span>`);
//         // ORIGINAL website/video discovery engine is preserved.
//         await initializeVideo(w.page,true,false);
//         await muteWorker(w);await hideLoadingUI(w.page);
//         w.status='CHECKING';w.preparing=false;w.lastTime=-1;w.lastFrames=-1;w.frozenSince=null;
//         console.log(`[✅] ${workerLabel(w)} prepared.`);return true;
//     }catch(e){w.preparing=false;w.status='DEAD';w.healthy=false;w.lastError=e.message||String(e);console.log(`[❌] ${workerLabel(w)} failed: ${w.lastError}`);await closeWorker(w);return false;}
// }

// // =========================================================================================
// // 🩺 PER-CHROME HEALTH — 8 SECOND FREEZE THRESHOLD
// // =========================================================================================
// async function probeVideoProgress(w, durationMs=2200){
//     if(!w?.page) return {progressed:false, reason:'NO_PAGE'};
//     const samples=[];
//     const started=Date.now();
//     while(Date.now()-started<durationMs){
//         try{
//             const r=await checkPageStatus(w.page);
//             if(r.status==='CRITICAL_ERROR') return {progressed:false, hardDead:true, reason:'CRITICAL_ERROR'};
//             if(r.status==='HEALTHY') samples.push({t:Number(r.currentTime)||0,f:Number(r.decodedFrames)||0});
//         }catch(e){}
//         await sleep(650);
//     }
//     const first=samples[0], last=samples[samples.length-1];
//     if(!first||!last) return {progressed:false, reason:'NO_HEALTH_SAMPLE'};
//     const timeMoved=Math.abs(last.t-first.t)>0.05;
//     const framesMoved=last.f>0 && first.f>0 && last.f>first.f;
//     return {progressed:timeMoved||framesMoved,timeMoved,framesMoved,samples:samples.length};
// }

// async function evaluateWorker(w){
//     if(!w?.page||w.preparing||w.restarting||w.checkInFlight||shuttingDown)return;
//     w.checkInFlight=true;
//     try{
//         if(!w.browser?.isConnected()){w.status='DEAD';w.healthy=false;return;}
//         const r=await checkPageStatus(w.page);
//         if(r.status==='HEALTHY'){
//             const changed=w.lastTime<0||Math.abs((Number(r.currentTime)||0)-(Number(w.lastTime)||0))>0.05||(r.decodedFrames>0&&r.decodedFrames!==w.lastFrames);
//             if(changed){
//                 w.lastTime=r.currentTime;w.lastFrames=r.decodedFrames;w.frozenSince=null;w.status='HEALTHY';w.healthy=true;w.lastHealthyAt=Date.now();
//             }else{
//                 if(!w.frozenSince)w.frozenSince=Date.now();
//                 const frozen=Date.now()-w.frozenSince;
//                 if(frozen>=FROZEN_THRESHOLD_MS){
//                     // IMPORTANT: 8s is a suspicion threshold, not an immediate kill.
//                     // A live player can briefly stop advancing currentTime/frame counters without actually being dead.
//                     console.log(`[🧪] ${workerLabel(w)} reached 8s no-progress -> running confirmation probe (NOT dead yet)...`);
//                     const probe=await probeVideoProgress(w,2200);
//                     if(probe.progressed){
//                         w.lastTime=r.currentTime;w.lastFrames=r.decodedFrames;w.frozenSince=null;w.status='HEALTHY';w.healthy=true;w.lastHealthyAt=Date.now();
//                         console.log(`[✅] ${workerLabel(w)} freeze FALSE POSITIVE: video progressed during confirmation probe (time=${probe.timeMoved?'YES':'NO'}, frames=${probe.framesMoved?'YES':'NO'}).`);
//                     }else if(probe.hardDead){
//                         w.status='CRITICAL_ERROR';w.healthy=false;console.log(`[💀] ${workerLabel(w)} confirmation probe found CRITICAL_ERROR.`);
//                     }else{
//                         w.status='FROZEN';w.healthy=false;
//                         console.log(`[🧊] ${workerLabel(w)} CONFIRMED FROZEN: 8s threshold + confirmation probe failed.`);
//                     }
//                 }else{
//                     w.status='HEALTHY';w.healthy=true;
//                 }
//             }
//         }else if(r.status==='CRITICAL_ERROR'){
//             w.status='CRITICAL_ERROR';w.healthy=false;w.frozenSince=null;
//         }else{
//             // Do not kill a stream because of ONE transient DEAD read.
//             // Give it two consecutive failed health reads before declaring DEAD.
//             w.deadReadCount=(w.deadReadCount||0)+1;
//             if(w.deadReadCount>=2){w.status='DEAD';w.healthy=false;w.frozenSince=null;}
//             else{w.status=w.active?'HEALTHY':'CHECKING';w.healthy=!!w.active;}
//         }
//         if(r.status==='HEALTHY')w.deadReadCount=0;
//         if(w.status!==w.lastLoggedStatus){
//             console.log(`[📊] ${workerLabel(w)} STATUS: ${w.lastLoggedStatus||'INIT'} -> ${w.status}${w.active?' | 👑 ACTIVE':''}`);
//             w.lastLoggedStatus=w.status;
//         }
//     }catch(e){
//         w.lastError=e.message||String(e);
//         w.errorCount=(w.errorCount||0)+1;
//         if(w.errorCount>=2){w.status='DEAD';w.healthy=false;}
//         if(w.status!==w.lastLoggedStatus){console.log(`[📊] ${workerLabel(w)} STATUS: ${w.lastLoggedStatus||'INIT'} -> ${w.status||'ERROR'} | ${w.lastError}`);w.lastLoggedStatus=w.status||'ERROR';}
//     }
//     finally{w.checkInFlight=false;}
// }

// // =========================================================================================
// // 👑 KING/JUDGE — HEALTH BASED, NEVER NEXT-INDEX BASED
// // =========================================================================================
// function chooseHealthy(){
//     const c=chromeWorkers.filter(w=>w!==activeWorker&&w.healthy&&w.status==='HEALTHY'&&w.page&&w.browser?.isConnected());
//     c.sort((a,b)=>(b.lastHealthyAt||0)-(a.lastHealthyAt||0));
//     return c[0]||null;
// }
// async function activateWorker(w,reason){
//     if(!w?.page||!w.browser?.isConnected())return false;
//     const old=activeWorker;
//     try{
//         console.log(`\n[👑] ==================================================`);
//         console.log(`[👑] ACTIVE ELECTION`);
//         console.log(`[👑] Reason : ${reason}`);
//         console.log(`[👑] Candidate : ${workerLabel(w)} | ${w.url}`);
//         console.log(`[👑] ==================================================`);
//         for(const x of chromeWorkers){
//             if(x!==w){x.active=false;await muteWorker(x);x.audioOk=false;}
//         }
//         w.active=true;activeWorker=w;
//         await w.page.bringToFront();
//         // This click is deliberately before the unmute/play attempt so the page gets a real user-activation-like event.
//         try{await w.page.mouse.click(Math.max(10,Math.floor(RES_W/2)),Math.max(10,Math.floor(RES_H/2)))}catch(e){}
//         await hideLoadingUI(w.page);
//         let audioOk=await unmuteWorker(w);
//         if(!audioOk){
//             console.log(`[🔁] ${workerLabel(w)} AUDIO RETRY: second unmute/play verification...`);
//             await sleep(500);
//             audioOk=await unmuteWorker(w);
//         }
//         await w.page.bringToFront();
//         if(obsConnected){try{await obs.call('SetCurrentProgramScene',{sceneName:'MainScene'});console.log(`[📡] OBS SCENE: MainScene -> ${workerLabel(w)}`);}catch(e){console.log(`[⚠️] OBS scene switch warning: ${e.message}`)}}
//         w.lastTime=-1;w.lastFrames=-1;w.frozenSince=null;w.status='HEALTHY';w.healthy=true;w.audioOk=audioOk;
//         console.log(`[🔊] ACTIVE AUDIO : ${audioOk?'ON / CONFIRMED':'⚠️ NOT CONFIRMED'}`);
//         console.log(`[🔄] ${old&&old!==w?'HOT-SWAP '+workerLabel(old)+' -> ':''}${workerLabel(w)} ACTIVE`);
//         console.log(`[🌐] ACTIVE URL   : ${w.url}`);
//         console.log(`[📺] VIDEO STATUS : HEALTHY`);
//         return true;
//     }catch(e){w.active=false;if(activeWorker===w)activeWorker=old||null;w.status='DEAD';w.healthy=false;console.log(`[❌] ${workerLabel(w)} activation failed: ${e.message}`);return false;}
// }
// async function electActive(reason){
//     if(activeWorker?.healthy&&activeWorker.status==='HEALTHY')return true;
//     const w=chooseHealthy();return w?activateWorker(w,reason):false;
// }

// // =========================================================================================
// // ♻️ BACKGROUND RECOVERY — NEVER DISTURB A HEALTHY ACTIVE STREAM
// // =========================================================================================
// async function restartWorker(w){
//     if(!w||w.restarting||shuttingDown)return;
//     if(w.restartCount>=MAX_WORKER_RESTARTS)return;
//     w.restarting=true;w.restartCount++;const wasActive=activeWorker===w;
//     console.log(`[♻️] ${workerLabel(w)} recovery #${w.restartCount}`);
//     if(wasActive){w.active=false;w.healthy=false;activeWorker=null}
//     await closeWorker(w);w.status='RESTARTING';await sleep(WORKER_RESTART_DELAY_MS);w.restarting=false;
//     await launchWorker(w);await sleep(250);await evaluateWorker(w);
//     if(wasActive||!activeWorker)await electActive(`recovered ${workerLabel(w)}`);
// }
// let lastHealthLogAt=0;
// function printHealthSnapshot(force=false){
//     const now=Date.now();
//     if(!force && now-lastHealthLogAt<10000)return;
//     lastHealthLogAt=now;
//     console.log(`\n[🩺] ================= HEALTH SNAPSHOT =================`);
//     for(const w of chromeWorkers){
//         const active=w===activeWorker?'👑 ACTIVE':'   BACKUP';
//         const audio=w.active?(w.audioOk?'🔊 ON':'🔇 CHECK'):'🔇 OFF';
//         const age=w.lastHealthyAt?Math.round((now-w.lastHealthyAt)/1000)+'s':'-';
//         console.log(`[🩺] ${workerLabel(w).padEnd(9)} | ${active} | ${String(w.status).padEnd(16)} | ${audio} | healthy-age ${age} | restarts ${w.restartCount}`);
//     }
//     console.log(`[🩺] ==================================================\n`);
// }

// async function healthTick(){
//     await Promise.all(chromeWorkers.map(evaluateWorker));
//     printHealthSnapshot();
//     if(activeWorker&&activeWorker.healthy&&activeWorker.status==='HEALTHY' && Date.now()-(activeWorker.audioCheckedAt||0)>10000){
//         const audio=await verifyActiveAudio(activeWorker);
//         if(audio.state==='MUTED_OR_PAUSED'){
//             console.log(`[⚠️] ACTIVE AUDIO STATE BAD: ${workerLabel(activeWorker)} video is healthy but player reports muted/paused. Retrying audio...`);
//             const ok=await unmuteWorker(activeWorker);
//             if(!ok){
//                 console.log(`[⚠️] ${workerLabel(activeWorker)} audio retry could not verify player-level audio state.`);
//             }
//         }else if(audio.state==='PLAYING_UNMUTED'){
//             // Normal state: stay quiet. Do NOT spam "AUDIO LOST".
//             if(!activeWorker.audioOk){
//                 activeWorker.audioOk=true;
//                 console.log(`[🔊] ${workerLabel(activeWorker)} AUDIO STATE: playing + unmuted (confirmed at player level)`);
//             }
//         }
//         // NOT_OBSERVABLE / UNKNOWN deliberately do not trigger a retry.
//         // Browser JS cannot reliably prove physical speaker output from an iframe.
//     }
//     if(!activeWorker||!activeWorker.healthy||activeWorker.status!=='HEALTHY'){
//         const old=activeWorker;activeWorker=null;if(old){old.active=false;await muteWorker(old)}
//         const elected=await electActive(`active ${old?workerLabel(old):'none'} unavailable`);
//         if(!elected)for(const w of chromeWorkers)if(['DEAD','FROZEN','CRITICAL_ERROR'].includes(w.status))restartWorker(w).catch(()=>{});
//     }
//     for(const w of chromeWorkers)if(w!==activeWorker&&['DEAD','FROZEN','CRITICAL_ERROR'].includes(w.status))restartWorker(w).catch(()=>{});
// }
// async function startHealthManager(){
//     if(healthManagerStarted)return;healthManagerStarted=true;console.log('[👑] HEALTH MANAGER: 2s polling / 8s freeze threshold');
//     while(!shuttingDown){try{await healthTick()}catch(e){}await sleep(HEALTH_CHECK_INTERVAL_MS)}
// }

// // =========================================================================================
// // 🚀 OBS + STAGGERED CHROMES: LINK1@0s, LINK2@5s, LINK3@10s...
// // =========================================================================================
// async function startDirectStreaming(){
//     console.log('[*] Starting OBS Studio FIRST...');setupOBSConfig();
//     obsProcess=spawn('obs',['--startstreaming','--minimize-to-tray']);
//     obsProcess.stdout.on('data',d=>console.log(`[OBS]: ${d.toString().trim()}`));
//     obsProcess.stderr.on('data',d=>{const m=d.toString().trim();if(m.includes('error')||m.includes('fail'))console.log(`[OBS Error]: ${m}`)});
//     await sleep(6000);obsConnected=false;
//     for(let attempt=1;attempt<=15;attempt++)try{await Promise.race([obs.connect('ws://127.0.0.1:4455','secret'),new Promise((_,rej)=>setTimeout(()=>rej(new Error('Timeout')),3000))]);obsConnected=true;console.log('[+] OBS WebSocket Connected Successfully!');break}catch(e){console.log(`[⏳] OBS retry ${attempt}/15`);await sleep(2000)}
//     if(obsConnected)try{await obs.call('SetCurrentProgramScene',{sceneName:'WaitingScene'})}catch(e){}

//     chromeWorkers=urlList.map((url,i)=>createWorker(i+1,url));
//     console.log(`\n[🧠] ==================================================`);console.log(`[🧠] STREAM ARCHITECTURE READY`);console.log(`[🧠] LINKS           : ${chromeWorkers.length}`);console.log(`[🧠] CHROMES         : ${chromeWorkers.length} independent instances`);console.log(`[🧠] STAGGER         : 5 seconds`);console.log(`[🧠] FREEZE LIMIT    : 8 seconds`);console.log(`[🧠] FAILOVER        : ANY HEALTHY CHROME`);console.log(`[🧠] ACTIVE AUDIO    : ONE CHROME ONLY / player-state verified`);console.log(`[🧠] ==================================================`);
//     const launches=[];
//     for(let i=0;i<chromeWorkers.length;i++){launches.push(launchWorker(chromeWorkers[i]));if(i<chromeWorkers.length-1){console.log(`[⏱️] 5s before ${workerLabel(chromeWorkers[i+1])}...`);await sleep(CHROME_START_STAGGER_MS)}}
//     await Promise.allSettled(launches);
//     await sleep(1000);await Promise.all(chromeWorkers.map(evaluateWorker));await electActive('initial healthy election');
//     if(obsConnected&&activeWorker)try{await obs.call('SetCurrentProgramScene',{sceneName:'MainScene'})}catch(e){}
//     console.log(`\n==================================================\n[🎥] INITIAL CAPTURE STATUS : ${activeWorker?'READY TO BROADCAST':'WAITING FOR HEALTHY CHROME'}\n[👑] ACTIVE CHROME           : ${activeWorker?workerLabel(activeWorker):'NONE'}\n[🌐] ACTIVE URL              : ${activeWorker?activeWorker.url:'NONE'}\n[🔊] ACTIVE AUDIO            : ${activeWorker?(activeWorker.audioOk?'ON / CONFIRMED':'⚠️ NOT CONFIRMED'):'N/A'}\n==================================================\n`);printHealthSnapshot(true);
//     await startHealthManager();
// }

// async function mainLoop(){while(true){try{shuttingDown=false;healthManagerStarted=false;await startDirectStreaming()}catch(e){console.error(`[!] ALERT: ${e.message}`);await cleanup();await sleep(3000)}}}

// async function cleanup(){
//     shuttingDown=true;healthManagerStarted=false;
//     for(const w of chromeWorkers)try{await closeWorker(w)}catch(e){}
//     chromeWorkers=[];activeWorker=null;obsConnected=false;
//     try{await obs.disconnect()}catch(e){}
//     if(obsProcess)try{obsProcess.kill('SIGKILL')}catch(e){}
//     obsProcess=null;
//     try{execSync('pkill -9 obs || true',{stdio:'ignore'});execSync('pkill -9 chrome || true',{stdio:'ignore'});execSync('pkill -9 puppeteer || true',{stdio:'ignore'})}catch(e){}
// }
// process.on('SIGINT',async()=>{await cleanup();process.exit(0)});
// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();






// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 4000000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let obsProcess = null;

// // =========================================================================================
// // 🧠 MULTI-CHROME STREAM WORKER ARCHITECTURE
// // 1 URL = 1 independent Puppeteer/Chrome instance.
// // =========================================================================================
// const CHROME_START_STAGGER_MS = 5000;
// const HEALTH_CHECK_INTERVAL_MS = 2000;
// const FROZEN_THRESHOLD_MS = 8000; // 8 seconds
// const WORKER_RESTART_DELAY_MS = 3000;
// const MAX_WORKER_RESTARTS = 5;
// let chromeWorkers = [];
// let activeWorker = null;
// let obsConnected = false;
// let shuttingDown = false;
// let healthManagerStarted = false;
// function createWorker(id,url){return {id,url,browser:null,page:null,status:'STARTING',healthy:false,active:false,audioOk:false,audioCheckedAt:0,lastLoggedStatus:'',lastTime:-1,lastFrames:-1,frozenSince:null,lastHealthyAt:0,lastCheckAt:0,startupAt:Date.now(),restartCount:0,preparing:false,restarting:false,checkInFlight:false,lastError:'',generation:0,deadReadCount:0,errorCount:0};}

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // Permanent root execution block for popup alerts & confirms
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; }`;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }


// // =========================================================================================
// // 🧩 MULTI-CHROME HELPERS
// // =========================================================================================
// function sleep(ms){ return new Promise(r=>setTimeout(r,ms)); }
// function workerLabel(w){ return `Chrome ${w.id}`; }
// function browserArgs(w){
//     const profile=path.join(process.cwd(),'.stream-chrome-profiles',`chrome-${w.id}`);
//     fs.mkdirSync(profile,{recursive:true});
//     const a=['--no-sandbox','--disable-setuid-sandbox',`--window-size=${RES_W},${RES_H}`,'--window-position=0,0','--kiosk','--start-fullscreen','--autoplay-policy=no-user-gesture-required','--disable-dev-shm-usage','--ignore-certificate-errors','--disable-web-security','--ignore-gpu-blocklist','--use-gl=egl','--disable-accelerated-video-decode','--disable-accelerated-video-encode','--disable-smooth-scrolling','--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion','--disable-background-timer-throttling','--disable-backgrounding-occluded-windows','--disable-renderer-backgrounding',`--disable-extensions-except=${path.join(process.cwd(),'ublock-lite')}`,`--load-extension=${path.join(process.cwd(),'ublock-lite')}`,`--user-data-dir=${profile}`];
//     if(PROXY_ENGINE.includes('Cloudflare')) a.push('--proxy-server=socks5://127.0.0.1:40000');
//     return a;
// }
// async function muteWorker(w){
//     if(!w?.page)return;
//     for(const f of w.page.frames()){try{if(!f.isDetached())await f.evaluate(()=>document.querySelectorAll('video,audio').forEach(m=>{m.muted=true;m.volume=0}));}catch(e){}}
// }
// async function unmuteWorker(w){
//     if(!w?.page)return false;
//     let audioOk=false;
//     try{
//         await triggerSmartUnmute(w.page);
//         for(const f of w.page.frames()){
//             try{
//                 if(f.isDetached())continue;
//                 const result=await f.evaluate(async()=>{
//                     // Allow autoplay permission on embedded players where the parent page controls the iframe.
//                     try{document.querySelectorAll('iframe').forEach(ifr=>{const allow=(ifr.getAttribute('allow')||'').split(';').map(x=>x.trim()).filter(Boolean);if(!allow.some(x=>x.toLowerCase()==='autoplay')){allow.push('autoplay');ifr.setAttribute('allow',allow.join('; '));}})}catch(e){}
//                     let audible=0, playing=0;
//                     const media=Array.from(document.querySelectorAll('video,audio'));
//                     for(const m of media){
//                         try{
//                             m.muted=false;
//                             m.defaultMuted=false;
//                             m.volume=1;
//                             if(m.paused){const p=m.play();if(p&&typeof p.catch==='function')await p.catch(()=>{});}
//                             if(!m.muted && m.volume>0)audible++;
//                             if(!m.paused && !m.ended)playing++;
//                         }catch(e){}
//                     }
//                     // Resume WebAudio contexts when the player uses Web Audio.
//                     try{
//                         if(window.AudioContext||window.webkitAudioContext){
//                             const C=window.AudioContext||window.webkitAudioContext;
//                             if(C._streamContexts)for(const c of C._streamContexts){try{if(c.state==='suspended')await c.resume();}catch(e){}}
//                         }
//                     }catch(e){}
//                     document.querySelectorAll('.jw-icon-volume.jw-off,.vjs-vol-muted,.plyr__control--pressed[data-plyr="mute"],[aria-label*="Unmute" i],[aria-label*="Volume" i]').forEach(b=>{try{b.click()}catch(e){}});
//                     return {media:media.length,audible,playing};
//                 });
//                 if(result?.audible>0 && result?.playing>0)audioOk=true;
//             }catch(e){}
//         }
//     }catch(e){}
//     w.audioOk=audioOk;
//     w.audioCheckedAt=Date.now();
//     console.log(audioOk ? `[🔊] ${workerLabel(w)} AUDIO: ON / audible / playback confirmed` : `[⚠️] ${workerLabel(w)} AUDIO: unmute requested but audible playback NOT confirmed`);
//     return audioOk;
// }

// async function verifyActiveAudio(w){
//     if(!w?.page)return false;
//     try{
//         for(const f of w.page.frames()){
//             if(f.isDetached())continue;
//             const r=await f.evaluate(()=>{
//                 const media=Array.from(document.querySelectorAll('video,audio'));
//                 const audible=media.filter(m=>!m.muted && m.volume>0 && !m.paused && !m.ended);
//                 return {total:media.length,audible:audible.length};
//             });
//             if(r?.audible>0){w.audioOk=true;w.audioCheckedAt=Date.now();return true;}
//         }
//     }catch(e){}
//     w.audioOk=false;w.audioCheckedAt=Date.now();return false;
// }
// async function closeWorker(w){
//     try{await muteWorker(w)}catch(e){}
//     try{if(w.browser)await w.browser.close()}catch(e){}
//     w.browser=null;w.page=null;
// }

// // =========================================================================================
// // 🚀 ONE URL = ONE INDEPENDENT CHROME
// // =========================================================================================
// async function launchWorker(w){
//     if(shuttingDown||w.restarting)return false;
//     w.status='STARTING';w.healthy=false;w.preparing=true;w.generation++;const gen=w.generation;
//     try{
//         console.log(`\n[🚀] ${workerLabel(w)} -> LINK ${w.id}: ${w.url}`);
//         w.browser=await puppeteer.launch({headless:false,defaultViewport:{width:RES_W,height:RES_H},ignoreDefaultArgs:['--enable-automation'],args:browserArgs(w)});
//         if(gen!==w.generation||shuttingDown)return false;
//         w.browser.on('targetcreated',async target=>{try{if(target.type()!=='page')return;const p=await target.page();setTimeout(async()=>{if(p&&p!==w.page)try{await p.close()}catch(e){}},500)}catch(e){}});
//         w.browser.on('disconnected',()=>{if(!shuttingDown&&gen===w.generation){w.status='DEAD';w.healthy=false;console.log(`[💥] ${workerLabel(w)} disconnected.`)}});
//         const pages=await w.browser.pages();w.page=pages[0]||await w.browser.newPage();
//         await setupNetworkAdBlocker(w.page);attachAntiAdListeners(w.page);await applyPreloadFirewall(w.page);
//         w.status='LOADING';
//         await w.page.goto(w.url,{waitUntil:'domcontentloaded',timeout:60000}).catch(e=>console.log(`[⚠️] ${workerLabel(w)} navigation: ${e.message}`));
//         if(gen!==w.generation||shuttingDown)return false;
//         await showLoadingUI(w.page,'STREAM LOADING',`Chrome ${w.id} connecting to live video <span class="stream-blink">...</span>`);
//         // ORIGINAL website/video discovery engine is preserved.
//         await initializeVideo(w.page,true,false);
//         await muteWorker(w);await hideLoadingUI(w.page);
//         w.status='CHECKING';w.preparing=false;w.lastTime=-1;w.lastFrames=-1;w.frozenSince=null;
//         console.log(`[✅] ${workerLabel(w)} prepared.`);return true;
//     }catch(e){w.preparing=false;w.status='DEAD';w.healthy=false;w.lastError=e.message||String(e);console.log(`[❌] ${workerLabel(w)} failed: ${w.lastError}`);await closeWorker(w);return false;}
// }

// // =========================================================================================
// // 🩺 PER-CHROME HEALTH — 8 SECOND FREEZE THRESHOLD
// // =========================================================================================
// async function probeVideoProgress(w, durationMs=2200){
//     if(!w?.page) return {progressed:false, reason:'NO_PAGE'};
//     const samples=[];
//     const started=Date.now();
//     while(Date.now()-started<durationMs){
//         try{
//             const r=await checkPageStatus(w.page);
//             if(r.status==='CRITICAL_ERROR') return {progressed:false, hardDead:true, reason:'CRITICAL_ERROR'};
//             if(r.status==='HEALTHY') samples.push({t:Number(r.currentTime)||0,f:Number(r.decodedFrames)||0});
//         }catch(e){}
//         await sleep(650);
//     }
//     const first=samples[0], last=samples[samples.length-1];
//     if(!first||!last) return {progressed:false, reason:'NO_HEALTH_SAMPLE'};
//     const timeMoved=Math.abs(last.t-first.t)>0.05;
//     const framesMoved=last.f>0 && first.f>0 && last.f>first.f;
//     return {progressed:timeMoved||framesMoved,timeMoved,framesMoved,samples:samples.length};
// }

// async function evaluateWorker(w){
//     if(!w?.page||w.preparing||w.restarting||w.checkInFlight||shuttingDown)return;
//     w.checkInFlight=true;
//     try{
//         if(!w.browser?.isConnected()){w.status='DEAD';w.healthy=false;return;}
//         const r=await checkPageStatus(w.page);
//         if(r.status==='HEALTHY'){
//             const changed=w.lastTime<0||Math.abs((Number(r.currentTime)||0)-(Number(w.lastTime)||0))>0.05||(r.decodedFrames>0&&r.decodedFrames!==w.lastFrames);
//             if(changed){
//                 w.lastTime=r.currentTime;w.lastFrames=r.decodedFrames;w.frozenSince=null;w.status='HEALTHY';w.healthy=true;w.lastHealthyAt=Date.now();
//             }else{
//                 if(!w.frozenSince)w.frozenSince=Date.now();
//                 const frozen=Date.now()-w.frozenSince;
//                 if(frozen>=FROZEN_THRESHOLD_MS){
//                     // IMPORTANT: 8s is a suspicion threshold, not an immediate kill.
//                     // A live player can briefly stop advancing currentTime/frame counters without actually being dead.
//                     console.log(`[🧪] ${workerLabel(w)} reached 8s no-progress -> running confirmation probe (NOT dead yet)...`);
//                     const probe=await probeVideoProgress(w,2200);
//                     if(probe.progressed){
//                         w.lastTime=r.currentTime;w.lastFrames=r.decodedFrames;w.frozenSince=null;w.status='HEALTHY';w.healthy=true;w.lastHealthyAt=Date.now();
//                         console.log(`[✅] ${workerLabel(w)} freeze FALSE POSITIVE: video progressed during confirmation probe (time=${probe.timeMoved?'YES':'NO'}, frames=${probe.framesMoved?'YES':'NO'}).`);
//                     }else if(probe.hardDead){
//                         w.status='CRITICAL_ERROR';w.healthy=false;console.log(`[💀] ${workerLabel(w)} confirmation probe found CRITICAL_ERROR.`);
//                     }else{
//                         w.status='FROZEN';w.healthy=false;
//                         console.log(`[🧊] ${workerLabel(w)} CONFIRMED FROZEN: 8s threshold + confirmation probe failed.`);
//                     }
//                 }else{
//                     w.status='HEALTHY';w.healthy=true;
//                 }
//             }
//         }else if(r.status==='CRITICAL_ERROR'){
//             w.status='CRITICAL_ERROR';w.healthy=false;w.frozenSince=null;
//         }else{
//             // Do not kill a stream because of ONE transient DEAD read.
//             // Give it two consecutive failed health reads before declaring DEAD.
//             w.deadReadCount=(w.deadReadCount||0)+1;
//             if(w.deadReadCount>=2){w.status='DEAD';w.healthy=false;w.frozenSince=null;}
//             else{w.status=w.active?'HEALTHY':'CHECKING';w.healthy=!!w.active;}
//         }
//         if(r.status==='HEALTHY')w.deadReadCount=0;
//         if(w.status!==w.lastLoggedStatus){
//             console.log(`[📊] ${workerLabel(w)} STATUS: ${w.lastLoggedStatus||'INIT'} -> ${w.status}${w.active?' | 👑 ACTIVE':''}`);
//             w.lastLoggedStatus=w.status;
//         }
//     }catch(e){
//         w.lastError=e.message||String(e);
//         w.errorCount=(w.errorCount||0)+1;
//         if(w.errorCount>=2){w.status='DEAD';w.healthy=false;}
//         if(w.status!==w.lastLoggedStatus){console.log(`[📊] ${workerLabel(w)} STATUS: ${w.lastLoggedStatus||'INIT'} -> ${w.status||'ERROR'} | ${w.lastError}`);w.lastLoggedStatus=w.status||'ERROR';}
//     }
//     finally{w.checkInFlight=false;}
// }

// // =========================================================================================
// // 👑 KING/JUDGE — HEALTH BASED, NEVER NEXT-INDEX BASED
// // =========================================================================================
// function chooseHealthy(){
//     const c=chromeWorkers.filter(w=>w!==activeWorker&&w.healthy&&w.status==='HEALTHY'&&w.page&&w.browser?.isConnected());
//     c.sort((a,b)=>(b.lastHealthyAt||0)-(a.lastHealthyAt||0));
//     return c[0]||null;
// }
// async function activateWorker(w,reason){
//     if(!w?.page||!w.browser?.isConnected())return false;
//     const old=activeWorker;
//     try{
//         console.log(`\n[👑] ==================================================`);
//         console.log(`[👑] ACTIVE ELECTION`);
//         console.log(`[👑] Reason : ${reason}`);
//         console.log(`[👑] Candidate : ${workerLabel(w)} | ${w.url}`);
//         console.log(`[👑] ==================================================`);
//         for(const x of chromeWorkers){
//             if(x!==w){x.active=false;await muteWorker(x);x.audioOk=false;}
//         }
//         w.active=true;activeWorker=w;
//         await w.page.bringToFront();
//         // This click is deliberately before the unmute/play attempt so the page gets a real user-activation-like event.
//         try{await w.page.mouse.click(Math.max(10,Math.floor(RES_W/2)),Math.max(10,Math.floor(RES_H/2)))}catch(e){}
//         await hideLoadingUI(w.page);
//         let audioOk=await unmuteWorker(w);
//         if(!audioOk){
//             console.log(`[🔁] ${workerLabel(w)} AUDIO RETRY: second unmute/play verification...`);
//             await sleep(500);
//             audioOk=await unmuteWorker(w);
//         }
//         await w.page.bringToFront();
//         if(obsConnected){try{await obs.call('SetCurrentProgramScene',{sceneName:'MainScene'});console.log(`[📡] OBS SCENE: MainScene -> ${workerLabel(w)}`);}catch(e){console.log(`[⚠️] OBS scene switch warning: ${e.message}`)}}
//         w.lastTime=-1;w.lastFrames=-1;w.frozenSince=null;w.status='HEALTHY';w.healthy=true;w.audioOk=audioOk;
//         console.log(`[🔊] ACTIVE AUDIO : ${audioOk?'ON / CONFIRMED':'⚠️ NOT CONFIRMED'}`);
//         console.log(`[🔄] ${old&&old!==w?'HOT-SWAP '+workerLabel(old)+' -> ':''}${workerLabel(w)} ACTIVE`);
//         console.log(`[🌐] ACTIVE URL   : ${w.url}`);
//         console.log(`[📺] VIDEO STATUS : HEALTHY`);
//         return true;
//     }catch(e){w.active=false;if(activeWorker===w)activeWorker=old||null;w.status='DEAD';w.healthy=false;console.log(`[❌] ${workerLabel(w)} activation failed: ${e.message}`);return false;}
// }
// async function electActive(reason){
//     if(activeWorker?.healthy&&activeWorker.status==='HEALTHY')return true;
//     const w=chooseHealthy();return w?activateWorker(w,reason):false;
// }

// // =========================================================================================
// // ♻️ BACKGROUND RECOVERY — NEVER DISTURB A HEALTHY ACTIVE STREAM
// // =========================================================================================
// async function restartWorker(w){
//     if(!w||w.restarting||shuttingDown)return;
//     if(w.restartCount>=MAX_WORKER_RESTARTS)return;
//     w.restarting=true;w.restartCount++;const wasActive=activeWorker===w;
//     console.log(`[♻️] ${workerLabel(w)} recovery #${w.restartCount}`);
//     if(wasActive){w.active=false;w.healthy=false;activeWorker=null}
//     await closeWorker(w);w.status='RESTARTING';await sleep(WORKER_RESTART_DELAY_MS);w.restarting=false;
//     await launchWorker(w);await sleep(250);await evaluateWorker(w);
//     if(wasActive||!activeWorker)await electActive(`recovered ${workerLabel(w)}`);
// }
// let lastHealthLogAt=0;
// function printHealthSnapshot(force=false){
//     const now=Date.now();
//     if(!force && now-lastHealthLogAt<10000)return;
//     lastHealthLogAt=now;
//     console.log(`\n[🩺] ================= HEALTH SNAPSHOT =================`);
//     for(const w of chromeWorkers){
//         const active=w===activeWorker?'👑 ACTIVE':'   BACKUP';
//         const audio=w.audioOk?'🔊 ON':'🔇 OFF';
//         const age=w.lastHealthyAt?Math.round((now-w.lastHealthyAt)/1000)+'s':'-';
//         console.log(`[🩺] ${workerLabel(w).padEnd(9)} | ${active} | ${String(w.status).padEnd(16)} | ${audio} | healthy-age ${age} | restarts ${w.restartCount}`);
//     }
//     console.log(`[🩺] ==================================================\n`);
// }

// async function healthTick(){
//     await Promise.all(chromeWorkers.map(evaluateWorker));
//     printHealthSnapshot();
//     if(activeWorker&&activeWorker.healthy&&activeWorker.status==='HEALTHY' && Date.now()-(activeWorker.audioCheckedAt||0)>4000){
//         const ok=await verifyActiveAudio(activeWorker);
//         if(!ok){
//             console.log(`[⚠️] ACTIVE AUDIO LOST: ${workerLabel(activeWorker)} is healthy video-wise but audio is not confirmed. Retrying audio...`);
//             await unmuteWorker(activeWorker);
//         }
//     }
//     if(!activeWorker||!activeWorker.healthy||activeWorker.status!=='HEALTHY'){
//         const old=activeWorker;activeWorker=null;if(old){old.active=false;await muteWorker(old)}
//         const elected=await electActive(`active ${old?workerLabel(old):'none'} unavailable`);
//         if(!elected)for(const w of chromeWorkers)if(['DEAD','FROZEN','CRITICAL_ERROR'].includes(w.status))restartWorker(w).catch(()=>{});
//     }
//     for(const w of chromeWorkers)if(w!==activeWorker&&['DEAD','FROZEN','CRITICAL_ERROR'].includes(w.status))restartWorker(w).catch(()=>{});
// }
// async function startHealthManager(){
//     if(healthManagerStarted)return;healthManagerStarted=true;console.log('[👑] HEALTH MANAGER: 2s polling / 8s freeze threshold');
//     while(!shuttingDown){try{await healthTick()}catch(e){}await sleep(HEALTH_CHECK_INTERVAL_MS)}
// }

// // =========================================================================================
// // 🚀 OBS + STAGGERED CHROMES: LINK1@0s, LINK2@5s, LINK3@10s...
// // =========================================================================================
// async function startDirectStreaming(){
//     console.log('[*] Starting OBS Studio FIRST...');setupOBSConfig();
//     obsProcess=spawn('obs',['--startstreaming','--minimize-to-tray']);
//     obsProcess.stdout.on('data',d=>console.log(`[OBS]: ${d.toString().trim()}`));
//     obsProcess.stderr.on('data',d=>{const m=d.toString().trim();if(m.includes('error')||m.includes('fail'))console.log(`[OBS Error]: ${m}`)});
//     await sleep(6000);obsConnected=false;
//     for(let attempt=1;attempt<=15;attempt++)try{await Promise.race([obs.connect('ws://127.0.0.1:4455','secret'),new Promise((_,rej)=>setTimeout(()=>rej(new Error('Timeout')),3000))]);obsConnected=true;console.log('[+] OBS WebSocket Connected Successfully!');break}catch(e){console.log(`[⏳] OBS retry ${attempt}/15`);await sleep(2000)}
//     if(obsConnected)try{await obs.call('SetCurrentProgramScene',{sceneName:'WaitingScene'})}catch(e){}

//     chromeWorkers=urlList.map((url,i)=>createWorker(i+1,url));
//     console.log(`\n[🧠] ==================================================`);console.log(`[🧠] STREAM ARCHITECTURE READY`);console.log(`[🧠] LINKS           : ${chromeWorkers.length}`);console.log(`[🧠] CHROMES         : ${chromeWorkers.length} independent instances`);console.log(`[🧠] STAGGER         : 5 seconds`);console.log(`[🧠] FREEZE LIMIT    : 8 seconds`);console.log(`[🧠] FAILOVER        : ANY HEALTHY CHROME`);console.log(`[🧠] ACTIVE AUDIO    : ONE CHROME ONLY`);console.log(`[🧠] ==================================================`);
//     const launches=[];
//     for(let i=0;i<chromeWorkers.length;i++){launches.push(launchWorker(chromeWorkers[i]));if(i<chromeWorkers.length-1){console.log(`[⏱️] 5s before ${workerLabel(chromeWorkers[i+1])}...`);await sleep(CHROME_START_STAGGER_MS)}}
//     await Promise.allSettled(launches);
//     await sleep(1000);await Promise.all(chromeWorkers.map(evaluateWorker));await electActive('initial healthy election');
//     if(obsConnected&&activeWorker)try{await obs.call('SetCurrentProgramScene',{sceneName:'MainScene'})}catch(e){}
//     console.log(`\n==================================================\n[🎥] INITIAL CAPTURE STATUS : ${activeWorker?'READY TO BROADCAST':'WAITING FOR HEALTHY CHROME'}\n[👑] ACTIVE CHROME           : ${activeWorker?workerLabel(activeWorker):'NONE'}\n[🌐] ACTIVE URL              : ${activeWorker?activeWorker.url:'NONE'}\n[🔊] ACTIVE AUDIO            : ${activeWorker?(activeWorker.audioOk?'ON / CONFIRMED':'⚠️ NOT CONFIRMED'):'N/A'}\n==================================================\n`);printHealthSnapshot(true);
//     await startHealthManager();
// }

// async function mainLoop(){while(true){try{shuttingDown=false;healthManagerStarted=false;await startDirectStreaming()}catch(e){console.error(`[!] ALERT: ${e.message}`);await cleanup();await sleep(3000)}}}

// async function cleanup(){
//     shuttingDown=true;healthManagerStarted=false;
//     for(const w of chromeWorkers)try{await closeWorker(w)}catch(e){}
//     chromeWorkers=[];activeWorker=null;obsConnected=false;
//     try{await obs.disconnect()}catch(e){}
//     if(obsProcess)try{obsProcess.kill('SIGKILL')}catch(e){}
//     obsProcess=null;
//     try{execSync('pkill -9 obs || true',{stdio:'ignore'});execSync('pkill -9 chrome || true',{stdio:'ignore'});execSync('pkill -9 puppeteer || true',{stdio:'ignore'})}catch(e){}
// }
// process.on('SIGINT',async()=>{await cleanup();process.exit(0)});
// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();







// chatgpt nchrome iss me autio nahey hai active waley k 



// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 4000000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let obsProcess = null;

// // =========================================================================================
// // 🧠 MULTI-CHROME STREAM WORKER ARCHITECTURE
// // 1 URL = 1 independent Puppeteer/Chrome instance.
// // =========================================================================================
// const CHROME_START_STAGGER_MS = 5000;
// const HEALTH_CHECK_INTERVAL_MS = 2000;
// const FROZEN_THRESHOLD_MS = 8000; // 8 seconds
// const WORKER_RESTART_DELAY_MS = 3000;
// const MAX_WORKER_RESTARTS = 5;
// let chromeWorkers = [];
// let activeWorker = null;
// let obsConnected = false;
// let shuttingDown = false;
// let healthManagerStarted = false;
// function createWorker(id,url){return {id,url,browser:null,page:null,status:'STARTING',healthy:false,active:false,lastTime:-1,lastFrames:-1,frozenSince:null,lastHealthyAt:0,lastCheckAt:0,startupAt:Date.now(),restartCount:0,preparing:false,restarting:false,checkInFlight:false,lastError:'',generation:0};}

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // Permanent root execution block for popup alerts & confirms
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; }`;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }


// // =========================================================================================
// // 🧩 MULTI-CHROME HELPERS
// // =========================================================================================
// function sleep(ms){ return new Promise(r=>setTimeout(r,ms)); }
// function workerLabel(w){ return `Chrome ${w.id}`; }
// function browserArgs(w){
//     const profile=path.join(process.cwd(),'.stream-chrome-profiles',`chrome-${w.id}`);
//     fs.mkdirSync(profile,{recursive:true});
//     const a=['--no-sandbox','--disable-setuid-sandbox',`--window-size=${RES_W},${RES_H}`,'--window-position=0,0','--kiosk','--start-fullscreen','--autoplay-policy=no-user-gesture-required','--disable-dev-shm-usage','--ignore-certificate-errors','--disable-web-security','--ignore-gpu-blocklist','--use-gl=egl','--disable-accelerated-video-decode','--disable-accelerated-video-encode','--disable-smooth-scrolling','--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion','--disable-background-timer-throttling','--disable-backgrounding-occluded-windows','--disable-renderer-backgrounding',`--disable-extensions-except=${path.join(process.cwd(),'ublock-lite')}`,`--load-extension=${path.join(process.cwd(),'ublock-lite')}`,`--user-data-dir=${profile}`];
//     if(PROXY_ENGINE.includes('Cloudflare')) a.push('--proxy-server=socks5://127.0.0.1:40000');
//     return a;
// }
// async function muteWorker(w){
//     if(!w?.page)return;
//     for(const f of w.page.frames()){try{if(!f.isDetached())await f.evaluate(()=>document.querySelectorAll('video,audio').forEach(m=>{m.muted=true;m.volume=0}));}catch(e){}}
// }
// async function unmuteWorker(w){
//     if(!w?.page)return;
//     try{await triggerSmartUnmute(w.page);}catch(e){}
//     for(const f of w.page.frames()){try{if(!f.isDetached())await f.evaluate(()=>{document.querySelectorAll('video,audio').forEach(m=>{m.muted=false;m.volume=1});document.querySelectorAll('.jw-icon-volume.jw-off,.vjs-vol-muted,.plyr__control--pressed[data-plyr="mute"]').forEach(b=>{try{b.click()}catch(e){}})});}catch(e){}}
// }
// async function closeWorker(w){
//     try{await muteWorker(w)}catch(e){}
//     try{if(w.browser)await w.browser.close()}catch(e){}
//     w.browser=null;w.page=null;
// }

// // =========================================================================================
// // 🚀 ONE URL = ONE INDEPENDENT CHROME
// // =========================================================================================
// async function launchWorker(w){
//     if(shuttingDown||w.restarting)return false;
//     w.status='STARTING';w.healthy=false;w.preparing=true;w.generation++;const gen=w.generation;
//     try{
//         console.log(`\n[🚀] ${workerLabel(w)} -> LINK ${w.id}: ${w.url}`);
//         w.browser=await puppeteer.launch({headless:false,defaultViewport:{width:RES_W,height:RES_H},ignoreDefaultArgs:['--enable-automation'],args:browserArgs(w)});
//         if(gen!==w.generation||shuttingDown)return false;
//         w.browser.on('targetcreated',async target=>{try{if(target.type()!=='page')return;const p=await target.page();setTimeout(async()=>{if(p&&p!==w.page)try{await p.close()}catch(e){}},500)}catch(e){}});
//         w.browser.on('disconnected',()=>{if(!shuttingDown&&gen===w.generation){w.status='DEAD';w.healthy=false;console.log(`[💥] ${workerLabel(w)} disconnected.`)}});
//         const pages=await w.browser.pages();w.page=pages[0]||await w.browser.newPage();
//         await setupNetworkAdBlocker(w.page);attachAntiAdListeners(w.page);await applyPreloadFirewall(w.page);
//         w.status='LOADING';
//         await w.page.goto(w.url,{waitUntil:'domcontentloaded',timeout:60000}).catch(e=>console.log(`[⚠️] ${workerLabel(w)} navigation: ${e.message}`));
//         if(gen!==w.generation||shuttingDown)return false;
//         await showLoadingUI(w.page,'STREAM LOADING',`Chrome ${w.id} connecting to live video <span class="stream-blink">...</span>`);
//         // ORIGINAL website/video discovery engine is preserved.
//         await initializeVideo(w.page,true,false);
//         await muteWorker(w);await hideLoadingUI(w.page);
//         w.status='CHECKING';w.preparing=false;w.lastTime=-1;w.lastFrames=-1;w.frozenSince=null;
//         console.log(`[✅] ${workerLabel(w)} prepared.`);return true;
//     }catch(e){w.preparing=false;w.status='DEAD';w.healthy=false;w.lastError=e.message||String(e);console.log(`[❌] ${workerLabel(w)} failed: ${w.lastError}`);await closeWorker(w);return false;}
// }

// // =========================================================================================
// // 🩺 PER-CHROME HEALTH — 8 SECOND FREEZE THRESHOLD
// // =========================================================================================
// async function evaluateWorker(w){
//     if(!w?.page||w.preparing||w.restarting||w.checkInFlight||shuttingDown)return;
//     w.checkInFlight=true;
//     try{
//         if(!w.browser?.isConnected()){w.status='DEAD';w.healthy=false;return;}
//         const r=await checkPageStatus(w.page);
//         if(r.status==='HEALTHY'){
//             const changed=w.lastTime<0||r.currentTime!==w.lastTime||(r.decodedFrames>0&&r.decodedFrames!==w.lastFrames);
//             if(changed){w.lastTime=r.currentTime;w.lastFrames=r.decodedFrames;w.frozenSince=null;w.status='HEALTHY';w.healthy=true;w.lastHealthyAt=Date.now();}
//             else{if(!w.frozenSince)w.frozenSince=Date.now();const frozen=Date.now()-w.frozenSince;if(frozen>=FROZEN_THRESHOLD_MS){w.status='FROZEN';w.healthy=false;console.log(`[🧊] ${workerLabel(w)} frozen > 8s.`)}else{w.status='HEALTHY';w.healthy=true;}}
//         }else{w.status=r.status||'DEAD';w.healthy=false;w.frozenSince=null;}
//     }catch(e){w.status='DEAD';w.healthy=false;w.lastError=e.message||String(e)}
//     finally{w.checkInFlight=false;}
// }

// // =========================================================================================
// // 👑 KING/JUDGE — HEALTH BASED, NEVER NEXT-INDEX BASED
// // =========================================================================================
// function chooseHealthy(){
//     const c=chromeWorkers.filter(w=>w!==activeWorker&&w.healthy&&w.status==='HEALTHY'&&w.page&&w.browser?.isConnected());
//     c.sort((a,b)=>(b.lastHealthyAt||0)-(a.lastHealthyAt||0));
//     return c[0]||null;
// }
// async function activateWorker(w,reason){
//     if(!w?.page||!w.browser?.isConnected())return false;
//     const old=activeWorker;
//     try{
//         console.log(`\n[👑] ACTIVE ELECTION: ${reason} -> ${workerLabel(w)}`);
//         for(const x of chromeWorkers)if(x!==w){x.active=false;await muteWorker(x)}
//         w.active=true;activeWorker=w;await w.page.bringToFront();try{await w.page.mouse.click(10,10)}catch(e){}
//         await hideLoadingUI(w.page);await unmuteWorker(w);await w.page.bringToFront();
//         if(obsConnected)try{await obs.call('SetCurrentProgramScene',{sceneName:'MainScene'})}catch(e){}
//         w.lastTime=-1;w.lastFrames=-1;w.frozenSince=null;w.status='HEALTHY';w.healthy=true;
//         console.log(`[🔄] ${old&&old!==w?'HOT-SWAP '+workerLabel(old)+' -> ':''}${workerLabel(w)} ACTIVE | ${w.url}`);
//         return true;
//     }catch(e){w.active=false;if(activeWorker===w)activeWorker=old||null;w.status='DEAD';w.healthy=false;return false;}
// }
// async function electActive(reason){
//     if(activeWorker?.healthy&&activeWorker.status==='HEALTHY')return true;
//     const w=chooseHealthy();return w?activateWorker(w,reason):false;
// }

// // =========================================================================================
// // ♻️ BACKGROUND RECOVERY — NEVER DISTURB A HEALTHY ACTIVE STREAM
// // =========================================================================================
// async function restartWorker(w){
//     if(!w||w.restarting||shuttingDown)return;
//     if(w.restartCount>=MAX_WORKER_RESTARTS)return;
//     w.restarting=true;w.restartCount++;const wasActive=activeWorker===w;
//     console.log(`[♻️] ${workerLabel(w)} recovery #${w.restartCount}`);
//     if(wasActive){w.active=false;w.healthy=false;activeWorker=null}
//     await closeWorker(w);w.status='RESTARTING';await sleep(WORKER_RESTART_DELAY_MS);w.restarting=false;
//     await launchWorker(w);await sleep(250);await evaluateWorker(w);
//     if(wasActive||!activeWorker)await electActive(`recovered ${workerLabel(w)}`);
// }
// async function healthTick(){
//     await Promise.all(chromeWorkers.map(evaluateWorker));
//     if(!activeWorker||!activeWorker.healthy||activeWorker.status!=='HEALTHY'){
//         const old=activeWorker;activeWorker=null;if(old){old.active=false;await muteWorker(old)}
//         const elected=await electActive(`active ${old?workerLabel(old):'none'} unavailable`);
//         if(!elected)for(const w of chromeWorkers)if(['DEAD','FROZEN','CRITICAL_ERROR'].includes(w.status))restartWorker(w).catch(()=>{});
//     }
//     for(const w of chromeWorkers)if(w!==activeWorker&&['DEAD','FROZEN','CRITICAL_ERROR'].includes(w.status))restartWorker(w).catch(()=>{});
// }
// async function startHealthManager(){
//     if(healthManagerStarted)return;healthManagerStarted=true;console.log('[👑] HEALTH MANAGER: 2s polling / 8s freeze threshold');
//     while(!shuttingDown){try{await healthTick()}catch(e){}await sleep(HEALTH_CHECK_INTERVAL_MS)}
// }

// // =========================================================================================
// // 🚀 OBS + STAGGERED CHROMES: LINK1@0s, LINK2@5s, LINK3@10s...
// // =========================================================================================
// async function startDirectStreaming(){
//     console.log('[*] Starting OBS Studio FIRST...');setupOBSConfig();
//     obsProcess=spawn('obs',['--startstreaming','--minimize-to-tray']);
//     obsProcess.stdout.on('data',d=>console.log(`[OBS]: ${d.toString().trim()}`));
//     obsProcess.stderr.on('data',d=>{const m=d.toString().trim();if(m.includes('error')||m.includes('fail'))console.log(`[OBS Error]: ${m}`)});
//     await sleep(6000);obsConnected=false;
//     for(let attempt=1;attempt<=15;attempt++)try{await Promise.race([obs.connect('ws://127.0.0.1:4455','secret'),new Promise((_,rej)=>setTimeout(()=>rej(new Error('Timeout')),3000))]);obsConnected=true;console.log('[+] OBS WebSocket Connected Successfully!');break}catch(e){console.log(`[⏳] OBS retry ${attempt}/15`);await sleep(2000)}
//     if(obsConnected)try{await obs.call('SetCurrentProgramScene',{sceneName:'WaitingScene'})}catch(e){}

//     chromeWorkers=urlList.map((url,i)=>createWorker(i+1,url));
//     console.log(`\n[🧠] ${chromeWorkers.length} LINKS => ${chromeWorkers.length} INDEPENDENT CHROMES | 5s stagger | 8s freeze | ANY HEALTHY FAILOVER`);
//     const launches=[];
//     for(let i=0;i<chromeWorkers.length;i++){launches.push(launchWorker(chromeWorkers[i]));if(i<chromeWorkers.length-1){console.log(`[⏱️] 5s before ${workerLabel(chromeWorkers[i+1])}...`);await sleep(CHROME_START_STAGGER_MS)}}
//     await Promise.allSettled(launches);
//     await sleep(1000);await Promise.all(chromeWorkers.map(evaluateWorker));await electActive('initial healthy election');
//     if(obsConnected&&activeWorker)try{await obs.call('SetCurrentProgramScene',{sceneName:'MainScene'})}catch(e){}
//     console.log(`\n==================================================\n[🎥] INITIAL CAPTURE STATUS: ${activeWorker?'Ready to Broadcast':'WAITING FOR HEALTHY CHROME'}\n[👑] ACTIVE: ${activeWorker?workerLabel(activeWorker)+' -> '+activeWorker.url:'NONE'}\n==================================================\n`);
//     await startHealthManager();
// }

// async function mainLoop(){while(true){try{shuttingDown=false;healthManagerStarted=false;await startDirectStreaming()}catch(e){console.error(`[!] ALERT: ${e.message}`);await cleanup();await sleep(3000)}}}

// async function cleanup(){
//     shuttingDown=true;healthManagerStarted=false;
//     for(const w of chromeWorkers)try{await closeWorker(w)}catch(e){}
//     chromeWorkers=[];activeWorker=null;obsConnected=false;
//     try{await obs.disconnect()}catch(e){}
//     if(obsProcess)try{obsProcess.kill('SIGKILL')}catch(e){}
//     obsProcess=null;
//     try{execSync('pkill -9 obs || true',{stdio:'ignore'});execSync('pkill -9 chrome || true',{stdio:'ignore'});execSync('pkill -9 puppeteer || true',{stdio:'ignore'})}catch(e){}
// }
// process.on('SIGINT',async()=>{await cleanup();process.exit(0)});
// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();














































































































































































// chatgpt sigle link code streamed work


// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let browser = null;
// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // Permanent root execution block for popup alerts & confirms
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; }`;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();

//     while (true) {
//         if (!browser || !browser.isConnected()) throw new Error("Browser closed.");

//         let activeStatus = await checkPageStatus(activePage);

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        
//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             console.log(`\n[💓] WATCHDOG HEARTBEAT: Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[▶️] CURRENTLY LIVE   : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
//             console.log(`[⏭️] NEXT IN QUEUE    : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//         }

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || backupStatus.status === 'DEAD') { 
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 let brokenPage = activePage; activePage = backupPage; backupPage = brokenPage;
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//             } else {
//                 console.error(`[!] ❌ Backup Tab is ALSO DEAD/FROZEN. Hard Restarting System...`);
//                 throw new Error("Both Active and Backup tabs failed.");
//             }
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }

//     browser = await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });

//     browser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => {
//                 if (newPage && newPage !== activePage && newPage !== backupPage) {
//                     console.log(`[🛡️] AD-BLOCKER: Killed an unwanted pop-up tab!`);
//                     try { await newPage.close(); } catch(e) {}
//                 }
//             }, 500);
//         }
//     });

//     const pages = await browser.pages();
//     activePage = pages[0]; 
//     backupPage = await browser.newPage();
    
//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//     await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
//     backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex]}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex]}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (browser) { try { await browser.close(); } catch(e) { } browser = null; }
//     if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//     try {
//         execSync('pkill -9 obs || true', { stdio: 'ignore' });
//         execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//         execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//     } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();

















// full last code wala tha old very old architecture . iska time 40 se hataa deya hai 



// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 4000000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let browser = null;
// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// // const FROZEN_THRESHOLD_MS = 8000; 
// const FROZEN_THRESHOLD_MS = 20 * 60 * 1000; // 20 minutes

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // Permanent root execution block for popup alerts & confirms
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; }`;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();

//     while (true) {
//         if (!browser || !browser.isConnected()) throw new Error("Browser closed.");

//         let activeStatus = await checkPageStatus(activePage);

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        
//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             console.log(`\n[💓] WATCHDOG HEARTBEAT: Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[▶️] CURRENTLY LIVE   : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
//             console.log(`[⏭️] NEXT IN QUEUE    : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//         }

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || backupStatus.status === 'DEAD') { 
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 let brokenPage = activePage; activePage = backupPage; backupPage = brokenPage;
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//             } else {
//                 console.error(`[!] ❌ Backup Tab is ALSO DEAD/FROZEN. Hard Restarting System...`);
//                 throw new Error("Both Active and Backup tabs failed.");
//             }
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }

//     browser = await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });

//     browser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => {
//                 if (newPage && newPage !== activePage && newPage !== backupPage) {
//                     console.log(`[🛡️] AD-BLOCKER: Killed an unwanted pop-up tab!`);
//                     try { await newPage.close(); } catch(e) {}
//                 }
//             }, 500);
//         }
//     });

//     const pages = await browser.pages();
//     activePage = pages[0]; 
//     backupPage = await browser.newPage();
    
//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//     await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
//     backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex]}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex]}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (browser) { try { await browser.close(); } catch(e) { } browser = null; }
//     if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//     try {
//         execSync('pkill -9 obs || true', { stdio: 'ignore' });
//         execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//         execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//     } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();











































// ------------- yeh bey chutya code  hai --- start -- iss code me background jab empty huta hu tuu next par nahey jata 


// const puppeteer = require('puppeteer-extra');let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let activeBrowser = null;
// let backupBrowser = null;

// // Name tags for tracking
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";

// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;




// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // =========================================================
//             // 🛑 ULTIMATE SHADOW DOM AD KILLER (Monkey Patching)
//             // =========================================================
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') {
//                     init.mode = 'open'; // Force open to allow inspection
//                 }
//                 const shadowRoot = originalAttachShadow.call(this, init);
                
//                 const observer = new MutationObserver(() => {
//                     // UPDATED: Hardened selector string catching wildcard IDs and payload sources
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) {
//                         console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
//                         this.remove(); // Kill the entire host element
//                     }
//                 });
                
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };

//             // Standard Protections
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             // UPDATED: Added fallback display:none for the mutated shadow tags
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // ⚠️ NEW: INSTANT HANG / RECOVERY SHIELD UI (Unique Radar Design)
// // =========================================================================================
// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 let isMuted = false;
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     // Check if actually muted
//                     if (media.muted || media.volume < 0.1) isMuted = true;
//                     // Force properties anyway
//                     media.muted = false;
//                     media.volume = 1.0;
//                 });

//                 // 🛑 AGAR AWAZ PEHLE SE KHULI HAI, TO KOI BUTTON CLICK MAT KARO! (Yeh Toggle rok dega)
//                 if (!isMuted) return;

//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             // 👇 FIX: Purane kisi bhi interval ko kill karo taake overlap na ho!
//             if (window.audioEnforcerInterval) {
//                 clearInterval(window.audioEnforcerInterval);
//             }

//             // Naye interval ko variable mein save karo
//             window.audioEnforcerInterval = setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     if (!document.head.querySelector('style[data-ui-hide]')) {
//                         style.setAttribute('data-ui-hide', 'true');
//                         document.head.appendChild(style);
//                     }

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         let actualMuted = false;
//                         document.querySelectorAll('video, audio').forEach(m => { if(m.muted || m.volume < 0.1) actualMuted = true; });
//                         if (actualMuted) {
//                             // Clicks sirf tab karein jab zaroorat ho, bar bar spam na karein
//                             document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                         }
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); // 👈 YEH NAYI LINE ADD KAREIN
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();
    
//     // 🔥 NEW: Track recovery UI state
//     let isRecoveryUIShown = false;

//     while (true) {
//         if (!activeBrowser || !activeBrowser.isConnected() || !backupBrowser || !backupBrowser.isConnected()) {
//             throw new Error("One or both browser instances disconnected.");
//         }

//         let activeStatus = await checkPageStatus(activePage);

// // 👇=== UPDATED BACKGROUND SHIELD CODE ===👇
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { // 👈 30 seconds ka cooldown add kiya
//             let backupStatus = await checkPageStatus(backupPage);
            
//             /// 👇 FIX: Background tab ko Chrome aksar pause kar deta hai (DEAD return karta hai).
//         // Isliye ab hum DEAD ya FROZEN par isay restart nahi karenge, sirf CRITICAL_ERROR par karenge!
//         if (backupStatus.status === 'CRITICAL_ERROR') {
//             console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] hit a Critical Error. Restoring...`);
                
//                 // Next link par jump karein
//                 backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
                
//                 backupWarmupTime = Date.now(); // 👈 COOLDOWN RESET (Taake agli dafa 30 sec wait kare)
                
//                 // Backup browser ko silently naye link par bhej dein
//                 // Backup browser ko silently naye link par bhej dein
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
                    
//                     // Await lazmi hai taake dom load hone ke baad video initialize ho
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     // 👇 FIX: Background tab ko initialize karo
//                 await initializeVideo(backupPage, true, false);
                
//                 // 🛡️ DISPLAY SHIELD: Agar background reload hone se screen par aa gaya hai, toh Active ko wapas upar lao!
//                 if (activePage) {
//                     await activePage.bringToFront().catch(()=>{});
//                 }
                
//                 // Backup ko dobara mute frame state mein set karein (Fail-safe)
                    
//                     // Backup ko dobara mute frame state mein set karein (Fail-safe)
//                     for (const frame of backupPage.frames()) {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch(e) {}
//             }
//         }
//         // 👆=== UPDATED CODE END ===👆
        

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 // 🔥 NEW: INSTANT HANG RECOVERY SHIELD TRIGGER (0 Seconds Delay)
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen. Triggering HOT-SWAP logic.`);
//                     }
//                     // Yahan se isRecoveryUIShown ko false karne wali line HATA DI GAYI HAI, 
//                     // taake jab tak Chrome 2 ready na ho, Radar UI screen par hold rahey.
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 // 🔥 NEW: STREAM RECOVERED (Instant hide)
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 let isMuted = false;
//                                 document.querySelectorAll('video, audio').forEach(m => { 
//                                     if(m.muted || m.volume < 0.1) isMuted = true;
//                                     m.muted = false; 
//                                     m.volume = 1.0; 
//                                 }); 
//                                 // if (isMuted) {
//                                 //     document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                                 // }
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        


//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             // Background (Chrome 2) ka status sirf logs print karne ke liye check karein
//             let logBackupStatus = await checkPageStatus(backupPage);
            
//             // Queue mein next URL calculate karein
//             let nextAfterBackupIndex = (backupUrlIndex + 1) % urlList.length;
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex];
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
// console.log(`[▶️] CURRENTLY LIVE             : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
// console.log(`--------------------------------------------------`);
// console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND      : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

        

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});

//                     // 👇 YEH NAYI LINE ADD KI HAI 👇
//                     if (activePage) await activePage.bringToFront().catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || backupStatus.status === 'DEAD') { 
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 // Swap Pages
// let brokenPage = activePage; 
// activePage = backupPage; 
// backupPage = brokenPage;

// // Swap Browsers
// let brokenBrowser = activeBrowser; 
// activeBrowser = backupBrowser; 
// backupBrowser = brokenBrowser;


// // Swap Names taake pata chale screen par konsa aya hai
// let brokenName = activeBrowserName;
// activeBrowserName = backupBrowserName;
// backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; // 🔥 Fresh slate for new active tab

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});

//                     // 👇 YEH NAYI LINE ADD KAREIN: Naye Active tab ka focus secure karein
//                     if (activePage) await activePage.bringToFront().catch(()=>{});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//                 } else {
//                 // 👇 NAYI LOGIC: Agar hang (FROZEN) hai aur Chrome 2 tayar nahi hai, toh ruk jao!
//                 if (activeStatus.status === 'FROZEN') {
//                     console.log(`[⏳] HOLDING SHIELD: Chrome 1 HUNG hai, par Chrome 2 abhi READY nahi hai (${backupStatus.status}).`);
//                     console.log(`[*] Chrome 1 screen par hold hai. Background mein Chrome 2 ko musalsal check kiya ja raha hai...`);
                    
//                     await new Promise(r => setTimeout(r, 1000));
//                     continue; // Loop yahan se dobara shuru hoga bina soft_reset kiye!
//                 } 
//                 // Agar tab mukammal CRASH (DEAD ya CRITICAL ERROR) hai, toh foran soft reset karo
//                 else {
//                     console.error(`\n[!] ❌ DONO TABS (Active & Backup) CRASHED! Initiating Soft Recovery...`);
//                     console.log(`[*] Shifting OBS to WaitingScene (Overlay) to keep stream alive...`);
                    
//                     try {
//                         await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//                     } catch (e) {}

//                     return "SOFT_RESET"; 
//                 }
//             }

                
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     // 👇=== YEH 2 LINES SHURU MEIN ADD KAREIN ===👇
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }

//     let keepStreaming = true;
    
//     while (keepStreaming) {
//         console.log(`\n[*] Starting ACTIVE browser instance (CHROME 1)...`);
//         activeBrowser = await createBrowserInstance(browserArgs);
//         const activePages = await activeBrowser.pages();
//         activePage = activePages[0];

//         console.log(`[*] Starting BACKUP browser instance (CHROME 2) in background...`);
//         backupBrowser = await createBrowserInstance(browserArgs);
//         const backupPages = await backupBrowser.pages();
//         backupPage = backupPages[0];

//         activeBrowser.on('targetcreated', async (target) => {
//             if (target.type() === 'page') {
//                 const newPage = await target.page();
//                 setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//             }
//         });

//         backupBrowser.on('targetcreated', async (target) => {
//             if (target.type() === 'page') {
//                 const newPage = await target.page();
//                 setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//             }
//         });

//         await setupNetworkAdBlocker(activePage);
//         await setupNetworkAdBlocker(backupPage);

//         attachAntiAdListeners(activePage);
//         attachAntiAdListeners(backupPage);

//         await applyPreloadFirewall(activePage);
//         await applyPreloadFirewall(backupPage);

//         await activePage.bringToFront(); 

//         console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//         await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
        
//         await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
        
//         await initializeVideo(activePage, false, true); 
//         await hideLoadingUI(activePage); 

//         if (isObsConnected) {
//             console.log('\n[*] Active Video is Ready! Shifting OBS from Overlay to LIVE Video (MainScene)...');
//             try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//         }

//         console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
        
//         // Await the navigation so it actually loads before initializing
//         await backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
        
//         // 👇 FIX: Ab backup page par bhi initializeVideo call hoga (startMuted = true, isActivePage = false)
//         console.log(`[*] Initializing Video pipeline on Backup Page...`);
//         await initializeVideo(backupPage, true, false);
        
//         await activePage.bringToFront();
//         try { await activePage.mouse.click(10, 10); } catch(e){} 
//         await hideLoadingUI(activePage);

//         console.log(`\n==================================================`);
//         console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//         console.log(`==================================================`);
//         console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
        
//         let watchdogResult = await startWatchdog();
        
//         if (watchdogResult === "SOFT_RESET") {
//             console.log(`\n==================================================`);
//             console.log(`[🔄] SOFT RESET EXECUTING: Closing old browsers safely...`);
//             console.log(`==================================================`);
            
//             if (activeBrowser) { try { await activeBrowser.close(); } catch(e){} activeBrowser = null; }
//             if (backupBrowser) { try { await backupBrowser.close(); } catch(e){} backupBrowser = null; }
            
//             currentUrlIndex = (currentUrlIndex + 1) % urlList.length;
//             backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
            
//             console.log(`[*] 🚀 Relaunching Fresh Chrome Instances for Next Servers...`);
//             await new Promise(r => setTimeout(r, 4000)); 
//         } else {
//             keepStreaming = false; 
//         }
//     }
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//         if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//         try {
//             execSync('pkill -9 obs || true', { stdio: 'ignore' });
//             execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//             execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//         } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();























// ---------------


// iss me issue hai issue yeh hai k background tabs ok.ru me show hu raha ahi juu chrome 1 means active hai wo teek hai lekin woo show nahey hu raha hai . 


// const puppeteer = require('puppeteer-extra');let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let activeBrowser = null;
// let backupBrowser = null;

// // Name tags for tracking
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";

// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;




// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // =========================================================
//             // 🛑 ULTIMATE SHADOW DOM AD KILLER (Monkey Patching)
//             // =========================================================
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') {
//                     init.mode = 'open'; // Force open to allow inspection
//                 }
//                 const shadowRoot = originalAttachShadow.call(this, init);
                
//                 const observer = new MutationObserver(() => {
//                     // UPDATED: Hardened selector string catching wildcard IDs and payload sources
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) {
//                         console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
//                         this.remove(); // Kill the entire host element
//                     }
//                 });
                
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };

//             // Standard Protections
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             // UPDATED: Added fallback display:none for the mutated shadow tags
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // ⚠️ NEW: INSTANT HANG / RECOVERY SHIELD UI (Unique Radar Design)
// // =========================================================================================
// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 let isMuted = false;
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     // Check if actually muted
//                     if (media.muted || media.volume < 0.1) isMuted = true;
//                     // Force properties anyway
//                     media.muted = false;
//                     media.volume = 1.0;
//                 });

//                 // 🛑 AGAR AWAZ PEHLE SE KHULI HAI, TO KOI BUTTON CLICK MAT KARO! (Yeh Toggle rok dega)
//                 if (!isMuted) return;

//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             // 👇 FIX: Purane kisi bhi interval ko kill karo taake overlap na ho!
//             if (window.audioEnforcerInterval) {
//                 clearInterval(window.audioEnforcerInterval);
//             }

//             // Naye interval ko variable mein save karo
//             window.audioEnforcerInterval = setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     if (!document.head.querySelector('style[data-ui-hide]')) {
//                         style.setAttribute('data-ui-hide', 'true');
//                         document.head.appendChild(style);
//                     }

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         let actualMuted = false;
//                         document.querySelectorAll('video, audio').forEach(m => { if(m.muted || m.volume < 0.1) actualMuted = true; });
//                         if (actualMuted) {
//                             // Clicks sirf tab karein jab zaroorat ho, bar bar spam na karein
//                             document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                         }
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); // 👈 YEH NAYI LINE ADD KAREIN
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();
    
//     // 🔥 NEW: Track recovery UI state
//     let isRecoveryUIShown = false;

//     while (true) {
//         if (!activeBrowser || !activeBrowser.isConnected() || !backupBrowser || !backupBrowser.isConnected()) {
//             throw new Error("One or both browser instances disconnected.");
//         }

//         let activeStatus = await checkPageStatus(activePage);

// // 👇=== UPDATED BACKGROUND SHIELD CODE ===👇
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { // 👈 30 seconds ka cooldown add kiya
//             let backupStatus = await checkPageStatus(backupPage);
            
//             // Agar backup background mein hang hai ya error de raha hai
//             if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
//                 console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
//                 // Next link par jump karein
//                 backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
                
//                 backupWarmupTime = Date.now(); // 👈 COOLDOWN RESET (Taake agli dafa 30 sec wait kare)
                
//                 // Backup browser ko silently naye link par bhej dein
//                 // Backup browser ko silently naye link par bhej dein
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
                    
//                     // Await lazmi hai taake dom load hone ke baad video initialize ho
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     // 👇 FIX: Background tab ko initialize karo taake checkPageStatus isay HEALTHY read kar sake
//                     await initializeVideo(backupPage, true, false);
                    
//                     // Backup ko dobara mute frame state mein set karein (Fail-safe)
//                     for (const frame of backupPage.frames()) {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch(e) {}
//             }
//         }
//         // 👆=== UPDATED CODE END ===👆
        

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 // 🔥 NEW: INSTANT HANG RECOVERY SHIELD TRIGGER (0 Seconds Delay)
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen. Triggering HOT-SWAP logic.`);
//                     }
//                     // Yahan se isRecoveryUIShown ko false karne wali line HATA DI GAYI HAI, 
//                     // taake jab tak Chrome 2 ready na ho, Radar UI screen par hold rahey.
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 // 🔥 NEW: STREAM RECOVERED (Instant hide)
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 let isMuted = false;
//                                 document.querySelectorAll('video, audio').forEach(m => { 
//                                     if(m.muted || m.volume < 0.1) isMuted = true;
//                                     m.muted = false; 
//                                     m.volume = 1.0; 
//                                 }); 
//                                 // if (isMuted) {
//                                 //     document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                                 // }
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        


//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             // Background (Chrome 2) ka status sirf logs print karne ke liye check karein
//             let logBackupStatus = await checkPageStatus(backupPage);
            
//             // Queue mein next URL calculate karein
//             let nextAfterBackupIndex = (backupUrlIndex + 1) % urlList.length;
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex];
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
// console.log(`[▶️] CURRENTLY LIVE             : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
// console.log(`--------------------------------------------------`);
// console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND      : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

        

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || backupStatus.status === 'DEAD') { 
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 // Swap Pages
// let brokenPage = activePage; 
// activePage = backupPage; 
// backupPage = brokenPage;

// // Swap Browsers
// let brokenBrowser = activeBrowser; 
// activeBrowser = backupBrowser; 
// backupBrowser = brokenBrowser;


// // Swap Names taake pata chale screen par konsa aya hai
// let brokenName = activeBrowserName;
// activeBrowserName = backupBrowserName;
// backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; // 🔥 Fresh slate for new active tab

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//                 } else {
//                 // 👇 NAYI LOGIC: Agar hang (FROZEN) hai aur Chrome 2 tayar nahi hai, toh ruk jao!
//                 if (activeStatus.status === 'FROZEN') {
//                     console.log(`[⏳] HOLDING SHIELD: Chrome 1 HUNG hai, par Chrome 2 abhi READY nahi hai (${backupStatus.status}).`);
//                     console.log(`[*] Chrome 1 screen par hold hai. Background mein Chrome 2 ko musalsal check kiya ja raha hai...`);
                    
//                     await new Promise(r => setTimeout(r, 1000));
//                     continue; // Loop yahan se dobara shuru hoga bina soft_reset kiye!
//                 } 
//                 // Agar tab mukammal CRASH (DEAD ya CRITICAL ERROR) hai, toh foran soft reset karo
//                 else {
//                     console.error(`\n[!] ❌ DONO TABS (Active & Backup) CRASHED! Initiating Soft Recovery...`);
//                     console.log(`[*] Shifting OBS to WaitingScene (Overlay) to keep stream alive...`);
                    
//                     try {
//                         await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//                     } catch (e) {}

//                     return "SOFT_RESET"; 
//                 }
//             }

                
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     // 👇=== YEH 2 LINES SHURU MEIN ADD KAREIN ===👇
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }

//     let keepStreaming = true;
    
//     while (keepStreaming) {
//         console.log(`\n[*] Starting ACTIVE browser instance (CHROME 1)...`);
//         activeBrowser = await createBrowserInstance(browserArgs);
//         const activePages = await activeBrowser.pages();
//         activePage = activePages[0];

//         console.log(`[*] Starting BACKUP browser instance (CHROME 2) in background...`);
//         backupBrowser = await createBrowserInstance(browserArgs);
//         const backupPages = await backupBrowser.pages();
//         backupPage = backupPages[0];

//         activeBrowser.on('targetcreated', async (target) => {
//             if (target.type() === 'page') {
//                 const newPage = await target.page();
//                 setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//             }
//         });

//         backupBrowser.on('targetcreated', async (target) => {
//             if (target.type() === 'page') {
//                 const newPage = await target.page();
//                 setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//             }
//         });

//         await setupNetworkAdBlocker(activePage);
//         await setupNetworkAdBlocker(backupPage);

//         attachAntiAdListeners(activePage);
//         attachAntiAdListeners(backupPage);

//         await applyPreloadFirewall(activePage);
//         await applyPreloadFirewall(backupPage);

//         await activePage.bringToFront(); 

//         console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//         await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
        
//         await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
        
//         await initializeVideo(activePage, false, true); 
//         await hideLoadingUI(activePage); 

//         if (isObsConnected) {
//             console.log('\n[*] Active Video is Ready! Shifting OBS from Overlay to LIVE Video (MainScene)...');
//             try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//         }

//         console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
        
//         // Await the navigation so it actually loads before initializing
//         await backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
        
//         // 👇 FIX: Ab backup page par bhi initializeVideo call hoga (startMuted = true, isActivePage = false)
//         console.log(`[*] Initializing Video pipeline on Backup Page...`);
//         await initializeVideo(backupPage, true, false);
        
//         await activePage.bringToFront();
//         try { await activePage.mouse.click(10, 10); } catch(e){} 
//         await hideLoadingUI(activePage);

//         console.log(`\n==================================================`);
//         console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//         console.log(`==================================================`);
//         console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
        
//         let watchdogResult = await startWatchdog();
        
//         if (watchdogResult === "SOFT_RESET") {
//             console.log(`\n==================================================`);
//             console.log(`[🔄] SOFT RESET EXECUTING: Closing old browsers safely...`);
//             console.log(`==================================================`);
            
//             if (activeBrowser) { try { await activeBrowser.close(); } catch(e){} activeBrowser = null; }
//             if (backupBrowser) { try { await backupBrowser.close(); } catch(e){} backupBrowser = null; }
            
//             currentUrlIndex = (currentUrlIndex + 1) % urlList.length;
//             backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
            
//             console.log(`[*] 🚀 Relaunching Fresh Chrome Instances for Next Servers...`);
//             await new Promise(r => setTimeout(r, 4000)); 
//         } else {
//             keepStreaming = false; 
//         }
//     }
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//         if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//         try {
//             execSync('pkill -9 obs || true', { stdio: 'ignore' });
//             execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//             execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//         } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();
















// 2 start **** yeh acha hai lekin iss mee background check hai lekin next swap nahey hu raha hai background 

// const puppeteer = require('puppeteer-extra');let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let activeBrowser = null;
// let backupBrowser = null;

// // Name tags for tracking
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";

// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;




// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // =========================================================
//             // 🛑 ULTIMATE SHADOW DOM AD KILLER (Monkey Patching)
//             // =========================================================
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') {
//                     init.mode = 'open'; // Force open to allow inspection
//                 }
//                 const shadowRoot = originalAttachShadow.call(this, init);
                
//                 const observer = new MutationObserver(() => {
//                     // UPDATED: Hardened selector string catching wildcard IDs and payload sources
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) {
//                         console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
//                         this.remove(); // Kill the entire host element
//                     }
//                 });
                
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };

//             // Standard Protections
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             // UPDATED: Added fallback display:none for the mutated shadow tags
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // ⚠️ NEW: INSTANT HANG / RECOVERY SHIELD UI (Unique Radar Design)
// // =========================================================================================
// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 let isMuted = false;
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     // Check if actually muted
//                     if (media.muted || media.volume < 0.1) isMuted = true;
//                     // Force properties anyway
//                     media.muted = false;
//                     media.volume = 1.0;
//                 });

//                 // 🛑 AGAR AWAZ PEHLE SE KHULI HAI, TO KOI BUTTON CLICK MAT KARO! (Yeh Toggle rok dega)
//                 if (!isMuted) return;

//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         let actualMuted = false;
//                         document.querySelectorAll('video, audio').forEach(m => { if(m.muted || m.volume < 0.1) actualMuted = true; });
//                         if (actualMuted) {
//                             document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                         }
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); // 👈 YEH NAYI LINE ADD KAREIN
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();
    
//     // 🔥 NEW: Track recovery UI state
//     let isRecoveryUIShown = false;

//     while (true) {
//         if (!activeBrowser || !activeBrowser.isConnected() || !backupBrowser || !backupBrowser.isConnected()) {
//             throw new Error("One or both browser instances disconnected.");
//         }

//         let activeStatus = await checkPageStatus(activePage);

// // 👇=== UPDATED BACKGROUND SHIELD CODE ===👇
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { // 👈 30 seconds ka cooldown add kiya
//             let backupStatus = await checkPageStatus(backupPage);
            
//             // Agar backup background mein hang hai ya error de raha hai
//             if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
//                 console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
//                 // Next link par jump karein
//                 backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
                
//                 backupWarmupTime = Date.now(); // 👈 COOLDOWN RESET (Taake agli dafa 30 sec wait kare)
                
//                 // Backup browser ko silently naye link par bhej dein
//                 // Backup browser ko silently naye link par bhej dein
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
                    
//                     // Await lazmi hai taake dom load hone ke baad video initialize ho
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     // 👇 FIX: Background tab ko initialize karo taake checkPageStatus isay HEALTHY read kar sake
//                     await initializeVideo(backupPage, true, false);
                    
//                     // Backup ko dobara mute frame state mein set karein (Fail-safe)
//                     for (const frame of backupPage.frames()) {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch(e) {}
//             }
//         }
//         // 👆=== UPDATED CODE END ===👆
        

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 // 🔥 NEW: INSTANT HANG RECOVERY SHIELD TRIGGER (0 Seconds Delay)
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen. Triggering HOT-SWAP logic.`);
//                     }
//                     // Yahan se isRecoveryUIShown ko false karne wali line HATA DI GAYI HAI, 
//                     // taake jab tak Chrome 2 ready na ho, Radar UI screen par hold rahey.
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 // 🔥 NEW: STREAM RECOVERED (Instant hide)
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 let isMuted = false;
//                                 document.querySelectorAll('video, audio').forEach(m => { 
//                                     if(m.muted || m.volume < 0.1) isMuted = true;
//                                     m.muted = false; 
//                                     m.volume = 1.0; 
//                                 }); 
//                                 if (isMuted) {
//                                     document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                                 }
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        


//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             // Background (Chrome 2) ka status sirf logs print karne ke liye check karein
//             let logBackupStatus = await checkPageStatus(backupPage);
            
//             // Queue mein next URL calculate karein
//             let nextAfterBackupIndex = (backupUrlIndex + 1) % urlList.length;
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex];
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
// console.log(`[▶️] CURRENTLY LIVE             : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
// console.log(`--------------------------------------------------`);
// console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND      : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

        

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || backupStatus.status === 'DEAD') { 
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 // Swap Pages
// let brokenPage = activePage; 
// activePage = backupPage; 
// backupPage = brokenPage;

// // Swap Browsers
// let brokenBrowser = activeBrowser; 
// activeBrowser = backupBrowser; 
// backupBrowser = brokenBrowser;


// // Swap Names taake pata chale screen par konsa aya hai
// let brokenName = activeBrowserName;
// activeBrowserName = backupBrowserName;
// backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; // 🔥 Fresh slate for new active tab

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//                 } else {
//                 // 👇 NAYI LOGIC: Agar hang (FROZEN) hai aur Chrome 2 tayar nahi hai, toh ruk jao!
//                 if (activeStatus.status === 'FROZEN') {
//                     console.log(`[⏳] HOLDING SHIELD: Chrome 1 HUNG hai, par Chrome 2 abhi READY nahi hai (${backupStatus.status}).`);
//                     console.log(`[*] Chrome 1 screen par hold hai. Background mein Chrome 2 ko musalsal check kiya ja raha hai...`);
                    
//                     await new Promise(r => setTimeout(r, 1000));
//                     continue; // Loop yahan se dobara shuru hoga bina soft_reset kiye!
//                 } 
//                 // Agar tab mukammal CRASH (DEAD ya CRITICAL ERROR) hai, toh foran soft reset karo
//                 else {
//                     console.error(`\n[!] ❌ DONO TABS (Active & Backup) CRASHED! Initiating Soft Recovery...`);
//                     console.log(`[*] Shifting OBS to WaitingScene (Overlay) to keep stream alive...`);
                    
//                     try {
//                         await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//                     } catch (e) {}

//                     return "SOFT_RESET"; 
//                 }
//             }

                
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     // 👇=== YEH 2 LINES SHURU MEIN ADD KAREIN ===👇
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }

//     let keepStreaming = true;
    
//     while (keepStreaming) {
//         console.log(`\n[*] Starting ACTIVE browser instance (CHROME 1)...`);
//         activeBrowser = await createBrowserInstance(browserArgs);
//         const activePages = await activeBrowser.pages();
//         activePage = activePages[0];

//         console.log(`[*] Starting BACKUP browser instance (CHROME 2) in background...`);
//         backupBrowser = await createBrowserInstance(browserArgs);
//         const backupPages = await backupBrowser.pages();
//         backupPage = backupPages[0];

//         activeBrowser.on('targetcreated', async (target) => {
//             if (target.type() === 'page') {
//                 const newPage = await target.page();
//                 setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//             }
//         });

//         backupBrowser.on('targetcreated', async (target) => {
//             if (target.type() === 'page') {
//                 const newPage = await target.page();
//                 setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//             }
//         });

//         await setupNetworkAdBlocker(activePage);
//         await setupNetworkAdBlocker(backupPage);

//         attachAntiAdListeners(activePage);
//         attachAntiAdListeners(backupPage);

//         await applyPreloadFirewall(activePage);
//         await applyPreloadFirewall(backupPage);

//         await activePage.bringToFront(); 

//         console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//         await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
        
//         await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
        
//         await initializeVideo(activePage, false, true); 
//         await hideLoadingUI(activePage); 

//         if (isObsConnected) {
//             console.log('\n[*] Active Video is Ready! Shifting OBS from Overlay to LIVE Video (MainScene)...');
//             try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//         }

//         console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
        
//         // Await the navigation so it actually loads before initializing
//         await backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
        
//         // 👇 FIX: Ab backup page par bhi initializeVideo call hoga (startMuted = true, isActivePage = false)
//         console.log(`[*] Initializing Video pipeline on Backup Page...`);
//         await initializeVideo(backupPage, true, false);
        
//         await activePage.bringToFront();
//         try { await activePage.mouse.click(10, 10); } catch(e){} 
//         await hideLoadingUI(activePage);

//         console.log(`\n==================================================`);
//         console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//         console.log(`==================================================`);
//         console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
        
//         let watchdogResult = await startWatchdog();
        
//         if (watchdogResult === "SOFT_RESET") {
//             console.log(`\n==================================================`);
//             console.log(`[🔄] SOFT RESET EXECUTING: Closing old browsers safely...`);
//             console.log(`==================================================`);
            
//             if (activeBrowser) { try { await activeBrowser.close(); } catch(e){} activeBrowser = null; }
//             if (backupBrowser) { try { await backupBrowser.close(); } catch(e){} backupBrowser = null; }
            
//             currentUrlIndex = (currentUrlIndex + 1) % urlList.length;
//             backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
            
//             console.log(`[*] 🚀 Relaunching Fresh Chrome Instances for Next Servers...`);
//             await new Promise(r => setTimeout(r, 4000)); 
//         } else {
//             keepStreaming = false; 
//         }
//     }
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//         if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//         try {
//             execSync('pkill -9 obs || true', { stdio: 'ignore' });
//             execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//             execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//         } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();





















// 1 done 


// const puppeteer = require('puppeteer-extra');let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let activeBrowser = null;
// let backupBrowser = null;

// // Name tags for tracking
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";

// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;




// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // =========================================================
//             // 🛑 ULTIMATE SHADOW DOM AD KILLER (Monkey Patching)
//             // =========================================================
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') {
//                     init.mode = 'open'; // Force open to allow inspection
//                 }
//                 const shadowRoot = originalAttachShadow.call(this, init);
                
//                 const observer = new MutationObserver(() => {
//                     // UPDATED: Hardened selector string catching wildcard IDs and payload sources
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) {
//                         console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
//                         this.remove(); // Kill the entire host element
//                     }
//                 });
                
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };

//             // Standard Protections
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             // UPDATED: Added fallback display:none for the mutated shadow tags
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // ⚠️ NEW: INSTANT HANG / RECOVERY SHIELD UI (Unique Radar Design)
// // =========================================================================================
// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); // 👈 YEH NAYI LINE ADD KAREIN
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();
    
//     // 🔥 NEW: Track recovery UI state
//     let isRecoveryUIShown = false;

//     while (true) {
//         if (!activeBrowser || !activeBrowser.isConnected() || !backupBrowser || !backupBrowser.isConnected()) {
//             throw new Error("One or both browser instances disconnected.");
//         }

//         let activeStatus = await checkPageStatus(activePage);

// // 👇=== UPDATED BACKGROUND SHIELD CODE ===👇
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { // 👈 30 seconds ka cooldown add kiya
//             let backupStatus = await checkPageStatus(backupPage);
            
//             // Agar backup background mein hang hai ya error de raha hai
//             if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
//                 console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
//                 // Next link par jump karein
//                 backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
                
//                 backupWarmupTime = Date.now(); // 👈 COOLDOWN RESET (Taake agli dafa 30 sec wait kare)
                
//                 // Backup browser ko silently naye link par bhej dein
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     // Backup ko dobara mute frame state mein set karein
//                     for (const frame of backupPage.frames()) {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch(e) {}
//             }
//         }
//         // 👆=== UPDATED CODE END ===👆
        

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 // 🔥 NEW: INSTANT HANG RECOVERY SHIELD TRIGGER (0 Seconds Delay)
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen. Triggering HOT-SWAP logic.`);
//                     }
//                     // Yahan se isRecoveryUIShown ko false karne wali line HATA DI GAYI HAI, 
//                     // taake jab tak Chrome 2 ready na ho, Radar UI screen par hold rahey.
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 // 🔥 NEW: STREAM RECOVERED (Instant hide)
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        


//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             // Background (Chrome 2) ka status sirf logs print karne ke liye check karein
//             let logBackupStatus = await checkPageStatus(backupPage);
            
//             // Queue mein next URL calculate karein
//             let nextAfterBackupIndex = (backupUrlIndex + 1) % urlList.length;
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex];
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
// console.log(`[▶️] CURRENTLY LIVE             : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
// console.log(`--------------------------------------------------`);
// console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND      : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

        

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || backupStatus.status === 'DEAD') { 
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 // Swap Pages
// let brokenPage = activePage; 
// activePage = backupPage; 
// backupPage = brokenPage;

// // Swap Browsers
// let brokenBrowser = activeBrowser; 
// activeBrowser = backupBrowser; 
// backupBrowser = brokenBrowser;


// // Swap Names taake pata chale screen par konsa aya hai
// let brokenName = activeBrowserName;
// activeBrowserName = backupBrowserName;
// backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; // 🔥 Fresh slate for new active tab

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//                 } else {
//                 // 👇 NAYI LOGIC: Agar hang (FROZEN) hai aur Chrome 2 tayar nahi hai, toh ruk jao!
//                 if (activeStatus.status === 'FROZEN') {
//                     console.log(`[⏳] HOLDING SHIELD: Chrome 1 HUNG hai, par Chrome 2 abhi READY nahi hai (${backupStatus.status}).`);
//                     console.log(`[*] Chrome 1 screen par hold hai. Background mein Chrome 2 ko musalsal check kiya ja raha hai...`);
                    
//                     await new Promise(r => setTimeout(r, 1000));
//                     continue; // Loop yahan se dobara shuru hoga bina soft_reset kiye!
//                 } 
//                 // Agar tab mukammal CRASH (DEAD ya CRITICAL ERROR) hai, toh foran soft reset karo
//                 else {
//                     console.error(`\n[!] ❌ DONO TABS (Active & Backup) CRASHED! Initiating Soft Recovery...`);
//                     console.log(`[*] Shifting OBS to WaitingScene (Overlay) to keep stream alive...`);
                    
//                     try {
//                         await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//                     } catch (e) {}

//                     return "SOFT_RESET"; 
//                 }
//             }

                
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     // 👇=== YEH 2 LINES SHURU MEIN ADD KAREIN ===👇
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }

//     let keepStreaming = true;
    
//     while (keepStreaming) {
//         console.log(`\n[*] Starting ACTIVE browser instance (CHROME 1)...`);
//         activeBrowser = await createBrowserInstance(browserArgs);
//         const activePages = await activeBrowser.pages();
//         activePage = activePages[0];

//         console.log(`[*] Starting BACKUP browser instance (CHROME 2) in background...`);
//         backupBrowser = await createBrowserInstance(browserArgs);
//         const backupPages = await backupBrowser.pages();
//         backupPage = backupPages[0];

//         activeBrowser.on('targetcreated', async (target) => {
//             if (target.type() === 'page') {
//                 const newPage = await target.page();
//                 setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//             }
//         });

//         backupBrowser.on('targetcreated', async (target) => {
//             if (target.type() === 'page') {
//                 const newPage = await target.page();
//                 setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//             }
//         });

//         await setupNetworkAdBlocker(activePage);
//         await setupNetworkAdBlocker(backupPage);

//         attachAntiAdListeners(activePage);
//         attachAntiAdListeners(backupPage);

//         await applyPreloadFirewall(activePage);
//         await applyPreloadFirewall(backupPage);

//         await activePage.bringToFront(); 

//         console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//         await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
        
//         await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
        
//         await initializeVideo(activePage, false, true); 
//         await hideLoadingUI(activePage); 

//         if (isObsConnected) {
//             console.log('\n[*] Active Video is Ready! Shifting OBS from Overlay to LIVE Video (MainScene)...');
//             try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//         }

//         console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
//         backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
        
//         await activePage.bringToFront();
//         try { await activePage.mouse.click(10, 10); } catch(e){} 
//         await hideLoadingUI(activePage);

//         console.log(`\n==================================================`);
//         console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//         console.log(`==================================================`);
//         console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
        
//         let watchdogResult = await startWatchdog();
        
//         if (watchdogResult === "SOFT_RESET") {
//             console.log(`\n==================================================`);
//             console.log(`[🔄] SOFT RESET EXECUTING: Closing old browsers safely...`);
//             console.log(`==================================================`);
            
//             if (activeBrowser) { try { await activeBrowser.close(); } catch(e){} activeBrowser = null; }
//             if (backupBrowser) { try { await backupBrowser.close(); } catch(e){} backupBrowser = null; }
            
//             currentUrlIndex = (currentUrlIndex + 1) % urlList.length;
//             backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
            
//             console.log(`[*] 🚀 Relaunching Fresh Chrome Instances for Next Servers...`);
//             await new Promise(r => setTimeout(r, 4000)); 
//         } else {
//             keepStreaming = false; 
//         }
//     }
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//         if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//         try {
//             execSync('pkill -9 obs || true', { stdio: 'ignore' });
//             execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//             execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//         } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();























































































































































































































































































































































































































































































































































































































































const puppeteer = require('puppeteer-extra');
let isWarmupPhase = true;
const StealthPlugin = require('puppeteer-extra-plugin-stealth');
puppeteer.use(StealthPlugin());

const fs = require('fs');
const path = require('path');
const os = require('os');
const { spawn, execSync, exec } = require('child_process');
const { OBSWebSocket } = require('obs-websocket-js'); 

// =========================================================================================
// 🛡️ GLOBAL CRASH PREVENTION SHIELD (DEBUGGING MODE)
// =========================================================================================
process.on('uncaughtException', (err) => {
    console.error('\n========================================');
    console.error('[💥] UNCAUGHT EXCEPTION');
    console.error(err);
    console.error(err.stack);
    console.error('========================================\n');
});

process.on('unhandledRejection', (reason) => {
    console.error('\n========================================');
    console.error('[💥] UNHANDLED REJECTION');
    console.error(reason);
    console.error('========================================\n');
});

const obs = new OBSWebSocket(); 

// =========================================================================================
// ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// =========================================================================================
const FORCE_REFRESH_MINUTES = 40000; 
const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// =========================================================================================
// 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// =========================================================================================
const NO_REFRESH_DOMAINS = [
    'youtube.com',
    'facebook.com',
    'streamed.pk',
    'cricstreams.', 
    'sport4u.online',
    'website-vercel-helper-d-jaja-3-2.vercel.app',
    'websitestream.netlify.app/?ch=Channel%20HD%2071'
];

// 🚀 Multi-Stream Key Manager
const STREAM_KEYS = {
    '1'   : '15254238731883_15281627925099_najspfkgne', 
    '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
    '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
    '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
    '2.1' : '15254308986475_15281761618539_3xca7oij3u',
    '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

    '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
    '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
    '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

    '4'   : '15255022345835_15283095800427_vwrupxzstm', 
    '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
    '4.2' : '15255045480043_15283135842923_tldl4bhmii',
    '4.3' : '15255208599147_15283449629291_abltofuc7m', 
    '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
    '4.5' : '15255227670123_15283486263915_jpntt54mve',

    '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
    '5.1' : '15273713933931_15317494860395_avj47smmim', 
    '5.2' : '15273722257003_15317510195819_6edjluvdqi',
    '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
    '5.4' : '15273750175339_15317561707115_csel26ku5a', 
    '5.5' : '15273760071275_15317579467371_cnewcj54me',
    '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
    's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
    's1.2'  : '14204288179759_14846247373359_tnsknmapva',
    's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
    's1.4'  : '14204331957807_14846326147631_dji2acqcze',
    's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
    's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
    's1.7'  : '14204370492975_14846393649711_6fduhdqite',
    's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
    's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
    's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

    's2.1'  : '14204490948143_14846603495983_kzevn36tii',
    's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
    's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
    's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
    's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
    's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
    's2.7'  : '14204577259055_14846756194863_3ecad2535u',
    's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
    's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
    's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
};

const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// =========================================================================================
// 🔄 DYNAMIC URL PARSER & METADATA EXTRACTOR
// =========================================================================================
let rawUrls = (process.env.TARGET_URLS || '').trim();
let urlList = [];

if (rawUrls !== '') {
    urlList = rawUrls.split(',').map(u => {
        let trimmed = u.trim();
        let hangThreshold = 8000; 
        
        if (trimmed.startsWith('!')) {
            hangThreshold = 20000; 
            trimmed = trimmed.substring(1); 
        }
        if (!trimmed.startsWith('http')) trimmed = 'https://' + trimmed;
        
        return { url: trimmed, hangTime: hangThreshold };
    });
} else {
    urlList = [
        { url: 'https://sport4u.online', hangTime: 8000 },
        { url: 'https://dadocric.st/player.php?id=starsp3&v=m', hangTime: 8000 }
    ];
}

function getSafeBackupIndex(activeIndex, currentIndex, list) {
    if (list.length <= 1) return 0; 
    let next = (currentIndex + 1) % list.length;
    if (next === activeIndex) {
        next = (next + 1) % list.length; 
    }
    return next;
}

let currentUrlIndex = 0;
let backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);

const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

let browserArgs = []; // Global Browser Args for Recovery Functions
let activeBrowser = null;
let backupBrowser = null;
let activeBrowserName = "CHROME 1";
let backupBrowserName = "CHROME 2";
let obsProcess = null;
let activePage = null;
let backupPage = null;

if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
let pendingScreenshots = [];
let uploadCycleCount = 0;

async function createBrowserInstance(args) {
    return await puppeteer.launch({
        headless: false, 
        defaultViewport: { width: RES_W, height: RES_H },
        ignoreDefaultArgs: ['--enable-automation'], 
        args: args
    });
}

// =========================================================================================
// 🛡️ SMART BROWSER RECOVERY
// =========================================================================================

async function preparePage(page) {
    if (!page) return;
    await setupNetworkAdBlocker(page);
    attachAntiAdListeners(page);
    await applyPreloadFirewall(page);
}

async function createFreshBackupBrowser() {
    console.log('\n[🛠️] BACKUP RECOVERY: Creating fresh backup Chrome...');
    try {
        if (backupBrowser && backupBrowser.isConnected()) {
            try {
                const pages = await backupBrowser.pages();
                for (const p of pages) {
                    try { await p.close(); } catch (e) {}
                }
            } catch (e) {}
        }
    } catch (e) {}

    try {
        if (backupBrowser && !backupBrowser.isConnected()) {
            backupBrowser = null;
        }
    } catch (e) {
        backupBrowser = null;
    }

    backupBrowser = await createBrowserInstance(browserArgs);
    const pages = await backupBrowser.pages();
    backupPage = pages[0];
    await preparePage(backupPage);

    backupBrowser.on('targetcreated', async (target) => {
        if (target.type() === 'page') {
            try {
                const newPage = await target.page();
                setTimeout(async () => {
                    if (newPage && newPage !== backupPage) {
                        try { await newPage.close(); } catch (e) {}
                    }
                }, 500);
            } catch (e) {}
        }
    });

    console.log('[✅] BACKUP RECOVERY: Fresh backup Chrome created.');
    return true;
}

async function createFreshActiveBrowser() {
    console.log('\n[🛠️] ACTIVE RECOVERY: Creating fresh active Chrome...');
    try {
        if (activeBrowser && activeBrowser.isConnected()) {
            try {
                const pages = await activeBrowser.pages();
                for (const p of pages) {
                    try { await p.close(); } catch (e) {}
                }
            } catch (e) {}
        }
    } catch (e) {}

    try {
        if (activeBrowser && !activeBrowser.isConnected()) {
            activeBrowser = null;
        }
    } catch (e) {
        activeBrowser = null;
    }

    activeBrowser = await createBrowserInstance(browserArgs);
    const pages = await activeBrowser.pages();
    activePage = pages[0];
    await preparePage(activePage);

    activeBrowser.on('targetcreated', async (target) => {
        if (target.type() === 'page') {
            try {
                const newPage = await target.page();
                setTimeout(async () => {
                    if (newPage && newPage !== activePage) {
                        try { await newPage.close(); } catch (e) {}
                    }
                }, 500);
            } catch (e) {}
        }
    });

    console.log('[✅] ACTIVE RECOVERY: Fresh active Chrome created.');
    return true;
}

// =========================================================================================
// 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// =========================================================================================
async function setupNetworkAdBlocker(page) {
    if (!page) return;
    try {
        await page.setRequestInterception(true);
        page.on('request', (request) => {
            const url = request.url().toLowerCase();
            const type = request.resourceType();

            if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
                const targetUrl = request.url().toLowerCase();
                const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
                const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

                if (isMaliciousAd) {
                    console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
                    request.abort().catch(()=>{});
                    return;
                }
            }

            if (
                url.includes('popads') || 
                url.includes('exoclick') || 
                url.includes('adsterra') || 
                url.includes('onclickads') || 
                url.includes('jerkmate') ||
                url.includes('adrevenue') ||
                url.includes('fanduel') ||
                url.includes('doubleclick') ||
                (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
            ) {
                request.abort().catch(()=>{});
            } else {
                request.continue().catch(()=>{});
            }
        });
    } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
}

async function applyPreloadFirewall(page) {
    if (!page) return;
    try {
        await page.evaluateOnNewDocument(() => {
            const originalAttachShadow = Element.prototype.attachShadow;
            Element.prototype.attachShadow = function(init) {
                if (init && init.mode === 'closed') {
                    init.mode = 'open'; 
                }
                const shadowRoot = originalAttachShadow.call(this, init);
                
                const observer = new MutationObserver(() => {
                    const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
                    if (adElements.length > 0) {
                        console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
                        this.remove(); 
                    }
                });
                
                observer.observe(shadowRoot, { childList: true, subtree: true });
                return shadowRoot;
            };
            
            Element.prototype.attachShadow.toString = function() { return "function attachShadow() { [native code] }"; };

            window.alert = function() {};
            window.confirm = function() { return true; };
            window.prompt = function() { return null; };
            window.open = function() { return null; };
            
            Object.defineProperty(window, 'onbeforeunload', {
                configurable: true,
                get: function() { return null; },
                set: function() { return null; }
            });

            document.addEventListener('click', (e) => {
                const target = e.target;
                if (target && (target.tagName === 'A' || target.closest('a'))) {
                    const link = target.tagName === 'A' ? target : target.closest('a');
                    if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
                        console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
                        e.preventDefault();
                        e.stopPropagation();
                        return false;
                    }
                }
            }, true);

            const style = document.createElement('style');
            style.textContent = `
                html, body { background-color: #000000 !important; overflow: hidden !important; }
                in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
            `;
            document.documentElement.appendChild(style);

            const attachOverlay = () => {
                let target = document.body || document.documentElement;
                if (target && !document.getElementById('smart-stream-overlay')) {
                    const overlay = document.createElement('div');
                    overlay.id = 'smart-stream-overlay';
                    overlay.innerHTML = `
                        <style>
                            #smart-stream-overlay {
                                position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
                                width: 100vw !important; height: 100vh !important; background: #000000 !important;
                                z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
                                justify-content: center !important; align-items: center !important; color: #ffffff !important;
                                font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
                                pointer-events: all !important;
                            }
                            .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
                            .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
                            .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
                            @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
                            @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
                            .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
                            .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
                            .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
                            @keyframes blinker { 50% { opacity: 0.3; } }
                        </style>
                        <div class="stream-spinner"></div>
                        <div class="progress-container"><div class="progress-bar-fill"></div></div>
                        <div class="stream-title">STREAM LOADING</div>
                        <div class="stream-sub">Connecting to secure stream engine...</div>
                    `;
                    target.appendChild(overlay);
                } else if (!target) {
                    requestAnimationFrame(attachOverlay);
                }
            };
            attachOverlay();
        });
    } catch (e) {
        console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
    }
}

async function takeAndBatchScreenshot(page, stepName) {
    if (!page) return;
    try {
        const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
        const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
        await page.screenshot({ path: filePath });
        console.log(`[📸] Screenshot saved: ${filePath}`);
        pendingScreenshots.push(filePath);

        if (pendingScreenshots.length >= 3) {
            try {
                const tag = 'live-stream-logs';
                try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
                try {
                    const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
                    for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
                } catch(e) {}

                const fileList = pendingScreenshots.join(' ');
                exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
                    if (!err) uploadCycleCount++;
                });
                pendingScreenshots = []; 
            } catch (err) { }
        }
    } catch (e) { }
}

async function showLoadingUI(page, title, sub) {
    try {
        await page.evaluate((t, s) => {
            if (window.self !== window.top) return; 
            let overlay = document.getElementById('smart-stream-overlay');

            if (overlay) {
                const titleEl = overlay.querySelector('.stream-title');
                const subEl = overlay.querySelector('.stream-sub');
                if (titleEl) titleEl.innerHTML = t;
                if (subEl) subEl.innerHTML = s;
                
                overlay.style.setProperty('display', 'flex', 'important');
                overlay.style.setProperty('opacity', '1', 'important');
                overlay.style.setProperty('z-index', '2147483647', 'important');
            } 
            else {
                overlay = document.createElement('div');
                overlay.id = 'smart-stream-overlay';
                overlay.innerHTML = `
                    <style>
                        #smart-stream-overlay {
                            position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
                            width: 100vw !important; height: 100vh !important; background: #000000 !important;
                            z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
                            justify-content: center !important; align-items: center !important; color: #ffffff !important;
                            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
                            pointer-events: all !important;
                        }
                        .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
                        .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
                        .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
                        @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
                        @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
                        .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
                        .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
                        .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
                        @keyframes blinker { 50% { opacity: 0.3; } }
                    </style>
                    <div class="stream-spinner"></div>
                    <div class="progress-container"><div class="progress-bar-fill"></div></div>
                    <div class="stream-title">${t}</div>
                    <div class="stream-sub">${s}</div>
                `;
                document.documentElement.appendChild(overlay);
            }
        }, title, sub);
    } catch (e) {}
}

async function hideLoadingUI(page) {
    try {
        await page.evaluate(() => {
            const overlay = document.getElementById('smart-stream-overlay');
            if (overlay) {
                overlay.style.setProperty('display', 'none', 'important');
                overlay.style.setProperty('opacity', '0', 'important');
                overlay.style.setProperty('z-index', '-9999', 'important');
                overlay.remove();
            }
        });
    } catch (e) {}
}

async function showRecoveryUI(page) {
    try {
        await page.evaluate(() => {
            if (window.self !== window.top) return; 
            let overlay = document.getElementById('stream-recovery-overlay');
            if (overlay) {
                overlay.style.setProperty('display', 'flex', 'important');
                return;
            }
            
            overlay = document.createElement('div');
            overlay.id = 'stream-recovery-overlay';
            overlay.innerHTML = `
                <style>
                    #stream-recovery-overlay {
                        position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
                        width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
                        z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
                        justify-content: center !important; align-items: center !important; color: #ffffff !important;
                        font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
                    }
                    .recovery-radar {
                        width: 100px; height: 100px; border-radius: 50%;
                        border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
                        animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
                        margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
                    }
                    .recovery-radar::before {
                        content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
                        border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
                        animation: radar-spin 2s linear infinite reverse;
                    }
                    @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
                    .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
                    .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
                    @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
                </style>
                <div class="recovery-radar"></div>
                <div class="warn-title">SIGNAL LOST</div>
                <div class="warn-sub">Attempting Auto-Recovery...</div>
            `;
            document.documentElement.appendChild(overlay);
        });
    } catch (e) {}
}

async function hideRecoveryUI(page) {
    try {
        await page.evaluate(() => {
            const overlay = document.getElementById('stream-recovery-overlay');
            if (overlay) {
                overlay.style.setProperty('display', 'none', 'important');
            }
        });
    } catch (e) {}
}

function setupOBSConfig() {
    const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
    const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
    const scenesDir = path.join(obsDir, 'basic', 'scenes');

    fs.mkdirSync(profilesDir, { recursive: true });
    fs.mkdirSync(scenesDir, { recursive: true });

    const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
    fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
    const basicIniContent = `[General]\nName=Untitled\n[Video]\nBaseCX=${RES_W}\nBaseCY=${RES_H}\nOutputCX=${RES_W}\nOutputCY=${RES_H}\nFPSCommon=30\n[Output]\nMode=Simple\n[SimpleOutput]\nVBitrate=${BITRATE}\nStreamEncoder=x264\nx264Preset=ultrafast\nx264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0\n`;
    fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

    const serviceJson = {
        "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
        "type": "rtmp_custom"
    };
    fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

    const sceneJson = {
        "current_scene": "WaitingScene", 
        "current_program_scene": "WaitingScene", 
        "name": "Untitled",
        "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
        "sources": [
            { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
            { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
            {
                "id": "scene", "name": "MainScene",
                "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
            },
            {
                "id": "scene", "name": "WaitingScene",
                "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
            }
        ]
    };
    fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
}

function attachAntiAdListeners(page) {
    page.on('dialog', async dialog => {
        try { await dialog.dismiss(); } catch(e){}
    });
}

async function triggerSmartUnmute(page) {
    for (const frame of page.frames()) {
        try {
            if (frame.isDetached()) continue;

            await frame.evaluate(() => {
                const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                potentialElements.forEach(el => {
                    const text = (el.innerText || el.textContent || '').trim().toUpperCase();
                    const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
                    const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
                    const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
                    const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
                    const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

                    if (matchesText || matchesJS || matchesAria) {
                        const rect = el.getBoundingClientRect();
                        const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

                        if (isVisible) {
                            console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
                            try { el.click(); } catch(e) {}
                            try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
                        }
                    }
                });

                document.querySelectorAll('video, audio').forEach(media => {
                    if (media.muted) {
                        media.muted = false;
                        media.volume = 1.0;
                    }
                });
            }).catch(() => {});
        } catch (e) {}
    }
}

async function initializeVideo(page, startMuted, isActivePage) {
    try {
        if (SERVER_SELECTION !== 'None') {
            console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
            let serverClicked = false; let serverAttempts = 0;
            while (!serverClicked && serverAttempts < 10) { 
                serverAttempts++;
                try {
                    const clickSuccess = await page.evaluate((serverName) => {
                        const buttons = Array.from(document.querySelectorAll('button'));
                        const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
                        if (targetBtn) { targetBtn.click(); return true; }
                        return false;
                    }, SERVER_SELECTION);

                    if (clickSuccess) {
                        serverClicked = true; 
                        console.log(`[+] Server Button clicked successfully!`);
                        await takeAndBatchScreenshot(page, `server-clicked`);
                        await new Promise(r => setTimeout(r, 2000)); 
                        if (isActivePage) await page.bringToFront(); 
                    } else await new Promise(r => setTimeout(r, 2000));
                } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
            }
        }

        console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
        let isVideoPlaying = false; 
        let attempts = 0;
        
        while (!isVideoPlaying && attempts < 15) {
            for (const frame of page.frames()) {
                try {
                    const autoPlayed = await frame.evaluate(() => {
                        let playing = false;
                        document.querySelectorAll('video').forEach(v => {
                            if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
                                v.muted = false; 
                                v.volume = 1.0;
                                playing = true;
                            }
                        });
                        return playing;
                    });

                    if (autoPlayed) {
                        isVideoPlaying = true;
                        break;
                    }

                    const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
                    if (playBtn) {
                        const isVisible = await frame.evaluate(el => {
                            const style = window.getComputedStyle(el);
                            return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
                        }, playBtn);

                        if (isVisible) {
                            await frame.evaluate(el => el.click(), playBtn); 
                            await takeAndBatchScreenshot(page, `play-btn-clicked`);
                            await new Promise(r => setTimeout(r, 3000)); 
                            isVideoPlaying = true;
                            break; 
                        }
                    }

                    if (!isVideoPlaying && attempts > 5) {
                        const forced = await frame.evaluate(async () => {
                            let played = false;
                            let vids = document.querySelectorAll('video');
                            for(let v of vids) {
                                if (v.clientWidth > 50) { 
                                    v.muted = false; v.volume = 1.0; 
                                    try { v.click(); } catch(e){}
                                    try {
                                        let p = v.play();
                                        if (p !== undefined) p.catch(()=>{});
                                        played = true;
                                    } catch(e) {}
                                }
                            }
                            return played;
                        });

                        if (forced) {
                            await takeAndBatchScreenshot(page, `force-play-applied`);
                            isVideoPlaying = true;
                            break;
                        }
                    }
                } catch (err) {}
            }
            if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
            attempts++;
        }

        console.log('[*] Scanning for Exact Real Video Player...');
        let targetFrame = null;
        for (const frame of page.frames()) {
            try {
                const isRealLiveStream = await frame.evaluate(() => {
                    const vid = document.querySelector('video');
                    return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
                });
                if (isRealLiveStream) { 
                    targetFrame = frame; 
                    console.log(`[+] Smart Scanner locked onto video frame!`);
                    break; 
                }
            } catch (e) { }
        }

        await page.evaluate(() => {
            setInterval(() => {
                try {
                    document.documentElement.style.setProperty('background-color', 'black', 'important');
                    document.body.style.setProperty('background-color', 'black', 'important');
                    document.body.style.setProperty('overflow', 'hidden', 'important');
                    document.documentElement.style.setProperty('overflow', 'hidden', 'important');

                    let iframes = Array.from(document.querySelectorAll('iframe'));
                    let mainIframe = null; let maxScore = -1;

                    iframes.forEach(ifr => {
                        let width = ifr.clientWidth;
                        let height = ifr.clientHeight;
                        let area = width * height;
                        if (area < 5000) return;
                        let score = area;
                        
                        if (ifr.hasAttribute('allowfullscreen') || 
                            ifr.hasAttribute('webkitallowfullscreen') || 
                            ifr.hasAttribute('mozallowfullscreen')) {
                            score += 10000000; 
                        }
                        
                        if (height > width) {
                            score = -1; 
                        }

                        if (score > maxScore) {
                            maxScore = score;
                            mainIframe = ifr;
                        }
                    });

                    if (!mainIframe && iframes.length > 0) {
                        mainIframe = iframes.find(ifr => 
                            ifr.getAttribute('allowfullscreen') !== null || 
                            (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
                        );
                    }

                    if (mainIframe) {
                        iframes.forEach(ifr => {
                            if (ifr !== mainIframe) {
                                ifr.style.setProperty('display', 'none', 'important');
                                ifr.style.setProperty('opacity', '0', 'important');
                                ifr.style.setProperty('z-index', '-9999', 'important');
                                
                                if (ifr.parentNode && ifr.parentNode !== document.body) {
                                    try { 
                                        ifr.parentNode.style.setProperty('display', 'none', 'important'); 
                                        ifr.parentNode.style.setProperty('opacity', '0', 'important');
                                    } catch(e) {}
                                }
                            }
                        });

                        mainIframe.style.setProperty('position', 'fixed', 'important');
                        mainIframe.style.setProperty('top', '0px', 'important');
                        mainIframe.style.setProperty('left', '0px', 'important');
                        mainIframe.style.setProperty('width', '100vw', 'important');
                        mainIframe.style.setProperty('height', '100vh', 'important');
                        mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
                        mainIframe.style.setProperty('background-color', 'black', 'important');
                        mainIframe.style.setProperty('border', 'none', 'important');
                        mainIframe.style.setProperty('opacity', '1', 'important');
                        mainIframe.style.setProperty('display', 'block', 'important');
                        mainIframe.style.setProperty('visibility', 'visible', 'important');
                    }

                    const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay):not([class*="player"]):not([class*="jw"]):not([class*="vjs"]), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
                    document.querySelectorAll(junkClasses).forEach(el => { 
                        try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
                    });

                    const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
                    document.querySelectorAll('div, section, span, a').forEach(el => {
                        if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
                        const style = window.getComputedStyle(el);
                        const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
                        if (isFloating && el.innerText) {
                            const textLower = el.innerText.toLowerCase();
                            const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
                            if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
                                try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
                            }
                        }
                    });

                } catch (err) {}
            }, 500); 
        }).catch(() => {});

        await targetFrame.evaluate((muteVideo) => {
            // FIX 1: Use window object to control audio globally to avoid Audio War
            window.isStreamMuted = muteVideo; 

            setInterval(() => {
                try {
                    const style = document.createElement('style');
                    style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
                    document.head.appendChild(style);

                    const mediaElements = document.querySelectorAll('video, audio');
                    const videos = Array.from(document.querySelectorAll('video'));
                    let realVideo = null;

                    mediaElements.forEach(media => { 
                        media.muted = window.isStreamMuted; 
                        media.volume = window.isStreamMuted ? 0.0 : 1.0; 
                    });

                    if (!window.isStreamMuted) {
                        document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
                    }

                    for (const v of videos) {
                        if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
                    }

                    if (!realVideo && videos.length > 0) {
                        realVideo = videos[0];
                    }

                    if (realVideo) { 
                        let playerWrap = realVideo.closest('.jwplayer, #player, .plyr, .vjs-player, .shaka-video-container, [data-player]') || realVideo;
                        
                        playerWrap.style.setProperty('position', 'fixed', 'important');
                        playerWrap.style.setProperty('top', '0px', 'important');
                        playerWrap.style.setProperty('left', '0px', 'important');
                        playerWrap.style.setProperty('width', '100vw', 'important');
                        playerWrap.style.setProperty('height', '100vh', 'important');
                        playerWrap.style.setProperty('z-index', '2147483646', 'important'); 
                        playerWrap.style.setProperty('background-color', 'black', 'important');
                        playerWrap.style.setProperty('opacity', '1', 'important');
                        playerWrap.style.setProperty('visibility', 'visible', 'important');
                        playerWrap.style.setProperty('display', 'block', 'important');
                        
                        if (playerWrap !== realVideo) {
                            realVideo.style.setProperty('width', '100%', 'important');
                            realVideo.style.setProperty('height', '100%', 'important');
                        }
                        realVideo.style.setProperty('object-fit', 'contain', 'important');
                    }
                } catch(err) {}
            }, 500); 
        }, startMuted).catch(() => {});

    } catch (e) { }

    if (!startMuted) {
        await triggerSmartUnmute(page);
        await new Promise(r => setTimeout(r, 1000));
    }
}

async function checkPageStatus(page) {
    if (!page) return { status: 'DEAD' };
    try {
        for (const frame of page.frames()) {
            try {
                if (frame.isDetached()) continue;
                const result = await Promise.race([
                    frame.evaluate(() => {
                        const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
                        if (
                            bodyText.includes("stream error") || 
                            bodyText.includes("not found") || 
                            bodyText.includes("domain is blocked") ||
                            bodyText.includes("error: forbidden") ||
                            bodyText.includes("does not have permission") ||
                            bodyText.includes("access denied") ||
                            (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
                        ) {
                            return { status: 'CRITICAL_ERROR' };
                        }
                        
                        const videos = Array.from(document.querySelectorAll('video'));
                        let targetV = null;

                        for (const v of videos) {
                            if (v.clientWidth > 0 && v.clientWidth < 100) continue;
                            if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
                                targetV = v; break;
                            }
                        }
                        
                        if (!targetV && videos.length > 0) {
                            targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
                        }
                        
                        if (targetV && !targetV.ended && targetV.currentTime > 0) {
                            let frames = 0;
                            if (targetV.getVideoPlaybackQuality) {
                                frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
                            } else if (targetV.webkitDecodedFrameCount !== undefined) {
                                frames = targetV.webkitDecodedFrameCount;
                            }
                            return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
                        }
                        return { status: 'DEAD' };
                    }),
                    new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
                ]);
                if (result && result.status !== 'DEAD') return result;
            } catch (err) {}
        }
    } catch (e) { return { status: 'DEAD' }; }
    return { status: 'DEAD' };
}

async function startWatchdog() {
    let lastActiveTime = -1;
    let lastDecodedFrames = -1; 
    let frozenCheckTimestamp = Date.now();
    let watchdogTicks = 0;
    
    let streamSetupTime = Date.now(); 
    let isWarmupPhase = true; 
    let backupWarmupTime = Date.now(); 
    const WARMUP_MAX_TIME = 15000; 

    let activeUrlStr = urlList[currentUrlIndex].url;
    let backupUrlStr = urlList[backupUrlIndex].url;

    let currentStreamStartTime = Date.now();
    let isRecoveryUIShown = false;

    while (true) {
        // =====================================================================================
        // 🛡️ NEVER THROW JUST BECAUSE ONE CHROME DISCONNECTED
        // Recover the affected side instead.
        // =====================================================================================
        const activeBrowserAlive = activeBrowser && activeBrowser.isConnected();
        const backupBrowserAlive = backupBrowser && backupBrowser.isConnected();

        // ---------------------------------------------------------------------
        // CASE 1: ACTIVE CHROME DEAD, BACKUP CHROME STILL ALIVE
        // ---------------------------------------------------------------------
        if (!activeBrowserAlive && backupBrowserAlive) {
            console.log('\n==================================================');
            console.log('[🚨] ACTIVE CHROME DISCONNECTED');
            console.log('[🔄] BACKUP CHROME IS ALIVE');
            console.log('[⚡] PROMOTING BACKUP -> ACTIVE');
            console.log('[🛡️] OBS WILL CONTINUE RUNNING');
            console.log('==================================================\n');

            const oldActiveBrowser = activeBrowser;
            const oldActivePage = activePage;
            activeBrowser = backupBrowser;
            activePage = backupPage;
            backupBrowser = oldActiveBrowser;
            backupPage = oldActivePage;

            const oldActiveName = activeBrowserName;
            activeBrowserName = backupBrowserName;
            backupBrowserName = oldActiveName;

            const previousActiveIndex = currentUrlIndex;
            currentUrlIndex = backupUrlIndex;
            activeUrlStr = urlList[currentUrlIndex].url;

            backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
            backupUrlStr = urlList[backupUrlIndex].url;

            lastActiveTime = -1; lastDecodedFrames = -1; frozenCheckTimestamp = Date.now();
            streamSetupTime = Date.now(); currentStreamStartTime = Date.now();
            isWarmupPhase = true; backupWarmupTime = Date.now(); isRecoveryUIShown = false;

            try { await activePage.bringToFront(); await hideLoadingUI(activePage); } catch (e) {}

            console.log('[✅] BACKUP PROMOTED SUCCESSFULLY');
            console.log(`[📺] NEW ACTIVE SERVER : [${currentUrlIndex}] -> ${activeUrlStr}`);
            console.log(`[🔊] ACTIVE AUDIO      : ON`);
            console.log(`[🛡️] NEW BACKUP SERVER : [${backupUrlIndex}] -> ${backupUrlStr}`);

            try {
                await createFreshBackupBrowser();
                await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                await initializeVideo(backupPage, true, false);
                console.log('[✅] NEW BACKUP SERVER IS BUFFERING IN BACKGROUND');
            } catch (e) {
                console.log(`[⚠️] Backup recreation failed. Watchdog will try the NEXT server.`);
            }
            continue;
        }

        // ---------------------------------------------------------------------
        // CASE 2: BACKUP CHROME DEAD, ACTIVE CHROME STILL ALIVE
        // ---------------------------------------------------------------------
        if (activeBrowserAlive && !backupBrowserAlive) {
            console.log('\n==================================================');
            console.log('[⚠️] BACKUP CHROME DISCONNECTED');
            console.log('[🛡️] ACTIVE STREAM WILL NOT BE TOUCHED');
            console.log('[🔄] REBUILDING BACKUP WITH NEXT SERVER');
            console.log('==================================================\n');

            try {
                backupUrlIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
                backupUrlStr = urlList[backupUrlIndex].url;

                console.log(`[*] NEXT BACKUP SERVER -> [${backupUrlIndex}] ${backupUrlStr}`);
                await createFreshBackupBrowser();
                await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                await initializeVideo(backupPage, true, false);
                backupWarmupTime = Date.now();
                console.log(`[✅] BACKUP RECOVERED -> Server [${backupUrlIndex}]`);
            } catch (e) {
                console.log(`[⚠️] Backup recovery failed. NEXT WATCHDOG CYCLE WILL TRY AGAIN.`);
            }
        }

        // ---------------------------------------------------------------------
        // CASE 3: BOTH CHROMES DEAD
        // ---------------------------------------------------------------------
        if (!activeBrowserAlive && !backupBrowserAlive) {
            console.log('\n==================================================');
            console.log('[🚨] BOTH CHROME INSTANCES DISCONNECTED');
            console.log('[🛠️] LOCAL RECOVERY MODE');
            console.log('[🛑] OBS WILL NOT BE RESTARTED');
            console.log('==================================================\n');

            try {
                currentUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
                activeUrlStr = urlList[currentUrlIndex].url;

                backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
                backupUrlStr = urlList[backupUrlIndex].url;

                console.log(`[*] RECOVERY ACTIVE -> [${currentUrlIndex}] ${activeUrlStr}`);
                console.log(`[*] RECOVERY BACKUP -> [${backupUrlIndex}] ${backupUrlStr}`);

                await createFreshActiveBrowser();
                await activePage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                await showLoadingUI(activePage, "SEARCHING SERVER", "Finding a stable stream connection <span class='stream-blink'>...</span>");
                await initializeVideo(activePage, false, true);
                await hideLoadingUI(activePage);

                console.log('[✅] ACTIVE CHROME RECOVERED');

                try {
                    await createFreshBackupBrowser();
                    await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    await initializeVideo(backupPage, true, false);
                    console.log('[✅] BACKUP CHROME RECOVERED');
                } catch (backupError) {
                    console.log('[⚠️] Backup failed. Active stream continues.');
                }

                try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}

                streamSetupTime = Date.now(); currentStreamStartTime = Date.now(); backupWarmupTime = Date.now();
                frozenCheckTimestamp = Date.now(); lastActiveTime = -1; lastDecodedFrames = -1;
                isWarmupPhase = true; isRecoveryUIShown = false;

                console.log('[✅] LOCAL RECOVERY COMPLETE — WATCHDOG CONTINUES');
            } catch (e) {
                console.log(`[❌] Local Chrome recovery failed: ${e.message}`);
                await new Promise(r => setTimeout(r, 3000));
            }
            continue;
        }

        let activeHangThresholdMs = urlList[currentUrlIndex].hangTime;
        let activeStatus = await checkPageStatus(activePage);

        // 🔄 1. BACKGROUND SHIELD
        if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { 
            let backupStatus = await checkPageStatus(backupPage);
            
            if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
                console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
                backupUrlIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
                backupUrlStr = urlList[backupUrlIndex].url;
                
                console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
                backupWarmupTime = Date.now();
                
                try {
                    await backupPage.goto('about:blank').catch(()=>{});
                    await applyPreloadFirewall(backupPage);
                    await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    await initializeVideo(backupPage, true, false);
                } catch(e) {}
            }
        }

        if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
            let elapsedMs = Date.now() - currentStreamStartTime;
            let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

            if (elapsedMs > FORCE_REFRESH_MS) {
                if (!isExempted) {
                    console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
                    activeStatus.status = 'FORCE_REFRESH'; 
                }
            }
        }

        // Active Tab Audio Watchdog Fix (Stop Audio War)
        if (activeStatus.status === 'HEALTHY') {
            await hideLoadingUI(activePage); 
            isWarmupPhase = false; 

            let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
            let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

            if (isTimeStuck || isFrameStuck) {
                if (!isRecoveryUIShown) {
                    await showRecoveryUI(activePage);
                    isRecoveryUIShown = true;
                    console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
                }

                if (Date.now() - frozenCheckTimestamp > activeHangThresholdMs) {
                    activeStatus.status = 'FROZEN';
                    if (isFrameStuck && !isTimeStuck) {
                        console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
                    }
                    isRecoveryUIShown = false; 
                }
            } else {
                lastActiveTime = activeStatus.currentTime; 
                lastDecodedFrames = activeStatus.decodedFrames; 
                frozenCheckTimestamp = Date.now();
                
                if (isRecoveryUIShown) {
                    await hideRecoveryUI(activePage);
                    isRecoveryUIShown = false;
                    console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
                }
                
                // Mute override for active page (Audio fix applied)
                for (const frame of activePage.frames()) {
                    try {
                        if (!frame.isDetached()) {
                            frame.evaluate(() => { 
                                window.isStreamMuted = false;
                                document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
                                document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
                            }).catch(()=>{});
                        }
                    } catch(e) {}
                }
            }
        }

        // Backup Tab Audio Watchdog Fix
        if (backupPage) {
            for (const frame of backupPage.frames()) {
                try {
                    if (!frame.isDetached()) {
                        frame.evaluate(() => { 
                            window.isStreamMuted = true;
                            document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); 
                        }).catch(()=>{});
                    }
                } catch(e) {}
            }
        }

        watchdogTicks++;

        if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
            let logBackupStatus = await checkPageStatus(backupPage);
            let nextAfterBackupIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
            let nextAfterBackupUrl = urlList[nextAfterBackupIndex].url;
            
            console.log(`\n==================================================`);
            console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'} (Limit: ${activeHangThresholdMs/1000}s)`);
            console.log(`[▶️] CURRENTLY LIVE              : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
            console.log(`--------------------------------------------------`);
            console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
            console.log(`[🔄] RUNNING IN BACKGROUND       : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
            console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
            console.log(`==================================================\n`);
        }

        if (watchdogTicks % 120 === 0) {
            await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
        }

        // =========================================================================================
        // 🔄 2. ACTIVE TAB HOT-SWAP SHIELD (UPDATED: INSTANT SEAMLESS PROMOTION)
        // =========================================================================================
        if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
            if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
                console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
                await new Promise(r => setTimeout(r, 2000));
                continue; 
            }

            let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

            if (isProactiveRefresh) {
                console.log(`\n==================================================`);
                console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
                console.log(`[*] Initiating forward rotation to prevent stream drop...`);
                console.log(`==================================================`);
            } else {
                console.log(`\n==================================================`);
                console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
                console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
                console.log(`==================================================`);
                await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
            }
            
            console.log(`[*] Checking Backup Tab status before switching...`);
            let backupStatus = await checkPageStatus(backupPage);


// =====================================



// ======================================

// --------------------------------------------------------------------
            // ⚡ SCENARIO A: INSTANT SEAMLESS HOT-SWAP (BACKUP IS ALREADY HEALTHY)
            // --------------------------------------------------------------------
            if (backupStatus.status === 'HEALTHY' && !isProactiveRefresh) {
                
                console.log('\n==================================================');
                console.log('[⚡] BACKUP STREAM ALREADY HEALTHY');
                console.log('[⚡] SHOWING TRANSITION UI & PROMOTING INSTANTLY');
                console.log('==================================================');

                // 1. Visually ek smooth "RECONNECTING" UI lagayein
                await showLoadingUI(backupPage, "RECONNECTING", "Establishing secure connection to backup server <span class='stream-blink'>...</span>");

                // 2. Tab ko screen par layein
                try { await backupPage.bringToFront(); } catch (e) {}

                // 3. Objects swap karein (Chrome 2 ab Chrome 1 ban gaya)
                let brokenPage = activePage; 
                activePage = backupPage; 
                backupPage = brokenPage;

                let brokenBrowser = activeBrowser; 
                activeBrowser = backupBrowser; 
                backupBrowser = brokenBrowser;

                let brokenName = activeBrowserName;
                activeBrowserName = backupBrowserName;
                backupBrowserName = brokenName;

                let previousActiveIndex = currentUrlIndex;
                currentUrlIndex = backupUrlIndex;
                activeUrlStr = urlList[currentUrlIndex].url; 
                
                backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
                backupUrlStr = urlList[backupUrlIndex].url;

                // 4. 🛠️ INSTANT AUDIO FIX: Background tab mute tha, isko foran unmute karein swap hotay hi!
                for (const frame of activePage.frames()) {
                    try {
                        if (!frame.isDetached()) {
                            await frame.evaluate(() => { 
                                window.isStreamMuted = false;
                                document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
                                document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
                            });
                        }
                    } catch(e) {}
                }

                // 5. State Reset (Keep isWarmupPhase FALSE because stream is already healthy)
                lastActiveTime = -1; 
                lastDecodedFrames = -1;
                frozenCheckTimestamp = Date.now();
                isRecoveryUIShown = false; 

                streamSetupTime = Date.now(); 
                currentStreamStartTime = Date.now();
                isWarmupPhase = false; 
                
                // Extra buffer time for background checks to avoid conflicts
                backupWarmupTime = Date.now() + 5000; 

                // 6. SMOOTH UI REMOVAL: Wait 1.5s for render paint to stabilize
                await new Promise(r => setTimeout(r, 1500));
                try { await hideLoadingUI(activePage); } catch(e) {}

                console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
                console.log(`[🔊] LIVE AUDIO STATUS : ON (Seamlessly Promoted & Unmuted)`);
                console.log(`--------------------------------------------------`);
                console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
                console.log(`==================================================\n`);

                // 7. 🛠️ CPU BOTTLENECK FIX: Heavy background rebuilding ko 3 seconds delay karein
                // Taa k watchdog ka immediate next active check timeout na ho aur "DEAD" issue na aye.
                setTimeout(async () => {
                    try {
                        console.log(`[⏳] Starting background buffer rebuilding safely...`);
                        await backupPage.goto('about:blank').catch(()=>{});
                        await applyPreloadFirewall(backupPage);
                        await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                        await initializeVideo(backupPage, true, false);
                    } catch (e) {
                        console.log(`[⚠️] Background buffer navigation handled safely.`);
                    }
                }, 3000); 
            }



// --------------------------------------------------------------------
            // ⚡ SCENARIO A: INSTANT SEAMLESS HOT-SWAP (BACKUP IS ALREADY HEALTHY)
            // --------------------------------------------------------------------
            // if (backupStatus.status === 'HEALTHY' && !isProactiveRefresh) {
                
            //     console.log('\n==================================================');
            //     console.log('[⚡] BACKUP STREAM ALREADY HEALTHY');
            //     console.log('[⚡] SHOWING TRANSITION UI & PROMOTING INSTANTLY');
            //     console.log('==================================================');

            //     // 1. Visually ek smooth "RECONNECTING" UI lagayein
            //     await showLoadingUI(backupPage, "RECONNECTING", "Establishing secure connection to backup server <span class='stream-blink'>...</span>");

            //     // 2. Tab ko screen par layein
            //     try { await backupPage.bringToFront(); } catch (e) {}

            //     // 3. Objects swap karein (Chrome 2 ab Chrome 1 ban gaya)
            //     let brokenPage = activePage; 
            //     activePage = backupPage; 
            //     backupPage = brokenPage;

            //     let brokenBrowser = activeBrowser; 
            //     activeBrowser = backupBrowser; 
            //     backupBrowser = brokenBrowser;

            //     let brokenName = activeBrowserName;
            //     activeBrowserName = backupBrowserName;
            //     backupBrowserName = brokenName;

            //     let previousActiveIndex = currentUrlIndex;
            //     currentUrlIndex = backupUrlIndex;
            //     activeUrlStr = urlList[currentUrlIndex].url; 
                
            //     backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
            //     backupUrlStr = urlList[backupUrlIndex].url;

            //     lastActiveTime = -1; 
            //     lastDecodedFrames = -1;
            //     frozenCheckTimestamp = Date.now();
            //     isRecoveryUIShown = false; 

            //     // streamSetupTime = Date.now(); 
            //     // currentStreamStartTime = Date.now();
            //     // isWarmupPhase = false; // Video pehle se ready hai
            //     streamSetupTime = Date.now(); 
            //     currentStreamStartTime = Date.now();
            //     isWarmupPhase = true; // IMPORTANT: Hot-swap k doran lag/timeout se bachne k liye grace period
                
            //     // 🚀 FIX: Lock background watchdog immediately BEFORE async rebuilding
            //     // Yeh single line us double-execution bug ko hamesha ke liye rok degi!
            //     backupWarmupTime = Date.now(); 

            //     // 4. SMOOTH UI REMOVAL: Background stream is already full-screened by CSS. 
            //     // We just wait 1.5 seconds for the foreground render paint to stabilize before removing overlay.
            //     await new Promise(r => setTimeout(r, 1500));
            //     try { await hideLoadingUI(activePage); } catch(e) {}

            //     console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
            //     console.log(`[🔊] LIVE AUDIO STATUS : ON (Seamlessly Promoted)`);
            //     console.log(`--------------------------------------------------`);
            //     console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
            //     console.log(`==================================================\n`);

            //     // 5. Old broken tab ko silently background mein rebuild karein (No Blocking)
            //     (async () => {
            //         try {
            //             await backupPage.goto('about:blank').catch(()=>{});
            //             await applyPreloadFirewall(backupPage);
            //             await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
            //             await initializeVideo(backupPage, true, false);
            //         } catch (e) {
            //             console.log(`[⏳] Background buffer navigation handled safely.`);
            //         }
            //     })();
            // } 


// ====================================



            // --------------------------------------------------------------------
            // 🔄 SCENARIO B: PROACTIVE REFRESH OR FORCED RECONNECTION
            // --------------------------------------------------------------------
            else if (isProactiveRefresh || (backupStatus.status === 'HEALTHY' && isProactiveRefresh)) {
                
                for (const frame of activePage.frames()) {
                    try { if (!frame.isDetached()) await frame.evaluate(() => { window.isStreamMuted = true; document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
                }
                
                await showLoadingUI(backupPage, "REFRESHING CONNECTION", "Optimizing current server stream <span class='stream-blink'>...</span>");
                await backupPage.bringToFront();
                await new Promise(r => setTimeout(r, 1000)); 
                
                try { await backupPage.mouse.click(10, 10); } catch(e){} 

                console.log(`[*] Initializing Video on the newly active tab...`);
                await initializeVideo(backupPage, false, true); 
                await hideLoadingUI(backupPage);

                let brokenPage = activePage; 
                activePage = backupPage; 
                backupPage = brokenPage;

                let brokenBrowser = activeBrowser; 
                activeBrowser = backupBrowser; 
                backupBrowser = brokenBrowser;

                let brokenName = activeBrowserName;
                activeBrowserName = backupBrowserName;
                backupBrowserName = brokenName;
                
                lastActiveTime = -1; frozenCheckTimestamp = Date.now();
                isRecoveryUIShown = false; 

                let previousActiveIndex = currentUrlIndex;
                currentUrlIndex = backupUrlIndex;
                activeUrlStr = urlList[currentUrlIndex].url; 
                
                backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
                backupUrlStr = urlList[backupUrlIndex].url;

                console.log(`\n==================================================`);
                console.log(`[🔄] PROACTIVE REFRESH EXECUTED SUCCESSFULLY`);
                console.log(`==================================================`);

                try {
                    await backupPage.goto('about:blank').catch(()=>{});
                    await applyPreloadFirewall(backupPage);
                    await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    await initializeVideo(backupPage, true, false);
                } catch (e) {}
                
                streamSetupTime = Date.now(); 
                isWarmupPhase = true;
                currentStreamStartTime = Date.now();
                // 🚀 FIX: Lock background watchdog for Proactive Refresh too!
                backupWarmupTime = Date.now(); 
            }


            // // --------------------------------------------------------------------
            // // ⚡ SCENARIO A: INSTANT SEAMLESS HOT-SWAP (BACKUP IS ALREADY HEALTHY)
            // // --------------------------------------------------------------------
            // if (backupStatus.status === 'HEALTHY' && !isProactiveRefresh) {
                
            //     console.log('\n==================================================');
            //     console.log('[⚡] BACKUP STREAM ALREADY HEALTHY');
            //     console.log('[⚡] SHOWING TRANSITION UI & PROMOTING INSTANTLY');
            //     console.log('==================================================');

            //     // 1. Visually ek smooth "RECONNECTING" UI lagayein
            //     await showLoadingUI(backupPage, "RECONNECTING", "Establishing secure connection to backup server <span class='stream-blink'>...</span>");

            //     // 2. Tab ko screen par layein
            //     try { await backupPage.bringToFront(); } catch (e) {}

            //     // 3. Objects swap karein (Chrome 2 ab Chrome 1 ban gaya)
            //     let brokenPage = activePage; 
            //     activePage = backupPage; 
            //     backupPage = brokenPage;

            //     let brokenBrowser = activeBrowser; 
            //     activeBrowser = backupBrowser; 
            //     backupBrowser = brokenBrowser;

            //     let brokenName = activeBrowserName;
            //     activeBrowserName = backupBrowserName;
            //     backupBrowserName = brokenName;

            //     let previousActiveIndex = currentUrlIndex;
            //     currentUrlIndex = backupUrlIndex;
            //     activeUrlStr = urlList[currentUrlIndex].url; 
                
            //     backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
            //     backupUrlStr = urlList[backupUrlIndex].url;

            //     lastActiveTime = -1; 
            //     lastDecodedFrames = -1;
            //     frozenCheckTimestamp = Date.now();
            //     isRecoveryUIShown = false; 

            //     streamSetupTime = Date.now(); 
            //     currentStreamStartTime = Date.now();
            //     isWarmupPhase = false; // Video pehle se ready hai

            //     // 4. 🧠 SMART CHECK: Wait until video is ACTUALLY Fullscreen before removing UI
            //     console.log(`[*] Verifying if stream is fullscreen before removing Reconnecting UI...`);
            //     let checkAttempts = 0;
            //     let isFullscreenReady = false;
                
            //     while (checkAttempts < 10) { // Max 5 seconds tak check karega
            //         try {
            //             isFullscreenReady = await activePage.evaluate(() => {
            //                 let vids = Array.from(document.querySelectorAll('video'));
            //                 for (let v of vids) {
            //                     // Agar video window screen ke barabar ya qareeb aa chuki hai
            //                     if (v.clientWidth >= window.innerWidth * 0.8 && v.clientHeight >= window.innerHeight * 0.8) {
            //                         return true;
            //                     }
            //                 }
            //                 return false;
            //             });
            //             if (isFullscreenReady) break; 
            //         } catch (e) {}
                    
            //         await new Promise(r => setTimeout(r, 500));
            //         checkAttempts++;
            //     }

            //     if (isFullscreenReady) {
            //         console.log(`[✅] Stream confirmed fullscreen! Removing UI...`);
            //     } else {
            //         console.log(`[⚠️] Fullscreen check timeout, removing UI anyway to continue stream.`);
            //     }
                
            //     try { await hideLoadingUI(activePage); } catch(e) {}

            //     console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
            //     console.log(`[🔊] LIVE AUDIO STATUS : ON (Seamlessly Promoted)`);
            //     console.log(`--------------------------------------------------`);
            //     console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
            //     console.log(`==================================================\n`);

            //     // 5. Old broken tab ko silently background mein rebuild karein (No Blocking)
            //     (async () => {
            //         try {
            //             await backupPage.goto('about:blank').catch(()=>{});
            //             await applyPreloadFirewall(backupPage);
            //             await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
            //             await initializeVideo(backupPage, true, false);
            //             backupWarmupTime = Date.now();
            //         } catch (e) {
            //             console.log(`[⏳] Background buffer navigation handled safely.`);
            //         }
            //     })();
            // } 
            // // --------------------------------------------------------------------
            // // 🔄 SCENARIO B: PROACTIVE REFRESH OR FORCED RECONNECTION
            // // --------------------------------------------------------------------
            // else if (isProactiveRefresh || (backupStatus.status === 'HEALTHY' && isProactiveRefresh)) {
                
            //     for (const frame of activePage.frames()) {
            //         try { if (!frame.isDetached()) await frame.evaluate(() => { window.isStreamMuted = true; document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
            //     }
                
            //     await showLoadingUI(backupPage, "REFRESHING CONNECTION", "Optimizing current server stream <span class='stream-blink'>...</span>");
            //     await backupPage.bringToFront();
            //     await new Promise(r => setTimeout(r, 1000)); 
                
            //     try { await backupPage.mouse.click(10, 10); } catch(e){} 

            //     console.log(`[*] Initializing Video on the newly active tab...`);
            //     await initializeVideo(backupPage, false, true); 
            //     await hideLoadingUI(backupPage);

            //     let brokenPage = activePage; 
            //     activePage = backupPage; 
            //     backupPage = brokenPage;

            //     let brokenBrowser = activeBrowser; 
            //     activeBrowser = backupBrowser; 
            //     backupBrowser = brokenBrowser;

            //     let brokenName = activeBrowserName;
            //     activeBrowserName = backupBrowserName;
            //     backupBrowserName = brokenName;
                
            //     lastActiveTime = -1; frozenCheckTimestamp = Date.now();
            //     isRecoveryUIShown = false; 

            //     let previousActiveIndex = currentUrlIndex;
            //     currentUrlIndex = backupUrlIndex;
            //     activeUrlStr = urlList[currentUrlIndex].url; 
                
            //     backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
            //     backupUrlStr = urlList[backupUrlIndex].url;

            //     console.log(`\n==================================================`);
            //     console.log(`[🔄] PROACTIVE REFRESH EXECUTED SUCCESSFULLY`);
            //     console.log(`==================================================`);

            //     try {
            //         await backupPage.goto('about:blank').catch(()=>{});
            //         await applyPreloadFirewall(backupPage);
            //         await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
            //         await initializeVideo(backupPage, true, false);
            //     } catch (e) {}
                
            //     streamSetupTime = Date.now(); 
            //     isWarmupPhase = true;
            //     currentStreamStartTime = Date.now();
            // } 




// =====================================
            // --------------------------------------------------------------------
            // ❌ SCENARIO C: BOTH TABS FAILED (Fresh Hunting Mode)
            // --------------------------------------------------------------------
            else {
                console.log(`\n==================================================`);
                console.log(`[!] ❌ BOTH TABS FAILED (Active & Backup Compromised)`);
                console.log(`[🔍] FRESH HUNTING MODE ACTIVATED: Spawning clean tabs...`);
                console.log(`==================================================`);
                
                try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch (e) {}

                currentUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
                activeUrlStr = urlList[currentUrlIndex].url;
                
                backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
                backupUrlStr = urlList[backupUrlIndex].url;

                console.log(`[*] Hunting Active -> Server [${currentUrlIndex}]`);
                console.log(`[*] Hunting Backup -> Server [${backupUrlIndex}]`);

                console.log(`[*] Closing crashed tabs and spawning fresh ones...`);
                try { await activePage.close(); } catch(e) {}
                try { await backupPage.close(); } catch(e) {}

                activePage = await activeBrowser.newPage();
                backupPage = await backupBrowser.newPage();

                await setupNetworkAdBlocker(activePage);
                await setupNetworkAdBlocker(backupPage);
                attachAntiAdListeners(activePage);
                attachAntiAdListeners(backupPage);
                await applyPreloadFirewall(activePage);
                await applyPreloadFirewall(backupPage);

                try {
                    await activePage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 });
                    await showLoadingUI(activePage, "SEARCHING SERVER", "Hunting for a stable stream connection <span class='stream-blink'>...</span>");
                    await initializeVideo(activePage, false, true); 
                    await hideLoadingUI(activePage);
                } catch(e) {}

                try {
                    await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
                    await initializeVideo(backupPage, true, false); 
                } catch(e) {}

                streamSetupTime = Date.now(); 
                isWarmupPhase = true; 
                currentStreamStartTime = Date.now();
                backupWarmupTime = Date.now();
                lastActiveTime = -1;
                frozenCheckTimestamp = Date.now();
                isRecoveryUIShown = false;

                try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
                
                console.log(`[*] Fresh tabs active. Waiting 15 seconds (Warm-up) for video to stabilize...`);
            }
        } 
       
        await new Promise(r => setTimeout(r, 2000)); 
    }
}

async function startDirectStreaming() {
    activeBrowserName = "CHROME 1";
    backupBrowserName = "CHROME 2";
    
    console.log(`[*] Starting OBS Studio FIRST...`);
    setupOBSConfig();

    obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
    obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
    obsProcess.stderr.on('data', (data) => {
        const msg = data.toString().trim();
        if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
    });

    console.log('[*] Waiting for OBS to initialize before launching browser...');
    await new Promise(r => setTimeout(r, 6000));

    let isObsConnected = false;
    console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
    for (let attempt = 1; attempt <= 15; attempt++) {
        try {
            await Promise.race([
                obs.connect('ws://127.0.0.1:4455', 'secret'),
                new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
            ]);
            isObsConnected = true;
            console.log('[+] OBS WebSocket Connected Successfully!');
            break;
        } catch (e) {
            console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
            await new Promise(r => setTimeout(r, 2000));
        }
    }

    if (isObsConnected) {
        try {
            await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
            console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
        } catch(e){}
    }

    browserArgs = [
        '--no-sandbox', 
        '--disable-setuid-sandbox',
        `--window-size=${RES_W},${RES_H}`, 
        '--window-position=0,0', 
        '--kiosk', 
        '--start-fullscreen',
        '--autoplay-policy=no-user-gesture-required',
        '--disable-dev-shm-usage', 
        '--ignore-certificate-errors',
        '--disable-web-security',
        '--ignore-gpu-blocklist', 
        '--use-gl=egl',
        '--disable-accelerated-video-decode', 
        '--disable-accelerated-video-encode',
        '--disable-smooth-scrolling',
        '--disable-blink-features=AutomationControlled',
        '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion,NetworkServiceInProcess2',
        '--disable-background-timer-throttling',
        '--disable-backgrounding-occluded-windows',
        '--disable-renderer-backgrounding',
        `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
        `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
    ];

    if (PROXY_ENGINE.includes('Cloudflare')) {
        browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
        console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
    } else {
        console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
    }

    console.log(`[*] Starting ACTIVE browser instance...`);
    activeBrowser = await createBrowserInstance(browserArgs);
    const activePages = await activeBrowser.pages();
    activePage = activePages[0];

    console.log(`[*] Starting BACKUP browser instance in background...`);
    backupBrowser = await createBrowserInstance(browserArgs);
    const backupPages = await backupBrowser.pages();
    backupPage = backupPages[0];

    activeBrowser.on('targetcreated', async (target) => {
        if (target.type() === 'page') {
            const newPage = await target.page();
            setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
        }
    });

    backupBrowser.on('targetcreated', async (target) => {
        if (target.type() === 'page') {
            const newPage = await target.page();
            setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
        }
    });

    await setupNetworkAdBlocker(activePage);
    await setupNetworkAdBlocker(backupPage);

    attachAntiAdListeners(activePage);
    attachAntiAdListeners(backupPage);

    await applyPreloadFirewall(activePage);
    await applyPreloadFirewall(backupPage);

    await activePage.bringToFront(); 

    console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex].url}`);
    try {
        await activePage.goto(urlList[currentUrlIndex].url, { waitUntil: 'domcontentloaded', timeout: 60000 });
    } catch (e) {
        console.log(`[⚠️] Network buffer safely handled for primary URL.`);
    }
    
    await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
    await initializeVideo(activePage, false, true); 
    await hideLoadingUI(activePage); 

    if (isObsConnected) {
        console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
        try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
    }

    console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex].url}`);
    try {
        await backupPage.goto(urlList[backupUrlIndex].url, { waitUntil: 'domcontentloaded', timeout: 60000 });
    } catch (e) {}
    
    await initializeVideo(backupPage, true, false);
    
    await activePage.bringToFront();
    try { await activePage.mouse.click(10, 10); } catch(e){} 
    await hideLoadingUI(activePage);

    console.log(`\n==================================================`);
    console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
    console.log(`==================================================`);
    console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex].url}`);
    console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
    console.log(`--------------------------------------------------`);
    console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex].url}`);
    console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
    console.log(`==================================================\n`);

    console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
    await startWatchdog();
}

async function mainLoop() {
    while (true) {
        try {
            await startDirectStreaming();
        } catch (error) {
            console.error('\n==================================================');
            console.error('[🚨] FATAL ENGINE ERROR');
            console.error(`[!] ${error.message}`);
            console.error('==================================================');

            if (activeBrowser && activeBrowser.isConnected()) {
                console.log('[🛡️] ACTIVE CHROME STILL ALIVE — NOT RESTARTING ENGINE.');
                await new Promise(resolve => setTimeout(resolve, 3000));
                continue;
            }

            if (backupBrowser && backupBrowser.isConnected()) {
                console.log('[🛡️] BACKUP CHROME STILL ALIVE — NOT RESTARTING ENGINE.');
                await new Promise(resolve => setTimeout(resolve, 3000));
                continue;
            }

            console.log('[⚠️] NO CHROME INSTANCE SURVIVED. Performing final recovery.');

            try { if (activeBrowser) await activeBrowser.close().catch(() => {}); } catch (e) {}
            try { if (backupBrowser) await backupBrowser.close().catch(() => {}); } catch (e) {}

            activeBrowser = null; backupBrowser = null; activePage = null; backupPage = null;

            await new Promise(resolve => setTimeout(resolve, 3000));
            await cleanup();
        }
    }
}

async function cleanup() {
    console.log('[*] Cleaning up resources...');
    try { await obs.disconnect(); } catch (e) { } 
    if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
    if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
    if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
    try {
        execSync('pkill -9 obs || true', { stdio: 'ignore' });
        execSync('pkill -9 chrome || true', { stdio: 'ignore' });
        execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
    } catch (e) { }
}

process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

const customDurationStr = process.env.CUSTOM_DURATION || 'None';
function parseDurationToMs(str) {
    if (!str || str.toLowerCase() === 'none') return null;
    let ms = 0;
    const hMatch = str.match(/(\d+)\s*h/i);
    const mMatch = str.match(/(\d+)\s*m/i);
    if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
    if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
    return ms > 0 ? ms : null;
}

const exactDurationMs = parseDurationToMs(customDurationStr);
if (exactDurationMs) {
    setTimeout(async () => {
        console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
        await cleanup();
        process.exit(0);
    }, exactDurationMs);
} else {
    setTimeout(() => {
        try {
            const targetUrls = process.env.TARGET_URLS || 'https://sport4u.online';
            const channel = process.env.OKRU_STREAM_ID || '1';
            const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
            const server = process.env.SERVER_SELECTION || 'None';
            const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
            execSync(cmd, { stdio: 'inherit' });
            setTimeout(async () => {
                await cleanup(); 
                process.exit(0); 
            }, 300000); 
        } catch (err) { }
    }, 21000000);
}

mainLoop();























// hahahahahaahahaha





// const puppeteer = require('puppeteer-extra');
// let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (DEBUGGING MODE)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     console.error('\n========================================');
//     console.error('[💥] UNCAUGHT EXCEPTION');
//     console.error(err);
//     console.error(err.stack);
//     console.error('========================================\n');
// });

// process.on('unhandledRejection', (reason) => {
//     console.error('\n========================================');
//     console.error('[💥] UNHANDLED REJECTION');
//     console.error(reason);
//     console.error('========================================\n');
// });

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', 
//     'sport4u.online',
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// // =========================================================================================
// // 🔄 DYNAMIC URL PARSER & METADATA EXTRACTOR
// // =========================================================================================
// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = [];

// if (rawUrls !== '') {
//     urlList = rawUrls.split(',').map(u => {
//         let trimmed = u.trim();
//         let hangThreshold = 8000; 
        
//         if (trimmed.startsWith('!')) {
//             hangThreshold = 20000; 
//             trimmed = trimmed.substring(1); 
//         }
//         if (!trimmed.startsWith('http')) trimmed = 'https://' + trimmed;
        
//         return { url: trimmed, hangTime: hangThreshold };
//     });
// } else {
//     urlList = [
//         { url: 'https://sport4u.online', hangTime: 8000 },
//         { url: 'https://dadocric.st/player.php?id=starsp3&v=m', hangTime: 8000 }
//     ];
// }

// function getSafeBackupIndex(activeIndex, currentIndex, list) {
//     if (list.length <= 1) return 0; 
//     let next = (currentIndex + 1) % list.length;
//     if (next === activeIndex) {
//         next = (next + 1) % list.length; 
//     }
//     return next;
// }

// let currentUrlIndex = 0;
// let backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let browserArgs = []; // Global Browser Args for Recovery Functions
// let activeBrowser = null;
// let backupBrowser = null;
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";
// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;

// async function createBrowserInstance(args) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: args
//     });
// }

// // =========================================================================================
// // 🛡️ SMART BROWSER RECOVERY
// // =========================================================================================

// async function preparePage(page) {
//     if (!page) return;
//     await setupNetworkAdBlocker(page);
//     attachAntiAdListeners(page);
//     await applyPreloadFirewall(page);
// }

// async function createFreshBackupBrowser() {
//     console.log('\n[🛠️] BACKUP RECOVERY: Creating fresh backup Chrome...');
//     try {
//         if (backupBrowser && backupBrowser.isConnected()) {
//             try {
//                 const pages = await backupBrowser.pages();
//                 for (const p of pages) {
//                     try { await p.close(); } catch (e) {}
//                 }
//             } catch (e) {}
//         }
//     } catch (e) {}

//     try {
//         if (backupBrowser && !backupBrowser.isConnected()) {
//             backupBrowser = null;
//         }
//     } catch (e) {
//         backupBrowser = null;
//     }

//     backupBrowser = await createBrowserInstance(browserArgs);
//     const pages = await backupBrowser.pages();
//     backupPage = pages[0];
//     await preparePage(backupPage);

//     backupBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             try {
//                 const newPage = await target.page();
//                 setTimeout(async () => {
//                     if (newPage && newPage !== backupPage) {
//                         try { await newPage.close(); } catch (e) {}
//                     }
//                 }, 500);
//             } catch (e) {}
//         }
//     });

//     console.log('[✅] BACKUP RECOVERY: Fresh backup Chrome created.');
//     return true;
// }

// async function createFreshActiveBrowser() {
//     console.log('\n[🛠️] ACTIVE RECOVERY: Creating fresh active Chrome...');
//     try {
//         if (activeBrowser && activeBrowser.isConnected()) {
//             try {
//                 const pages = await activeBrowser.pages();
//                 for (const p of pages) {
//                     try { await p.close(); } catch (e) {}
//                 }
//             } catch (e) {}
//         }
//     } catch (e) {}

//     try {
//         if (activeBrowser && !activeBrowser.isConnected()) {
//             activeBrowser = null;
//         }
//     } catch (e) {
//         activeBrowser = null;
//     }

//     activeBrowser = await createBrowserInstance(browserArgs);
//     const pages = await activeBrowser.pages();
//     activePage = pages[0];
//     await preparePage(activePage);

//     activeBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             try {
//                 const newPage = await target.page();
//                 setTimeout(async () => {
//                     if (newPage && newPage !== activePage) {
//                         try { await newPage.close(); } catch (e) {}
//                     }
//                 }, 500);
//             } catch (e) {}
//         }
//     });

//     console.log('[✅] ACTIVE RECOVERY: Fresh active Chrome created.');
//     return true;
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') {
//                     init.mode = 'open'; 
//                 }
//                 const shadowRoot = originalAttachShadow.call(this, init);
                
//                 const observer = new MutationObserver(() => {
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) {
//                         console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
//                         this.remove(); 
//                     }
//                 });
                
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };
            
//             Element.prototype.attachShadow.toString = function() { return "function attachShadow() { [native code] }"; };

//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]\nName=Untitled\n[Video]\nBaseCX=${RES_W}\nBaseCY=${RES_H}\nOutputCX=${RES_W}\nOutputCY=${RES_H}\nFPSCommon=30\n[Output]\nMode=Simple\n[SimpleOutput]\nVBitrate=${BITRATE}\nStreamEncoder=x264\nx264Preset=ultrafast\nx264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0\n`;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;
//                         if (area < 5000) return;
//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay):not([class*="player"]):not([class*="jw"]):not([class*="vjs"]), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             // FIX 1: Use window object to control audio globally to avoid Audio War
//             window.isStreamMuted = muteVideo; 

//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = window.isStreamMuted; 
//                         media.volume = window.isStreamMuted ? 0.0 : 1.0; 
//                     });

//                     if (!window.isStreamMuted) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         let playerWrap = realVideo.closest('.jwplayer, #player, .plyr, .vjs-player, .shaka-video-container, [data-player]') || realVideo;
                        
//                         playerWrap.style.setProperty('position', 'fixed', 'important');
//                         playerWrap.style.setProperty('top', '0px', 'important');
//                         playerWrap.style.setProperty('left', '0px', 'important');
//                         playerWrap.style.setProperty('width', '100vw', 'important');
//                         playerWrap.style.setProperty('height', '100vh', 'important');
//                         playerWrap.style.setProperty('z-index', '2147483646', 'important'); 
//                         playerWrap.style.setProperty('background-color', 'black', 'important');
//                         playerWrap.style.setProperty('opacity', '1', 'important');
//                         playerWrap.style.setProperty('visibility', 'visible', 'important');
//                         playerWrap.style.setProperty('display', 'block', 'important');
                        
//                         if (playerWrap !== realVideo) {
//                             realVideo.style.setProperty('width', '100%', 'important');
//                             realVideo.style.setProperty('height', '100%', 'important');
//                         }
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     if (!startMuted) {
//         await triggerSmartUnmute(page);
//         await new Promise(r => setTimeout(r, 1000));
//     }
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); 
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex].url;
//     let backupUrlStr = urlList[backupUrlIndex].url;

//     let currentStreamStartTime = Date.now();
//     let isRecoveryUIShown = false;

//     while (true) {
//         // =====================================================================================
//         // 🛡️ NEVER THROW JUST BECAUSE ONE CHROME DISCONNECTED
//         // Recover the affected side instead.
//         // =====================================================================================
//         const activeBrowserAlive = activeBrowser && activeBrowser.isConnected();
//         const backupBrowserAlive = backupBrowser && backupBrowser.isConnected();

//         // ---------------------------------------------------------------------
//         // CASE 1: ACTIVE CHROME DEAD, BACKUP CHROME STILL ALIVE
//         // ---------------------------------------------------------------------
//         if (!activeBrowserAlive && backupBrowserAlive) {
//             console.log('\n==================================================');
//             console.log('[🚨] ACTIVE CHROME DISCONNECTED');
//             console.log('[🔄] BACKUP CHROME IS ALIVE');
//             console.log('[⚡] PROMOTING BACKUP -> ACTIVE');
//             console.log('[🛡️] OBS WILL CONTINUE RUNNING');
//             console.log('==================================================\n');

//             const oldActiveBrowser = activeBrowser;
//             const oldActivePage = activePage;
//             activeBrowser = backupBrowser;
//             activePage = backupPage;
//             backupBrowser = oldActiveBrowser;
//             backupPage = oldActivePage;

//             const oldActiveName = activeBrowserName;
//             activeBrowserName = backupBrowserName;
//             backupBrowserName = oldActiveName;

//             const previousActiveIndex = currentUrlIndex;
//             currentUrlIndex = backupUrlIndex;
//             activeUrlStr = urlList[currentUrlIndex].url;

//             backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
//             backupUrlStr = urlList[backupUrlIndex].url;

//             lastActiveTime = -1; lastDecodedFrames = -1; frozenCheckTimestamp = Date.now();
//             streamSetupTime = Date.now(); currentStreamStartTime = Date.now();
//             isWarmupPhase = true; backupWarmupTime = Date.now(); isRecoveryUIShown = false;

//             try { await activePage.bringToFront(); await hideLoadingUI(activePage); } catch (e) {}

//             console.log('[✅] BACKUP PROMOTED SUCCESSFULLY');
//             console.log(`[📺] NEW ACTIVE SERVER : [${currentUrlIndex}] -> ${activeUrlStr}`);
//             console.log(`[🔊] ACTIVE AUDIO      : ON`);
//             console.log(`[🛡️] NEW BACKUP SERVER : [${backupUrlIndex}] -> ${backupUrlStr}`);

//             try {
//                 await createFreshBackupBrowser();
//                 await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 await initializeVideo(backupPage, true, false);
//                 console.log('[✅] NEW BACKUP SERVER IS BUFFERING IN BACKGROUND');
//             } catch (e) {
//                 console.log(`[⚠️] Backup recreation failed. Watchdog will try the NEXT server.`);
//             }
//             continue;
//         }

//         // ---------------------------------------------------------------------
//         // CASE 2: BACKUP CHROME DEAD, ACTIVE CHROME STILL ALIVE
//         // ---------------------------------------------------------------------
//         if (activeBrowserAlive && !backupBrowserAlive) {
//             console.log('\n==================================================');
//             console.log('[⚠️] BACKUP CHROME DISCONNECTED');
//             console.log('[🛡️] ACTIVE STREAM WILL NOT BE TOUCHED');
//             console.log('[🔄] REBUILDING BACKUP WITH NEXT SERVER');
//             console.log('==================================================\n');

//             try {
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`[*] NEXT BACKUP SERVER -> [${backupUrlIndex}] ${backupUrlStr}`);
//                 await createFreshBackupBrowser();
//                 await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 await initializeVideo(backupPage, true, false);
//                 backupWarmupTime = Date.now();
//                 console.log(`[✅] BACKUP RECOVERED -> Server [${backupUrlIndex}]`);
//             } catch (e) {
//                 console.log(`[⚠️] Backup recovery failed. NEXT WATCHDOG CYCLE WILL TRY AGAIN.`);
//             }
//         }

//         // ---------------------------------------------------------------------
//         // CASE 3: BOTH CHROMES DEAD
//         // ---------------------------------------------------------------------
//         if (!activeBrowserAlive && !backupBrowserAlive) {
//             console.log('\n==================================================');
//             console.log('[🚨] BOTH CHROME INSTANCES DISCONNECTED');
//             console.log('[🛠️] LOCAL RECOVERY MODE');
//             console.log('[🛑] OBS WILL NOT BE RESTARTED');
//             console.log('==================================================\n');

//             try {
//                 currentUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 activeUrlStr = urlList[currentUrlIndex].url;

//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`[*] RECOVERY ACTIVE -> [${currentUrlIndex}] ${activeUrlStr}`);
//                 console.log(`[*] RECOVERY BACKUP -> [${backupUrlIndex}] ${backupUrlStr}`);

//                 await createFreshActiveBrowser();
//                 await activePage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 await showLoadingUI(activePage, "SEARCHING SERVER", "Finding a stable stream connection <span class='stream-blink'>...</span>");
//                 await initializeVideo(activePage, false, true);
//                 await hideLoadingUI(activePage);

//                 console.log('[✅] ACTIVE CHROME RECOVERED');

//                 try {
//                     await createFreshBackupBrowser();
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                     await initializeVideo(backupPage, true, false);
//                     console.log('[✅] BACKUP CHROME RECOVERED');
//                 } catch (backupError) {
//                     console.log('[⚠️] Backup failed. Active stream continues.');
//                 }

//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}

//                 streamSetupTime = Date.now(); currentStreamStartTime = Date.now(); backupWarmupTime = Date.now();
//                 frozenCheckTimestamp = Date.now(); lastActiveTime = -1; lastDecodedFrames = -1;
//                 isWarmupPhase = true; isRecoveryUIShown = false;

//                 console.log('[✅] LOCAL RECOVERY COMPLETE — WATCHDOG CONTINUES');
//             } catch (e) {
//                 console.log(`[❌] Local Chrome recovery failed: ${e.message}`);
//                 await new Promise(r => setTimeout(r, 3000));
//             }
//             continue;
//         }

//         let activeHangThresholdMs = urlList[currentUrlIndex].hangTime;
//         let activeStatus = await checkPageStatus(activePage);

//         // 🔄 1. BACKGROUND SHIELD
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { 
//             let backupStatus = await checkPageStatus(backupPage);
            
//             if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
//                 console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
//                 backupWarmupTime = Date.now();
                
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                     await initializeVideo(backupPage, true, false);
//                 } catch(e) {}
//             }
//         }

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         // Active Tab Audio Watchdog Fix (Stop Audio War)
//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > activeHangThresholdMs) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                     isRecoveryUIShown = false; 
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 // Mute override for active page (Audio fix applied)
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 window.isStreamMuted = false;
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         // Backup Tab Audio Watchdog Fix
//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { 
//                             window.isStreamMuted = true;
//                             document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); 
//                         }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;

//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             let logBackupStatus = await checkPageStatus(backupPage);
//             let nextAfterBackupIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex].url;
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'} (Limit: ${activeHangThresholdMs/1000}s)`);
//             console.log(`[▶️] CURRENTLY LIVE              : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
//             console.log(`--------------------------------------------------`);
//             console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND       : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         // =========================================================================================
//         // 🔄 2. ACTIVE TAB HOT-SWAP SHIELD (UPDATED: INSTANT SEAMLESS PROMOTION)
//         // =========================================================================================
//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Initiating forward rotation to prevent stream drop...`);
//                 console.log(`==================================================`);
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);


// // =====================================

// // --------------------------------------------------------------------
//             // ⚡ SCENARIO A: INSTANT SEAMLESS HOT-SWAP (BACKUP IS ALREADY HEALTHY)
//             // --------------------------------------------------------------------
//             if (backupStatus.status === 'HEALTHY' && !isProactiveRefresh) {
                
//                 console.log('\n==================================================');
//                 console.log('[⚡] BACKUP STREAM ALREADY HEALTHY');
//                 console.log('[⚡] SHOWING TRANSITION UI & PROMOTING INSTANTLY');
//                 console.log('==================================================');

//                 // 1. Visually ek smooth "RECONNECTING" UI lagayein
//                 await showLoadingUI(backupPage, "RECONNECTING", "Establishing secure connection to backup server <span class='stream-blink'>...</span>");

//                 // 2. Tab ko screen par layein
//                 try { await backupPage.bringToFront(); } catch (e) {}

//                 // 3. Objects swap karein (Chrome 2 ab Chrome 1 ban gaya)
//                 let brokenPage = activePage; 
//                 activePage = backupPage; 
//                 backupPage = brokenPage;

//                 let brokenBrowser = activeBrowser; 
//                 activeBrowser = backupBrowser; 
//                 backupBrowser = brokenBrowser;

//                 let brokenName = activeBrowserName;
//                 activeBrowserName = backupBrowserName;
//                 backupBrowserName = brokenName;

//                 let previousActiveIndex = currentUrlIndex;
//                 currentUrlIndex = backupUrlIndex;
//                 activeUrlStr = urlList[currentUrlIndex].url; 
                
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 lastActiveTime = -1; 
//                 lastDecodedFrames = -1;
//                 frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; 

//                 streamSetupTime = Date.now(); 
//                 currentStreamStartTime = Date.now();
//                 isWarmupPhase = false; // Video pehle se ready hai
                
//                 // 🚀 FIX: Lock background watchdog immediately BEFORE async rebuilding
//                 // Yeh single line us double-execution bug ko hamesha ke liye rok degi!
//                 backupWarmupTime = Date.now(); 

//                 // 4. SMOOTH UI REMOVAL: Background stream is already full-screened by CSS. 
//                 // We just wait 1.5 seconds for the foreground render paint to stabilize before removing overlay.
//                 await new Promise(r => setTimeout(r, 1500));
//                 try { await hideLoadingUI(activePage); } catch(e) {}

//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Seamlessly Promoted)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`==================================================\n`);

//                 // 5. Old broken tab ko silently background mein rebuild karein (No Blocking)
//                 (async () => {
//                     try {
//                         await backupPage.goto('about:blank').catch(()=>{});
//                         await applyPreloadFirewall(backupPage);
//                         await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                         await initializeVideo(backupPage, true, false);
//                     } catch (e) {
//                         console.log(`[⏳] Background buffer navigation handled safely.`);
//                     }
//                 })();
//             } 
//             // --------------------------------------------------------------------
//             // 🔄 SCENARIO B: PROACTIVE REFRESH OR FORCED RECONNECTION
//             // --------------------------------------------------------------------
//             else if (isProactiveRefresh || (backupStatus.status === 'HEALTHY' && isProactiveRefresh)) {
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { window.isStreamMuted = true; document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }
                
//                 await showLoadingUI(backupPage, "REFRESHING CONNECTION", "Optimizing current server stream <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 let brokenPage = activePage; 
//                 activePage = backupPage; 
//                 backupPage = brokenPage;

//                 let brokenBrowser = activeBrowser; 
//                 activeBrowser = backupBrowser; 
//                 backupBrowser = brokenBrowser;

//                 let brokenName = activeBrowserName;
//                 activeBrowserName = backupBrowserName;
//                 backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; 

//                 let previousActiveIndex = currentUrlIndex;
//                 currentUrlIndex = backupUrlIndex;
//                 activeUrlStr = urlList[currentUrlIndex].url; 
                
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`\n==================================================`);
//                 console.log(`[🔄] PROACTIVE REFRESH EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                     await initializeVideo(backupPage, true, false);
//                 } catch (e) {}
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();
//                 // 🚀 FIX: Lock background watchdog for Proactive Refresh too!
//                 backupWarmupTime = Date.now(); 
//             }


//             // // --------------------------------------------------------------------
//             // // ⚡ SCENARIO A: INSTANT SEAMLESS HOT-SWAP (BACKUP IS ALREADY HEALTHY)
//             // // --------------------------------------------------------------------
//             // if (backupStatus.status === 'HEALTHY' && !isProactiveRefresh) {
                
//             //     console.log('\n==================================================');
//             //     console.log('[⚡] BACKUP STREAM ALREADY HEALTHY');
//             //     console.log('[⚡] SHOWING TRANSITION UI & PROMOTING INSTANTLY');
//             //     console.log('==================================================');

//             //     // 1. Visually ek smooth "RECONNECTING" UI lagayein
//             //     await showLoadingUI(backupPage, "RECONNECTING", "Establishing secure connection to backup server <span class='stream-blink'>...</span>");

//             //     // 2. Tab ko screen par layein
//             //     try { await backupPage.bringToFront(); } catch (e) {}

//             //     // 3. Objects swap karein (Chrome 2 ab Chrome 1 ban gaya)
//             //     let brokenPage = activePage; 
//             //     activePage = backupPage; 
//             //     backupPage = brokenPage;

//             //     let brokenBrowser = activeBrowser; 
//             //     activeBrowser = backupBrowser; 
//             //     backupBrowser = brokenBrowser;

//             //     let brokenName = activeBrowserName;
//             //     activeBrowserName = backupBrowserName;
//             //     backupBrowserName = brokenName;

//             //     let previousActiveIndex = currentUrlIndex;
//             //     currentUrlIndex = backupUrlIndex;
//             //     activeUrlStr = urlList[currentUrlIndex].url; 
                
//             //     backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
//             //     backupUrlStr = urlList[backupUrlIndex].url;

//             //     lastActiveTime = -1; 
//             //     lastDecodedFrames = -1;
//             //     frozenCheckTimestamp = Date.now();
//             //     isRecoveryUIShown = false; 

//             //     streamSetupTime = Date.now(); 
//             //     currentStreamStartTime = Date.now();
//             //     isWarmupPhase = false; // Video pehle se ready hai

//             //     // 4. 🧠 SMART CHECK: Wait until video is ACTUALLY Fullscreen before removing UI
//             //     console.log(`[*] Verifying if stream is fullscreen before removing Reconnecting UI...`);
//             //     let checkAttempts = 0;
//             //     let isFullscreenReady = false;
                
//             //     while (checkAttempts < 10) { // Max 5 seconds tak check karega
//             //         try {
//             //             isFullscreenReady = await activePage.evaluate(() => {
//             //                 let vids = Array.from(document.querySelectorAll('video'));
//             //                 for (let v of vids) {
//             //                     // Agar video window screen ke barabar ya qareeb aa chuki hai
//             //                     if (v.clientWidth >= window.innerWidth * 0.8 && v.clientHeight >= window.innerHeight * 0.8) {
//             //                         return true;
//             //                     }
//             //                 }
//             //                 return false;
//             //             });
//             //             if (isFullscreenReady) break; 
//             //         } catch (e) {}
                    
//             //         await new Promise(r => setTimeout(r, 500));
//             //         checkAttempts++;
//             //     }

//             //     if (isFullscreenReady) {
//             //         console.log(`[✅] Stream confirmed fullscreen! Removing UI...`);
//             //     } else {
//             //         console.log(`[⚠️] Fullscreen check timeout, removing UI anyway to continue stream.`);
//             //     }
                
//             //     try { await hideLoadingUI(activePage); } catch(e) {}

//             //     console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//             //     console.log(`[🔊] LIVE AUDIO STATUS : ON (Seamlessly Promoted)`);
//             //     console.log(`--------------------------------------------------`);
//             //     console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//             //     console.log(`==================================================\n`);

//             //     // 5. Old broken tab ko silently background mein rebuild karein (No Blocking)
//             //     (async () => {
//             //         try {
//             //             await backupPage.goto('about:blank').catch(()=>{});
//             //             await applyPreloadFirewall(backupPage);
//             //             await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//             //             await initializeVideo(backupPage, true, false);
//             //             backupWarmupTime = Date.now();
//             //         } catch (e) {
//             //             console.log(`[⏳] Background buffer navigation handled safely.`);
//             //         }
//             //     })();
//             // } 
//             // // --------------------------------------------------------------------
//             // // 🔄 SCENARIO B: PROACTIVE REFRESH OR FORCED RECONNECTION
//             // // --------------------------------------------------------------------
//             // else if (isProactiveRefresh || (backupStatus.status === 'HEALTHY' && isProactiveRefresh)) {
                
//             //     for (const frame of activePage.frames()) {
//             //         try { if (!frame.isDetached()) await frame.evaluate(() => { window.isStreamMuted = true; document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//             //     }
                
//             //     await showLoadingUI(backupPage, "REFRESHING CONNECTION", "Optimizing current server stream <span class='stream-blink'>...</span>");
//             //     await backupPage.bringToFront();
//             //     await new Promise(r => setTimeout(r, 1000)); 
                
//             //     try { await backupPage.mouse.click(10, 10); } catch(e){} 

//             //     console.log(`[*] Initializing Video on the newly active tab...`);
//             //     await initializeVideo(backupPage, false, true); 
//             //     await hideLoadingUI(backupPage);

//             //     let brokenPage = activePage; 
//             //     activePage = backupPage; 
//             //     backupPage = brokenPage;

//             //     let brokenBrowser = activeBrowser; 
//             //     activeBrowser = backupBrowser; 
//             //     backupBrowser = brokenBrowser;

//             //     let brokenName = activeBrowserName;
//             //     activeBrowserName = backupBrowserName;
//             //     backupBrowserName = brokenName;
                
//             //     lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//             //     isRecoveryUIShown = false; 

//             //     let previousActiveIndex = currentUrlIndex;
//             //     currentUrlIndex = backupUrlIndex;
//             //     activeUrlStr = urlList[currentUrlIndex].url; 
                
//             //     backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
//             //     backupUrlStr = urlList[backupUrlIndex].url;

//             //     console.log(`\n==================================================`);
//             //     console.log(`[🔄] PROACTIVE REFRESH EXECUTED SUCCESSFULLY`);
//             //     console.log(`==================================================`);

//             //     try {
//             //         await backupPage.goto('about:blank').catch(()=>{});
//             //         await applyPreloadFirewall(backupPage);
//             //         await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//             //         await initializeVideo(backupPage, true, false);
//             //     } catch (e) {}
                
//             //     streamSetupTime = Date.now(); 
//             //     isWarmupPhase = true;
//             //     currentStreamStartTime = Date.now();
//             // } 




// // =====================================
//             // --------------------------------------------------------------------
//             // ❌ SCENARIO C: BOTH TABS FAILED (Fresh Hunting Mode)
//             // --------------------------------------------------------------------
//             else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ BOTH TABS FAILED (Active & Backup Compromised)`);
//                 console.log(`[🔍] FRESH HUNTING MODE ACTIVATED: Spawning clean tabs...`);
//                 console.log(`==================================================`);
                
//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch (e) {}

//                 currentUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 activeUrlStr = urlList[currentUrlIndex].url;
                
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`[*] Hunting Active -> Server [${currentUrlIndex}]`);
//                 console.log(`[*] Hunting Backup -> Server [${backupUrlIndex}]`);

//                 console.log(`[*] Closing crashed tabs and spawning fresh ones...`);
//                 try { await activePage.close(); } catch(e) {}
//                 try { await backupPage.close(); } catch(e) {}

//                 activePage = await activeBrowser.newPage();
//                 backupPage = await backupBrowser.newPage();

//                 await setupNetworkAdBlocker(activePage);
//                 await setupNetworkAdBlocker(backupPage);
//                 attachAntiAdListeners(activePage);
//                 attachAntiAdListeners(backupPage);
//                 await applyPreloadFirewall(activePage);
//                 await applyPreloadFirewall(backupPage);

//                 try {
//                     await activePage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 });
//                     await showLoadingUI(activePage, "SEARCHING SERVER", "Hunting for a stable stream connection <span class='stream-blink'>...</span>");
//                     await initializeVideo(activePage, false, true); 
//                     await hideLoadingUI(activePage);
//                 } catch(e) {}

//                 try {
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                     await initializeVideo(backupPage, true, false); 
//                 } catch(e) {}

//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true; 
//                 currentStreamStartTime = Date.now();
//                 backupWarmupTime = Date.now();
//                 lastActiveTime = -1;
//                 frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false;

//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
                
//                 console.log(`[*] Fresh tabs active. Waiting 15 seconds (Warm-up) for video to stabilize...`);
//             }
//         } 
       
//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-blink-features=AutomationControlled',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion,NetworkServiceInProcess2',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }

//     console.log(`[*] Starting ACTIVE browser instance...`);
//     activeBrowser = await createBrowserInstance(browserArgs);
//     const activePages = await activeBrowser.pages();
//     activePage = activePages[0];

//     console.log(`[*] Starting BACKUP browser instance in background...`);
//     backupBrowser = await createBrowserInstance(browserArgs);
//     const backupPages = await backupBrowser.pages();
//     backupPage = backupPages[0];

//     activeBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//         }
//     });

//     backupBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//         }
//     });

//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex].url}`);
//     try {
//         await activePage.goto(urlList[currentUrlIndex].url, { waitUntil: 'domcontentloaded', timeout: 60000 });
//     } catch (e) {
//         console.log(`[⚠️] Network buffer safely handled for primary URL.`);
//     }
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex].url}`);
//     try {
//         await backupPage.goto(urlList[backupUrlIndex].url, { waitUntil: 'domcontentloaded', timeout: 60000 });
//     } catch (e) {}
    
//     await initializeVideo(backupPage, true, false);
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex].url}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex].url}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try {
//             await startDirectStreaming();
//         } catch (error) {
//             console.error('\n==================================================');
//             console.error('[🚨] FATAL ENGINE ERROR');
//             console.error(`[!] ${error.message}`);
//             console.error('==================================================');

//             if (activeBrowser && activeBrowser.isConnected()) {
//                 console.log('[🛡️] ACTIVE CHROME STILL ALIVE — NOT RESTARTING ENGINE.');
//                 await new Promise(resolve => setTimeout(resolve, 3000));
//                 continue;
//             }

//             if (backupBrowser && backupBrowser.isConnected()) {
//                 console.log('[🛡️] BACKUP CHROME STILL ALIVE — NOT RESTARTING ENGINE.');
//                 await new Promise(resolve => setTimeout(resolve, 3000));
//                 continue;
//             }

//             console.log('[⚠️] NO CHROME INSTANCE SURVIVED. Performing final recovery.');

//             try { if (activeBrowser) await activeBrowser.close().catch(() => {}); } catch (e) {}
//             try { if (backupBrowser) await backupBrowser.close().catch(() => {}); } catch (e) {}

//             activeBrowser = null; backupBrowser = null; activePage = null; backupPage = null;

//             await new Promise(resolve => setTimeout(resolve, 3000));
//             await cleanup();
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//     if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//     try {
//         execSync('pkill -9 obs || true', { stdio: 'ignore' });
//         execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//         execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//     } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://sport4u.online';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();



























// const puppeteer = require('puppeteer-extra');
// let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (DEBUGGING MODE)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     console.error('\n========================================');
//     console.error('[💥] UNCAUGHT EXCEPTION');
//     console.error(err);
//     console.error(err.stack);
//     console.error('========================================\n');
// });

// process.on('unhandledRejection', (reason) => {
//     console.error('\n========================================');
//     console.error('[💥] UNHANDLED REJECTION');
//     console.error(reason);
//     console.error('========================================\n');
// });

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', 
//     'sport4u.online',
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// // =========================================================================================
// // 🔄 DYNAMIC URL PARSER & METADATA EXTRACTOR
// // =========================================================================================
// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = [];

// if (rawUrls !== '') {
//     urlList = rawUrls.split(',').map(u => {
//         let trimmed = u.trim();
//         let hangThreshold = 8000; 
        
//         if (trimmed.startsWith('!')) {
//             hangThreshold = 20000; 
//             trimmed = trimmed.substring(1); 
//         }
//         if (!trimmed.startsWith('http')) trimmed = 'https://' + trimmed;
        
//         return { url: trimmed, hangTime: hangThreshold };
//     });
// } else {
//     urlList = [
//         { url: 'https://sport4u.online', hangTime: 8000 },
//         { url: 'https://dadocric.st/player.php?id=starsp3&v=m', hangTime: 8000 }
//     ];
// }

// function getSafeBackupIndex(activeIndex, currentIndex, list) {
//     if (list.length <= 1) return 0; 
//     let next = (currentIndex + 1) % list.length;
//     if (next === activeIndex) {
//         next = (next + 1) % list.length; 
//     }
//     return next;
// }

// let currentUrlIndex = 0;
// let backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let browserArgs = []; // Global Browser Args for Recovery Functions
// let activeBrowser = null;
// let backupBrowser = null;
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";
// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;

// async function createBrowserInstance(args) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: args
//     });
// }

// // =========================================================================================
// // 🛡️ SMART BROWSER RECOVERY
// // IMPORTANT: Browser failure NEVER restarts OBS / whole streaming engine.
// // It only repairs the failed browser side.
// // =========================================================================================

// async function preparePage(page) {
//     if (!page) return;
//     await setupNetworkAdBlocker(page);
//     attachAntiAdListeners(page);
//     await applyPreloadFirewall(page);
// }

// async function createFreshBackupBrowser() {
//     console.log('\n[🛠️] BACKUP RECOVERY: Creating fresh backup Chrome...');

//     try {
//         if (backupBrowser && backupBrowser.isConnected()) {
//             try {
//                 const pages = await backupBrowser.pages();
//                 for (const p of pages) {
//                     try { await p.close(); } catch (e) {}
//                 }
//             } catch (e) {}
//         }
//     } catch (e) {}

//     // Do NOT touch activeBrowser.
//     try {
//         if (backupBrowser && !backupBrowser.isConnected()) {
//             backupBrowser = null;
//         }
//     } catch (e) {
//         backupBrowser = null;
//     }

//     backupBrowser = await createBrowserInstance(browserArgs);

//     const pages = await backupBrowser.pages();
//     backupPage = pages[0];

//     await preparePage(backupPage);

//     backupBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             try {
//                 const newPage = await target.page();
//                 setTimeout(async () => {
//                     if (newPage && newPage !== backupPage) {
//                         try { await newPage.close(); } catch (e) {}
//                     }
//                 }, 500);
//             } catch (e) {}
//         }
//     });

//     console.log('[✅] BACKUP RECOVERY: Fresh backup Chrome created.');
//     return true;
// }

// async function createFreshActiveBrowser() {
//     console.log('\n[🛠️] ACTIVE RECOVERY: Creating fresh active Chrome...');

//     try {
//         if (activeBrowser && activeBrowser.isConnected()) {
//             try {
//                 const pages = await activeBrowser.pages();
//                 for (const p of pages) {
//                     try { await p.close(); } catch (e) {}
//                 }
//             } catch (e) {}
//         }
//     } catch (e) {}

//     try {
//         if (activeBrowser && !activeBrowser.isConnected()) {
//             activeBrowser = null;
//         }
//     } catch (e) {
//         activeBrowser = null;
//     }

//     activeBrowser = await createBrowserInstance(browserArgs);

//     const pages = await activeBrowser.pages();
//     activePage = pages[0];

//     await preparePage(activePage);

//     activeBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             try {
//                 const newPage = await target.page();
//                 setTimeout(async () => {
//                     if (newPage && newPage !== activePage) {
//                         try { await newPage.close(); } catch (e) {}
//                     }
//                 }, 500);
//             } catch (e) {}
//         }
//     });

//     console.log('[✅] ACTIVE RECOVERY: Fresh active Chrome created.');
//     return true;
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') {
//                     init.mode = 'open'; 
//                 }
//                 const shadowRoot = originalAttachShadow.call(this, init);
                
//                 const observer = new MutationObserver(() => {
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) {
//                         console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
//                         this.remove(); 
//                     }
//                 });
                
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };
            
//             Element.prototype.attachShadow.toString = function() { return "function attachShadow() { [native code] }"; };

//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]\nName=Untitled\n[Video]\nBaseCX=${RES_W}\nBaseCY=${RES_H}\nOutputCX=${RES_W}\nOutputCY=${RES_H}\nFPSCommon=30\n[Output]\nMode=Simple\n[SimpleOutput]\nVBitrate=${BITRATE}\nStreamEncoder=x264\nx264Preset=ultrafast\nx264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0\n`;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;
//                         if (area < 5000) return;
//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay):not([class*="player"]):not([class*="jw"]):not([class*="vjs"]), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         let playerWrap = realVideo.closest('.jwplayer, #player, .plyr, .vjs-player, .shaka-video-container, [data-player]') || realVideo;
                        
//                         playerWrap.style.setProperty('position', 'fixed', 'important');
//                         playerWrap.style.setProperty('top', '0px', 'important');
//                         playerWrap.style.setProperty('left', '0px', 'important');
//                         playerWrap.style.setProperty('width', '100vw', 'important');
//                         playerWrap.style.setProperty('height', '100vh', 'important');
//                         playerWrap.style.setProperty('z-index', '2147483646', 'important'); 
//                         playerWrap.style.setProperty('background-color', 'black', 'important');
//                         playerWrap.style.setProperty('opacity', '1', 'important');
//                         playerWrap.style.setProperty('visibility', 'visible', 'important');
//                         playerWrap.style.setProperty('display', 'block', 'important');
                        
//                         if (playerWrap !== realVideo) {
//                             realVideo.style.setProperty('width', '100%', 'important');
//                             realVideo.style.setProperty('height', '100%', 'important');
//                         }
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     if (!startMuted) {
//         await triggerSmartUnmute(page);
//         await new Promise(r => setTimeout(r, 1000));
//     }
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); 
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex].url;
//     let backupUrlStr = urlList[backupUrlIndex].url;

//     let currentStreamStartTime = Date.now();
//     let isRecoveryUIShown = false;

//     while (true) {
//         // =====================================================================================
//         // 🛡️ NEVER THROW JUST BECAUSE ONE CHROME DISCONNECTED
//         // Recover the affected side instead.
//         // =====================================================================================
//         const activeBrowserAlive = activeBrowser && activeBrowser.isConnected();
//         const backupBrowserAlive = backupBrowser && backupBrowser.isConnected();

//         // ---------------------------------------------------------------------
//         // CASE 1: ACTIVE CHROME DEAD, BACKUP CHROME STILL ALIVE
//         // Promote backup immediately.
//         // ---------------------------------------------------------------------
//         if (!activeBrowserAlive && backupBrowserAlive) {
//             console.log('\n==================================================');
//             console.log('[🚨] ACTIVE CHROME DISCONNECTED');
//             console.log('[🔄] BACKUP CHROME IS ALIVE');
//             console.log('[⚡] PROMOTING BACKUP -> ACTIVE');
//             console.log('[🛡️] OBS WILL CONTINUE RUNNING');
//             console.log('==================================================\n');

//             const oldActiveBrowser = activeBrowser;
//             const oldActivePage = activePage;
//             activeBrowser = backupBrowser;
//             activePage = backupPage;
//             backupBrowser = oldActiveBrowser;
//             backupPage = oldActivePage;

//             const oldActiveName = activeBrowserName;
//             activeBrowserName = backupBrowserName;
//             backupBrowserName = oldActiveName;

//             const previousActiveIndex = currentUrlIndex;
//             currentUrlIndex = backupUrlIndex;
//             activeUrlStr = urlList[currentUrlIndex].url;

//             backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
//             backupUrlStr = urlList[backupUrlIndex].url;

//             lastActiveTime = -1; lastDecodedFrames = -1; frozenCheckTimestamp = Date.now();
//             streamSetupTime = Date.now(); currentStreamStartTime = Date.now();
//             isWarmupPhase = true; backupWarmupTime = Date.now(); isRecoveryUIShown = false;

//             try { await activePage.bringToFront(); await hideLoadingUI(activePage); } catch (e) {}

//             console.log('[✅] BACKUP PROMOTED SUCCESSFULLY');
//             console.log(`[📺] NEW ACTIVE SERVER : [${currentUrlIndex}] -> ${activeUrlStr}`);
//             console.log(`[🔊] ACTIVE AUDIO      : ON`);
//             console.log(`[🛡️] NEW BACKUP SERVER : [${backupUrlIndex}] -> ${backupUrlStr}`);

//             try {
//                 await createFreshBackupBrowser();
//                 await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 await initializeVideo(backupPage, true, false);
//                 console.log('[✅] NEW BACKUP SERVER IS BUFFERING IN BACKGROUND');
//             } catch (e) {
//                 console.log(`[⚠️] Backup recreation failed. Watchdog will try the NEXT server.`);
//             }
//             continue;
//         }

//         // ---------------------------------------------------------------------
//         // CASE 2: BACKUP CHROME DEAD, ACTIVE CHROME STILL ALIVE
//         // NEVER touch active stream.
//         // ---------------------------------------------------------------------
//         if (activeBrowserAlive && !backupBrowserAlive) {
//             console.log('\n==================================================');
//             console.log('[⚠️] BACKUP CHROME DISCONNECTED');
//             console.log('[🛡️] ACTIVE STREAM WILL NOT BE TOUCHED');
//             console.log('[🔄] REBUILDING BACKUP WITH NEXT SERVER');
//             console.log('==================================================\n');

//             try {
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`[*] NEXT BACKUP SERVER -> [${backupUrlIndex}] ${backupUrlStr}`);
//                 await createFreshBackupBrowser();
//                 await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 await initializeVideo(backupPage, true, false);
//                 backupWarmupTime = Date.now();
//                 console.log(`[✅] BACKUP RECOVERED -> Server [${backupUrlIndex}]`);
//             } catch (e) {
//                 console.log(`[⚠️] Backup recovery failed. NEXT WATCHDOG CYCLE WILL TRY AGAIN.`);
//             }
//         }

//         // ---------------------------------------------------------------------
//         // CASE 3: BOTH CHROMES DEAD
//         // Try to create ACTIVE first. OBS stays alive.
//         // ---------------------------------------------------------------------
//         if (!activeBrowserAlive && !backupBrowserAlive) {
//             console.log('\n==================================================');
//             console.log('[🚨] BOTH CHROME INSTANCES DISCONNECTED');
//             console.log('[🛠️] LOCAL RECOVERY MODE');
//             console.log('[🛑] OBS WILL NOT BE RESTARTED');
//             console.log('==================================================\n');

//             try {
//                 currentUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 activeUrlStr = urlList[currentUrlIndex].url;

//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`[*] RECOVERY ACTIVE -> [${currentUrlIndex}] ${activeUrlStr}`);
//                 console.log(`[*] RECOVERY BACKUP -> [${backupUrlIndex}] ${backupUrlStr}`);

//                 await createFreshActiveBrowser();
//                 await activePage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 await showLoadingUI(activePage, "SEARCHING SERVER", "Finding a stable stream connection <span class='stream-blink'>...</span>");
//                 await initializeVideo(activePage, false, true);
//                 await hideLoadingUI(activePage);

//                 console.log('[✅] ACTIVE CHROME RECOVERED');

//                 try {
//                     await createFreshBackupBrowser();
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                     await initializeVideo(backupPage, true, false);
//                     console.log('[✅] BACKUP CHROME RECOVERED');
//                 } catch (backupError) {
//                     console.log('[⚠️] Backup failed. Active stream continues.');
//                 }

//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}

//                 streamSetupTime = Date.now(); currentStreamStartTime = Date.now(); backupWarmupTime = Date.now();
//                 frozenCheckTimestamp = Date.now(); lastActiveTime = -1; lastDecodedFrames = -1;
//                 isWarmupPhase = true; isRecoveryUIShown = false;

//                 console.log('[✅] LOCAL RECOVERY COMPLETE — WATCHDOG CONTINUES');
//             } catch (e) {
//                 console.log(`[❌] Local Chrome recovery failed: ${e.message}`);
//                 await new Promise(r => setTimeout(r, 3000));
//             }
//             continue;
//         }

//         let activeHangThresholdMs = urlList[currentUrlIndex].hangTime;
//         let activeStatus = await checkPageStatus(activePage);

//         // 🔄 1. BACKGROUND SHIELD: Independent rotation without touching Active Browser
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { 
//             let backupStatus = await checkPageStatus(backupPage);
            
//             if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
//                 console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
//                 backupWarmupTime = Date.now();
                
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     for (const frame of backupPage.frames()) {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch(e) {}
//             }
//         }

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > activeHangThresholdMs) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                     isRecoveryUIShown = false; 
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;

//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             let logBackupStatus = await checkPageStatus(backupPage);
//             let nextAfterBackupIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex].url;
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'} (Limit: ${activeHangThresholdMs/1000}s)`);
//             console.log(`[▶️] CURRENTLY LIVE              : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
//             console.log(`--------------------------------------------------`);
//             console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND       : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         // =========================================================================================
//         // 🔄 2. ACTIVE TAB HOT-SWAP SHIELD (UPDATED: INSTANT SEAMLESS PROMOTION)
//         // =========================================================================================
//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Initiating forward rotation to prevent stream drop...`);
//                 console.log(`==================================================`);
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             // --------------------------------------------------------------------
//             // ⚡ SCENARIO A: INSTANT SEAMLESS HOT-SWAP (BACKUP IS ALREADY HEALTHY)
//             // --------------------------------------------------------------------
//             if (backupStatus.status === 'HEALTHY' && !isProactiveRefresh) {
                
//                 console.log('\n==================================================');
//                 console.log('[⚡] BACKUP STREAM ALREADY HEALTHY');
//                 console.log('[⚡] INSTANT PROMOTION — NO RELOAD / NO REINITIALIZATION');
//                 console.log('==================================================');

//                 // 1. Sirf tab ko foreground mein layein
//                 try { await backupPage.bringToFront(); } catch (e) {}

//                 // 2. Objects swap karein INSTANTLY (Koi loading UI nahi, koi JS execution nahi)
//                 let brokenPage = activePage; 
//                 activePage = backupPage; 
//                 backupPage = brokenPage;

//                 let brokenBrowser = activeBrowser; 
//                 activeBrowser = backupBrowser; 
//                 backupBrowser = brokenBrowser;

//                 let brokenName = activeBrowserName;
//                 activeBrowserName = backupBrowserName;
//                 backupBrowserName = brokenName;

//                 let previousActiveIndex = currentUrlIndex;
//                 currentUrlIndex = backupUrlIndex;
//                 activeUrlStr = urlList[currentUrlIndex].url; 
                
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 // 3. Variables reset karein
//                 lastActiveTime = -1; 
//                 lastDecodedFrames = -1;
//                 frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; 

//                 // 4. CRITICAL FIX: Warmup false kyunke backup pehle se chal raha tha
//                 streamSetupTime = Date.now(); 
//                 currentStreamStartTime = Date.now();
//                 isWarmupPhase = false;

//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Seamlessly Promoted)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`==================================================\n`);

//                 // 5. Old broken tab ko silently background mein dobara build karein
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                     await initializeVideo(backupPage, true, false);
//                     backupWarmupTime = Date.now();
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
//             } 
//             // --------------------------------------------------------------------
//             // 🔄 SCENARIO B: PROACTIVE REFRESH OR FORCED RECONNECTION
//             // --------------------------------------------------------------------
//             else if (isProactiveRefresh || (backupStatus.status === 'HEALTHY' && isProactiveRefresh)) {
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }
                
//                 await showLoadingUI(backupPage, "REFRESHING CONNECTION", "Optimizing current server stream <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 let brokenPage = activePage; 
//                 activePage = backupPage; 
//                 backupPage = brokenPage;

//                 let brokenBrowser = activeBrowser; 
//                 activeBrowser = backupBrowser; 
//                 backupBrowser = brokenBrowser;

//                 let brokenName = activeBrowserName;
//                 activeBrowserName = backupBrowserName;
//                 backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; 

//                 let previousActiveIndex = currentUrlIndex;
//                 currentUrlIndex = backupUrlIndex;
//                 activeUrlStr = urlList[currentUrlIndex].url; 
                
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`\n==================================================`);
//                 console.log(`[🔄] PROACTIVE REFRESH EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                     await initializeVideo(backupPage, true, false);
//                 } catch (e) {}
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();
//             } 
//             // --------------------------------------------------------------------
//             // ❌ SCENARIO C: BOTH TABS FAILED (Fresh Hunting Mode)
//             // --------------------------------------------------------------------
//             else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ BOTH TABS FAILED (Active & Backup Compromised)`);
//                 console.log(`[🔍] FRESH HUNTING MODE ACTIVATED: Spawning clean tabs...`);
//                 console.log(`==================================================`);
                
//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch (e) {}

//                 currentUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 activeUrlStr = urlList[currentUrlIndex].url;
                
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`[*] Hunting Active -> Server [${currentUrlIndex}]`);
//                 console.log(`[*] Hunting Backup -> Server [${backupUrlIndex}]`);

//                 console.log(`[*] Closing crashed tabs and spawning fresh ones...`);
//                 try { await activePage.close(); } catch(e) {}
//                 try { await backupPage.close(); } catch(e) {}

//                 activePage = await activeBrowser.newPage();
//                 backupPage = await backupBrowser.newPage();

//                 await setupNetworkAdBlocker(activePage);
//                 await setupNetworkAdBlocker(backupPage);
//                 attachAntiAdListeners(activePage);
//                 attachAntiAdListeners(backupPage);
//                 await applyPreloadFirewall(activePage);
//                 await applyPreloadFirewall(backupPage);

//                 try {
//                     await activePage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 });
//                     await showLoadingUI(activePage, "SEARCHING SERVER", "Hunting for a stable stream connection <span class='stream-blink'>...</span>");
//                     await initializeVideo(activePage, false, true); 
//                     await hideLoadingUI(activePage);
//                 } catch(e) {}

//                 try {
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                     await initializeVideo(backupPage, true, false); 
//                 } catch(e) {}

//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true; 
//                 currentStreamStartTime = Date.now();
//                 backupWarmupTime = Date.now();
//                 lastActiveTime = -1;
//                 frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false;

//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
                
//                 console.log(`[*] Fresh tabs active. Waiting 15 seconds (Warm-up) for video to stabilize...`);
//             }
//         } // <-- YEH NAYA CODE YAHAN KHATAM HOTA HAI
       
//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-blink-features=AutomationControlled',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion,NetworkServiceInProcess2',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }

//     console.log(`[*] Starting ACTIVE browser instance...`);
//     activeBrowser = await createBrowserInstance(browserArgs);
//     const activePages = await activeBrowser.pages();
//     activePage = activePages[0];

//     console.log(`[*] Starting BACKUP browser instance in background...`);
//     backupBrowser = await createBrowserInstance(browserArgs);
//     const backupPages = await backupBrowser.pages();
//     backupPage = backupPages[0];

//     activeBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//         }
//     });

//     backupBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//         }
//     });

//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex].url}`);
//     try {
//         await activePage.goto(urlList[currentUrlIndex].url, { waitUntil: 'domcontentloaded', timeout: 60000 });
//     } catch (e) {
//         console.log(`[⚠️] Network buffer safely handled for primary URL.`);
//     }
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex].url}`);
//     try {
//         await backupPage.goto(urlList[backupUrlIndex].url, { waitUntil: 'domcontentloaded', timeout: 60000 });
//     } catch (e) {}
    
//     await initializeVideo(backupPage, true, false);
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex].url}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex].url}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try {
//             await startDirectStreaming();
//         } catch (error) {
//             console.error('\n==================================================');
//             console.error('[🚨] FATAL ENGINE ERROR');
//             console.error(`[!] ${error.message}`);
//             console.error('==================================================');

//             if (activeBrowser && activeBrowser.isConnected()) {
//                 console.log('[🛡️] ACTIVE CHROME STILL ALIVE — NOT RESTARTING ENGINE.');
//                 await new Promise(resolve => setTimeout(resolve, 3000));
//                 continue;
//             }

//             if (backupBrowser && backupBrowser.isConnected()) {
//                 console.log('[🛡️] BACKUP CHROME STILL ALIVE — NOT RESTARTING ENGINE.');
//                 await new Promise(resolve => setTimeout(resolve, 3000));
//                 continue;
//             }

//             console.log('[⚠️] NO CHROME INSTANCE SURVIVED. Performing final recovery.');

//             try { if (activeBrowser) await activeBrowser.close().catch(() => {}); } catch (e) {}
//             try { if (backupBrowser) await backupBrowser.close().catch(() => {}); } catch (e) {}

//             activeBrowser = null; backupBrowser = null; activePage = null; backupPage = null;

//             await new Promise(resolve => setTimeout(resolve, 3000));
//             await cleanup();
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//     if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//     try {
//         execSync('pkill -9 obs || true', { stdio: 'ignore' });
//         execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//         execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//     } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://sport4u.online';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();






























 // yeh code bey teek hai , lekin iss me eek or baat hai ismee unnecessary laoding huta hai jab active chrome unhealty hu ta hai lekin background correct huta isskee bawajood bey unnecassarruy loading




// const puppeteer = require('puppeteer-extra');
// let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (DEBUGGING MODE)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     console.error('\n========================================');
//     console.error('[💥] UNCAUGHT EXCEPTION');
//     console.error(err);
//     console.error(err.stack);
//     console.error('========================================\n');
// });

// process.on('unhandledRejection', (reason) => {
//     console.error('\n========================================');
//     console.error('[💥] UNHANDLED REJECTION');
//     console.error(reason);
//     console.error('========================================\n');
// });

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', 
//     'sport4u.online',
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// // =========================================================================================
// // 🔄 DYNAMIC URL PARSER & METADATA EXTRACTOR
// // =========================================================================================
// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = [];

// if (rawUrls !== '') {
//     urlList = rawUrls.split(',').map(u => {
//         let trimmed = u.trim();
//         let hangThreshold = 8000; 
        
//         if (trimmed.startsWith('!')) {
//             hangThreshold = 20000; 
//             trimmed = trimmed.substring(1); 
//         }
//         if (!trimmed.startsWith('http')) trimmed = 'https://' + trimmed;
        
//         return { url: trimmed, hangTime: hangThreshold };
//     });
// } else {
//     urlList = [
//         { url: 'https://sport4u.online', hangTime: 8000 },
//         { url: 'https://dadocric.st/player.php?id=starsp3&v=m', hangTime: 8000 }
//     ];
// }

// function getSafeBackupIndex(activeIndex, currentIndex, list) {
//     if (list.length <= 1) return 0; 
//     let next = (currentIndex + 1) % list.length;
//     if (next === activeIndex) {
//         next = (next + 1) % list.length; 
//     }
//     return next;
// }

// let currentUrlIndex = 0;
// let backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let browserArgs = []; // Global Browser Args for Recovery Functions
// let activeBrowser = null;
// let backupBrowser = null;
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";
// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;

// async function createBrowserInstance(args) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: args
//     });
// }

// // =========================================================================================
// // 🛡️ SMART BROWSER RECOVERY
// // IMPORTANT: Browser failure NEVER restarts OBS / whole streaming engine.
// // It only repairs the failed browser side.
// // =========================================================================================

// async function preparePage(page) {
//     if (!page) return;
//     await setupNetworkAdBlocker(page);
//     attachAntiAdListeners(page);
//     await applyPreloadFirewall(page);
// }

// async function createFreshBackupBrowser() {
//     console.log('\n[🛠️] BACKUP RECOVERY: Creating fresh backup Chrome...');

//     try {
//         if (backupBrowser && backupBrowser.isConnected()) {
//             try {
//                 const pages = await backupBrowser.pages();
//                 for (const p of pages) {
//                     try { await p.close(); } catch (e) {}
//                 }
//             } catch (e) {}
//         }
//     } catch (e) {}

//     // Do NOT touch activeBrowser.
//     try {
//         if (backupBrowser && !backupBrowser.isConnected()) {
//             backupBrowser = null;
//         }
//     } catch (e) {
//         backupBrowser = null;
//     }

//     backupBrowser = await createBrowserInstance(browserArgs);

//     const pages = await backupBrowser.pages();
//     backupPage = pages[0];

//     await preparePage(backupPage);

//     backupBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             try {
//                 const newPage = await target.page();
//                 setTimeout(async () => {
//                     if (newPage && newPage !== backupPage) {
//                         try { await newPage.close(); } catch (e) {}
//                     }
//                 }, 500);
//             } catch (e) {}
//         }
//     });

//     console.log('[✅] BACKUP RECOVERY: Fresh backup Chrome created.');
//     return true;
// }

// async function createFreshActiveBrowser() {
//     console.log('\n[🛠️] ACTIVE RECOVERY: Creating fresh active Chrome...');

//     try {
//         if (activeBrowser && activeBrowser.isConnected()) {
//             try {
//                 const pages = await activeBrowser.pages();
//                 for (const p of pages) {
//                     try { await p.close(); } catch (e) {}
//                 }
//             } catch (e) {}
//         }
//     } catch (e) {}

//     try {
//         if (activeBrowser && !activeBrowser.isConnected()) {
//             activeBrowser = null;
//         }
//     } catch (e) {
//         activeBrowser = null;
//     }

//     activeBrowser = await createBrowserInstance(browserArgs);

//     const pages = await activeBrowser.pages();
//     activePage = pages[0];

//     await preparePage(activePage);

//     activeBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             try {
//                 const newPage = await target.page();
//                 setTimeout(async () => {
//                     if (newPage && newPage !== activePage) {
//                         try { await newPage.close(); } catch (e) {}
//                     }
//                 }, 500);
//             } catch (e) {}
//         }
//     });

//     console.log('[✅] ACTIVE RECOVERY: Fresh active Chrome created.');
//     return true;
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') {
//                     init.mode = 'open'; 
//                 }
//                 const shadowRoot = originalAttachShadow.call(this, init);
                
//                 const observer = new MutationObserver(() => {
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) {
//                         console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
//                         this.remove(); 
//                     }
//                 });
                
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };
            
//             Element.prototype.attachShadow.toString = function() { return "function attachShadow() { [native code] }"; };

//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]\nName=Untitled\n[Video]\nBaseCX=${RES_W}\nBaseCY=${RES_H}\nOutputCX=${RES_W}\nOutputCY=${RES_H}\nFPSCommon=30\n[Output]\nMode=Simple\n[SimpleOutput]\nVBitrate=${BITRATE}\nStreamEncoder=x264\nx264Preset=ultrafast\nx264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0\n`;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;
//                         if (area < 5000) return;
//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay):not([class*="player"]):not([class*="jw"]):not([class*="vjs"]), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         let playerWrap = realVideo.closest('.jwplayer, #player, .plyr, .vjs-player, .shaka-video-container, [data-player]') || realVideo;
                        
//                         playerWrap.style.setProperty('position', 'fixed', 'important');
//                         playerWrap.style.setProperty('top', '0px', 'important');
//                         playerWrap.style.setProperty('left', '0px', 'important');
//                         playerWrap.style.setProperty('width', '100vw', 'important');
//                         playerWrap.style.setProperty('height', '100vh', 'important');
//                         playerWrap.style.setProperty('z-index', '2147483646', 'important'); 
//                         playerWrap.style.setProperty('background-color', 'black', 'important');
//                         playerWrap.style.setProperty('opacity', '1', 'important');
//                         playerWrap.style.setProperty('visibility', 'visible', 'important');
//                         playerWrap.style.setProperty('display', 'block', 'important');
                        
//                         if (playerWrap !== realVideo) {
//                             realVideo.style.setProperty('width', '100%', 'important');
//                             realVideo.style.setProperty('height', '100%', 'important');
//                         }
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     if (!startMuted) {
//         await triggerSmartUnmute(page);
//         await new Promise(r => setTimeout(r, 1000));
//     }
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); 
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex].url;
//     let backupUrlStr = urlList[backupUrlIndex].url;

//     let currentStreamStartTime = Date.now();
//     let isRecoveryUIShown = false;

//     while (true) {
//         // =====================================================================================
//         // 🛡️ NEVER THROW JUST BECAUSE ONE CHROME DISCONNECTED
//         // Recover the affected side instead.
//         // =====================================================================================
//         const activeBrowserAlive = activeBrowser && activeBrowser.isConnected();
//         const backupBrowserAlive = backupBrowser && backupBrowser.isConnected();

//         // ---------------------------------------------------------------------
//         // CASE 1: ACTIVE CHROME DEAD, BACKUP CHROME STILL ALIVE
//         // Promote backup immediately.
//         // ---------------------------------------------------------------------
//         if (!activeBrowserAlive && backupBrowserAlive) {
//             console.log('\n==================================================');
//             console.log('[🚨] ACTIVE CHROME DISCONNECTED');
//             console.log('[🔄] BACKUP CHROME IS ALIVE');
//             console.log('[⚡] PROMOTING BACKUP -> ACTIVE');
//             console.log('[🛡️] OBS WILL CONTINUE RUNNING');
//             console.log('==================================================\n');

//             const oldActiveBrowser = activeBrowser;
//             const oldActivePage = activePage;
//             activeBrowser = backupBrowser;
//             activePage = backupPage;
//             backupBrowser = oldActiveBrowser;
//             backupPage = oldActivePage;

//             const oldActiveName = activeBrowserName;
//             activeBrowserName = backupBrowserName;
//             backupBrowserName = oldActiveName;

//             const previousActiveIndex = currentUrlIndex;
//             currentUrlIndex = backupUrlIndex;
//             activeUrlStr = urlList[currentUrlIndex].url;

//             backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
//             backupUrlStr = urlList[backupUrlIndex].url;

//             lastActiveTime = -1; lastDecodedFrames = -1; frozenCheckTimestamp = Date.now();
//             streamSetupTime = Date.now(); currentStreamStartTime = Date.now();
//             isWarmupPhase = true; backupWarmupTime = Date.now(); isRecoveryUIShown = false;

//             try { await activePage.bringToFront(); await hideLoadingUI(activePage); } catch (e) {}

//             console.log('[✅] BACKUP PROMOTED SUCCESSFULLY');
//             console.log(`[📺] NEW ACTIVE SERVER : [${currentUrlIndex}] -> ${activeUrlStr}`);
//             console.log(`[🔊] ACTIVE AUDIO      : ON`);
//             console.log(`[🛡️] NEW BACKUP SERVER : [${backupUrlIndex}] -> ${backupUrlStr}`);

//             try {
//                 await createFreshBackupBrowser();
//                 await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 await initializeVideo(backupPage, true, false);
//                 console.log('[✅] NEW BACKUP SERVER IS BUFFERING IN BACKGROUND');
//             } catch (e) {
//                 console.log(`[⚠️] Backup recreation failed. Watchdog will try the NEXT server.`);
//             }
//             continue;
//         }

//         // ---------------------------------------------------------------------
//         // CASE 2: BACKUP CHROME DEAD, ACTIVE CHROME STILL ALIVE
//         // NEVER touch active stream.
//         // ---------------------------------------------------------------------
//         if (activeBrowserAlive && !backupBrowserAlive) {
//             console.log('\n==================================================');
//             console.log('[⚠️] BACKUP CHROME DISCONNECTED');
//             console.log('[🛡️] ACTIVE STREAM WILL NOT BE TOUCHED');
//             console.log('[🔄] REBUILDING BACKUP WITH NEXT SERVER');
//             console.log('==================================================\n');

//             try {
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`[*] NEXT BACKUP SERVER -> [${backupUrlIndex}] ${backupUrlStr}`);
//                 await createFreshBackupBrowser();
//                 await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 await initializeVideo(backupPage, true, false);
//                 backupWarmupTime = Date.now();
//                 console.log(`[✅] BACKUP RECOVERED -> Server [${backupUrlIndex}]`);
//             } catch (e) {
//                 console.log(`[⚠️] Backup recovery failed. NEXT WATCHDOG CYCLE WILL TRY AGAIN.`);
//             }
//         }

//         // ---------------------------------------------------------------------
//         // CASE 3: BOTH CHROMES DEAD
//         // Try to create ACTIVE first. OBS stays alive.
//         // ---------------------------------------------------------------------
//         if (!activeBrowserAlive && !backupBrowserAlive) {
//             console.log('\n==================================================');
//             console.log('[🚨] BOTH CHROME INSTANCES DISCONNECTED');
//             console.log('[🛠️] LOCAL RECOVERY MODE');
//             console.log('[🛑] OBS WILL NOT BE RESTARTED');
//             console.log('==================================================\n');

//             try {
//                 currentUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 activeUrlStr = urlList[currentUrlIndex].url;

//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`[*] RECOVERY ACTIVE -> [${currentUrlIndex}] ${activeUrlStr}`);
//                 console.log(`[*] RECOVERY BACKUP -> [${backupUrlIndex}] ${backupUrlStr}`);

//                 await createFreshActiveBrowser();
//                 await activePage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 await showLoadingUI(activePage, "SEARCHING SERVER", "Finding a stable stream connection <span class='stream-blink'>...</span>");
//                 await initializeVideo(activePage, false, true);
//                 await hideLoadingUI(activePage);

//                 console.log('[✅] ACTIVE CHROME RECOVERED');

//                 try {
//                     await createFreshBackupBrowser();
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                     await initializeVideo(backupPage, true, false);
//                     console.log('[✅] BACKUP CHROME RECOVERED');
//                 } catch (backupError) {
//                     console.log('[⚠️] Backup failed. Active stream continues.');
//                 }

//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}

//                 streamSetupTime = Date.now(); currentStreamStartTime = Date.now(); backupWarmupTime = Date.now();
//                 frozenCheckTimestamp = Date.now(); lastActiveTime = -1; lastDecodedFrames = -1;
//                 isWarmupPhase = true; isRecoveryUIShown = false;

//                 console.log('[✅] LOCAL RECOVERY COMPLETE — WATCHDOG CONTINUES');
//             } catch (e) {
//                 console.log(`[❌] Local Chrome recovery failed: ${e.message}`);
//                 await new Promise(r => setTimeout(r, 3000));
//             }
//             continue;
//         }

//         let activeHangThresholdMs = urlList[currentUrlIndex].hangTime;
//         let activeStatus = await checkPageStatus(activePage);

//         // 🔄 1. BACKGROUND SHIELD: Independent rotation without touching Active Browser
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { 
//             let backupStatus = await checkPageStatus(backupPage);
            
//             if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
//                 console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
//                 backupWarmupTime = Date.now();
                
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     for (const frame of backupPage.frames()) {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch(e) {}
//             }
//         }

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > activeHangThresholdMs) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                     isRecoveryUIShown = false; 
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;

//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             let logBackupStatus = await checkPageStatus(backupPage);
//             let nextAfterBackupIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex].url;
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'} (Limit: ${activeHangThresholdMs/1000}s)`);
//             console.log(`[▶️] CURRENTLY LIVE              : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
//             console.log(`--------------------------------------------------`);
//             console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND       : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         // 🔄 2. ACTIVE TAB HOT-SWAP SHIELD
//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Initiating forward rotation to prevent stream drop...`);
//                 console.log(`==================================================`);
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || isProactiveRefresh) {
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 let brokenPage = activePage; 
//                 activePage = backupPage; 
//                 backupPage = brokenPage;

//                 let brokenBrowser = activeBrowser; 
//                 activeBrowser = backupBrowser; 
//                 backupBrowser = brokenBrowser;

//                 let brokenName = activeBrowserName;
//                 activeBrowserName = backupBrowserName;
//                 backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; 

//                 let previousActiveIndex = currentUrlIndex;
//                 currentUrlIndex = backupUrlIndex;
//                 activeUrlStr = urlList[currentUrlIndex].url; 
                
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     await initializeVideo(backupPage, true, false);
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ BOTH TABS FAILED (Active & Backup Compromised)`);
//                 console.log(`[🔍] FRESH HUNTING MODE ACTIVATED: Spawning clean tabs...`);
//                 console.log(`==================================================`);
                
//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch (e) {}

//                 currentUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 activeUrlStr = urlList[currentUrlIndex].url;
                
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`[*] Hunting Active -> Server [${currentUrlIndex}]`);
//                 console.log(`[*] Hunting Backup -> Server [${backupUrlIndex}]`);

//                 console.log(`[*] Closing crashed tabs and spawning fresh ones...`);
//                 try { await activePage.close(); } catch(e) {}
//                 try { await backupPage.close(); } catch(e) {}

//                 activePage = await activeBrowser.newPage();
//                 backupPage = await backupBrowser.newPage();

//                 await setupNetworkAdBlocker(activePage);
//                 await setupNetworkAdBlocker(backupPage);
//                 attachAntiAdListeners(activePage);
//                 attachAntiAdListeners(backupPage);
//                 await applyPreloadFirewall(activePage);
//                 await applyPreloadFirewall(backupPage);

//                 try {
//                     await activePage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 });
//                     await showLoadingUI(activePage, "SEARCHING SERVER", "Hunting for a stable stream connection <span class='stream-blink'>...</span>");
//                     await initializeVideo(activePage, false, true); 
//                     await hideLoadingUI(activePage);
//                 } catch(e) {}

//                 try {
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                     await initializeVideo(backupPage, true, false); 
//                 } catch(e) {}

//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true; 
//                 currentStreamStartTime = Date.now();
//                 backupWarmupTime = Date.now();
//                 lastActiveTime = -1;
//                 frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false;

//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
                
//                 console.log(`[*] Fresh tabs active. Waiting 15 seconds (Warm-up) for video to stabilize...`);
//             }
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-blink-features=AutomationControlled',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion,NetworkServiceInProcess2',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }

//     console.log(`[*] Starting ACTIVE browser instance...`);
//     activeBrowser = await createBrowserInstance(browserArgs);
//     const activePages = await activeBrowser.pages();
//     activePage = activePages[0];

//     console.log(`[*] Starting BACKUP browser instance in background...`);
//     backupBrowser = await createBrowserInstance(browserArgs);
//     const backupPages = await backupBrowser.pages();
//     backupPage = backupPages[0];

//     activeBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//         }
//     });

//     backupBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//         }
//     });

//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex].url}`);
//     try {
//         await activePage.goto(urlList[currentUrlIndex].url, { waitUntil: 'domcontentloaded', timeout: 60000 });
//     } catch (e) {
//         console.log(`[⚠️] Network buffer safely handled for primary URL.`);
//     }
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex].url}`);
//     try {
//         await backupPage.goto(urlList[backupUrlIndex].url, { waitUntil: 'domcontentloaded', timeout: 60000 });
//     } catch (e) {}
    
//     await initializeVideo(backupPage, true, false);
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex].url}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex].url}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try {
//             await startDirectStreaming();
//         } catch (error) {
//             console.error('\n==================================================');
//             console.error('[🚨] FATAL ENGINE ERROR');
//             console.error(`[!] ${error.message}`);
//             console.error('==================================================');

//             if (activeBrowser && activeBrowser.isConnected()) {
//                 console.log('[🛡️] ACTIVE CHROME STILL ALIVE — NOT RESTARTING ENGINE.');
//                 await new Promise(resolve => setTimeout(resolve, 3000));
//                 continue;
//             }

//             if (backupBrowser && backupBrowser.isConnected()) {
//                 console.log('[🛡️] BACKUP CHROME STILL ALIVE — NOT RESTARTING ENGINE.');
//                 await new Promise(resolve => setTimeout(resolve, 3000));
//                 continue;
//             }

//             console.log('[⚠️] NO CHROME INSTANCE SURVIVED. Performing final recovery.');

//             try { if (activeBrowser) await activeBrowser.close().catch(() => {}); } catch (e) {}
//             try { if (backupBrowser) await backupBrowser.close().catch(() => {}); } catch (e) {}

//             activeBrowser = null; backupBrowser = null; activePage = null; backupPage = null;

//             await new Promise(resolve => setTimeout(resolve, 3000));
//             await cleanup();
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//     if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//     try {
//         execSync('pkill -9 obs || true', { stdio: 'ignore' });
//         execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//         execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//     } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://sport4u.online';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();





















// yeh teek hai dai khaisar sra bas eek eek issue hai, jab chrome unhelaty huta hai tuu sab kuch destroy karkey pher see obs start ok.ru connection isko fix karna hai 





// const puppeteer = require('puppeteer-extra');
// let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', 
//     'sport4u.online',
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// // =========================================================================================
// // 🔄 DYNAMIC URL PARSER & METADATA EXTRACTOR (NEW UPDATE)
// // =========================================================================================
// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = [];

// if (rawUrls !== '') {
//     urlList = rawUrls.split(',').map(u => {
//         let trimmed = u.trim();
//         let hangThreshold = 8000; // Default 8 Seconds limit
        
//         // Agar link ke shuru mein "!" hai, toh 20 sec delay tolerate karega
//         if (trimmed.startsWith('!')) {
//             hangThreshold = 20000; // 20 Seconds Extended Tolerance
//             trimmed = trimmed.substring(1); 
//         }
//         if (!trimmed.startsWith('http')) trimmed = 'https://' + trimmed;
        
//         return { url: trimmed, hangTime: hangThreshold };
//     });
// } else {
//     urlList = [
//         { url: 'https://sport4u.online', hangTime: 8000 },
//         { url: 'https://dadocric.st/player.php?id=starsp3&v=m', hangTime: 8000 }
//     ];
// }

// // Helper: Collision-Free Backup Index Generator (NEW UPDATE)
// function getSafeBackupIndex(activeIndex, currentIndex, list) {
//     if (list.length <= 1) return 0; 
//     let next = (currentIndex + 1) % list.length;
//     if (next === activeIndex) {
//         next = (next + 1) % list.length; // Active ko bypass karo
//     }
//     return next;
// }

// let currentUrlIndex = 0;
// let backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let activeBrowser = null;
// let backupBrowser = null;

// // Name tags for tracking
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";

// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;

// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // =========================================================
//             // 🛑 ULTIMATE SHADOW DOM AD KILLER (Monkey Patching 2026 Stealth)
//             // =========================================================
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') {
//                     init.mode = 'open'; // Force open to allow inspection
//                 }
//                 const shadowRoot = originalAttachShadow.call(this, init);
                
//                 const observer = new MutationObserver(() => {
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) {
//                         console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
//                         this.remove(); 
//                     }
//                 });
                
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };
            
//             // 🛡️ 2026 STEALTH: Mask the override so site JS thinks it is untouched native code
//             Element.prototype.attachShadow.toString = function() { return "function attachShadow() { [native code] }"; };

//             // Standard Protections
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]\nName=Untitled\n[Video]\nBaseCX=${RES_W}\nBaseCY=${RES_H}\nOutputCX=${RES_W}\nOutputCY=${RES_H}\nFPSCommon=30\n[Output]\nMode=Simple\n[SimpleOutput]\nVBitrate=${BITRATE}\nStreamEncoder=x264\nx264Preset=ultrafast\nx264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0\n`;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;
//                         if (area < 5000) return;
//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay):not([class*="player"]):not([class*="jw"]):not([class*="vjs"]), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         let playerWrap = realVideo.closest('.jwplayer, #player, .plyr, .vjs-player, .shaka-video-container, [data-player]') || realVideo;
                        
//                         playerWrap.style.setProperty('position', 'fixed', 'important');
//                         playerWrap.style.setProperty('top', '0px', 'important');
//                         playerWrap.style.setProperty('left', '0px', 'important');
//                         playerWrap.style.setProperty('width', '100vw', 'important');
//                         playerWrap.style.setProperty('height', '100vh', 'important');
//                         playerWrap.style.setProperty('z-index', '2147483646', 'important'); 
//                         playerWrap.style.setProperty('background-color', 'black', 'important');
//                         playerWrap.style.setProperty('opacity', '1', 'important');
//                         playerWrap.style.setProperty('visibility', 'visible', 'important');
//                         playerWrap.style.setProperty('display', 'block', 'important');
                        
//                         if (playerWrap !== realVideo) {
//                             realVideo.style.setProperty('width', '100%', 'important');
//                             realVideo.style.setProperty('height', '100%', 'important');
//                         }
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     if (!startMuted) {
//         await triggerSmartUnmute(page);
//         await new Promise(r => setTimeout(r, 1000));
//     }
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); 
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex].url;
//     let backupUrlStr = urlList[backupUrlIndex].url;

//     let currentStreamStartTime = Date.now();
//     let isRecoveryUIShown = false;

//     while (true) {
//         if (!activeBrowser || !activeBrowser.isConnected() || !backupBrowser || !backupBrowser.isConnected()) {
//             throw new Error("One or both browser instances disconnected.");
//         }

//         let activeHangThresholdMs = urlList[currentUrlIndex].hangTime;
//         let activeStatus = await checkPageStatus(activePage);

//         // 🔄 1. BACKGROUND SHIELD: Independent rotation without touching Active Browser
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { 
//             let backupStatus = await checkPageStatus(backupPage);
            
//             if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
//                 console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
//                 // Active link ko bypass karke naya safe index dhundna
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
//                 backupWarmupTime = Date.now();
                
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     for (const frame of backupPage.frames()) {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch(e) {}
//             }
//         }

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 // 🔥 Dynamic limit (8s ya 20s) use hogi yahan
//                 if (Date.now() - frozenCheckTimestamp > activeHangThresholdMs) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                     isRecoveryUIShown = false; 
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;

//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             let logBackupStatus = await checkPageStatus(backupPage);
            
//             // Queue report collision bypass karke
//             let nextAfterBackupIndex = getSafeBackupIndex(currentUrlIndex, backupUrlIndex, urlList);
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex].url;
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'} (Limit: ${activeHangThresholdMs/1000}s)`);
//             console.log(`[▶️] CURRENTLY LIVE              : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
//             console.log(`--------------------------------------------------`);
//             console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND       : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         // 🔄 2. ACTIVE TAB HOT-SWAP SHIELD
//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Initiating forward rotation to prevent stream drop...`);
//                 console.log(`==================================================`);
//                 // No need to inject active link into backup anymore. Forward rotation will handle it.
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || isProactiveRefresh) {
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 let brokenPage = activePage; 
//                 activePage = backupPage; 
//                 backupPage = brokenPage;

//                 let brokenBrowser = activeBrowser; 
//                 activeBrowser = backupBrowser; 
//                 backupBrowser = brokenBrowser;

//                 let brokenName = activeBrowserName;
//                 activeBrowserName = backupBrowserName;
//                 backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; 

//                 // Rotate index avoiding collision
//                 let previousActiveIndex = currentUrlIndex;
//                 currentUrlIndex = backupUrlIndex;
//                 activeUrlStr = urlList[currentUrlIndex].url; 
                
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, previousActiveIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     await initializeVideo(backupPage, true, false);
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ BOTH TABS FAILED (Active & Backup Compromised)`);
//                 console.log(`[🔍] FRESH HUNTING MODE ACTIVATED: Spawning clean tabs...`);
//                 console.log(`==================================================`);
                
//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch (e) {}

//                 currentUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 activeUrlStr = urlList[currentUrlIndex].url;
                
//                 backupUrlIndex = getSafeBackupIndex(currentUrlIndex, currentUrlIndex, urlList);
//                 backupUrlStr = urlList[backupUrlIndex].url;

//                 console.log(`[*] Hunting Active -> Server [${currentUrlIndex}]`);
//                 console.log(`[*] Hunting Backup -> Server [${backupUrlIndex}]`);

//                 console.log(`[*] Closing crashed tabs and spawning fresh ones...`);
//                 try { await activePage.close(); } catch(e) {}
//                 try { await backupPage.close(); } catch(e) {}

//                 activePage = await activeBrowser.newPage();
//                 backupPage = await backupBrowser.newPage();

//                 await setupNetworkAdBlocker(activePage);
//                 await setupNetworkAdBlocker(backupPage);
//                 attachAntiAdListeners(activePage);
//                 attachAntiAdListeners(backupPage);
//                 await applyPreloadFirewall(activePage);
//                 await applyPreloadFirewall(backupPage);

//                 try {
//                     await activePage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 });
//                     await showLoadingUI(activePage, "SEARCHING SERVER", "Hunting for a stable stream connection <span class='stream-blink'>...</span>");
//                     await initializeVideo(activePage, false, true); 
//                     await hideLoadingUI(activePage);
//                 } catch(e) {}

//                 try {
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                     await initializeVideo(backupPage, true, false); 
//                 } catch(e) {}

//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true; 
//                 currentStreamStartTime = Date.now();
//                 backupWarmupTime = Date.now();
//                 lastActiveTime = -1;
//                 frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false;

//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
                
//                 console.log(`[*] Fresh tabs active. Waiting 15 seconds (Warm-up) for video to stabilize...`);
//             }
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     // 🛡️ 2026 ANTI-BOT ENGINE FLAGS UPDATED HERE
//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         // --- 2026 Stealth & Fixes Layer ---
//         '--disable-blink-features=AutomationControlled',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion,NetworkServiceInProcess2',
//         // ----------------------------------
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }

//     console.log(`[*] Starting ACTIVE browser instance...`);
//     activeBrowser = await createBrowserInstance(browserArgs);
//     const activePages = await activeBrowser.pages();
//     activePage = activePages[0];

//     console.log(`[*] Starting BACKUP browser instance in background...`);
//     backupBrowser = await createBrowserInstance(browserArgs);
//     const backupPages = await backupBrowser.pages();
//     backupPage = backupPages[0];

//     activeBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//         }
//     });

//     backupBrowser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//         }
//     });

//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex].url}`);
//     try {
//         await activePage.goto(urlList[currentUrlIndex].url, { waitUntil: 'domcontentloaded', timeout: 60000 });
//     } catch (e) {
//         console.log(`[⚠️] Network buffer safely handled for primary URL.`);
//     }
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex].url}`);
//     try {
//         await backupPage.goto(urlList[backupUrlIndex].url, { waitUntil: 'domcontentloaded', timeout: 60000 });
//     } catch (e) {}
    
//     await initializeVideo(backupPage, true, false);
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex].url}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex].url}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//     if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//     try {
//         execSync('pkill -9 obs || true', { stdio: 'ignore' });
//         execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//         execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//     } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://sport4u.online';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();













// <<<<<<<<<<<------------ Alllah Thallah khaa laraaa Ameen ---------------->>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>


// const puppeteer = require('puppeteer-extra');let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let activeBrowser = null;
// let backupBrowser = null;

// // Name tags for tracking
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";

// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;




// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // =========================================================
//             // 🛑 ULTIMATE SHADOW DOM AD KILLER (Monkey Patching)
//             // =========================================================
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') {
//                     init.mode = 'open'; // Force open to allow inspection
//                 }
//                 const shadowRoot = originalAttachShadow.call(this, init);
                
//                 const observer = new MutationObserver(() => {
//                     // UPDATED: Hardened selector string catching wildcard IDs and payload sources
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) {
//                         console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
//                         this.remove(); // Kill the entire host element
//                     }
//                 });
                
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };

//             // Standard Protections
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             // UPDATED: Added fallback display:none for the mutated shadow tags
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // ⚠️ NEW: INSTANT HANG / RECOVERY SHIELD UI (Unique Radar Design)
// // =========================================================================================
// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     // const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay):not([class*="player"]):not([class*="jw"]):not([class*="vjs"]), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     // if (realVideo) { 
//                     //     realVideo.style.setProperty('position', 'fixed', 'important');
//                     //     realVideo.style.setProperty('top', '0px', 'important');
//                     //     realVideo.style.setProperty('left', '0px', 'important');
//                     //     realVideo.style.setProperty('width', '100vw', 'important');
//                     //     realVideo.style.setProperty('height', '100vh', 'important');
//                     //     realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                     //     realVideo.style.setProperty('background-color', 'black', 'important');
//                     //     realVideo.style.setProperty('object-fit', 'contain', 'important');
//                     //     realVideo.style.setProperty('opacity', '1', 'important');
//                     //     realVideo.style.setProperty('visibility', 'visible', 'important');
//                     //     realVideo.style.setProperty('display', 'block', 'important');
//                     // }
//                     if (realVideo) { 
//                         // 🛡️ FIX FOR STREAMED.PK: Target the wrapper instead of breaking the video tag out
//                         let playerWrap = realVideo.closest('.jwplayer, #player, .plyr, .vjs-player, .shaka-video-container, [data-player]') || realVideo;
                        
//                         playerWrap.style.setProperty('position', 'fixed', 'important');
//                         playerWrap.style.setProperty('top', '0px', 'important');
//                         playerWrap.style.setProperty('left', '0px', 'important');
//                         playerWrap.style.setProperty('width', '100vw', 'important');
//                         playerWrap.style.setProperty('height', '100vh', 'important');
//                         playerWrap.style.setProperty('z-index', '2147483646', 'important'); 
//                         playerWrap.style.setProperty('background-color', 'black', 'important');
//                         playerWrap.style.setProperty('opacity', '1', 'important');
//                         playerWrap.style.setProperty('visibility', 'visible', 'important');
//                         playerWrap.style.setProperty('display', 'block', 'important');
                        
//                         // Agar wrap mil gaya hai, toh andar wali video ko wrapper ke hisaab se fit karein
//                         if (playerWrap !== realVideo) {
//                             realVideo.style.setProperty('width', '100%', 'important');
//                             realVideo.style.setProperty('height', '100%', 'important');
//                         }
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here (ONLY FOR ACTIVE TAB)
//     if (!startMuted) {
//         await triggerSmartUnmute(page);
//         await new Promise(r => setTimeout(r, 1000));
//     }
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); // 👈 YEH NAYI LINE ADD KAREIN
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();
    
//     // 🔥 NEW: Track recovery UI state
//     let isRecoveryUIShown = false;

//     while (true) {
//         if (!activeBrowser || !activeBrowser.isConnected() || !backupBrowser || !backupBrowser.isConnected()) {
//             throw new Error("One or both browser instances disconnected.");
//         }

//         let activeStatus = await checkPageStatus(activePage);

// // 👇=== UPDATED BACKGROUND SHIELD CODE ===👇
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { // 👈 30 seconds ka cooldown add kiya
//             let backupStatus = await checkPageStatus(backupPage);
            
//             // Agar backup background mein hang hai ya error de raha hai
//             if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
//                 console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
//                 // Next link par jump karein
//                 backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
                
//                 backupWarmupTime = Date.now(); // 👈 COOLDOWN RESET (Taake agli dafa 30 sec wait kare)
                
//                 // Backup browser ko silently naye link par bhej dein
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     // Backup ko dobara mute frame state mein set karein
//                     for (const frame of backupPage.frames()) {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch(e) {}
//             }
//         }
//         // 👆=== UPDATED CODE END ===👆
        

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 // 🔥 NEW: INSTANT HANG RECOVERY SHIELD TRIGGER (0 Seconds Delay)
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                     isRecoveryUIShown = false; // Reset state for swap
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 // 🔥 NEW: STREAM RECOVERED (Instant hide)
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        


//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             // Background (Chrome 2) ka status sirf logs print karne ke liye check karein
//             let logBackupStatus = await checkPageStatus(backupPage);
            
//             // Queue mein next URL calculate karein
//             let nextAfterBackupIndex = (backupUrlIndex + 1) % urlList.length;
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex];
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
// console.log(`[▶️] CURRENTLY LIVE             : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
// console.log(`--------------------------------------------------`);
// console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND      : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

        

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             // 🔥 SMART FIX: Agar backup perfectly healthy hai, ya yeh intentional proactive refresh hai, tabhi swap karo!
//             // Agar hang aaya hai aur backup ready nahi hai, toh direct HUNTING MODE (Old Flow) mein jao.
//             if (backupStatus.status === 'HEALTHY' || isProactiveRefresh) {
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 // Swap Pages
// let brokenPage = activePage; 
// activePage = backupPage; 
// backupPage = brokenPage;

// // Swap Browsers
// let brokenBrowser = activeBrowser; 
// activeBrowser = backupBrowser; 
// backupBrowser = brokenBrowser;


// // Swap Names taake pata chale screen par konsa aya hai
// let brokenName = activeBrowserName;
// activeBrowserName = backupBrowserName;
// backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; // 🔥 Fresh slate for new active tab

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     // 🔥 NEW: Background tab par naya link khulte hi silently video prepare kar lo
//                     await initializeVideo(backupPage, true, false);
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

// } else {
//                 // 🔥 NEW LOGIC: FRESH TAB HUNTING MODE
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ BOTH TABS FAILED (Active & Backup Compromised)`);
//                 console.log(`[🔍] FRESH HUNTING MODE ACTIVATED: Spawning clean tabs...`);
//                 console.log(`==================================================`);
                
//                 // Step 1: OBS ko temporarily Waiting Scene (Buffer) par bhejein taake screen par blank/error na aaye
//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch (e) {}

//                 // Step 2: Naye Links Calculate Karein
//                 currentUrlIndex = (currentUrlIndex + 1) % urlList.length;
//                 activeUrlStr = urlList[currentUrlIndex];
                
//                 backupUrlIndex = (currentUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];

//                 console.log(`[*] Hunting Active -> Server [${currentUrlIndex}]`);
//                 console.log(`[*] Hunting Backup -> Server [${backupUrlIndex}]`);

//                 // Step 3: Purane bugged tabs ko permanently close kar dein (Memory Leak Prevention)
//                 console.log(`[*] Closing crashed tabs and spawning fresh ones...`);
//                 try { await activePage.close(); } catch(e) {}
//                 try { await backupPage.close(); } catch(e) {}

//                 // Step 4: Naye fresh tabs create karein aur sab security/adblockers dobara lagayein
//                 activePage = await activeBrowser.newPage();
//                 backupPage = await backupBrowser.newPage();

//                 await setupNetworkAdBlocker(activePage);
//                 await setupNetworkAdBlocker(backupPage);
//                 attachAntiAdListeners(activePage);
//                 attachAntiAdListeners(backupPage);
//                 await applyPreloadFirewall(activePage);
//                 await applyPreloadFirewall(backupPage);

//                 // Step 5: Active tab ko naye link par load karein aur video initialize karein
//                 try {
//                     await activePage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 });
//                     await showLoadingUI(activePage, "SEARCHING SERVER", "Hunting for a stable stream connection <span class='stream-blink'>...</span>");
//                     await initializeVideo(activePage, false, true); 
//                     await hideLoadingUI(activePage);
//                 } catch(e) {}

//                 // Step 6: Backup tab ko background mein mute karke load karein aur video initialize karein
//                 try {
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                     await initializeVideo(backupPage, true, false); // 🔥 Muted and isolated in background
//                 } catch(e) {}

//                 // Step 7: Sab Watchdog Timers Reset Karein (Taake system ko lagay stream abhi start hui hai)
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true; 
//                 currentStreamStartTime = Date.now();
//                 backupWarmupTime = Date.now();
//                 lastActiveTime = -1;
//                 frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false;

//                 // Step 8: OBS ko wapas MainScene par le aayein (Kyunke ab naya tab safely load ho chuka hai)
//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
                
//                 console.log(`[*] Fresh tabs active. Waiting 15 seconds (Warm-up) for video to stabilize...`);
//             }
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     // 👇=== YEH 2 LINES SHURU MEIN ADD KAREIN ===👇
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }


//     console.log(`[*] Starting ACTIVE browser instance...`);
// activeBrowser = await createBrowserInstance(browserArgs);
// const activePages = await activeBrowser.pages();
// activePage = activePages[0];

// console.log(`[*] Starting BACKUP browser instance in background...`);
// backupBrowser = await createBrowserInstance(browserArgs);
// const backupPages = await backupBrowser.pages();
// backupPage = backupPages[0];

// activeBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

// backupBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

    
    
//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//     await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
//     await backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
    
//     // 🔥 NEW: Background tab par bhi silently video initialize karein (Muted = true, isActive = false)
//     await initializeVideo(backupPage, true, false);
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex]}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex]}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//         if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//         try {
//             execSync('pkill -9 obs || true', { stdio: 'ignore' });
//             execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//             execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//         } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();

























// ==========================================
// ==========================================
// ==========================================






// ======yeh codebey  sahey nahey hai ============== Alhamdullah

// const puppeteer = require('puppeteer-extra');let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let activeBrowser = null;
// let backupBrowser = null;

// // Name tags for tracking
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";

// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;




// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // =========================================================
//             // 🛑 ULTIMATE SHADOW DOM AD KILLER (Monkey Patching)
//             // =========================================================
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') {
//                     init.mode = 'open'; // Force open to allow inspection
//                 }
//                 const shadowRoot = originalAttachShadow.call(this, init);
                
//                 const observer = new MutationObserver(() => {
//                     // UPDATED: Hardened selector string catching wildcard IDs and payload sources
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) {
//                         console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
//                         this.remove(); // Kill the entire host element
//                     }
//                 });
                
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };

//             // Standard Protections
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             // UPDATED: Added fallback display:none for the mutated shadow tags
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // ⚠️ NEW: INSTANT HANG / RECOVERY SHIELD UI (Unique Radar Design)
// // =========================================================================================
// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     // const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay):not([class*="player"]):not([class*="jw"]):not([class*="vjs"]), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     // if (realVideo) { 
//                     //     realVideo.style.setProperty('position', 'fixed', 'important');
//                     //     realVideo.style.setProperty('top', '0px', 'important');
//                     //     realVideo.style.setProperty('left', '0px', 'important');
//                     //     realVideo.style.setProperty('width', '100vw', 'important');
//                     //     realVideo.style.setProperty('height', '100vh', 'important');
//                     //     realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                     //     realVideo.style.setProperty('background-color', 'black', 'important');
//                     //     realVideo.style.setProperty('object-fit', 'contain', 'important');
//                     //     realVideo.style.setProperty('opacity', '1', 'important');
//                     //     realVideo.style.setProperty('visibility', 'visible', 'important');
//                     //     realVideo.style.setProperty('display', 'block', 'important');
//                     // }
//                     if (realVideo) { 
//                         // 🛡️ FIX FOR STREAMED.PK: Target the wrapper instead of breaking the video tag out
//                         let playerWrap = realVideo.closest('.jwplayer, #player, .plyr, .vjs-player, .shaka-video-container, [data-player]') || realVideo;
                        
//                         playerWrap.style.setProperty('position', 'fixed', 'important');
//                         playerWrap.style.setProperty('top', '0px', 'important');
//                         playerWrap.style.setProperty('left', '0px', 'important');
//                         playerWrap.style.setProperty('width', '100vw', 'important');
//                         playerWrap.style.setProperty('height', '100vh', 'important');
//                         playerWrap.style.setProperty('z-index', '2147483646', 'important'); 
//                         playerWrap.style.setProperty('background-color', 'black', 'important');
//                         playerWrap.style.setProperty('opacity', '1', 'important');
//                         playerWrap.style.setProperty('visibility', 'visible', 'important');
//                         playerWrap.style.setProperty('display', 'block', 'important');
                        
//                         // Agar wrap mil gaya hai, toh andar wali video ko wrapper ke hisaab se fit karein
//                         if (playerWrap !== realVideo) {
//                             realVideo.style.setProperty('width', '100%', 'important');
//                             realVideo.style.setProperty('height', '100%', 'important');
//                         }
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); // 👈 YEH NAYI LINE ADD KAREIN
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();
    
//     // 🔥 NEW: Track recovery UI state
//     let isRecoveryUIShown = false;

//     while (true) {
//         if (!activeBrowser || !activeBrowser.isConnected() || !backupBrowser || !backupBrowser.isConnected()) {
//             throw new Error("One or both browser instances disconnected.");
//         }

//         let activeStatus = await checkPageStatus(activePage);

// // 👇=== UPDATED BACKGROUND SHIELD CODE ===👇
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { // 👈 30 seconds ka cooldown add kiya
//             let backupStatus = await checkPageStatus(backupPage);
            
//             // Agar backup background mein hang hai ya error de raha hai
//             if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
//                 console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
//                 // Next link par jump karein
//                 backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
                
//                 backupWarmupTime = Date.now(); // 👈 COOLDOWN RESET (Taake agli dafa 30 sec wait kare)
                
//                 // Backup browser ko silently naye link par bhej dein
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     // Backup ko dobara mute frame state mein set karein
//                     for (const frame of backupPage.frames()) {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch(e) {}
//             }
//         }
//         // 👆=== UPDATED CODE END ===👆
        

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 // 🔥 NEW: INSTANT HANG RECOVERY SHIELD TRIGGER (0 Seconds Delay)
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                     isRecoveryUIShown = false; // Reset state for swap
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 // 🔥 NEW: STREAM RECOVERED (Instant hide)
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        


//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             // Background (Chrome 2) ka status sirf logs print karne ke liye check karein
//             let logBackupStatus = await checkPageStatus(backupPage);
            
//             // Queue mein next URL calculate karein
//             let nextAfterBackupIndex = (backupUrlIndex + 1) % urlList.length;
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex];
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
// console.log(`[▶️] CURRENTLY LIVE             : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
// console.log(`--------------------------------------------------`);
// console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND      : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

        

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             // 🔥 SMART FIX: Agar backup perfectly healthy hai, ya yeh intentional proactive refresh hai, tabhi swap karo!
//             // Agar hang aaya hai aur backup ready nahi hai, toh direct HUNTING MODE (Old Flow) mein jao.
//             if (backupStatus.status === 'HEALTHY' || isProactiveRefresh) {
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 // Swap Pages
// let brokenPage = activePage; 
// activePage = backupPage; 
// backupPage = brokenPage;

// // Swap Browsers
// let brokenBrowser = activeBrowser; 
// activeBrowser = backupBrowser; 
// backupBrowser = brokenBrowser;


// // Swap Names taake pata chale screen par konsa aya hai
// let brokenName = activeBrowserName;
// activeBrowserName = backupBrowserName;
// backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; // 🔥 Fresh slate for new active tab

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

// } else {
//                 // 🔥 NEW LOGIC: FRESH TAB HUNTING MODE
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ BOTH TABS FAILED (Active & Backup Compromised)`);
//                 console.log(`[🔍] FRESH HUNTING MODE ACTIVATED: Spawning clean tabs...`);
//                 console.log(`==================================================`);
                
//                 // Step 1: OBS ko temporarily Waiting Scene (Buffer) par bhejein taake screen par blank/error na aaye
//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch (e) {}

//                 // Step 2: Naye Links Calculate Karein
//                 currentUrlIndex = (currentUrlIndex + 1) % urlList.length;
//                 activeUrlStr = urlList[currentUrlIndex];
                
//                 backupUrlIndex = (currentUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];

//                 console.log(`[*] Hunting Active -> Server [${currentUrlIndex}]`);
//                 console.log(`[*] Hunting Backup -> Server [${backupUrlIndex}]`);

//                 // Step 3: Purane bugged tabs ko permanently close kar dein (Memory Leak Prevention)
//                 console.log(`[*] Closing crashed tabs and spawning fresh ones...`);
//                 try { await activePage.close(); } catch(e) {}
//                 try { await backupPage.close(); } catch(e) {}

//                 // Step 4: Naye fresh tabs create karein aur sab security/adblockers dobara lagayein
//                 activePage = await activeBrowser.newPage();
//                 backupPage = await backupBrowser.newPage();

//                 await setupNetworkAdBlocker(activePage);
//                 await setupNetworkAdBlocker(backupPage);
//                 attachAntiAdListeners(activePage);
//                 attachAntiAdListeners(backupPage);
//                 await applyPreloadFirewall(activePage);
//                 await applyPreloadFirewall(backupPage);

//                 // Step 5: Active tab ko naye link par load karein aur video initialize karein
//                 try {
//                     await activePage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 });
//                     await showLoadingUI(activePage, "SEARCHING SERVER", "Hunting for a stable stream connection <span class='stream-blink'>...</span>");
//                     await initializeVideo(activePage, false, true); 
//                     await hideLoadingUI(activePage);
//                 } catch(e) {}

//                 // Step 6: Backup tab ko background mein mute karke load karein
//                 try {
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                     for (const frame of backupPage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 } catch(e) {}

//                 // Step 7: Sab Watchdog Timers Reset Karein (Taake system ko lagay stream abhi start hui hai)
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true; 
//                 currentStreamStartTime = Date.now();
//                 backupWarmupTime = Date.now();
//                 lastActiveTime = -1;
//                 frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false;

//                 // Step 8: OBS ko wapas MainScene par le aayein (Kyunke ab naya tab safely load ho chuka hai)
//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
                
//                 console.log(`[*] Fresh tabs active. Waiting 15 seconds (Warm-up) for video to stabilize...`);
//             }
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     // 👇=== YEH 2 LINES SHURU MEIN ADD KAREIN ===👇
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }


//     console.log(`[*] Starting ACTIVE browser instance...`);
// activeBrowser = await createBrowserInstance(browserArgs);
// const activePages = await activeBrowser.pages();
// activePage = activePages[0];

// console.log(`[*] Starting BACKUP browser instance in background...`);
// backupBrowser = await createBrowserInstance(browserArgs);
// const backupPages = await backupBrowser.pages();
// backupPage = backupPages[0];

// activeBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

// backupBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

    
    
//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//     await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
//     backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex]}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex]}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//         if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//         try {
//             execSync('pkill -9 obs || true', { stdio: 'ignore' });
//             execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//             execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//         } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();












//-------- yeh chuta code hai pata nahey q real madrdi me issue tha -*(*())( pehly yeh code tha hamara -------- yeh belwo code Alhamdullah zbdst hai hai . isme new update add akrty hai k agar jab bey hang hu tuu woo background me check karey keya beckend healty hai, if yes tu direct swap -----------------------------------------------------


// const puppeteer = require('puppeteer-extra');let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let activeBrowser = null;
// let backupBrowser = null;

// // Name tags for tracking
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";

// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;




// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // =========================================================
//             // 🛑 ULTIMATE SHADOW DOM AD KILLER (Monkey Patching)
//             // =========================================================
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') {
//                     init.mode = 'open'; // Force open to allow inspection
//                 }
//                 const shadowRoot = originalAttachShadow.call(this, init);
                
//                 const observer = new MutationObserver(() => {
//                     // UPDATED: Hardened selector string catching wildcard IDs and payload sources
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) {
//                         console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
//                         this.remove(); // Kill the entire host element
//                     }
//                 });
                
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };

//             // Standard Protections
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             // UPDATED: Added fallback display:none for the mutated shadow tags
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // ⚠️ NEW: INSTANT HANG / RECOVERY SHIELD UI (Unique Radar Design)
// // =========================================================================================
// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     // const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay):not([class*="player"]):not([class*="jw"]):not([class*="vjs"]), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     // if (realVideo) { 
//                     //     realVideo.style.setProperty('position', 'fixed', 'important');
//                     //     realVideo.style.setProperty('top', '0px', 'important');
//                     //     realVideo.style.setProperty('left', '0px', 'important');
//                     //     realVideo.style.setProperty('width', '100vw', 'important');
//                     //     realVideo.style.setProperty('height', '100vh', 'important');
//                     //     realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                     //     realVideo.style.setProperty('background-color', 'black', 'important');
//                     //     realVideo.style.setProperty('object-fit', 'contain', 'important');
//                     //     realVideo.style.setProperty('opacity', '1', 'important');
//                     //     realVideo.style.setProperty('visibility', 'visible', 'important');
//                     //     realVideo.style.setProperty('display', 'block', 'important');
//                     // }
//                     if (realVideo) { 
//                         // 🛡️ FIX FOR STREAMED.PK: Target the wrapper instead of breaking the video tag out
//                         let playerWrap = realVideo.closest('.jwplayer, #player, .plyr, .vjs-player, .shaka-video-container, [data-player]') || realVideo;
                        
//                         playerWrap.style.setProperty('position', 'fixed', 'important');
//                         playerWrap.style.setProperty('top', '0px', 'important');
//                         playerWrap.style.setProperty('left', '0px', 'important');
//                         playerWrap.style.setProperty('width', '100vw', 'important');
//                         playerWrap.style.setProperty('height', '100vh', 'important');
//                         playerWrap.style.setProperty('z-index', '2147483646', 'important'); 
//                         playerWrap.style.setProperty('background-color', 'black', 'important');
//                         playerWrap.style.setProperty('opacity', '1', 'important');
//                         playerWrap.style.setProperty('visibility', 'visible', 'important');
//                         playerWrap.style.setProperty('display', 'block', 'important');
                        
//                         // Agar wrap mil gaya hai, toh andar wali video ko wrapper ke hisaab se fit karein
//                         if (playerWrap !== realVideo) {
//                             realVideo.style.setProperty('width', '100%', 'important');
//                             realVideo.style.setProperty('height', '100%', 'important');
//                         }
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); // 👈 YEH NAYI LINE ADD KAREIN
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();
    
//     // 🔥 NEW: Track recovery UI state
//     let isRecoveryUIShown = false;

//     while (true) {
//         if (!activeBrowser || !activeBrowser.isConnected() || !backupBrowser || !backupBrowser.isConnected()) {
//             throw new Error("One or both browser instances disconnected.");
//         }

//         let activeStatus = await checkPageStatus(activePage);

// // 👇=== UPDATED BACKGROUND SHIELD CODE ===👇
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { // 👈 30 seconds ka cooldown add kiya
//             let backupStatus = await checkPageStatus(backupPage);
            
//             // Agar backup background mein hang hai ya error de raha hai
//             if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
//                 console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
//                 // Next link par jump karein
//                 backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
                
//                 backupWarmupTime = Date.now(); // 👈 COOLDOWN RESET (Taake agli dafa 30 sec wait kare)
                
//                 // Backup browser ko silently naye link par bhej dein
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     // Backup ko dobara mute frame state mein set karein
//                     for (const frame of backupPage.frames()) {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch(e) {}
//             }
//         }
//         // 👆=== UPDATED CODE END ===👆
        

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 // 🔥 NEW: INSTANT HANG RECOVERY SHIELD TRIGGER (0 Seconds Delay)
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                     isRecoveryUIShown = false; // Reset state for swap
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 // 🔥 NEW: STREAM RECOVERED (Instant hide)
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        


//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             // Background (Chrome 2) ka status sirf logs print karne ke liye check karein
//             let logBackupStatus = await checkPageStatus(backupPage);
            
//             // Queue mein next URL calculate karein
//             let nextAfterBackupIndex = (backupUrlIndex + 1) % urlList.length;
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex];
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
// console.log(`[▶️] CURRENTLY LIVE             : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
// console.log(`--------------------------------------------------`);
// console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND      : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

        

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || backupStatus.status === 'DEAD') { 
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 // Swap Pages
// let brokenPage = activePage; 
// activePage = backupPage; 
// backupPage = brokenPage;

// // Swap Browsers
// let brokenBrowser = activeBrowser; 
// activeBrowser = backupBrowser; 
// backupBrowser = brokenBrowser;


// // Swap Names taake pata chale screen par konsa aya hai
// let brokenName = activeBrowserName;
// activeBrowserName = backupBrowserName;
// backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; // 🔥 Fresh slate for new active tab

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//             } else {
//                 // 🔥 NEW LOGIC: HUNTING MODE INSTEAD OF HARD RESTART
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ BOTH TABS FAILED (Active & Backup Compromised)`);
//                 console.log(`[🔍] HUNTING MODE ACTIVATED: Searching next available server...`);
//                 console.log(`==================================================`);
                
//                 // Step 1: OBS ko temporarily Waiting Scene (Buffer) par bhejein taake blank screen na aaye
//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch (e) {}

//                 // Step 2: Naye Links Calculate Karein
//                 currentUrlIndex = (currentUrlIndex + 1) % urlList.length;
//                 activeUrlStr = urlList[currentUrlIndex];
                
//                 backupUrlIndex = (currentUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];

//                 console.log(`[*] Hunting Active -> Server [${currentUrlIndex}]`);
//                 console.log(`[*] Hunting Backup -> Server [${backupUrlIndex}]`);

//                 // Step 3: Active Page ko naye link par le jayein aur overlay lagayein
//                 try {
//                     await activePage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(activePage);
//                     await activePage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
                    
//                     // Naye page par Foran Reconnecting UI Laga dein
//                     await showLoadingUI(activePage, "SEARCHING SERVER", "Hunting for a stable stream connection <span class='stream-blink'>...</span>");
//                 } catch(e) {}

//                 // Step 4: Backup Page ko bhi next-to-next link par bhej dein
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
                    
//                     // Backup ko mute rakhein
//                     for (const frame of backupPage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 } catch(e) {}

//                 // Step 5: Sab Watchdog Timers Reset Karein (Taake system ko lagay stream abhi start hui hai)
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true; 
//                 currentStreamStartTime = Date.now();
//                 backupWarmupTime = Date.now();
//                 lastActiveTime = -1;
//                 frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false;

//                 // Step 6: OBS ko wapas MainScene par le aayein jahan humara naya Overlay chal raha hoga
//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
                
//                 console.log(`[*] Link injected. Waiting 15 seconds (Warm-up) for video to play...`);
//             }
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     // 👇=== YEH 2 LINES SHURU MEIN ADD KAREIN ===👇
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }


//     console.log(`[*] Starting ACTIVE browser instance...`);
// activeBrowser = await createBrowserInstance(browserArgs);
// const activePages = await activeBrowser.pages();
// activePage = activePages[0];

// console.log(`[*] Starting BACKUP browser instance in background...`);
// backupBrowser = await createBrowserInstance(browserArgs);
// const backupPages = await backupBrowser.pages();
// backupPage = backupPages[0];

// activeBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

// backupBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

    
    
//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//     await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
//     backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex]}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex]}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//         if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//         try {
//             execSync('pkill -9 obs || true', { stdio: 'ignore' });
//             execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//             execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//         } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();














// const puppeteer = require('puppeteer-extra');let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let activeBrowser = null;
// let backupBrowser = null;

// // Name tags for tracking
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";

// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;




// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // =========================================================
//             // 🛑 ULTIMATE SHADOW DOM AD KILLER (Monkey Patching)
//             // =========================================================
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') {
//                     init.mode = 'open'; // Force open to allow inspection
//                 }
//                 const shadowRoot = originalAttachShadow.call(this, init);
                
//                 const observer = new MutationObserver(() => {
//                     // UPDATED: Hardened selector string catching wildcard IDs and payload sources
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) {
//                         console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
//                         this.remove(); // Kill the entire host element
//                     }
//                 });
                
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };

//             // Standard Protections
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             // UPDATED: Added fallback display:none for the mutated shadow tags
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // ⚠️ NEW: INSTANT HANG / RECOVERY SHIELD UI (Unique Radar Design)
// // =========================================================================================
// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); // 👈 YEH NAYI LINE ADD KAREIN
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();
    
//     // 🔥 NEW: Track recovery UI state
//     let isRecoveryUIShown = false;

//     while (true) {
//         if (!activeBrowser || !activeBrowser.isConnected() || !backupBrowser || !backupBrowser.isConnected()) {
//             throw new Error("One or both browser instances disconnected.");
//         }

//         let activeStatus = await checkPageStatus(activePage);

// // 👇=== UPDATED BACKGROUND SHIELD CODE ===👇
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { // 👈 30 seconds ka cooldown add kiya
//             let backupStatus = await checkPageStatus(backupPage);
            
//             // Agar backup background mein hang hai ya error de raha hai
//             if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
//                 console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
//                 // Next link par jump karein
//                 backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
                
//                 backupWarmupTime = Date.now(); // 👈 COOLDOWN RESET (Taake agli dafa 30 sec wait kare)
                
//                 // Backup browser ko silently naye link par bhej dein
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     // Backup ko dobara mute frame state mein set karein
//                     for (const frame of backupPage.frames()) {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch(e) {}
//             }
//         }
//         // 👆=== UPDATED CODE END ===👆
        

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 // 🔥 NEW: INSTANT HANG RECOVERY SHIELD TRIGGER (0 Seconds Delay)
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                     isRecoveryUIShown = false; // Reset state for swap
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 // 🔥 NEW: STREAM RECOVERED (Instant hide)
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        


//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             // Background (Chrome 2) ka status sirf logs print karne ke liye check karein
//             let logBackupStatus = await checkPageStatus(backupPage);
            
//             // Queue mein next URL calculate karein
//             let nextAfterBackupIndex = (backupUrlIndex + 1) % urlList.length;
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex];
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
// console.log(`[▶️] CURRENTLY LIVE             : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
// console.log(`--------------------------------------------------`);
// console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND      : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

        

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || backupStatus.status === 'DEAD') { 
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 // Swap Pages
// let brokenPage = activePage; 
// activePage = backupPage; 
// backupPage = brokenPage;

// // Swap Browsers
// let brokenBrowser = activeBrowser; 
// activeBrowser = backupBrowser; 
// backupBrowser = brokenBrowser;


// // Swap Names taake pata chale screen par konsa aya hai
// let brokenName = activeBrowserName;
// activeBrowserName = backupBrowserName;
// backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; // 🔥 Fresh slate for new active tab

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//             } else {
//                 // 🔥 NEW LOGIC: HUNTING MODE INSTEAD OF HARD RESTART
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ BOTH TABS FAILED (Active & Backup Compromised)`);
//                 console.log(`[🔍] HUNTING MODE ACTIVATED: Searching next available server...`);
//                 console.log(`==================================================`);
                
//                 // Step 1: OBS ko temporarily Waiting Scene (Buffer) par bhejein taake blank screen na aaye
//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' }); } catch (e) {}

//                 // Step 2: Naye Links Calculate Karein
//                 currentUrlIndex = (currentUrlIndex + 1) % urlList.length;
//                 activeUrlStr = urlList[currentUrlIndex];
                
//                 backupUrlIndex = (currentUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];

//                 console.log(`[*] Hunting Active -> Server [${currentUrlIndex}]`);
//                 console.log(`[*] Hunting Backup -> Server [${backupUrlIndex}]`);

//                 // Step 3: Active Page ko naye link par le jayein aur overlay lagayein
//                 try {
//                     await activePage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(activePage);
//                     await activePage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
                    
//                     // Naye page par Foran Reconnecting UI Laga dein
//                     await showLoadingUI(activePage, "SEARCHING SERVER", "Hunting for a stable stream connection <span class='stream-blink'>...</span>");
//                 } catch(e) {}

//                 // Step 4: Backup Page ko bhi next-to-next link par bhej dein
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
                    
//                     // Backup ko mute rakhein
//                     for (const frame of backupPage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 } catch(e) {}

//                 // Step 5: Sab Watchdog Timers Reset Karein (Taake system ko lagay stream abhi start hui hai)
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true; 
//                 currentStreamStartTime = Date.now();
//                 backupWarmupTime = Date.now();
//                 lastActiveTime = -1;
//                 frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false;

//                 // Step 6: OBS ko wapas MainScene par le aayein jahan humara naya Overlay chal raha hoga
//                 try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
                
//                 console.log(`[*] Link injected. Waiting 15 seconds (Warm-up) for video to play...`);
//             }
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     // 👇=== YEH 2 LINES SHURU MEIN ADD KAREIN ===👇
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }


//     console.log(`[*] Starting ACTIVE browser instance...`);
// activeBrowser = await createBrowserInstance(browserArgs);
// const activePages = await activeBrowser.pages();
// activePage = activePages[0];

// console.log(`[*] Starting BACKUP browser instance in background...`);
// backupBrowser = await createBrowserInstance(browserArgs);
// const backupPages = await backupBrowser.pages();
// backupPage = backupPages[0];

// activeBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

// backupBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

    
    
//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//     await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
//     backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex]}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex]}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//         if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//         try {
//             execSync('pkill -9 obs || true', { stdio: 'ignore' });
//             execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//             execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//         } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();


















// ========== ALhamdullah Alhamdullah 2 chromes and 1 obs setup ALhamdullah Ready ===============================================
// ========== ALhamdullah Alhamdullah 2 chromes and 1 obs setup ALhamdullah Ready ===============================================
// ========== ALhamdullah Alhamdullah 2 chromes and 1 obs setup ALhamdullah Ready ===============================================





// const puppeteer = require('puppeteer-extra');let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let activeBrowser = null;
// let backupBrowser = null;

// // Name tags for tracking
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";

// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;




// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // =========================================================
//             // 🛑 ULTIMATE SHADOW DOM AD KILLER (Monkey Patching)
//             // =========================================================
//             const originalAttachShadow = Element.prototype.attachShadow;
//             Element.prototype.attachShadow = function(init) {
//                 if (init && init.mode === 'closed') {
//                     init.mode = 'open'; // Force open to allow inspection
//                 }
//                 const shadowRoot = originalAttachShadow.call(this, init);
                
//                 const observer = new MutationObserver(() => {
//                     // UPDATED: Hardened selector string catching wildcard IDs and payload sources
//                     const adElements = shadowRoot.querySelectorAll('in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"], [src*="adexchangerapid"]');
//                     if (adElements.length > 0) {
//                         console.log('[🛡️] SHIELD: Shadow DOM Ad Detected & Destroyed!');
//                         this.remove(); // Kill the entire host element
//                     }
//                 });
                
//                 observer.observe(shadowRoot, { childList: true, subtree: true });
//                 return shadowRoot;
//             };

//             // Standard Protections
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             // UPDATED: Added fallback display:none for the mutated shadow tags
//             style.textContent = `
//                 html, body { background-color: #000000 !important; overflow: hidden !important; }
//                 in-page-message, [id^="note-"], [id^="missclick-"], [id^="close-"] { display: none !important; opacity: 0 !important; pointer-events: none !important; }
//             `;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // ⚠️ NEW: INSTANT HANG / RECOVERY SHIELD UI (Unique Radar Design)
// // =========================================================================================
// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); // 👈 YEH NAYI LINE ADD KAREIN
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();
    
//     // 🔥 NEW: Track recovery UI state
//     let isRecoveryUIShown = false;

//     while (true) {
//         if (!activeBrowser || !activeBrowser.isConnected() || !backupBrowser || !backupBrowser.isConnected()) {
//             throw new Error("One or both browser instances disconnected.");
//         }

//         let activeStatus = await checkPageStatus(activePage);

// // 👇=== UPDATED BACKGROUND SHIELD CODE ===👇
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { // 👈 30 seconds ka cooldown add kiya
//             let backupStatus = await checkPageStatus(backupPage);
            
//             // Agar backup background mein hang hai ya error de raha hai
//             if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
//                 console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
//                 // Next link par jump karein
//                 backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
                
//                 backupWarmupTime = Date.now(); // 👈 COOLDOWN RESET (Taake agli dafa 30 sec wait kare)
                
//                 // Backup browser ko silently naye link par bhej dein
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     // Backup ko dobara mute frame state mein set karein
//                     for (const frame of backupPage.frames()) {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch(e) {}
//             }
//         }
//         // 👆=== UPDATED CODE END ===👆
        

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 // 🔥 NEW: INSTANT HANG RECOVERY SHIELD TRIGGER (0 Seconds Delay)
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                     isRecoveryUIShown = false; // Reset state for swap
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 // 🔥 NEW: STREAM RECOVERED (Instant hide)
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        


//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             // Background (Chrome 2) ka status sirf logs print karne ke liye check karein
//             let logBackupStatus = await checkPageStatus(backupPage);
            
//             // Queue mein next URL calculate karein
//             let nextAfterBackupIndex = (backupUrlIndex + 1) % urlList.length;
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex];
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
// console.log(`[▶️] CURRENTLY LIVE             : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
// console.log(`--------------------------------------------------`);
// console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND      : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

        

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || backupStatus.status === 'DEAD') { 
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 // Swap Pages
// let brokenPage = activePage; 
// activePage = backupPage; 
// backupPage = brokenPage;

// // Swap Browsers
// let brokenBrowser = activeBrowser; 
// activeBrowser = backupBrowser; 
// backupBrowser = brokenBrowser;


// // Swap Names taake pata chale screen par konsa aya hai
// let brokenName = activeBrowserName;
// activeBrowserName = backupBrowserName;
// backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; // 🔥 Fresh slate for new active tab

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//             } else {
//                 console.error(`[!] ❌ Backup Tab is ALSO DEAD/FROZEN. Hard Restarting System...`);
//                 throw new Error("Both Active and Backup tabs failed.");
//             }
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     // 👇=== YEH 2 LINES SHURU MEIN ADD KAREIN ===👇
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }


//     console.log(`[*] Starting ACTIVE browser instance...`);
// activeBrowser = await createBrowserInstance(browserArgs);
// const activePages = await activeBrowser.pages();
// activePage = activePages[0];

// console.log(`[*] Starting BACKUP browser instance in background...`);
// backupBrowser = await createBrowserInstance(browserArgs);
// const backupPages = await backupBrowser.pages();
// backupPage = backupPages[0];

// activeBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

// backupBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

    
    
//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//     await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
//     backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex]}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex]}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//         if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//         try {
//             execSync('pkill -9 obs || true', { stdio: 'ignore' });
//             execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//             execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//         } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();






















































































// =================== Alhamdullah 2 chromes and 1 obs system architecture king kinhg @@@@@@@@@@@@@@@###############################$$$$$$$$$$$$$$$$$$$$$$$$$$ =======================================



// const puppeteer = require('puppeteer-extra');let isWarmupPhase = true;
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let activeBrowser = null;
// let backupBrowser = null;

// // Name tags for tracking
// let activeBrowserName = "CHROME 1";
// let backupBrowserName = "CHROME 2";

// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;




// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // Permanent root execution block for popup alerts & confirms
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; }`;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // ⚠️ NEW: INSTANT HANG / RECOVERY SHIELD UI (Unique Radar Design)
// // =========================================================================================
// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     let backupWarmupTime = Date.now(); // 👈 YEH NAYI LINE ADD KAREIN
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();
    
//     // 🔥 NEW: Track recovery UI state
//     let isRecoveryUIShown = false;

//     while (true) {
//         if (!activeBrowser || !activeBrowser.isConnected() || !backupBrowser || !backupBrowser.isConnected()) {
//             throw new Error("One or both browser instances disconnected.");
//         }

//         let activeStatus = await checkPageStatus(activePage);

// // 👇=== UPDATED BACKGROUND SHIELD CODE ===👇
//         if (!isWarmupPhase && (Date.now() - backupWarmupTime > 30000)) { // 👈 30 seconds ka cooldown add kiya
//             let backupStatus = await checkPageStatus(backupPage);
            
//             // Agar backup background mein hang hai ya error de raha hai
//             if (backupStatus.status === 'DEAD' || backupStatus.status === 'CRITICAL_ERROR' || backupStatus.status === 'FROZEN') {
//                 console.log(`\n[⚠️] BACKGROUND SHIELD: Backup Server [${backupUrlIndex}] failed silently.`);
                
//                 // Next link par jump karein
//                 backupUrlIndex = (backupUrlIndex + 1) % urlList.length;
//                 backupUrlStr = urlList[backupUrlIndex];
                
//                 console.log(`[*] Shifting Backup Chrome to NEXT link -> Server [${backupUrlIndex}]`);
                
//                 backupWarmupTime = Date.now(); // 👈 COOLDOWN RESET (Taake agli dafa 30 sec wait kare)
                
//                 // Backup browser ko silently naye link par bhej dein
//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
                    
//                     // Backup ko dobara mute frame state mein set karein
//                     for (const frame of backupPage.frames()) {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                         }
//                     }
//                 } catch(e) {}
//             }
//         }
//         // 👆=== UPDATED CODE END ===👆
        

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 // 🔥 NEW: INSTANT HANG RECOVERY SHIELD TRIGGER (0 Seconds Delay)
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                     isRecoveryUIShown = false; // Reset state for swap
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 // 🔥 NEW: STREAM RECOVERED (Instant hide)
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        


//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             // Background (Chrome 2) ka status sirf logs print karne ke liye check karein
//             let logBackupStatus = await checkPageStatus(backupPage);
            
//             // Queue mein next URL calculate karein
//             let nextAfterBackupIndex = (backupUrlIndex + 1) % urlList.length;
//             let nextAfterBackupUrl = urlList[nextAfterBackupIndex];
            
//             console.log(`\n==================================================`);
//             console.log(`[💓] ACTIVE HEARTBEAT (${activeBrowserName}): Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
// console.log(`[▶️] CURRENTLY LIVE             : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
// console.log(`--------------------------------------------------`);
// console.log(`[🖤] BACKUP HEARTBEAT (${backupBrowserName}): Status is ${logBackupStatus.status} | Video Time: ${logBackupStatus.currentTime ? logBackupStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[🔄] RUNNING IN BACKGROUND      : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//             console.log(`[⏭️] NEXT QUEUE (AFTER BACKUP)  : Server [${nextAfterBackupIndex}] -> ${nextAfterBackupUrl}`);
//             console.log(`==================================================\n`);
//         }

        

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || backupStatus.status === 'DEAD') { 
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 // Swap Pages
// let brokenPage = activePage; 
// activePage = backupPage; 
// backupPage = brokenPage;

// // Swap Browsers
// let brokenBrowser = activeBrowser; 
// activeBrowser = backupBrowser; 
// backupBrowser = brokenBrowser;


// // Swap Names taake pata chale screen par konsa aya hai
// let brokenName = activeBrowserName;
// activeBrowserName = backupBrowserName;
// backupBrowserName = brokenName;
                
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; // 🔥 Fresh slate for new active tab

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//             } else {
//                 console.error(`[!] ❌ Backup Tab is ALSO DEAD/FROZEN. Hard Restarting System...`);
//                 throw new Error("Both Active and Backup tabs failed.");
//             }
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     // 👇=== YEH 2 LINES SHURU MEIN ADD KAREIN ===👇
//     activeBrowserName = "CHROME 1";
//     backupBrowserName = "CHROME 2";
    
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }


//     console.log(`[*] Starting ACTIVE browser instance...`);
// activeBrowser = await createBrowserInstance(browserArgs);
// const activePages = await activeBrowser.pages();
// activePage = activePages[0];

// console.log(`[*] Starting BACKUP browser instance in background...`);
// backupBrowser = await createBrowserInstance(browserArgs);
// const backupPages = await backupBrowser.pages();
// backupPage = backupPages[0];

// activeBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

// backupBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

    
    
//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//     await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
//     backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex]}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex]}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { try { await activeBrowser.close(); } catch(e) { } activeBrowser = null; }
//     if (backupBrowser) { try { await backupBrowser.close(); } catch(e) { } backupBrowser = null; }
    
//         if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//         try {
//             execSync('pkill -9 obs || true', { stdio: 'ignore' });
//             execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//             execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//         } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();









































































































// ========================================================================================
//     ================================================================================================
//     ========================================================================================================
// ========================================================================================
// //     ================================================================================================
// //     ========================================================================================================
// ========================================================================================
// //     ================================================================================================
// //     ========================================================================================================














// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let activeBrowser = null;
// let backupBrowser = null;

// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;




// async function createBrowserInstance(browserArgs) {
//     return await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });
// }

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // Permanent root execution block for popup alerts & confirms
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; }`;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // ⚠️ NEW: INSTANT HANG / RECOVERY SHIELD UI (Unique Radar Design)
// // =========================================================================================
// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();
    
//     // 🔥 NEW: Track recovery UI state
//     let isRecoveryUIShown = false;

//     while (true) {
//         if (!browser || !browser.isConnected()) throw new Error("Browser closed.");

//         let activeStatus = await checkPageStatus(activePage);

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 // 🔥 NEW: INSTANT HANG RECOVERY SHIELD TRIGGER (0 Seconds Delay)
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                     isRecoveryUIShown = false; // Reset state for swap
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 // 🔥 NEW: STREAM RECOVERED (Instant hide)
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        
//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             console.log(`\n[💓] WATCHDOG HEARTBEAT: Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[▶️] CURRENTLY LIVE   : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
//             console.log(`[⏭️] NEXT IN QUEUE    : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//         }

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || backupStatus.status === 'DEAD') { 
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 // Swap Pages
// let brokenPage = activePage; 
// activePage = backupPage; 
// backupPage = brokenPage;

// // Swap Browsers
// let brokenBrowser = activeBrowser; 
// activeBrowser = backupBrowser; 
// backupBrowser = brokenBrowser;
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; // 🔥 Fresh slate for new active tab

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//             } else {
//                 console.error(`[!] ❌ Backup Tab is ALSO DEAD/FROZEN. Hard Restarting System...`);
//                 throw new Error("Both Active and Backup tabs failed.");
//             }
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }


//     console.log(`[*] Starting ACTIVE browser instance...`);
// activeBrowser = await createBrowserInstance(browserArgs);
// const activePages = await activeBrowser.pages();
// activePage = activePages[0];

// console.log(`[*] Starting BACKUP browser instance in background...`);
// backupBrowser = await createBrowserInstance(browserArgs);
// const backupPages = await backupBrowser.pages();
// backupPage = backupPages[0];

// activeBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== activePage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

// backupBrowser.on('targetcreated', async (target) => {
//     if (target.type() === 'page') {
//         const newPage = await target.page();
//         setTimeout(async () => { if (newPage && newPage !== backupPage) { try { await newPage.close(); } catch(e) {} } }, 500);
//     }
// });

    
    
//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//     await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
//     backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex]}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex]}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (activeBrowser) { 
//     try { await activeBrowser.close(); } catch(e) { } 
//     activeBrowser = null; 
// }
// if (backupBrowser) { 
//     try { await backupBrowser.close(); } catch(e) { } 
//     backupBrowser = null; 
// }
//     if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//     try {
//         execSync('pkill -9 obs || true', { stdio: 'ignore' });
//         execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//         execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//     } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();

































// firefox


// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// // Stealth plugin is essential to bypass basic Cloudflare checks
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', 
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);

// // Fix for URL handling to prevent ProtocolError
// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().replace(/^\\+/, '').startsWith('http') ? u.trim().replace(/^\\+/, '') : 'https://' + u.trim().replace(/^\\+/, '')) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let browser = null;
// let obsProcess = null;
// let activePage = null;

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD (Original)
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // Permanent root execution block for popup alerts & confirms
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; }`;
//             document.documentElement.appendChild(style);
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "MainScene", 
//         "current_program_scene": "MainScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });

// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (Original)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     console.log('[🔊] Applying Smart Unmute Logic...');
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         // THE MAGIC: Isolating and expanding the actual <video> element.
//         if (targetFrame) {
//             await targetFrame.evaluate((muteVideo) => {
//                 setInterval(() => {
//                     try {
//                         const style = document.createElement('style');
//                         style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                         document.head.appendChild(style);

//                         const mediaElements = document.querySelectorAll('video, audio');
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let realVideo = null;

//                         mediaElements.forEach(media => { 
//                             media.muted = muteVideo; 
//                             media.volume = muteVideo ? 0.0 : 1.0; 
//                         });

//                         if (!muteVideo) {
//                             document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                         }

//                         for (const v of videos) {
//                             if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                         }

//                         if (!realVideo && videos.length > 0) {
//                             realVideo = videos[0];
//                         }

//                         if (realVideo) { 
//                             realVideo.style.setProperty('position', 'fixed', 'important');
//                             realVideo.style.setProperty('top', '0px', 'important');
//                             realVideo.style.setProperty('left', '0px', 'important');
//                             realVideo.style.setProperty('width', '100vw', 'important');
//                             realVideo.style.setProperty('height', '100vh', 'important');
//                             realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                             realVideo.style.setProperty('background-color', 'black', 'important');
//                             realVideo.style.setProperty('object-fit', 'contain', 'important');
//                             realVideo.style.setProperty('opacity', '1', 'important');
//                             realVideo.style.setProperty('visibility', 'visible', 'important');
//                             realVideo.style.setProperty('display', 'block', 'important');
//                         }
//                     } catch(err) {}
//                 }, 500); 
//             }, startMuted).catch(() => {});
//         }

//     } catch (e) { }

//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// // =========================================================================================
// // 🔄 WATCHDOG: Re-added to handle manifestError via reload
// // =========================================================================================
// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         // Detecting manifest load error
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("manifestloaderror") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startSingleTabWatchdog() {
//     let watchdogTicks = 0;
    
//     while (true) {
//         if (!browser || !browser.isConnected()) throw new Error("Browser closed.");

//         let activeStatus = await checkPageStatus(activePage);

//         watchdogTicks++;
//         if (watchdogTicks === 1 || watchdogTicks % 9 === 0) {
//             console.log(`\n[💓] WATCHDOG HEARTBEAT: Status is ${activeStatus.status} | Single-Tab Monitoring`);
//         }

//         // If manifest error occurs, perform a fresh reload on the same tab
//         if (activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FROZEN') {
//             console.log(`\n==================================================`);
//             console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}. Manifest Error possible.`);
//             console.log(`[🔄] Auto-Reloading the page to acquire fresh M3U8 token...`);
//             console.log(`==================================================`);
            
//             try {
//                 await activePage.goto('about:blank').catch(()=>{});
//                 await applyPreloadFirewall(activePage);
//                 await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 await initializeVideo(activePage, false, true);
//             } catch(e) {
//                 console.log(`[⏳] Refresh handled safely.`);
//             }
//         }

//         await new Promise(r => setTimeout(r, 10000)); 
//     }
// }

// // =========================================================================================
// // 🎬 ENGINE INITIALIZATION
// // =========================================================================================
// async function startDirectStreaming() {
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     // 🔥 2026 FIX 1: Removed --minimize-to-tray (fails in Xvfb) and added safemode flags
//     obsProcess = spawn('obs', [
//         '--startstreaming', 
//         '--disable-updater',
//         '--disable-missing-files-check',
//         '--multi',
//         // '--safemode'
//         '--portable'
       
//     ]);

    
    
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     // =====================================================================
//     // 🛡️ CONTINUOUS WINDOW SHIELD (OBS INVISIBLE ENGINE)
//     // =====================================================================
//     console.log('[🛡️] Starting OS-Level Window Shield to hide OBS...');
//     setInterval(() => {
//         try {
//             // OBS ki window ko virtual display se permanently unmap (hide) karo
//             exec('xdotool search --class "obs" windowunmap 2>/dev/null');
//             // Browser ko full screen kar ke top layer par lock karo
//             exec('xdotool search --class "firefox" windowactivate windowraise 2>/dev/null');
//             exec('xdotool search --class "Firefox" windowactivate windowraise 2>/dev/null');
//         } catch (e) {
//             // Fail silently
//         }
//     }, 2000);
//     // =====================================================================

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     try {
//         await obs.connect('ws://127.0.0.1:4455', 'secret');
//         isObsConnected = true;
//         console.log('[+] OBS WebSocket Connected Successfully!');
//     } catch (e) {}

//     // Firefox Compatible Browser Arguments
//     let browserArgs = [
//         `--width=${RES_W}`,
//         `--height=${RES_H}`,
//         '--kiosk' // Fullscreen functionality for Firefox
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting FIREFOX with [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting FIREFOX...`);
//     }

//     browser = await puppeteer.launch({
//         product: 'firefox',
//         executablePath: '/usr/bin/firefox', // 👈 Yeh nayi line add karni hai
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });

//     // POPUP KILLER
//     browser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => {
//                 if (newPage && newPage !== activePage) {
//                     console.log(`[🛡️] AD-BLOCKER: Killed an unwanted pop-up tab!`);
//                     try { await newPage.close(); } catch(e) {}
//                 }
//             }, 500);
//         }
//     });

//     const pages = await browser.pages();
//     activePage = pages[0]; 
    
//     await setupNetworkAdBlocker(activePage);
//     attachAntiAdListeners(activePage);
//     await applyPreloadFirewall(activePage);

//     await activePage.bringToFront(); 

//     console.log(`[*] Loading URL: ${urlList[currentUrlIndex]}`);
//     await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
    
//     // Simulate user interaction to bypass basic bot checks
//     try { await activePage.mouse.click(10, 10); console.log('[🖱️] Simulated physical click'); } catch(e){}

//     await initializeVideo(activePage, false, true); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`\n==================================================`);
//     console.log(`[🎥] STREAM IS LIVE ON SINGLE TAB`);
//     console.log(`==================================================\n`);

//     await startSingleTabWatchdog();
// }

// startDirectStreaming();






































// done play but obs wizard haldf apper




// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// // Stealth plugin is essential to bypass basic Cloudflare checks
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', 
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);

// // Fix for URL handling to prevent ProtocolError
// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().replace(/^\\+/, '').startsWith('http') ? u.trim().replace(/^\\+/, '') : 'https://' + u.trim().replace(/^\\+/, '')) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let browser = null;
// let obsProcess = null;
// let activePage = null;

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD (Original)
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // Permanent root execution block for popup alerts & confirms
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; }`;
//             document.documentElement.appendChild(style);
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "MainScene", 
//         "current_program_scene": "MainScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (Original)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     console.log('[🔊] Applying Smart Unmute Logic...');
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         // THE MAGIC: Isolating and expanding the actual <video> element.
//         if (targetFrame) {
//             await targetFrame.evaluate((muteVideo) => {
//                 setInterval(() => {
//                     try {
//                         const style = document.createElement('style');
//                         style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                         document.head.appendChild(style);

//                         const mediaElements = document.querySelectorAll('video, audio');
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let realVideo = null;

//                         mediaElements.forEach(media => { 
//                             media.muted = muteVideo; 
//                             media.volume = muteVideo ? 0.0 : 1.0; 
//                         });

//                         if (!muteVideo) {
//                             document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                         }

//                         for (const v of videos) {
//                             if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                         }

//                         if (!realVideo && videos.length > 0) {
//                             realVideo = videos[0];
//                         }

//                         if (realVideo) { 
//                             realVideo.style.setProperty('position', 'fixed', 'important');
//                             realVideo.style.setProperty('top', '0px', 'important');
//                             realVideo.style.setProperty('left', '0px', 'important');
//                             realVideo.style.setProperty('width', '100vw', 'important');
//                             realVideo.style.setProperty('height', '100vh', 'important');
//                             realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                             realVideo.style.setProperty('background-color', 'black', 'important');
//                             realVideo.style.setProperty('object-fit', 'contain', 'important');
//                             realVideo.style.setProperty('opacity', '1', 'important');
//                             realVideo.style.setProperty('visibility', 'visible', 'important');
//                             realVideo.style.setProperty('display', 'block', 'important');
//                         }
//                     } catch(err) {}
//                 }, 500); 
//             }, startMuted).catch(() => {});
//         }

//     } catch (e) { }

//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// // =========================================================================================
// // 🔄 WATCHDOG: Re-added to handle manifestError via reload
// // =========================================================================================
// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         // Detecting manifest load error
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("manifestloaderror") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startSingleTabWatchdog() {
//     let watchdogTicks = 0;
    
//     while (true) {
//         if (!browser || !browser.isConnected()) throw new Error("Browser closed.");

//         let activeStatus = await checkPageStatus(activePage);

//         watchdogTicks++;
//         if (watchdogTicks === 1 || watchdogTicks % 9 === 0) {
//             console.log(`\n[💓] WATCHDOG HEARTBEAT: Status is ${activeStatus.status} | Single-Tab Monitoring`);
//         }

//         // If manifest error occurs, perform a fresh reload on the same tab
//         if (activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FROZEN') {
//             console.log(`\n==================================================`);
//             console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}. Manifest Error possible.`);
//             console.log(`[🔄] Auto-Reloading the page to acquire fresh M3U8 token...`);
//             console.log(`==================================================`);
            
//             try {
//                 await activePage.goto('about:blank').catch(()=>{});
//                 await applyPreloadFirewall(activePage);
//                 await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 await initializeVideo(activePage, false, true);
//             } catch(e) {
//                 console.log(`[⏳] Refresh handled safely.`);
//             }
//         }

//         await new Promise(r => setTimeout(r, 10000)); 
//     }
// }

// // =========================================================================================
// // 🎬 ENGINE INITIALIZATION
// // =========================================================================================
// async function startDirectStreaming() {
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     // 🔥 2026 FIX 1: Removed --minimize-to-tray (fails in Xvfb) and added safemode flags
//     obsProcess = spawn('obs', [
//         '--startstreaming', 
//         '--disable-updater',
//         '--disable-missing-files-check',
//         '--multi',
//         // '--safemode'
//         '--portable'
       
//     ]);



    
    
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     // =====================================================================
//     // 🛡️ CONTINUOUS WINDOW SHIELD (OBS INVISIBLE ENGINE)
//     // =====================================================================
//     console.log('[🛡️] Starting OS-Level Window Shield to hide OBS...');
//     setInterval(() => {
//         try {
//             // OBS ki window ko virtual display se permanently unmap (hide) karo
//             exec('xdotool search --class "obs" windowunmap 2>/dev/null');
//             // Browser ko full screen kar ke top layer par lock karo
//             exec('xdotool search --class "chrome" windowactivate windowraise 2>/dev/null');
//             exec('xdotool search --class "chromium" windowactivate windowraise 2>/dev/null');
//         } catch (e) {
//             // Fail silently
//         }
//     }, 2000);
//     // =====================================================================

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     try {
//         await obs.connect('ws://127.0.0.1:4455', 'secret');
//         isObsConnected = true;
//         console.log('[+] OBS WebSocket Connected Successfully!');
//     } catch (e) {}

//     // ALL ORIGINAL BROWSER FLAGS RESTORED
//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding'
//         // REMOVED ublock-lite flag. ublock-lite blocks the scripts that fetch m3u8.
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with [CLOUDFLARE WARP] Proxy...`);
//     }

//     browser = await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });

//     // POPUP KILLER
//     browser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => {
//                 if (newPage && newPage !== activePage) {
//                     console.log(`[🛡️] AD-BLOCKER: Killed an unwanted pop-up tab!`);
//                     try { await newPage.close(); } catch(e) {}
//                 }
//             }, 500);
//         }
//     });

//     const pages = await browser.pages();
//     activePage = pages[0]; 
    
//     await setupNetworkAdBlocker(activePage);
//     attachAntiAdListeners(activePage);
//     await applyPreloadFirewall(activePage);

//     await activePage.bringToFront(); 

//     console.log(`[*] Loading URL: ${urlList[currentUrlIndex]}`);
//     await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
    
//     // Simulate user interaction to bypass basic bot checks
//     try { await activePage.mouse.click(10, 10); console.log('[🖱️] Simulated physical click'); } catch(e){}

//     await initializeVideo(activePage, false, true); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`\n==================================================`);
//     console.log(`[🎥] STREAM IS LIVE ON SINGLE TAB`);
//     console.log(`==================================================\n`);

//     await startSingleTabWatchdog();
// }

// startDirectStreaming();









































// =============== antigravity ================


// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });
// process.on('unhandledRejection', (reason) => {
//     const msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ FORCE AUTO-REFRESH TIME
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40; 
// const FORCE_REFRESH_MS      = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.',
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app'
// ];

// // =========================================================================================
// // 🚀 STREAM KEYS
// // =========================================================================================
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 
//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',
//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',
//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',
//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// // =========================================================================================
// // ⚙️ QUALITY CONFIG
// // =========================================================================================
// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;
// if      (selectedQuality === '360p')  { RES_W = 640;  RES_H = 360;  BITRATE = 800;  }
// else if (selectedQuality === '480p')  { RES_W = 854;  RES_H = 480;  BITRATE = 1500; }
// else if (selectedQuality === '720p')  { RES_W = 1280; RES_H = 720;  BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else                                  { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== ''
//     ? rawUrls.split(',').map(u => u.trim().replace(/^\\+/, '').startsWith('http') ? u.trim().replace(/^\\+/, '') : 'https://' + u.trim().replace(/^\\+/, ''))
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex  = urlList.length > 1 ? 1 : 0;

// const SELECTED_CHANNEL  = process.env.OKRU_STREAM_ID  || '1';
// const SERVER_SELECTION  = process.env.SERVER_SELECTION || 'None';
// const PROXY_ENGINE      = process.env.PROXY_ENGINE      || 'Cloudflare WARP (Recommended)';
// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let browser    = null;
// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_REAL_MS = 8000;   
// const FROZEN_THRESHOLD_HLS_MS  = 15000;  

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount   = 0;

// // =========================================================================================
// // 📡 NETWORK & CONSOLE DIAGNOSTICS
// // =========================================================================================
// function setupNetworkDiagnostics(page, pageName) {
//     if (!page) return;

//     page.on("console", msg => {
//         const type = msg.type();
//         const text = msg.text();
//         const textLower = text.toLowerCase();
//         if (type === 'error' || type === 'warning' || textLower.includes('manifest') || textLower.includes('hls') || textLower.includes('network')) {
//             console.log(`[💻] ${pageName.toUpperCase()} CONSOLE [${type.toUpperCase()}]: ${text}`);
//         }
//     });

//     page.on("requestfailed", req => {
//         const url = req.url();
//         const errText = req.failure() ? req.failure().errorText : 'UNKNOWN_ERROR';
//         if (url.includes('.m3u8') || url.includes('.ts') || url.includes('.mpd')) {
//             console.log(`[❌] ${pageName.toUpperCase()} MEDIA REQ FAILED: ${errText} -> ${url.substring(0, 150)}`);
//         }
//     });

//     page.on("response", async (res) => {
//         const url = res.url();
//         const status = res.status();
//         if (url.includes('.m3u8') || url.includes('.ts') || url.includes('.mpd')) {
//             let symbol = status >= 200 && status < 300 ? '✅' : '🔴';
//             if (status >= 400) symbol = '❌';
//             console.log(`[📡] ${pageName.toUpperCase()} MEDIA RES: ${symbol} ${status} -> ${url.substring(0, 150)}`);
//         }
//     });
// }

// // =========================================================================================
// // 🛡️ NETWORK AD-BLOCKER (REFINED FOR 2026 OBFS & M3U8 TOKENS)
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url  = request.url().toLowerCase();
//             const type = request.resourceType();

//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const adKw = ['fanduel','bet','casino','adrevenue'];
//                 if (adKw.some(k => url.includes(k))) {
//                     request.abort().catch(() => {}); return;
//                 }
//             }

//             if (url.includes('doubleclick') || (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('zone')))) {
//                 request.abort().catch(() => {});
//             } else {
//                 request.continue().catch(() => {});
//             }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // 🔥 PRELOAD FIREWALL
// // =========================================================================================
// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             window.alert   = function() {};
//             window.confirm = function() { return true; };
//             window.prompt  = function() { return null; };
//             window.open    = function() { return null; };
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true, get: function() { return null; }, set: function() { return null; }
//             });
//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000 !important; overflow: hidden !important; }`;
//             document.documentElement.appendChild(style);
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // 📸 SCREENSHOT + BATCH UPLOAD
// // =========================================================================================
// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const ts   = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${ts}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         pendingScreenshots.push(filePath);
//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 exec(`gh release upload ${tag} ${pendingScreenshots.join(' ')} --clobber`, (err) => { if (!err) uploadCycleCount++; });
//                 pendingScreenshots = [];
//             } catch (err) {}
//         }
//     } catch (e) {}
// }

// // =========================================================================================
// // 🖥️ LOADING UI
// // =========================================================================================
// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return;
//             let ov = document.getElementById('smart-stream-overlay');
//             if (ov) {
//                 ov.style.setProperty('display', 'flex', 'important');
//             } else {
//                 ov = document.createElement('div');
//                 ov.id = 'smart-stream-overlay';
//                 ov.innerHTML = `
//                 <style>
//                     #smart-stream-overlay{position:fixed!important;top:0!important;left:0!important;right:0!important;bottom:0!important;
//                     width:100vw!important;height:100vh!important;background:#000!important;z-index:2147483647!important;
//                     display:flex!important;flex-direction:column!important;justify-content:center!important;align-items:center!important;
//                     color:#fff!important;font-family:Arial,sans-serif!important;}
//                     .ss-spin{width:80px;height:80px;border:6px solid rgba(255,255,255,0.1);border-top:6px solid #e50914;border-radius:50%;
//                     animation:ss-s 1s linear infinite;margin-bottom:25px;}
//                     @keyframes ss-s{0%{transform:rotate(0deg)}100%{transform:rotate(360deg)}}
//                     .stream-title{font-size:36px!important;font-weight:800!important;margin-bottom:15px!important;}
//                     .stream-sub{font-size:20px!important;color:#ccc!important;}
//                 </style>
//                 <div class="ss-spin"></div>
//                 <div class="stream-title">${t}</div>
//                 <div class="stream-sub">${s}</div>`;
//                 document.documentElement.appendChild(ov);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const ov = document.getElementById('smart-stream-overlay');
//             if (ov) { ov.style.setProperty('display', 'none', 'important'); ov.remove(); }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // ⚙️ OBS CONFIG
// // =========================================================================================
// function setupOBSConfig() {
//     const obsDir      = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir   = path.join(obsDir, 'basic', 'scenes');
//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir,   { recursive: true });

//     fs.writeFileSync(path.join(obsDir, 'global.ini'),
//         `[General]\nLicenseAccepted=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`);

//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'),
//         `[General]\nName=Untitled\n[Video]\nBaseCX=${RES_W}\nBaseCY=${RES_H}\nOutputCX=${RES_W}\nOutputCY=${RES_H}\nFPSCommon=30\n[Output]\nMode=Simple\n[SimpleOutput]\nVBitrate=${BITRATE}\nStreamEncoder=x264\nx264Preset=ultrafast\n`);

//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify({
//         settings: { server: 'rtmp://vsu.okcdn.ru/input/', key: ACTIVE_STREAM_KEY },
//         type: 'rtmp_custom'
//     }, null, 2));

//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify({
//         current_scene: 'MainScene', current_program_scene: 'MainScene', name: 'Untitled',
//         scene_order: [{ name: 'MainScene' }],
//         sources: [
//             { id: 'xshm_input',            name: 'Screen', settings: { show_cursor: false } },
//             { id: 'pulse_output_capture', name: 'Audio',  settings: {} },
//             { id: 'scene', name: 'MainScene',    settings: { items: [{ name: 'Screen', id: 1, visible: true }, { name: 'Audio', id: 2, visible: true }] } }
//         ]
//     }, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async d => { try { await d.dismiss(); } catch(e) {} });
// }

// // =========================================================================================
// // 🔊 SMART UNMUTE ENGINE
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;
//             await frame.evaluate(() => {
//                 Array.from(document.querySelectorAll('button, div, span, a, i')).forEach(el => {
//                     const text  = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onclick = (el.getAttribute('onclick') || '').toLowerCase();
//                     if (text.includes('UNMUTE') || text.includes('AUDIO') || onclick.includes('unmute') || onclick.includes('volume')) {
//                         const r = el.getBoundingClientRect();
//                         if (r.width > 0 && r.height > 0) { try { el.click(); } catch(e) {} }
//                     }
//                 });
//                 document.querySelectorAll('video, audio').forEach(m => { if (m.muted) { m.muted = false; m.volume = 1.0; } });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }

// // =========================================================================================
// // 🎬 VIDEO INITIALIZER
// // =========================================================================================
// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         console.log('[*] Checking autoplay / play button...');
//         let playing = false, tries = 0;
//         while (!playing && tries < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let p = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) { v.muted = false; v.volume = 1.0; p = true; }
//                         });
//                         return p;
//                     });
//                     if (autoPlayed) { playing = true; break; }

//                     const btn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, .fp-play');
//                     if (btn) {
//                         await frame.evaluate(el => el.click(), btn);
//                         playing = true; break;
//                     }

//                     if (!playing && tries > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let done = false;
//                             for (const v of document.querySelectorAll('video')) {
//                                 if (v.clientWidth > 50) {
//                                     v.muted = false; v.volume = 1.0;
//                                     try { v.click(); } catch(e) {}
//                                     try { const p = v.play(); if (p) p.catch(() => {}); done = true; } catch(e) {}
//                                 }
//                             }
//                             return done;
//                         });
//                         if (forced) { playing = true; break; }
//                     }
//                 } catch (err) {}
//             }
//             if (!playing) await new Promise(r => setTimeout(r, 2000));
//             tries++;
//         }

//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isReal = await frame.evaluate(() => {
//                     const v = document.querySelector('video');
//                     return v && v.clientWidth > 50 && v.clientHeight > 50;
//                 });
//                 if (isReal) { targetFrame = frame; break; }
//             } catch (e) {}
//         }
//         if (!targetFrame) targetFrame = page.mainFrame();

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');

//                     const iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null, maxScore = -1;
//                     iframes.forEach(ifr => {
//                         const area = ifr.clientWidth * ifr.clientHeight;
//                         if (area < 5000) return;
//                         let score = area;
//                         if (ifr.hasAttribute('allowfullscreen')) score += 10000000;
//                         if (score > maxScore) { maxScore = score; mainIframe = ifr; }
//                     });
//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) ifr.style.setProperty('display', 'none', 'important');
//                         });
//                         ['position:fixed','top:0','left:0','width:100vw','height:100vh','z-index:2147483645','border:none'].forEach(s => {
//                             const [k, v] = s.split(':');
//                             mainIframe.style.setProperty(k, v, 'important');
//                         });
//                     }
//                 } catch (err) {}
//             }, 500);
//         }).catch(() => {});

//         await targetFrame.evaluate((muted) => {
//             setInterval(() => {
//                 try {
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     document.querySelectorAll('video, audio').forEach(m => { m.muted = muted; m.volume = muted ? 0 : 1.0; });
//                     const realVideo = videos.find(v => v.clientWidth > 100 && v.clientHeight > 100) || videos[0];
//                     if (realVideo) {
//                         ['position:fixed','top:0','left:0','width:100vw','height:100vh','z-index:2147483646','object-fit:contain','opacity:1','display:block'].forEach(s => {
//                             const [k, v] = s.split(':');
//                             realVideo.style.setProperty(k, v, 'important');
//                         });
//                     }
//                 } catch(err) {}
//             }, 500);
//         }, startMuted).catch(() => {});

//     } catch (e) {}

//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// // =========================================================================================
// // 🔬 CHECK PAGE STATUS
// // =========================================================================================
// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : '';
//                         if (bodyText.includes('stream error') || bodyText.includes('manifestloaderror') || bodyText.includes('not found') || bodyText.includes('error: forbidden')) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = videos.find(v => (v.src && v.src.startsWith('blob:')) || v.matches('.jw-video,.plyr__video,.vjs-tech')) || videos[0];
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// // =========================================================================================
// // 🔄 HOT-SWAP / REFRESH
// // =========================================================================================
// async function performHotSwap(reason) {
//     console.log(`\n[🔄] TRIGGERING RECOVERY/SWAP -> Reason: ${reason}`);
//     try {
//         await showLoadingUI(activePage, 'RECONNECTING', 'Optimizing live stream connection...');
//         await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 30000 });
//         await new Promise(r => setTimeout(r, 3000));
//         await initializeVideo(activePage, false, true);
//         await hideLoadingUI(activePage);
//         console.log(`[✅] Recovery successful!`);
//     } catch (err) {
//         console.log(`[⚠️] Swap error: ${err.message}`);
//     }
// }

// // =========================================================================================
// // 👁️ WATCHDOG
// // =========================================================================================
// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1;
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
//     let isWarmupPhase = true;
//     let currentStreamStartTime = Date.now();

//     while (true) {
//         if (!browser || !browser.isConnected()) throw new Error('Browser closed.');

//         let activeStatus = await checkPageStatus(activePage);

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             const elapsed = Date.now() - currentStreamStartTime;
//             if (elapsed > FORCE_REFRESH_MS) {
//                 activeStatus.status = 'FORCE_REFRESH';
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage);
//             isWarmupPhase = false;
//             await triggerSmartUnmute(activePage);

//             const isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             const isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_REAL_MS) {
//                     activeStatus.status = 'FROZEN';
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime;
//                 lastDecodedFrames = activeStatus.decodedFrames;
//                 frozenCheckTimestamp = Date.now();
//             }
//         }

//         watchdogTicks++;
//         if (watchdogTicks % 90 === 0) {
//             console.log(`[💓] WATCHDOG HEARTBEAT: Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//         }

//         if (['FROZEN', 'CRITICAL_ERROR', 'DEAD', 'FORCE_REFRESH'].includes(activeStatus.status)) {
//             if (isWarmupPhase && (Date.now() - currentStreamStartTime < 15000)) {
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue;
//             }
//             await performHotSwap(activeStatus.status);
//             lastActiveTime = -1;
//             frozenCheckTimestamp = Date.now();
//             currentStreamStartTime = Date.now();
//         }

//         await new Promise(r => setTimeout(r, 2000));
//     }
// }

// // =========================================================================================
// // 🚀 MAIN
// // =========================================================================================
// async function main() {
//     console.log(`\n[🚀] SMART STREAM ENGINE STARTING...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray'], { detached: true, stdio: 'ignore' });
//     obsProcess.unref();
//     await new Promise(r => setTimeout(r, 6000));

//     try {
//         await obs.connect('ws://127.0.0.1:4455', 'secret');
//         console.log('[+] OBS WebSocket Connected Successfully!');
//     } catch (e) {
//         console.log('[⚠️] OBS WS Connection failed, running standalone.');
//     }

//     let browserArgs = [
//         '--no-sandbox', '--disable-setuid-sandbox', '--disable-dev-shm-usage',
//         '--disable-blink-features=AutomationControlled',
//         '--autoplay-policy=no-user-gesture-required', '--no-first-run',
//         '--disable-background-timer-throttling', '--disable-backgrounding-occluded-windows',
//         `--window-size=${RES_W},${RES_H}`, '--start-fullscreen',
//         '--disable-web-security', '--ignore-certificate-errors'
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//     }

//     browser = await puppeteer.launch({
//         headless: false,
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'],
//         args: browserArgs
//     });

//     // 🛡️ POPUP KILLER (Kills rogue ads instantly while letting main network pass)
//     browser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             try {
//                 const newPage = await target.page();
//                 if (newPage && newPage !== activePage) {
//                     await newPage.close().catch(() => {});
//                 }
//             } catch (e) {}
//         }
//     });

//     const pages = await browser.pages();
//     activePage = pages[0];

//     await applyPreloadFirewall(activePage);
//     await setupNetworkAdBlocker(activePage);
//     attachAntiAdListeners(activePage);
//     setupNetworkDiagnostics(activePage, 'Active');

//     const activeUrl = urlList[currentUrlIndex];
//     console.log(`[*] Loading URL: ${activeUrl}`);
//     await showLoadingUI(activePage, 'STREAM LOADING', 'Connecting...');

//     try {
//         await activePage.goto(activeUrl, { waitUntil: 'domcontentloaded', timeout: 45000 });
//     } catch (e) {}

//     // Physical click simulation to satisfy modern browser media rules
//     try { await activePage.mouse.click(10, 10); } catch(e) {}

//     await initializeVideo(activePage, false, true);
//     await hideLoadingUI(activePage);

//     console.log(`\n[🟢] ALL SET! STREAM IS LIVE ON SINGLE TAB\n`);
//     await startWatchdog();
// }

// main().catch(async (err) => {
//     console.error(`\n[💥] FATAL ERROR: ${err.message}`);
//     try { if (browser) await browser.close(); } catch(e) {}
//     try { if (obsProcess) obsProcess.kill(); } catch(e) {}
//     setTimeout(() => main(), 10000);
// });






















// # ================== yeh king hey below yeh original haoi %^^%$%&***@^#%%##&#*( =================







// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let browser = null;
// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // Permanent root execution block for popup alerts & confirms
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; }`;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                             .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                             @keyframes blinker { 50% { opacity: 0.3; } }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// // =========================================================================================
// // ⚠️ NEW: INSTANT HANG / RECOVERY SHIELD UI (Unique Radar Design)
// // =========================================================================================
// async function showRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 return;
//             }
            
//             overlay = document.createElement('div');
//             overlay.id = 'stream-recovery-overlay';
//             overlay.innerHTML = `
//                 <style>
//                     #stream-recovery-overlay {
//                         position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                         width: 100vw !important; height: 100vh !important; background: rgba(0, 0, 0, 0.95) !important;
//                         z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                         justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                         font-family: Arial, sans-serif !important; pointer-events: all !important; backdrop-filter: blur(8px);
//                     }
//                     .recovery-radar {
//                         width: 100px; height: 100px; border-radius: 50%;
//                         border: 3px solid transparent; border-top-color: #ff9800; border-bottom-color: #ff9800;
//                         animation: radar-spin 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) infinite;
//                         margin-bottom: 20px; box-shadow: 0 0 30px rgba(255, 152, 0, 0.3);
//                     }
//                     .recovery-radar::before {
//                         content: ''; position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
//                         border-radius: 50%; border: 3px solid transparent; border-left-color: #f44336; border-right-color: #f44336;
//                         animation: radar-spin 2s linear infinite reverse;
//                     }
//                     @keyframes radar-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                     .warn-title { font-size: 32px !important; font-weight: 800 !important; color: #ff9800 !important; letter-spacing: 2px !important; margin-bottom: 10px !important; text-transform: uppercase !important; }
//                     .warn-sub { font-size: 18px !important; color: #dddddd !important; animation: pulse-text 1.5s infinite; }
//                     @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
//                 </style>
//                 <div class="recovery-radar"></div>
//                 <div class="warn-title">SIGNAL LOST</div>
//                 <div class="warn-sub">Attempting Auto-Recovery...</div>
//             `;
//             document.documentElement.appendChild(overlay);
//         });
//     } catch (e) {}
// }

// async function hideRecoveryUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('stream-recovery-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay):not(#stream-recovery-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay' || el.id === 'stream-recovery-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();
    
//     // 🔥 NEW: Track recovery UI state
//     let isRecoveryUIShown = false;

//     while (true) {
//         if (!browser || !browser.isConnected()) throw new Error("Browser closed.");

//         let activeStatus = await checkPageStatus(activePage);

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 // 🔥 NEW: INSTANT HANG RECOVERY SHIELD TRIGGER (0 Seconds Delay)
//                 if (!isRecoveryUIShown) {
//                     await showRecoveryUI(activePage);
//                     isRecoveryUIShown = true;
//                     console.log(`[⚠️] Stream Hang Detected! Showing Signal Recovery Shield instantly...`);
//                 }

//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                     isRecoveryUIShown = false; // Reset state for swap
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 // 🔥 NEW: STREAM RECOVERED (Instant hide)
//                 if (isRecoveryUIShown) {
//                     await hideRecoveryUI(activePage);
//                     isRecoveryUIShown = false;
//                     console.log(`[✅] Stream Recovered! Signal Recovery Shield removed instantly.`);
//                 }
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        
//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             console.log(`\n[💓] WATCHDOG HEARTBEAT: Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[▶️] CURRENTLY LIVE   : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
//             console.log(`[⏭️] NEXT IN QUEUE    : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//         }

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || backupStatus.status === 'DEAD') { 
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 let brokenPage = activePage; activePage = backupPage; backupPage = brokenPage;
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();
//                 isRecoveryUIShown = false; // 🔥 Fresh slate for new active tab

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//             } else {
//                 console.error(`[!] ❌ Backup Tab is ALSO DEAD/FROZEN. Hard Restarting System...`);
//                 throw new Error("Both Active and Backup tabs failed.");
//             }
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }

//     browser = await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });

//     browser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => {
//                 if (newPage && newPage !== activePage && newPage !== backupPage) {
//                     console.log(`[🛡️] AD-BLOCKER: Killed an unwanted pop-up tab!`);
//                     try { await newPage.close(); } catch(e) {}
//                 }
//             }, 500);
//         }
//     });

//     const pages = await browser.pages();
//     activePage = pages[0]; 
//     backupPage = await browser.newPage();
    
//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//     await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
//     backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex]}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex]}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (browser) { try { await browser.close(); } catch(e) { } browser = null; }
//     if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//     try {
//         execSync('pkill -9 obs || true', { stdio: 'ignore' });
//         execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//         execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//     } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();

















// streamed.pk alhamdullah successfully play
// =========== Old and best code :::::::)(**&^%^())(*&^%$$# ALhamdullah ALhamdullah yeh teek hai alhamdullah bas upper eek player hai new player jiska website k naem hai "https://embedsport.live/channel/tsn1/111/" iss me custom measn video k play button nahey bulkey html k play button hai . Jiskoo click karney see stream start huty hai. iska code upper lekty hai srf eek line change karna hai and wo line yeh hai old line is : const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play'); =======================



// const puppeteer = require('puppeteer-extra');
// const StealthPlugin = require('puppeteer-extra-plugin-stealth');
// puppeteer.use(StealthPlugin());

// const fs = require('fs');
// const path = require('path');
// const os = require('os');
// const { spawn, execSync, exec } = require('child_process');
// const { OBSWebSocket } = require('obs-websocket-js'); 

// // =========================================================================================
// // 🛡️ GLOBAL CRASH PREVENTION SHIELD (2026 LATEST FIX)
// // =========================================================================================
// process.on('uncaughtException', (err) => {
//     if (err.message && err.message.includes('Requesting main frame too early')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored stealth plugin background frame error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNCAUGHT EXCEPTION: ${err.message}`);
//     }
// });

// process.on('unhandledRejection', (reason, promise) => {
//     let msg = reason && reason.message ? reason.message : reason;
//     if (msg && msg.includes('Protocol error')) {
//         console.log(`[🛡️] SYSTEM SHIELD: Ignored detached frame protocol error.`);
//     } else {
//         console.log(`[⚠️] IGNORED UNHANDLED REJECTION: ${msg}`);
//     }
// });
// // =========================================================================================

// const obs = new OBSWebSocket(); 

// // =========================================================================================
// // ⏱️ BIG VARIABLE: FORCE AUTO-REFRESH TIME (IN MINUTES)
// // =========================================================================================
// const FORCE_REFRESH_MINUTES = 40000000; 
// const FORCE_REFRESH_MS = FORCE_REFRESH_MINUTES * 60 * 1000;

// // =========================================================================================
// // 🛡️ NO-REFRESH WHITELIST (CONTINUOUS PLAY DOMAINS)
// // =========================================================================================
// const NO_REFRESH_DOMAINS = [
//     'youtube.com',
//     'facebook.com',
//     'streamed.pk',
//     'cricstreams.', // crichd
//     'website-vercel-helper-d-jaja-3-2.vercel.app',
//     'websitestream.netlify.app/?ch=Channel%20HD%2071'
// ];

// // 🚀 Multi-Stream Key Manager
// const STREAM_KEYS = {
//     '1'   : '15254238731883_15281627925099_najspfkgne', 
//     '1.1' : '15254260751979_15281671637611_2plrcfqzze', 
//     '1.2' : '15254285524587_15281717840491_7e6qdknzsu',
    
//     '2'   : '15254299352683_15281743071851_7dvz3h5d7q',
//     '2.1' : '15254308986475_15281761618539_3xca7oij3u',
//     '2.2' : '15254328122987_15281795566187_zjqa6bqzoq', 

//     '3'   : '15254341885547_15281821059691_hhlpb5vicy', 
//     '3.1' : '15254357089899_15281848322667_sxeexgvzl4', 
//     '3.2' : '15254367510123_15281868180075_pc4jrytfgm',

//     '4'   : '15255022345835_15283095800427_vwrupxzstm', 
//     '4.1' : '15255038074475_15283122080363_ai5qqp2we4', 
//     '4.2' : '15255045480043_15283135842923_tldl4bhmii',
//     '4.3' : '15255208599147_15283449629291_abltofuc7m', 
//     '4.4' : '15255217708651_15283466603115_bojrrqtlmu', 
//     '4.5' : '15255227670123_15283486263915_jpntt54mve',

//     '5'   : '15273689226859_15317451606635_d7zzy3c7qi', 
//     '5.1' : '15273713933931_15317494860395_avj47smmim', 
//     '5.2' : '15273722257003_15317510195819_6edjluvdqi',
//     '5.3' : '15273739624043_15317541653099_ii4bxpvabe',
//     '5.4' : '15273750175339_15317561707115_csel26ku5a', 
//     '5.5' : '15273760071275_15317579467371_cnewcj54me',
//     '5.6' : '15273767935595_15317595851371_3q43tk7tvm', 
    
//     's1.1'  : '14204232736303_14846150314543_37jq4ryehq',
//     's1.2'  : '14204288179759_14846247373359_tnsknmapva',
//     's1.3'  : '14204319768111_14846302489135_sr4ht4ccwq',
//     's1.4'  : '14204331957807_14846326147631_dji2acqcze',
//     's1.5'  : '14204346572335_14846351641135_7gvns4o5ue',
//     's1.6'  : '14204361252399_14846376479279_cjajhf4d3y',
//     's1.7'  : '14204370492975_14846393649711_6fduhdqite',
//     's1.8'  : '14204395527727_14846438017583_s2jlti7lsm',
//     's1.9'  : '14204411387439_14846464887343_f5lxgcqj5y',
//     's1.10' : '14204424691247_14846487562799_xmbvntt6wa',

//     's2.1'  : '14204490948143_14846603495983_kzevn36tii',
//     's2.2'  : '14204506742319_14846634494511_ta2rxyg2oy',
//     's2.3'  : '14204523322927_14846661233199_foqb3q7zb4',
//     's2.4'  : '14204540034607_14846689085999_gjejdie4uy',
//     's2.5'  : '14204555304495_14846715497007_zdanghuxzu',
//     's2.6'  : '14204565200431_14846734371375_ap3bqpabpu',
//     's2.7'  : '14204577259055_14846756194863_3ecad2535u',
//     's2.8'  : '14204592528943_14846785227311_4hjl46y62e',
//     's2.9'  : '14204602621487_14846802594351_ilnp6lxekq',
//     's2.10' : '14206184136239_14849618610735_ihnbx7hkoi'
// };

// const selectedQuality = process.env.STREAM_QUALITY || 'Original (1080p Max)';
// let RES_W = 1920, RES_H = 1080, BITRATE = 5000;

// if (selectedQuality === '360p') { RES_W = 640; RES_H = 360; BITRATE = 800; }
// else if (selectedQuality === '480p') { RES_W = 854; RES_H = 480; BITRATE = 1500; }
// else if (selectedQuality === '720p') { RES_W = 1280; RES_H = 720; BITRATE = 3000; }
// else if (selectedQuality === '1080p') { RES_W = 1920; RES_H = 1080; BITRATE = 4500; }
// else { RES_W = 1920; RES_H = 1080; BITRATE = 6000; }

// console.log(`[🚀] Smart Engine Locked to: ${RES_W}x${RES_H} @ ${BITRATE}kbps`);
// console.log(`[⏱️] Auto-Refresh Time Set To: ${FORCE_REFRESH_MINUTES} Minutes`);

// let rawUrls = (process.env.TARGET_URLS || '').trim();
// let urlList = rawUrls !== '' 
//     ? rawUrls.split(',').map(u => u.trim().startsWith('http') ? u.trim() : 'https://' + u.trim()) 
//     : ['https://dadocric.st/player.php?id=starsp3&v=m'];

// let currentUrlIndex = 0;
// let backupUrlIndex = urlList.length > 1 ? 1 : 0; 

// const SELECTED_CHANNEL = process.env.OKRU_STREAM_ID || '1';
// const SERVER_SELECTION = process.env.SERVER_SELECTION || 'None'; 
// const PROXY_ENGINE = process.env.PROXY_ENGINE || 'Cloudflare WARP (Recommended)';

// const ACTIVE_STREAM_KEY = STREAM_KEYS[SELECTED_CHANNEL] || STREAM_KEYS['1'];

// let browser = null;
// let obsProcess = null;
// let activePage = null;
// let backupPage = null;

// const FROZEN_THRESHOLD_MS = 8000; 

// if (!fs.existsSync('./screenshots')) fs.mkdirSync('./screenshots');
// let pendingScreenshots = [];
// let uploadCycleCount = 0;

// // =========================================================================================
// // 🛡️ ADVANCED NETWORK INTELLIGENCE & NAVIGATION SHIELD
// // =========================================================================================
// async function setupNetworkAdBlocker(page) {
//     if (!page) return;
//     try {
//         await page.setRequestInterception(true);
//         page.on('request', (request) => {
//             const url = request.url().toLowerCase();
//             const type = request.resourceType();

//             // 🚫 SHIELD: Same-Tab Hostile Redirect Hijacking Block
//             if (request.isNavigationRequest() && request.frame() === page.mainFrame()) {
//                 const targetUrl = request.url().toLowerCase();
                
//                 const adKeywords = ['popads', 'exoclick', 'adsterra', 'onclickads', 'jerkmate', 'adrevenue', 'fanduel', 'bet', 'casino'];
//                 const isMaliciousAd = adKeywords.some(keyword => targetUrl.includes(keyword));

//                 if (isMaliciousAd) {
//                     console.log(`[🛡️] NAVIGATION SHIELD: Blocked malicious ad redirection to -> ${targetUrl.substring(0, 70)}...`);
//                     request.abort().catch(()=>{});
//                     return;
//                 }
//             }

//             // Strict Ad Infrastructure Block list
//             if (
//                 url.includes('popads') || 
//                 url.includes('exoclick') || 
//                 url.includes('adsterra') || 
//                 url.includes('onclickads') || 
//                 url.includes('jerkmate') ||
//                 url.includes('adrevenue') ||
//                 url.includes('fanduel') ||
//                 url.includes('doubleclick') ||
//                 (type === 'script' && (url.includes('analytics') || url.includes('tracking') || url.includes('ad-delivery') || url.includes('pop') || url.includes('zone')))
//             ) {
//                 request.abort().catch(()=>{});
//             } else {
//                 request.continue().catch(()=>{});
//             }
//         });
//     } catch (e) { console.log('[⚠️] Request interception setup failed.'); }
// }

// async function applyPreloadFirewall(page) {
//     if (!page) return;
//     try {
//         await page.evaluateOnNewDocument(() => {
//             // Permanent root execution block for popup alerts & confirms
//             window.alert = function() {};
//             window.confirm = function() { return true; };
//             window.prompt = function() { return null; };
//             window.open = function() { return null; };
            
//             // 🚫 ANTI-DIALOG FIX: Neutralize onbeforeunload modal box popup completely
//             Object.defineProperty(window, 'onbeforeunload', {
//                 configurable: true,
//                 get: function() { return null; },
//                 set: function() { return null; }
//             });

//             document.addEventListener('click', (e) => {
//                 const target = e.target;
//                 if (target && (target.tagName === 'A' || target.closest('a'))) {
//                     const link = target.tagName === 'A' ? target : target.closest('a');
//                     if (link.href && !link.href.includes(window.location.hostname) && !link.href.includes('javascript')) {
//                         console.log("[🛡️] RE-DIRECT SHIELD: Blocked navigation to external ad domain.");
//                         e.preventDefault();
//                         e.stopPropagation();
//                         return false;
//                     }
//                 }
//             }, true);

//             const style = document.createElement('style');
//             style.textContent = `html, body { background-color: #000000 !important; overflow: hidden !important; }`;
//             document.documentElement.appendChild(style);

//             const attachOverlay = () => {
//                 let target = document.body || document.documentElement;
//                 if (target && !document.getElementById('smart-stream-overlay')) {
//                     const overlay = document.createElement('div');
//                     overlay.id = 'smart-stream-overlay';
//                     overlay.innerHTML = `
//                         <style>
//                             #smart-stream-overlay {
//                                 position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                                 width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                                 z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                                 justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                                 font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                                 pointer-events: all !important;
//                             }
//                             .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                             .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                             .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                             @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                             @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                             .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                             .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         </style>
//                         <div class="stream-spinner"></div>
//                         <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                         <div class="stream-title">STREAM LOADING</div>
//                         <div class="stream-sub">Connecting to secure stream engine...</div>
//                     `;
//                     target.appendChild(overlay);
//                 } else if (!target) {
//                     requestAnimationFrame(attachOverlay);
//                 }
//             };
//             attachOverlay();
//         });
//     } catch (e) {
//         console.log(`[🛡️] SYSTEM SHIELD: Preload firewall safe injection caught an error.`);
//     }
// }

// async function takeAndBatchScreenshot(page, stepName) {
//     if (!page) return;
//     try {
//         const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
//         const filePath = `./screenshots/snap_${timestamp}_${stepName}.png`;
//         await page.screenshot({ path: filePath });
//         console.log(`[📸] Screenshot saved: ${filePath}`);
//         pendingScreenshots.push(filePath);

//         if (pendingScreenshots.length >= 3) {
//             try {
//                 const tag = 'live-stream-logs';
//                 try { execSync(`gh release view ${tag} || gh release create ${tag} -t "Live Logs"`, { stdio: 'ignore' }); } catch(e) {}
//                 try {
//                     const oldAssets = execSync(`gh release view ${tag} --json assets -q ".assets[].name"`, { encoding: 'utf-8' }).trim().split('\n');
//                     for (const asset of oldAssets) if (asset) execSync(`gh release delete-asset ${tag} "${asset}" -y`, { stdio: 'ignore' });
//                 } catch(e) {}

//                 const fileList = pendingScreenshots.join(' ');
//                 exec(`gh release upload ${tag} ${fileList} --clobber`, (err) => {
//                     if (!err) uploadCycleCount++;
//                 });
//                 pendingScreenshots = []; 
//             } catch (err) { }
//         }
//     } catch (e) { }
// }

// async function showLoadingUI(page, title, sub) {
//     try {
//         await page.evaluate((t, s) => {
//             if (window.self !== window.top) return; 
//             let overlay = document.getElementById('smart-stream-overlay');

//             if (overlay) {
//                 const titleEl = overlay.querySelector('.stream-title');
//                 const subEl = overlay.querySelector('.stream-sub');
//                 if (titleEl) titleEl.innerHTML = t;
//                 if (subEl) subEl.innerHTML = s;
                
//                 overlay.style.setProperty('display', 'flex', 'important');
//                 overlay.style.setProperty('opacity', '1', 'important');
//                 overlay.style.setProperty('z-index', '2147483647', 'important');
//             } 
//             else {
//                 overlay = document.createElement('div');
//                 overlay.id = 'smart-stream-overlay';
//                 overlay.innerHTML = `
//                     <style>
//                         #smart-stream-overlay {
//                             position: fixed !important; top: 0 !important; left: 0 !important; right: 0 !important; bottom: 0 !important;
//                             width: 100vw !important; height: 100vh !important; background: #000000 !important;
//                             z-index: 2147483647 !important; display: flex !important; flex-direction: column !important;
//                             justify-content: center !important; align-items: center !important; color: #ffffff !important;
//                             font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif !important;
//                             pointer-events: all !important;
//                         }
//                         .stream-spinner { width: 80px; height: 80px; border: 6px solid rgba(255, 255, 255, 0.1); border-top: 6px solid #e50914; border-radius: 50%; animation: spin-overlay 1s linear infinite; margin-bottom: 25px; box-shadow: 0 0 25px rgba(229, 9, 20, 0.4); }
//                         .progress-container { width: 300px; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin-bottom: 30px; overflow: hidden; position: relative; }
//                         .progress-bar-fill { width: 100%; height: 100%; background: linear-gradient(90deg, #e50914, #ff4d4d); position: absolute; left: -100%; animation: shift-progress 2s cubic-bezier(0.4, 0, 0.2, 1) infinite; }
//                         @keyframes spin-overlay { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
//                         @keyframes shift-progress { 0% { left: -100%; } 50% { left: 0; } 100% { left: 100%; } }
//                         .stream-title { font-size: 36px !important; font-weight: 800 !important; letter-spacing: 3px !important; margin-bottom: 15px !important; text-transform: uppercase !important; text-shadow: 0px 4px 10px rgba(0,0,0,0.8) !important; }
//                         .stream-sub { font-size: 20px !important; color: #cccccc !important; text-align: center !important; line-height: 1.6 !important; }
//                         .stream-blink { animation: blinker 1.5s linear infinite; color: #e50914; font-weight: bold; }
//                         @keyframes blinker { 50% { opacity: 0.3; } }
//                     </style>
//                     <div class="stream-spinner"></div>
//                     <div class="progress-container"><div class="progress-bar-fill"></div></div>
//                     <div class="stream-title">${t}</div>
//                     <div class="stream-sub">${s}</div>
//                 `;
//                 document.documentElement.appendChild(overlay);
//             }
//         }, title, sub);
//     } catch (e) {}
// }

// async function hideLoadingUI(page) {
//     try {
//         await page.evaluate(() => {
//             const overlay = document.getElementById('smart-stream-overlay');
//             if (overlay) {
//                 overlay.style.setProperty('display', 'none', 'important');
//                 overlay.style.setProperty('opacity', '0', 'important');
//                 overlay.style.setProperty('z-index', '-9999', 'important');
//                 overlay.remove();
//             }
//         });
//     } catch (e) {}
// }

// function setupOBSConfig() {
//     const obsDir = path.join(os.homedir(), '.config', 'obs-studio');
//     const profilesDir = path.join(obsDir, 'basic', 'profiles', 'Untitled');
//     const scenesDir = path.join(obsDir, 'basic', 'scenes');

//     fs.mkdirSync(profilesDir, { recursive: true });
//     fs.mkdirSync(scenesDir, { recursive: true });

//     const globalIniContent = `[General]\nLicenseAccepted=true\n[BasicWindow]\nShowAutoConfig=false\nWarned=true\n[OBSWebSocket]\nServerEnabled=true\nServerPort=4455\nServerPassword=secret\n`;
//     fs.writeFileSync(path.join(obsDir, 'global.ini'), globalIniContent);
    
//     const basicIniContent = `[General]
// Name=Untitled
// [Video]
// BaseCX=${RES_W}
// BaseCY=${RES_H}
// OutputCX=${RES_W}
// OutputCY=${RES_H}
// FPSCommon=30
// [Output]
// Mode=Simple
// [SimpleOutput]
// VBitrate=${BITRATE}
// StreamEncoder=x264
// x264Preset=ultrafast
// x264Settings=keyint=60 tune=zerolatency profile=main threads=4 rc-lookahead=0
// `;
//     fs.writeFileSync(path.join(profilesDir, 'basic.ini'), basicIniContent);

//     const serviceJson = {
//         "settings": { "server": "rtmp://vsu.okcdn.ru/input/", "key": ACTIVE_STREAM_KEY },
//         "type": "rtmp_custom"
//     };
//     fs.writeFileSync(path.join(profilesDir, 'service.json'), JSON.stringify(serviceJson, null, 2));

//     const sceneJson = {
//         "current_scene": "WaitingScene", 
//         "current_program_scene": "WaitingScene", 
//         "name": "Untitled",
//         "scene_order": [{"name": "WaitingScene"}, {"name": "MainScene"}],
//         "sources": [
//             { "id": "xshm_input", "name": "Screen", "settings": { "show_cursor": false } },
//             { "id": "pulse_output_capture", "name": "Audio", "settings": {} },
//             {
//                 "id": "scene", "name": "MainScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true}, {"name": "Audio", "id": 2, "visible": true} ] }
//             },
//             {
//                 "id": "scene", "name": "WaitingScene",
//                 "settings": { "items": [ {"name": "Screen", "id": 1, "visible": true} ] } 
//             }
//         ]
//     };
//     fs.writeFileSync(path.join(scenesDir, 'Untitled.json'), JSON.stringify(sceneJson, null, 2));
// }

// function attachAntiAdListeners(page) {
//     page.on('dialog', async dialog => {
//         try { await dialog.dismiss(); } catch(e){}
//     });
// }

// // =========================================================================================
// // 🔊 2026 INTELLIGENT FUZZY UNMUTE ENGINE (No Class/ID Dependence)
// // =========================================================================================
// async function triggerSmartUnmute(page) {
//     for (const frame of page.frames()) {
//         try {
//             if (frame.isDetached()) continue;

//             await frame.evaluate(() => {
//                 // 1. Scan all interactive elements
//                 const potentialElements = Array.from(document.querySelectorAll('button, div, span, a, i'));
                
//                 potentialElements.forEach(el => {
//                     const text = (el.innerText || el.textContent || '').trim().toUpperCase();
//                     const onClickStr = (el.getAttribute('onclick') || '').toLowerCase();
//                     const ariaLabel = (el.getAttribute('aria-label') || '').toUpperCase();
                    
//                     // Fuzzy match checking
//                     const matchesText = text.includes('UNMUTE') || text.includes('MUTE ME') || text.includes('STREAM UNMUTE') || text.includes('AUDIO');
//                     const matchesJS = onClickStr.includes('unmute') || onClickStr.includes('volume') || onClickStr.includes('audio');
//                     const matchesAria = ariaLabel.includes('UNMUTE') || ariaLabel.includes('VOLUME');

//                     if (matchesText || matchesJS || matchesAria) {
//                         const rect = el.getBoundingClientRect();
//                         const isVisible = rect.width > 0 && rect.height > 0 && window.getComputedStyle(el).display !== 'none';

//                         if (isVisible) {
//                             console.log(`[🔊 ENGINE]: Dynamically triggered click on element with text: "${text || 'JS Action'}"`);
//                             try { el.click(); } catch(e) {}
//                             try { el.dispatchEvent(new MouseEvent('click', { bubbles: true, cancelable: true })); } catch(e) {}
//                         }
//                     }
//                 });

//                 // 2. Bruteforce Browser Native Media Layer
//                 document.querySelectorAll('video, audio').forEach(media => {
//                     if (media.muted) {
//                         media.muted = false;
//                         media.volume = 1.0;
//                     }
//                 });
//             }).catch(() => {});
//         } catch (e) {}
//     }
// }
// // =========================================================================================

// async function initializeVideo(page, startMuted, isActivePage) {
//     try {
//         if (SERVER_SELECTION !== 'None') {
//             console.log(`[*] Clicking specific Server: ${SERVER_SELECTION}`);
//             let serverClicked = false; let serverAttempts = 0;
//             while (!serverClicked && serverAttempts < 10) { 
//                 serverAttempts++;
//                 try {
//                     const clickSuccess = await page.evaluate((serverName) => {
//                         const buttons = Array.from(document.querySelectorAll('button'));
//                         const targetBtn = buttons.find(b => b.innerText && b.innerText.trim().includes(serverName));
//                         if (targetBtn) { targetBtn.click(); return true; }
//                         return false;
//                     }, SERVER_SELECTION);

//                     if (clickSuccess) {
//                         serverClicked = true; 
//                         console.log(`[+] Server Button clicked successfully!`);
//                         await takeAndBatchScreenshot(page, `server-clicked`);
//                         await new Promise(r => setTimeout(r, 2000)); 
//                         if (isActivePage) await page.bringToFront(); 
//                     } else await new Promise(r => setTimeout(r, 2000));
//                 } catch (err) { await new Promise(r => setTimeout(r, 2000)); }
//             }
//         }

//         console.log('[*] Checking if Video is Autoplaying or Needs a Play Button...');
//         let isVideoPlaying = false; 
//         let attempts = 0;
        
//         while (!isVideoPlaying && attempts < 15) {
//             for (const frame of page.frames()) {
//                 try {
//                     const autoPlayed = await frame.evaluate(() => {
//                         let playing = false;
//                         document.querySelectorAll('video').forEach(v => {
//                             if (v.clientWidth > 50 && !v.paused && v.currentTime > 0) {
//                                 v.muted = false; 
//                                 v.volume = 1.0;
//                                 playing = true;
//                             }
//                         });
//                         return playing;
//                     });

//                     if (autoPlayed) {
//                         isVideoPlaying = true;
//                         break;
//                     }

//                     const playBtn = await frame.$('.jw-icon-display[aria-label="Play"], button[data-plyr="play"], .vjs-big-play-button, [class*="unmute"], .fp-play');
//                     if (playBtn) {
//                         const isVisible = await frame.evaluate(el => {
//                             const style = window.getComputedStyle(el);
//                             return style.display !== 'none' && style.visibility !== 'hidden' && style.opacity !== '0';
//                         }, playBtn);

//                         if (isVisible) {
//                             await frame.evaluate(el => el.click(), playBtn); 
//                             await takeAndBatchScreenshot(page, `play-btn-clicked`);
//                             await new Promise(r => setTimeout(r, 3000)); 
//                             isVideoPlaying = true;
//                             break; 
//                         }
//                     }

//                     if (!isVideoPlaying && attempts > 5) {
//                         const forced = await frame.evaluate(async () => {
//                             let played = false;
//                             let vids = document.querySelectorAll('video');
//                             for(let v of vids) {
//                                 if (v.clientWidth > 50) { 
//                                     v.muted = false; v.volume = 1.0; 
//                                     try { v.click(); } catch(e){}
//                                     try {
//                                         let p = v.play();
//                                         if (p !== undefined) p.catch(()=>{});
//                                         played = true;
//                                     } catch(e) {}
//                                 }
//                             }
//                             return played;
//                         });

//                         if (forced) {
//                             await takeAndBatchScreenshot(page, `force-play-applied`);
//                             isVideoPlaying = true;
//                             break;
//                         }
//                     }
//                 } catch (err) {}
//             }
//             if (!isVideoPlaying) await new Promise(r => setTimeout(r, 2000));
//             attempts++;
//         }

//         console.log('[*] Scanning for Exact Real Video Player...');
//         let targetFrame = null;
//         for (const frame of page.frames()) {
//             try {
//                 const isRealLiveStream = await frame.evaluate(() => {
//                     const vid = document.querySelector('video');
//                     return vid && vid.clientWidth > 50 && vid.clientHeight > 50;
//                 });
//                 if (isRealLiveStream) { 
//                     targetFrame = frame; 
//                     console.log(`[+] Smart Scanner locked onto video frame!`);
//                     break; 
//                 }
//             } catch (e) { }
//         }

//         await page.evaluate(() => {
//             setInterval(() => {
//                 try {
//                     document.documentElement.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('background-color', 'black', 'important');
//                     document.body.style.setProperty('overflow', 'hidden', 'important');
//                     document.documentElement.style.setProperty('overflow', 'hidden', 'important');

//                     let iframes = Array.from(document.querySelectorAll('iframe'));
//                     let mainIframe = null; let maxScore = -1;

//                     // 1. ADVANCED GEOMETRIC SCORING
//                     iframes.forEach(ifr => {
//                         let width = ifr.clientWidth;
//                         let height = ifr.clientHeight;
//                         let area = width * height;

//                         if (area < 5000) return;

//                         let score = area;
                        
//                         if (ifr.hasAttribute('allowfullscreen') || 
//                             ifr.hasAttribute('webkitallowfullscreen') || 
//                             ifr.hasAttribute('mozallowfullscreen')) {
//                             score += 10000000; 
//                         }
                        
//                         if (height > width) {
//                             score = -1; 
//                         }

//                         if (score > maxScore) {
//                             maxScore = score;
//                             mainIframe = ifr;
//                         }
//                     });

//                     if (!mainIframe && iframes.length > 0) {
//                         mainIframe = iframes.find(ifr => 
//                             ifr.getAttribute('allowfullscreen') !== null || 
//                             (ifr.src && (ifr.src.includes('player') || ifr.src.includes('embed') || ifr.src.includes('stream') || ifr.src.includes('watch')))
//                         );
//                     }

//                     if (mainIframe) {
//                         iframes.forEach(ifr => {
//                             if (ifr !== mainIframe) {
//                                 ifr.style.setProperty('display', 'none', 'important');
//                                 ifr.style.setProperty('opacity', '0', 'important');
//                                 ifr.style.setProperty('z-index', '-9999', 'important');
                                
//                                 if (ifr.parentNode && ifr.parentNode !== document.body) {
//                                     try { 
//                                         ifr.parentNode.style.setProperty('display', 'none', 'important'); 
//                                         ifr.parentNode.style.setProperty('opacity', '0', 'important');
//                                     } catch(e) {}
//                                 }
//                             }
//                         });

//                         mainIframe.style.setProperty('position', 'fixed', 'important');
//                         mainIframe.style.setProperty('top', '0px', 'important');
//                         mainIframe.style.setProperty('left', '0px', 'important');
//                         mainIframe.style.setProperty('width', '100vw', 'important');
//                         mainIframe.style.setProperty('height', '100vh', 'important');
//                         mainIframe.style.setProperty('z-index', '2147483645', 'important'); 
//                         mainIframe.style.setProperty('background-color', 'black', 'important');
//                         mainIframe.style.setProperty('border', 'none', 'important');
//                         mainIframe.style.setProperty('opacity', '1', 'important');
//                         mainIframe.style.setProperty('display', 'block', 'important');
//                         mainIframe.style.setProperty('visibility', 'visible', 'important');
//                     }

//                     const junkClasses = '.chat, #chat, header, footer, .sidebar, .banner, .ads, [class*="overlay"]:not(#smart-stream-overlay), [id*="pop"], [class*="pop"], a[href*="extension"], [class*="notification"], [id*="notification"]';
//                     document.querySelectorAll(junkClasses).forEach(el => { 
//                         try { el.remove(); } catch(e){ el.style.setProperty('display', 'none', 'important'); } 
//                     });

//                     const adKeywords = ['jerk', 'mate', 'free', 'online', 'adult', 'dating', 'close', 'notification', 'justine', 'paying', 'job'];
//                     document.querySelectorAll('div, section, span, a').forEach(el => {
//                         if (el.id === 'smart-stream-overlay') return;
                        
//                         const style = window.getComputedStyle(el);
//                         const isFloating = style.position === 'fixed' || style.position === 'absolute';
                        
//                         if (isFloating && el.innerText) {
//                             const textLower = el.innerText.toLowerCase();
//                             const hasBadKeyword = adKeywords.some(keyword => textLower.includes(keyword));
                            
//                             if (hasBadKeyword || (parseInt(style.zIndex) > 100000 && !el.querySelector('video') && !el.querySelector('iframe'))) {
//                                 try { el.remove(); } catch(e) { el.style.setProperty('display', 'none', 'important'); }
//                             }
//                         }
//                     });

//                 } catch (err) {}
//             }, 500); 
//         }).catch(() => {});

//         await targetFrame.evaluate((muteVideo) => {
//             setInterval(() => {
//                 try {
//                     const style = document.createElement('style');
//                     style.innerHTML = `.jw-controls, .jw-ui, .plyr__controls, .vjs-control-bar, [data-player] .controls { display: none !important; opacity: 0 !important; visibility: hidden !important; }`;
//                     document.head.appendChild(style);

//                     const mediaElements = document.querySelectorAll('video, audio');
//                     const videos = Array.from(document.querySelectorAll('video'));
//                     let realVideo = null;

//                     mediaElements.forEach(media => { 
//                         media.muted = muteVideo; 
//                         media.volume = muteVideo ? 0.0 : 1.0; 
//                     });

//                     if (!muteVideo) {
//                         document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                     }

//                     for (const v of videos) {
//                         if (v.clientWidth > 100 && v.clientHeight > 100) { realVideo = v; break; }
//                     }

//                     if (!realVideo && videos.length > 0) {
//                         realVideo = videos[0];
//                     }

//                     if (realVideo) { 
//                         realVideo.style.setProperty('position', 'fixed', 'important');
//                         realVideo.style.setProperty('top', '0px', 'important');
//                         realVideo.style.setProperty('left', '0px', 'important');
//                         realVideo.style.setProperty('width', '100vw', 'important');
//                         realVideo.style.setProperty('height', '100vh', 'important');
//                         realVideo.style.setProperty('z-index', '2147483646', 'important'); 
//                         realVideo.style.setProperty('background-color', 'black', 'important');
//                         realVideo.style.setProperty('object-fit', 'contain', 'important');
//                         realVideo.style.setProperty('opacity', '1', 'important');
//                         realVideo.style.setProperty('visibility', 'visible', 'important');
//                         realVideo.style.setProperty('display', 'block', 'important');
//                     }
//                 } catch(err) {}
//             }, 500); 
//         }, startMuted).catch(() => {});

//     } catch (e) { }

//     // 🔥 Added Smart Unmute Execution Here
//     await triggerSmartUnmute(page);
//     await new Promise(r => setTimeout(r, 1000));
// }

// async function checkPageStatus(page) {
//     if (!page) return { status: 'DEAD' };
//     try {
//         for (const frame of page.frames()) {
//             try {
//                 if (frame.isDetached()) continue;
//                 const result = await Promise.race([
//                     frame.evaluate(() => {
//                         const bodyText = document.body ? document.body.innerText.toLowerCase() : "";
                        
//                         if (
//                             bodyText.includes("stream error") || 
//                             bodyText.includes("not found") || 
//                             bodyText.includes("domain is blocked") ||
//                             bodyText.includes("error: forbidden") ||
//                             bodyText.includes("does not have permission") ||
//                             bodyText.includes("access denied") ||
//                             (bodyText.includes("cloudflare") && bodyText.includes("blocked"))
//                         ) {
//                             return { status: 'CRITICAL_ERROR' };
//                         }
                        
//                         const videos = Array.from(document.querySelectorAll('video'));
//                         let targetV = null;

//                         for (const v of videos) {
//                             if (v.clientWidth > 0 && v.clientWidth < 100) continue;
//                             if ((v.src && v.src.startsWith('blob:')) || v.matches('.jw-video, .plyr__video, .vjs-tech')) {
//                                 targetV = v; break;
//                             }
//                         }
                        
//                         if (!targetV && videos.length > 0) {
//                             targetV = videos.sort((a, b) => (b.clientWidth * b.clientHeight) - (a.clientWidth * a.clientHeight))[0];
//                         }
                        
//                         if (targetV && !targetV.ended && targetV.currentTime > 0) {
//                             let frames = 0;
//                             if (targetV.getVideoPlaybackQuality) {
//                                 frames = targetV.getVideoPlaybackQuality().totalVideoFrames;
//                             } else if (targetV.webkitDecodedFrameCount !== undefined) {
//                                 frames = targetV.webkitDecodedFrameCount;
//                             }
//                             return { status: 'HEALTHY', currentTime: targetV.currentTime, decodedFrames: frames };
//                         }
//                         return { status: 'DEAD' };
//                     }),
//                     new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 2500))
//                 ]);
//                 if (result && result.status !== 'DEAD') return result;
//             } catch (err) {}
//         }
//     } catch (e) { return { status: 'DEAD' }; }
//     return { status: 'DEAD' };
// }

// async function startWatchdog() {
//     let lastActiveTime = -1;
//     let lastDecodedFrames = -1; 
//     let frozenCheckTimestamp = Date.now();
//     let watchdogTicks = 0;
    
//     let streamSetupTime = Date.now(); 
//     let isWarmupPhase = true; 
//     const WARMUP_MAX_TIME = 15000; 

//     let activeUrlStr = urlList[currentUrlIndex];
//     let backupUrlStr = urlList[backupUrlIndex];

//     let currentStreamStartTime = Date.now();

//     while (true) {
//         if (!browser || !browser.isConnected()) throw new Error("Browser closed.");

//         let activeStatus = await checkPageStatus(activePage);

//         if (activeStatus.status === 'HEALTHY' && !isWarmupPhase) {
//             let elapsedMs = Date.now() - currentStreamStartTime;
//             let isExempted = NO_REFRESH_DOMAINS.some(domain => activeUrlStr.includes(domain));

//             if (elapsedMs > FORCE_REFRESH_MS) {
//                 if (!isExempted) {
//                     console.log(`\n[⏱️ PROACTIVE REFRESH]: Stream ran smoothly for ${FORCE_REFRESH_MINUTES} minutes! Forcing SAME LINK swap to keep connection fresh...`);
//                     activeStatus.status = 'FORCE_REFRESH'; 
//                 }
//             }
//         }

//         if (activeStatus.status === 'HEALTHY') {
//             await hideLoadingUI(activePage); 
//             isWarmupPhase = false; 

//             // 🔥 Added Smart Unmute Continuous Engine Here
//             await triggerSmartUnmute(activePage);

//             let isTimeStuck = (activeStatus.currentTime === lastActiveTime);
//             let isFrameStuck = (activeStatus.decodedFrames === lastDecodedFrames && activeStatus.decodedFrames > 0);

//             if (isTimeStuck || isFrameStuck) {
//                 if (Date.now() - frozenCheckTimestamp > FROZEN_THRESHOLD_MS) {
//                     activeStatus.status = 'FROZEN';
//                     if (isFrameStuck && !isTimeStuck) {
//                         console.log(`[!] ⚠️ SYSTEM SHIELD: Detected Black Screen (Audio playing, but video frames stuck). Triggering HOT-SWAP.`);
//                     }
//                 }
//             } else {
//                 lastActiveTime = activeStatus.currentTime; 
//                 lastDecodedFrames = activeStatus.decodedFrames; 
//                 frozenCheckTimestamp = Date.now();
                
//                 for (const frame of activePage.frames()) {
//                     try {
//                         if (!frame.isDetached()) {
//                             frame.evaluate(() => { 
//                                 document.querySelectorAll('video, audio').forEach(m => { m.muted = false; m.volume = 1.0; }); 
//                                 document.querySelectorAll('.jw-icon-volume.jw-off, .vjs-vol-muted, .plyr__control--pressed[data-plyr="mute"]').forEach(btn => { try { btn.click(); } catch(e){} });
//                             }).catch(()=>{});
//                         }
//                     } catch(e) {}
//                 }
//             }
//         }

//         if (backupPage) {
//             for (const frame of backupPage.frames()) {
//                 try {
//                     if (!frame.isDetached()) {
//                         frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }).catch(()=>{});
//                     }
//                 } catch(e) {}
//             }
//         }

//         watchdogTicks++;
        
//         if (watchdogTicks === 1 || watchdogTicks % 90 === 0) {
//             console.log(`\n[💓] WATCHDOG HEARTBEAT: Status is ${activeStatus.status} | Video Time: ${activeStatus.currentTime ? activeStatus.currentTime.toFixed(1) + 's' : 'N/A'}`);
//             console.log(`[▶️] CURRENTLY LIVE   : Server [${currentUrlIndex}] (Audio ON) -> ${activeUrlStr}`);
//             console.log(`[⏭️] NEXT IN QUEUE    : Server [${backupUrlIndex}] (Audio MUTED) -> ${backupUrlStr}`);
//         }

//         if (watchdogTicks % 120 === 0) {
//             await takeAndBatchScreenshot(activePage, `heartbeat-tick-${watchdogTicks}`);
//         }

//         if (activeStatus.status === 'FROZEN' || activeStatus.status === 'CRITICAL_ERROR' || activeStatus.status === 'DEAD' || activeStatus.status === 'FORCE_REFRESH') {
            
//             if (isWarmupPhase && (Date.now() - streamSetupTime < WARMUP_MAX_TIME)) { 
//                 console.log(`[⏳] Watchdog detected '${activeStatus.status}', but stream is in WARM-UP phase. Waiting...`);
//                 await new Promise(r => setTimeout(r, 2000));
//                 continue; 
//             }

//             let isProactiveRefresh = (activeStatus.status === 'FORCE_REFRESH');

//             if (isProactiveRefresh) {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] 🔄 PROACTIVE REFRESH TRIGGERED`);
//                 console.log(`[*] Preparing a FRESH copy of SAME Server [${currentUrlIndex}] in background...`);
//                 console.log(`==================================================`);
                
//                 for (const frame of activePage.frames()) {
//                     try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                 }

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     await backupPage.goto(activeUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(()=>{});
//                 } catch(e) {
//                     console.log(`[⏳] Proactive refresh buffer navigation handled safely.`);
//                 }
//             } else {
//                 console.log(`\n==================================================`);
//                 console.log(`[!] ❌ WATCHDOG DETECTED ISSUE: ${activeStatus.status}`);
//                 console.log(`[💀] FAILED STREAM: Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`==================================================`);
//                 await takeAndBatchScreenshot(activePage, `error-${activeStatus.status.toLowerCase()}`);
//             }
            
//             console.log(`[*] Checking Backup Tab status before switching...`);
//             let backupStatus = await checkPageStatus(backupPage);

//             if (backupStatus.status === 'HEALTHY' || backupStatus.status === 'DEAD') { 
                
//                 if (!isProactiveRefresh) {
//                     for (const frame of activePage.frames()) {
//                         try { if (!frame.isDetached()) await frame.evaluate(() => { document.querySelectorAll('video, audio').forEach(m => { m.muted = true; m.volume = 0.0; }); }); } catch(e) {}
//                     }
//                 }
                
//                 await showLoadingUI(backupPage, isProactiveRefresh ? "REFRESHING CONNECTION" : "RECONNECTING", isProactiveRefresh ? "Optimizing current server stream <span class='stream-blink'>...</span>" : "Establishing secure connection to backup server <span class='stream-blink'>...</span>");
//                 await backupPage.bringToFront();
//                 await new Promise(r => setTimeout(r, 1000)); 
                
//                 try { await backupPage.mouse.click(10, 10); } catch(e){} 

//                 console.log(`[*] Initializing Video on the newly active tab...`);
//                 await initializeVideo(backupPage, false, true); 
//                 await hideLoadingUI(backupPage);

//                 let brokenPage = activePage; activePage = backupPage; backupPage = brokenPage;
//                 lastActiveTime = -1; frozenCheckTimestamp = Date.now();

//                 if (!isProactiveRefresh) {
//                     currentUrlIndex = backupUrlIndex; activeUrlStr = urlList[currentUrlIndex]; 
//                     backupUrlIndex = (backupUrlIndex + 1) % urlList.length; backupUrlStr = urlList[backupUrlIndex]; 
//                 } 

//                 console.log(`\n==================================================`);
//                 console.log(isProactiveRefresh ? `[🔄] SAME-SERVER FRESH SWAP EXECUTED SUCCESSFULLY` : `[🔄] SMART HOT-SWAP TO NEXT SERVER EXECUTED SUCCESSFULLY`);
//                 console.log(`==================================================`);
//                 console.log(`[📺] NEW ACTIVE STREAM : Server [${currentUrlIndex}] -> ${activeUrlStr}`);
//                 console.log(`[🔊] LIVE AUDIO STATUS : ON (Unmuted & Forced)`);
//                 console.log(`--------------------------------------------------`);
//                 console.log(`[🛡️] NEXT BACKUP QUEUE : Server [${backupUrlIndex}] -> ${backupUrlStr}`);
//                 console.log(`[🔇] BACKUP AUDIO      : MUTED (Background Loading)`);
//                 console.log(`==================================================\n`);

//                 try {
//                     await backupPage.goto('about:blank').catch(()=>{});
//                     await applyPreloadFirewall(backupPage);
//                     backupPage.goto(backupUrlStr, { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
//                 } catch (e) {
//                     console.log(`[⏳] Background buffer navigation handled safely.`);
//                 }
                
//                 streamSetupTime = Date.now(); 
//                 isWarmupPhase = true;
//                 currentStreamStartTime = Date.now();

//             } else {
//                 console.error(`[!] ❌ Backup Tab is ALSO DEAD/FROZEN. Hard Restarting System...`);
//                 throw new Error("Both Active and Backup tabs failed.");
//             }
//         }

//         await new Promise(r => setTimeout(r, 2000)); 
//     }
// }

// async function startDirectStreaming() {
//     console.log(`[*] Starting OBS Studio FIRST...`);
//     setupOBSConfig();

//     obsProcess = spawn('obs', ['--startstreaming', '--minimize-to-tray']);
//     obsProcess.stdout.on('data', (data) => console.log(`[OBS]: ${data.toString().trim()}`));
//     obsProcess.stderr.on('data', (data) => {
//         const msg = data.toString().trim();
//         if (msg.includes('error') || msg.includes('fail')) console.log(`[OBS Error]: ${msg}`);
//     });

//     console.log('[*] Waiting for OBS to initialize before launching browser...');
//     await new Promise(r => setTimeout(r, 6000));

//     let isObsConnected = false;
//     console.log('[*] Attempting to connect to OBS WebSocket (Polling Engine Active)...');
//     for (let attempt = 1; attempt <= 15; attempt++) {
//         try {
//             await Promise.race([
//                 obs.connect('ws://127.0.0.1:4455', 'secret'),
//                 new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), 3000))
//             ]);
//             isObsConnected = true;
//             console.log('[+] OBS WebSocket Connected Successfully!');
//             break;
//         } catch (e) {
//             console.log(`[⏳] OBS Port 4455 not ready yet. Retrying (${attempt}/15)...`);
//             await new Promise(r => setTimeout(r, 2000));
//         }
//     }

//     if (isObsConnected) {
//         try {
//             await obs.call('SetCurrentProgramScene', { sceneName: 'WaitingScene' });
//             console.log('[+] Enforced WaitingScene (Loading Bar Buffer Active)');
//         } catch(e){}
//     }

//     let browserArgs = [
//         '--no-sandbox', 
//         '--disable-setuid-sandbox',
//         `--window-size=${RES_W},${RES_H}`, 
//         '--window-position=0,0', 
//         '--kiosk', 
//         '--start-fullscreen',
//         '--autoplay-policy=no-user-gesture-required',
//         '--disable-dev-shm-usage', 
//         '--ignore-certificate-errors',
//         '--disable-web-security',
//         '--ignore-gpu-blocklist', 
//         '--use-gl=egl',
//         '--disable-accelerated-video-decode', 
//         '--disable-accelerated-video-encode',
//         '--disable-smooth-scrolling',
//         '--disable-features=Translate,BlinkGenPropertyTrees,CalculateNativeWinOcclusion',
//         '--disable-background-timer-throttling',
//         '--disable-backgrounding-occluded-windows',
//         '--disable-renderer-backgrounding',
        
//         `--disable-extensions-except=${path.join(process.cwd(), 'ublock-lite')}`,
//         `--load-extension=${path.join(process.cwd(), 'ublock-lite')}`
//     ];

//     if (PROXY_ENGINE.includes('Cloudflare')) {
//         browserArgs.push('--proxy-server=socks5://127.0.0.1:40000');
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} and [CLOUDFLARE WARP] Proxy...`);
//     } else {
//         console.log(`[*] Starting browser with EXACT viewport dimensions: ${RES_W}x${RES_H} using [DIRECT GITHUB IP]...`);
//     }

//     browser = await puppeteer.launch({
//         headless: false, 
//         defaultViewport: { width: RES_W, height: RES_H },
//         ignoreDefaultArgs: ['--enable-automation'], 
//         args: browserArgs
//     });

//     browser.on('targetcreated', async (target) => {
//         if (target.type() === 'page') {
//             const newPage = await target.page();
//             setTimeout(async () => {
//                 if (newPage && newPage !== activePage && newPage !== backupPage) {
//                     console.log(`[🛡️] AD-BLOCKER: Killed an unwanted pop-up tab!`);
//                     try { await newPage.close(); } catch(e) {}
//                 }
//             }, 500);
//         }
//     });

//     const pages = await browser.pages();
//     activePage = pages[0]; 
//     backupPage = await browser.newPage();
    
//     await setupNetworkAdBlocker(activePage);
//     await setupNetworkAdBlocker(backupPage);

//     attachAntiAdListeners(activePage);
//     attachAntiAdListeners(backupPage);

//     await applyPreloadFirewall(activePage);
//     await applyPreloadFirewall(backupPage);

//     await activePage.bringToFront(); 

//     console.log(`[*] STEP 1: Loading Server [${currentUrlIndex}] on Active Page: ${urlList[currentUrlIndex]}`);
//     await activePage.goto(urlList[currentUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 });
    
//     await showLoadingUI(activePage, "STREAM LOADING", "Optimizing live video connection <span class='stream-blink'>...</span>");
    
//     await initializeVideo(activePage, false, true); 
//     await hideLoadingUI(activePage); 

//     if (isObsConnected) {
//         console.log('\n[*] Active Video is Ready! Shifting OBS from Animated Buffer to LIVE Video (MainScene)...');
//         try { await obs.call('SetCurrentProgramScene', { sceneName: 'MainScene' }); } catch (e) {}
//     }

//     console.log(`[*] STEP 2: Silently preparing Server [${backupUrlIndex}] on Backup Page: ${urlList[backupUrlIndex]}`);
//     backupPage.goto(urlList[backupUrlIndex], { waitUntil: 'domcontentloaded', timeout: 60000 }).catch(() => {});
    
//     await activePage.bringToFront();
//     try { await activePage.mouse.click(10, 10); } catch(e){} 
//     await hideLoadingUI(activePage);

//     console.log(`\n==================================================`);
//     console.log(`[🎥] INITIAL CAPTURE STATUS: Ready to Broadcast`);
//     console.log(`==================================================`);
//     console.log(`[📺] CURRENT ACTIVE LIVE : Server [${currentUrlIndex}] -> ${urlList[currentUrlIndex]}`);
//     console.log(`[🔊] LIVE AUDIO STATUS   : ON (Unmuted)`);
//     console.log(`--------------------------------------------------`);
//     console.log(`[🛡️] NEXT BACKUP QUEUE   : Server [${backupUrlIndex}] -> ${urlList[backupUrlIndex]}`);
//     console.log(`[🔇] BACKUP AUDIO STATUS : MUTED (Background)`);
//     console.log(`==================================================\n`);

//     console.log('[*] Everything Setup! Dual-Tab Monitoring is Active.');
//     await startWatchdog();
// }

// async function mainLoop() {
//     while (true) {
//         try { await startDirectStreaming(); } 
//         catch (error) {
//             console.error(`\n[!] ALERT: ${error.message}`);
//             console.log('[*] 🔄 Hard Restarting everything in 3 seconds...');
//             await cleanup();
//             await new Promise(resolve => setTimeout(resolve, 3000));
//         }
//     }
// }

// async function cleanup() {
//     console.log('[*] Cleaning up resources...');
//     try { await obs.disconnect(); } catch (e) { } 
//     if (browser) { try { await browser.close(); } catch(e) { } browser = null; }
//     if (obsProcess) { try { obsProcess.kill('SIGKILL'); } catch(e) { } obsProcess = null; }
//     try {
//         execSync('pkill -9 obs || true', { stdio: 'ignore' });
//         execSync('pkill -9 chrome || true', { stdio: 'ignore' });
//         execSync('pkill -9 puppeteer || true', { stdio: 'ignore' });
//     } catch (e) { }
// }

// process.on('SIGINT', async () => { await cleanup(); process.exit(0); });

// const customDurationStr = process.env.CUSTOM_DURATION || 'None';
// function parseDurationToMs(str) {
//     if (!str || str.toLowerCase() === 'none') return null;
//     let ms = 0;
//     const hMatch = str.match(/(\d+)\s*h/i);
//     const mMatch = str.match(/(\d+)\s*m/i);
//     if (hMatch) ms += parseInt(hMatch[1]) * 60 * 60 * 1000;
//     if (mMatch) ms += parseInt(mMatch[1]) * 60 * 1000;
//     return ms > 0 ? ms : null;
// }

// const exactDurationMs = parseDurationToMs(customDurationStr);
// if (exactDurationMs) {
//     setTimeout(async () => {
//         console.log(`\n[*] 🛑 Time's up! The assigned duration (${customDurationStr}) is complete. Shutting down cleanly...`);
//         await cleanup();
//         process.exit(0);
//     }, exactDurationMs);
// } else {
//     setTimeout(() => {
//         try {
//             const targetUrls = process.env.TARGET_URLS || 'https://dadocric.st/player.php?id=starsp3&v=m';
//             const channel = process.env.OKRU_STREAM_ID || '1';
//             const quality = process.env.STREAM_QUALITY || '110KBps (Balanced 480p)';
//             const server = process.env.SERVER_SELECTION || 'None';
//             const cmd = `gh workflow run main.yml -f target_urls="${targetUrls}" -f okru_stream_channel="${channel}" -f stream_quality="${quality}" -f server_selection="${server}" -f proxy_engine="${PROXY_ENGINE}" -f custom_duration="None"`;
//             execSync(cmd, { stdio: 'inherit' });
//             setTimeout(async () => {
//                 await cleanup(); 
//                 process.exit(0); 
//             }, 300000); 
//         } catch (err) { }
//     }, 21000000);
// }

// mainLoop();
